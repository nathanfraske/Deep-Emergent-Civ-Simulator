# Materials framing: foundation audit result (Agent B, source-grounded, non-deferential)

Verdict (2026-07-12), on whether the materials framing (the oracle spec + the generator architecture + the breathing resolution) is the correct fundamentals to build the materials system off of. Audited by Agent B, which built the Ewald/QEq/modulus machinery and measured the walls the framing explains; verified non-deferentially against source and physics, and cross-checked by the gate.

## VERDICT: YES, this is the correct fundamentals to build off of.
The core is physically sound, citation-solid, and coherent, and the three load-bearing insights are correct and deep:
- **Charge is not an observable** (MgO: Bader +1.7, Mulliken +1.2-1.5, fitted-QEq +1.6 span ~0.8e; the only measured one, the Born effective charge via LO-TO phonon splitting, is ~+1.96-2.0, essentially formal). So +1.6 is a fitted-model property, not a property of MgO. Vindicates the refusal to fit and explains the QEq +2.08 over-ionization (no derive-first route to a non-existent target).
- **The EA2<0 boolean** (free O2- unbound, the anion Madelung-created) forecasts all three findings a priori: rung-1 oxide stiffness (periclase ~266 vs ~165), the Cauchy fingerprint (MgO C12~95, C44~156, a real violation; NaCl C12~C44~12.7 satisfied), and the free-ion escalation inversion. One boolean, three consumers.
- **Gordon-Kim/PIB** is the correct parameter-free rung and the citations are solid (Gordon-Kim 1972, Muhlhausen-Gordon 1981 Watson sphere, Boyer-Mehl-Cohen PIB, Mehl-Hemley-Boyer 1986, Isaak-Cohen-Mehl 1990, Wolf-Bukowinski 1988; built by geophysicists for these exact mantle oxides). The sqrt(2*IE) density-tail decay is the correct asymptotic; breathing is a volume-dependent many-body term, which is why it breaks the Cauchy relation and completes the triptych.
Citations otherwise clean (Rappe-Goddard, Mortier, Parr-Pearson, BKS Si +2.4, bond-valence, SPuDS, AIRSS, George 2020's 13%, the MLIP ~30 meV tier, Hashin-Shtrikman, von Bargen-Waff, Frost-Ashby, Dodson). The resolution-ladder rule is the correct coherence backbone (estimator ranks formation at the 100 kJ/mol scale, never polymorphs at units), and the energy-vs-curvature distinction is handled right (the disposer's ~5% Born-Lande ENERGY is fine for formation-ranking; the ~1.65x MODULUS overestimate is a separate second-derivative band).

## ONE FIX (Layer 1, off the materials path, verified by the gate)
The electron g-factor is written `g = 2 + alpha/2pi` (~2.00116). Schwinger's anomaly is `a_e = (g-2)/2 = alpha/2pi`, so `g = 2 + alpha/pi` (~2.00232, measured 2.002319). The spec halved it. A real factor-of-2 in a `[D]` value; correct it in the doc and when the constant is coded.

## THREE GAPS TO CLOSE BEFORE BUILDING PAST RUNG 1 (extensions the framing's own logic reaches toward, not contradictions)
1. **The shear/elastic TRIPTYCH is incomplete for the molecular and H-bonded classes.** Embedding (metals), Keating (covalents), breathing (Madelung-stabilized ionics) covers the rock-formers, but the energy plane already carries London and the E_HB ladder for MOLECULAR and H-BONDED solids (ices H2O/N2/CO2/CH4, in scope via the Pluto N2-CO self-check and argon C6), and those classes have NO shear/elastic term. Add a dispersion/vdW elastic route and an H-bond elastic route before emitting elastic properties for icy phases. Same shape as the "ionics: none was NaCl overreach" correction, one class over.
2. **Amorphous / glassy phases have no representation.** The assemblage machinery (prototypes, Madelung, Ewald, bond-valence positions) assumes crystalline order. A quench-frozen glass (silicate glass, obsidian, amorphous ice, real materials) has no lattice, so no Madelung and no prototype, and Stage-6 emission needs the density picture without a lattice. The freezer handles metastable CRYSTALLINE inheritance (diamond) but not a fully amorphous arrested state. Decide: explicit phase representation or acknowledged out-of-scope.
3. **The "one flow" is a self-consistent LOOP, and its deterministic convergence is load-bearing but unstated.** Positions need the energy (AIRSS relaxes on the classical energy; bond-valence distances depend on oxidation state), charges need positions (QEq over the Ewald matrix), energy needs both: positions <-> charges <-> energy is a coupled self-consistent solve, not feedforward. The solver law (pure, deterministic function) must converge this loop deterministically, and that requirement must be explicit, because a self-consistent loop is exactly where a determinism or convergence hazard hides (the class of the shielded-vs-bare-Ewald stability catch).
(The covalent-metallic sulfide middle is an already-acknowledged soft spot, not a missed gap.)

## Built vs specified (accurate)
BUILT: rung 1 (the ionic bulk modulus [E] with documented bias), the Ewald generator, the shielded QEq (demoted to disposer-coarse), the IE/EA columns. SPECIFIED-not-built: the EA2 boolean, the Cauchy diagnostic and the Cij tensor, Gordon-Kim/PIB rung 2, the breathing dispatcher, the triptych shear terms, the escalation gate. That ratio (mostly specified) is right for a foundation.

---

# RESOLUTIONS (owner, twenty-first audit): all four items closed

Owner (2026-07-12) resolved every item, each the same way the last two class gaps resolved (the class-completeness pattern) or with already-legislated machinery pointed at a new loop.

## g-factor: FIXED + a spec rule
`g = 2 + alpha/pi ~ 2.00232` (the anomaly `a = (g-2)/2 = alpha/2pi`, so `g = 2(1 + alpha/2pi)`); the spec halved the correction, a transcription error in a `[D]` value (the worst place, since derived entries are the ledger's bedrock). Materials-path impact nil now, nonzero later (EPR g-factors, precision magnetometry). SPEC RULE (routed to A's Phase-2 register): a `[D]` entry with a closed form STORES the closed form and RE-EVALUATES it in the validation battery, never hand-copied. The counts-are-queries rule extended to constants. Check-the-input applied to arithmetic.

## Gap 1 (triptych's missing legs): RESOLVED, and it promotes the Cauchy audit to a universal litmus
- MOLECULAR crystals (N2, CO, CH4) are rotationally-disordered PLASTIC phases through most of their stability fields, so orientationally-averaged dispersion is near-central: the rigid rung is in-band for B, and shear needs only a PLASTIC-PHASE HONESTY FLAG (soft against shear near melting because rotational entropy already paid the anisotropy away, C44 collapsing to the molecular-crystal floor). For low-T ORDERED phases (alpha-N2 quadrupole ordering) the class term is the ELECTROSTATIC MULTIPOLE LADDER (quadrupole-quadrupole for N2/CO2, octopole for CH4), computed from banked columns (molecular geometry + charge distribution, measured quadrupole moments as the `[M]` floor); EQQ lattice sums explain alpha-N2 [verify Scott 1976].
- H-BONDED ices: the H-bond is directional by construction (the chi_AR donor geometry), so ice inherits a KEATING-class angular term with bend stiffness priced off the banked E_HB ladder (`k ~ E_HB/theta0^2`), estimator grade, NO new fit. Plus a derived shear-anomaly floor: Pauling residual proton disorder (the ice rules, `W = (3/2)^N`, the banked contingency accident earning rent) + Bjerrum-defect migration in the g-framework give the anelastic softening the glacier module already consumes. Validation: ice Ih Cij [M, Gammon 1983], solid N2 elastic [M, verify].
- THE CAUCHY AUDIT IS PROMOTED to the standing universal bonding-class LITMUS: halides pass (central), oxides break by breathing, metals by embedding, covalents by Keating, ices by H-bond angularity, molecular plastics quasi-pass by rotational averaging. Each violation mechanism is a class tag; C12-vs-C44 sorts the class before any fitting.

## Gap 2 (amorphous/glass): RESOLVED, glass is the freezer working as designed
Glass is NOT a missing phase; it is the FREEZER's arrested liquid, represented in the WRITTEN-STATE registry (not the crystalline assemblage) as `(composition, fictive temperature T_f, path)`, all existing slots. `T_g` falls out of the banked Arrhenius machinery (where `tau_relax(T)` crosses the cooling timescale, the same closure that dated minerals and froze Jupiter's CO), so `T_g` is cooling-rate-dependent (obsidian vs pumice vs tektite at different `T_f`, a self-written record via hydration-rind dating). Properties by continuous-random-network: B within ~10-20% of the crystal, G softened. One new `[D-in-form]` constant: the Wright-Thorpe/Phillips RIGIDITY THRESHOLD, mean coordination `<r> = 2.4` separating floppy from rigid networks by Maxwell constraint counting, pure combinatorics on banked valences (silica <r>=2.67 makes strong glass; chalcogenides tune across; metallic melts crystallize unless frustrated, Inoue rules [E]). Glass-forming ability = TTT-nose competition (nucleation vs Arrhenius viscosity), estimator-grade with the CNT flag inherited. Written-state gains `T_f`. Consumers: obsidian conchoidal fracture (isotropy derived, why it knaps), slag/flux/glazes (glass tech), amorphous ice (Pluto).

## Gap 3 (positions <-> charges <-> energy loop): RESOLVED as a named inner solve with a convexity clause
Legislate the loop under the solver law: it runs as a NESTED CONTRACTION inside the stage-4 disposer (QEq's linear solve is convex and unique at fixed positions, the positive-definite shielded hardness matrix B built; positions relax at fixed charges; alternate), with fixed iteration order, lattice-quantized exchanges, warm-started along the continuation path, converging to exact lattice equality. Two failure modes PRE-DECLARED: (1) charge-transfer runaway at large separation (QEq's over-delocalization flag, now a loop hazard: dissociating fragments must charge-neutralize, enforced by a split-charge/atom-condensed ACKS2-class constraint [verify Verstraelen 2013]); (2) bistability near symmetry-breaking (charge-ordered vs uniform), which is a FOLD, handed to the banked continuation-plus-seeded-draw machinery. COST: the loop lives inside memoized disposer calls, so it prices per `(composition, E-bucket)` key, not per world-cell.

## Ledger delta (twenty-first audit)
Layer 1's `g` corrected to `2 + alpha/pi` with the re-evaluate rule [engine spec]. Layer 3 gains the multipole ladder [D-in-form / M moments], the H-bond Keating term priced off E_HB [E], the plastic-phase shear flag, the rigidity threshold <r>=2.4 [D-in-form], Inoue class rules [E], the loop's constraint clause. Written-state gains fictive temperature T_f. The Cauchy audit is promoted to the standing class-litmus in the validation battery. Layer 4 gains nothing.

Verify Schwinger 1948, Scott 1976 (EQQ nitrogen), Gammon 1983 (ice elasticity), Pauling 1935, Phillips 1979 and Thorpe 1983 (constraint counting), Inoue 2000, Verstraelen 2013, Angell 1995 (T_f/fragility).
