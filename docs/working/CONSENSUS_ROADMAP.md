# Consensus Roadmap: the status board (is X done yet?)

This file is the LIVING status board and nothing else: the authoritative "has X been built" lookup for the whole project. A question like "can people chop down trees yet?" is answered by reading this, not by asking. It is read at every session START and edited IN PLACE on STOP whenever a segment makes progress (flip a status, tombstone a done feature where it stands and point to where the work landed, deprecate or re-classify an item, or add a newly-flagged one). It is never append-only and unrelated segments are never touched. The stop-gate hook enforces this.

The prior reconciliation narrative (the headline reconciliation, Tiers A through E, the derive-from-fundamentals audit, and the dated build-history sections) moved to `docs/working/ROADMAP_REFERENCE.md`, still useful for the deeper order-of-work reasoning and the history; this file stays lean so the board never gets buried.

## Feature status board: is X done yet? (the single lookup)

This is the authoritative "has X been built" table, kept current so a question like "can people chop down trees yet?" is answered by reading this rather than by asking. Status is one of: DONE (built and proven in code), PARTIAL (some of it built, a named piece missing), GATED (built but off pending an owner calibration/decision), IN PROGRESS (being built now), NOT DONE (planned, unbuilt, with the arc that would build it). Update this on every arc that changes what is built.

**World liveliness and agency (a being acting in and on the world):**
- DONE: a being forages and eats plants of its own volition, steered by its evolved controller (the run-path locomotion + intake, armed in `--scenario full`/`living`).
- DONE: the food cycle plant loop, grow producer -> graze -> die -> corpse tissue -> decompose -> soil nutrient -> grow (the matter cycle + decomposer life + corpse deposit, armed `full`/`living`).
- GATED: the full REAL biosphere thriving (real per-plant food value T3, and creature metabolic survival) awaits the biosphere-balance calibration (`worldbuild.rs` T3 owner-gate; creatures starve at small body scale). The loop turns; making it thrive is calibration.
- DONE: a being invents a novel action (the discovery/ideation loop samples a candidate from its afforded primitives x perceived targets, armed `discovery`/`viability`).
- DONE: a being makes and uses tools, and cuts/crushes/extracts/digs/strikes matter with them (the made-world arc; CUT via shear, wear, breakage, craft).
- DONE: a being's convictions are shaped by its own lived experience (hardship erodes or hardens belief), and its convictions bias its behaviour (the experiential-conviction substrate; armed `--convictions`, founder-zero).
- DONE: language emerges and drifts; belief forms from evidence; gossip, enculturation, and move-by-move dialogue run; reproduction, inheritance, genetic drift, speciation, aging, and mortality all run.
- PARTIAL: a being picks a plant for its PARTS (not just grazes it) is unproven; the grazing path is done, the pick-for-resources seam is the biosphere-meets-made-world integration. FOUNDATION DONE (corrected-T3, Agent C, PR #112, `environ.rs` `set_producer_food`): whole-plant food value now DERIVES from the plant's own material composition at its real magnitude (the blind panel corrected the spec: carry the FULL de-normalized composition VECTOR, NOT a plant-side gross-energy scalar which would author digestibility=1; the per-consumer usable value emerges in the existing alien-clean `physiology::physical_intake`, folding it against the consumer's own assim/eta through the shared `bio.energy_density` reserve bridge). So an energy-dense plant feeds more than a woody one (`a_more_energy_dense_plant_carries_more_food_value_corrected_t3`), and a mana/silicon feeder is a data row. The consumption side now completes T3 (`locomotion.rs` INGEST): where a class's supply on a cell is a real producer-composition magnitude, INGEST eats it at `content = supply`, superseding the reserved `food_energy_density` anchor (kept as the fallback for composition-less supply), branched by a PER-CLASS `ResourceField::real_composition` marker (the cell's `producer_food` axes; per-tick derived, unhashed). The §9 correctness lens caught that a per-CELL marker over-scoped the supersession onto the always-written water axis; the per-class marker confines it to the food axes, so water and any non-composition axis keep the anchor. Byte-neutral: the four tracked pins hold bit-exact (they seed no producer food) AND `--scenario living` is byte-identical (hash 07d867a5; the water leak was unreachable there). The behaviour change is in `living`: producer cells now carry their real derived food value (`volume x bio.energy_density` [0,38] kJ/g) while composition-less supply keeps the `x3000` anchor. Counterfactual (verified): `living` ALSO collapses at the parent commit (peak 19, MIXED thirst+starvation), where the superseded version peaks at 10 and dies of PURE starvation, so the collapse is PRE-EXISTING (the biosphere-balance calibration: body scale / metabolic rate / productivity) and this change SHARPENS the starvation component rather than causing the collapse. Owner-gated, surfaced not tuned. The PER-PART follow-on (owner-ratified 2026-07-09) is the same derivation per part: a plant's parts (bark edible, woody core not, fruiting bodies as distinct organs) carry different compositions and a being chooses which part to eat.
- NOT DONE (owner-directed 2026-07-09): MULTI-CELL and sub-cell spatial granularity. A plant or creature that SPANS multiple cells, and a creature SIZED relative to a cell (larger than one cell) rather than a point-in-a-cell. This is the substrate the per-part food above and larger-bodied creatures both need: a multi-cell plant's parts sit in different cells, a creature bigger than a cell occupies a region. Couples to the coordinate/field model (today one entity per place tag) and the reach wire's `Coord3` separation; a spatial-extent datum on the entity, not yet built.
- DONE (Agent B sibling piece, PR #110): a world DATA-declares which affordance percepts its beings SELECT and in what order, by naming the fixed kernels (`AffordancePerceptRegistry::from_names`, `crates/sim/src/affordance_percept.rs`), with a tested opaque-slot invariant (the name resolves once and is never read downstream, so it cannot become a semantic hook). HONEST SCOPE (the blind framing panel corrected the prior overclaim): this makes the SELECTION among the existing kernels data; it does NOT make the affordance space or the kernels' CONTENTS world-declared or alien-clean. The kernel SET stays fixed Rust. Byte-neutral (no live caller; the world-build wire that reads scenario/manifest data into `from_names` is a coordinated follow-on on Agent A's `runner.rs`/`scenario.rs` surface).
- NOT DONE (Tier C, gated piece; affordance-percept contents): the `Sharpness` kernel is a purpose-laden COMPOSITE (`CapabilityKernel::Pierce` over tool geometry and hardness), so "piercing is a salient affordance" is AUTHORED as a percept rather than EMERGING. The framing (PR #111) fixed the approach: multi-axis composition must EMERGE from a learner over the single-axis transduction primitives under selection, routed through the discovery loop (`discovery.rs:61-63`, "NO coded primitive-to-affordance pairing"), so a composite is a DESCRIPTION of what emerged and a declared tree is at most a seed. Touches the discovery/controller surface (may intersect Agent A's percept-wiring); frame blind on its own PR when Tier B's live wire lands.
- PARTIAL (Tier B MECHANISM landed, PR #111): the `FracturePotential` reference stress was PERCEIVER-INDEPENDENT (one embodiment-wide scale, `emb.affordance_refs`), so a mite and a giant sensed identical breakability. Fixed at the mechanism level by the single-axis TRANSDUCTION form (`SingleAxisTransduction` / `ReferenceSource` / `TransductionRegistry`, `affordance_percept.rs`), which keys the reference on the PERCEIVER's own body axis (or a cited floor constant) through a typed `ReferenceSource` with NO literal field (the value-authoring line enforced by the type). Byte-neutral, no live caller, tested (a small vs large body reads the same matter differently; no literal terminal is representable). REMAINING (deferred, coordinated with Agent A): the live run-path wire feeding the being's `BodyPlan` into `perceive` at `runner.rs:4966`/`5126` (a signature and call-site change on A's surface, behaviour-changing, not byte-neutral).
- NOT DONE (flagged seam, R-XXX candidate; affordance-percept ceilings, framed PR #111): two honest bounds the composition work does NOT close, tracked so they do not go invisible. (i) The READABLE-AXIS ceiling: alien-admission is gated by floor-axis coverage, so a being whose native affordance needs an axis the floor does not model still forces the develop-the-floor path (the transduction substrate does nothing for it). (ii) The COMBINATOR-SET ceiling: the closed transduction/combinator kernel set (`TransductionKernel`, one kernel today) is fixed Rust, so a transform not in the set is a code change. Both are the honest limit.
- PARTIAL (framed PR #111, fork b; Tier B mechanism landed): the affordance composition substrate, corrected by the blind panel from an overclaim to an honest split. Tier B (single-axis transduction as data, a P9/P11 "sensor" keyed on the perceiver's own body, hard-schema value-line) is BUILT at the mechanism level (byte-neutral; live wire deferred, above). Tier C (multi-axis composition must EMERGE through the discovery loop under selection, never authored trees) is the deeper P8 fix, deferred as its own gated piece (above). HONEST HEADLINE: a P11 win over EXISTING axes plus an emergent-composition path for the P8 fix, with alien-admission gated by floor-axis coverage (the ceilings above). The "a NEW affordance formula is data" framing is RETIRED: a new fold over existing axes is data, a new AXIS is floor development, and multi-axis bundling must emerge.
- NOT DONE: **predation on the run path** (a creature perceives, chases, and kills another creature). Blocked on the being-perception percept. Arc: "Being-perception percept" (the keystone; same pattern as the conviction-percept).
- IN PROGRESS: the **perception-substrate arc** (reframed Arc 3, the prerequisite the being-perception percept consumes; on `claude/liveliness-arc`, PR #109). Slice 1, the REACH WIRE, is BUILT end to end, section-9 audited, and GATE-SIGNED-OFF as a byte-neutral opt-in substrate (no live caller yet; all four run_world pins hold): the general dimensionality-parameterized `geometric_spread` kernel (`crates/physics/src/laws.rs`, byte-identical to inverse-square at D=3), the channel reach registry (`perception_reach.rs`, dispatch by named id, never channel identity), the reach read (`received_reach`: 3D `Coord3` distance including the vertical z, structural dimensionality read, medium-sampled optical depth with emergent occlusion), and the run-path resolver (`resolve_reach`/`absorption_along`: reads the row, samples the medium's own absorption along the `Coord3` line, dispatches by kernel id). The section-9 five-lens audit (6 blind panelists) hardened one major (a frequency-dependent row now fails loud rather than silently reading the optical axis; the frequency-dependent absorption path is a reserved follow-on) plus nits (the representability bound derived from the `i32` cast, overflow-safe optical-depth accumulation). Flagged follow-ons: the medium-confinement substrate that sets D<3 for surface/duct signals (overlaps the Europa z-stacked-medium item below), the non-geometric propagation-law-as-data and the medium-aggregation-kernel-as-data (overlaps the AbioticField field-kind registry item below), the frequency-dependent acoustic absorption path plus its body-resonance frequency source, and the acoustic absorption axis the floor does not yet carry. Slice 2, the SENSORIUM-GATED MAGNITUDE PERCEPT, is BUILT (4 segments) and section-9 audited byte-neutral (all four pins hold), pending the gate's completion ruling: the transduction+discrimination floor family (`ResponseLaw`/`transduce`, `DiscriminationLaw`/`discriminate`, Linear/AbsoluteStep byte-identical to the existing linear-mul/`feature_bucket`), the percept module (`perception_percept.rs`: `sense` transduce->threshold->discriminate, `perceive` folding the read/not-read gate into one being-derived threshold), the optical-gain-derived / non-optical-reserved-fail-loud derivation, and the class-string-keyed percept-channel binding. The section-9 audit hardened two majors (the binding re-keyed off the positional `PerceptId` onto the stable class string; the optical derivation reworded to the honest lens-eye limit with lensless light detection a data row) plus flagged follow-ons (saturating/non-monotone response laws, the class->channel binding deriving from the substance's floor coupling, a light-absorption capability for lensless sensing, the per-channel anatomy-transduction kernel). The gate ruled fork (a) for the keystone: an explicit anatomy transduction-KIND marker retiring the `opt.refractive_index` placeholder borrow. Slice 3, the RECEIVER-SIDE VALENCE LEARNER core, is BUILT and section-9 audited byte-neutral (all four pins hold), pending the gate's completion ruling: `learn::being_signal_observation` mints a HARMS/BENIGN observation for a perceived being-signal through the SHARED minting the environmental-feature learner uses, so a signal's valence emerges receiver-side from the receiver's own correlated outcomes, never stamped at the emitter. The section-9 audit (6 blind panelists, core clean across all five lenses) hardened 8 minor/nit findings (a shared `observation_toward` helper so identity is structural; the reserved likelihood basis corrected to the empirical co-occurrence, since a being-signal is a harm predictor not a cause; the subject-namespace collision flagged as a keystone-wiring seam). Reserved for the keystone or shared A+B builds: the per-being likelihood estimator (shared with Agent B's composer), the per-axis noise floor and the harm-path eligibility trace (live-learner behaviour changes that move the pins), referential meaning (flagged limit), and the subject-namespace offset / `SenseChannelId`-fit (owner-held `SEQ_FIELD_BITS` packing). With slices 1-3 built and audited, the perception-substrate arc is COMPLETE pending the gate's whole-arc gate for the merge; the being-percept keystone that wires the percept live (consuming the reach, the sensorium-gated percept, and this valence learner, and doing the reserved estimator/eligibility/packing, sequenced with Agent B) is the payoff follow-on that unblocks predation on the run path.
- NOT DONE: **people hunt creatures and use their resources**, and **creatures hunt people**, and **creatures flee predators**. Same blocker: the being-perception percept, then the hunt/flee loop (perceive -> pursue -> strike a live being -> butcher). The strike/bite mechanisms exist but act only on matter/tissue underfoot; `body::strike` (wound a live body) is tested, not run-wired for being-vs-being.
- NOT DONE: **chop down a tree for wood/resources**. The discovery loop, CUT, and tools all exist; exposing a biosphere producer (a tree) as a cuttable affordance-target with a resource yield is the biosphere-meets-made-world seam, not yet driven end to end.
- NOT DONE: **war/raiding/violence as an emergent outcome**. `conflict_pressure` is computed and tested but has ZERO run-loop consumers; Crucible's "war emerges" has no mechanism. Arc: a group/territory/raiding system consuming `conflict_pressure`.
- IN PROGRESS (Agent B, #113): lifespan DERIVED from anatomy (owner directive R3). R-AGING resolved 2026-07-09 to form (c): lifespan is the FIRST-PASSAGE time of a per-part damage accumulator against each part's material tolerance, fed by the existing floor insults (Archard wear, toxin, thermal, corrosion, starvation) minus a tissue-turnover repair flux, with NO size-duration exponent written (the size-longevity slope is a pure output). Metabolic throughput stays energy-drain, not a wear coefficient. The authored `Race.lifespan_years` (option A) stays the fail-loud interim scaffold until (c) validates; the one new floor axis (tissue-turnover repair rate, reserved-with-basis) is the gating dependency. De-biaser wf_5cdb2a3a overturned the reviewer's (B1) lean (which rested on a nonexistent repair axis and reproduced the contested mass^0.25 by construction). Residual owner-taste open downstream: the default size-longevity correlation strength, and whether the coupling strength emerges from selection.

**World build and calibration (standing a world up under `Profile::Calibrated`):**
- IN PROGRESS: **Mirror** (Earth 1:1, the owner-gated control) and **Tempest** (Mirror + `.high` dials). The loader is mostly present (`build_dawn_runner` takes a Profile + ScenarioResolution and arms the social/belief/language/reproduction/biosphere-supply layers fail-loud). The 6-domain Earth-value research is done (`docs/working/MIRROR_CALIBRATION_RESEARCH.md`, 162 cited proposals). DONE (Arc 1 slice 2): the opt-in learning/discovery/conviction/tool features moved onto the canonical `build_dawn_runner` path, armed fail-loud from the manifest and GATED by a scenario `[features]` block (`ScenarioFeatures`); `from_manifest` threaded for `DiscoveryCalib` and `FeltConvictionCalib` (the reward one already existed), with dev-fixture and reserved-with-basis manifest entries. Proven: the loader refuses to build under `Profile::Calibrated` while its values are reserved and builds under Development (the fail-loud calibration gate that maps Arc 2); the FEATURE-block fail-loud is proven end to end separately (a feature armed with only its own calib reserved refuses at the feature arming). Remaining: set the reserved values and boot Mirror/Tempest under Calibrated (Arc 2).
- DONE (assessment): the world substrate-readiness map (`docs/working/WORLD_SUBSTRATE_READINESS.md`), verified against source.
- PARTIAL: the four universal physical constants (Stefan-Boltzmann sigma, Coulomb k, vacuum permeability mu_0, gas constant R) to CODATA, coupled to the R-UNITS-PIN per-quantity fixed-point scales; big G to be forward-declared (no consumer until orbital mechanics; little g stays per-world). DONE (Arc 2 seg 1): Stefan-Boltzmann set to CODATA (the one constant with a live sim consumer, plus six other derive-audited floor/world scalars); Coulomb k, mu_0, R await the units-scale wiring (`crates/units`). Arc 2 also RETIRED `metabolism.surface_emissivity`: the radiant thermoregulatory term now DERIVES emissivity from the being's covering `opt.emissivity` floor axis (`physiology::covering_emissivity`, `crates/sim/src/physiology.rs`), per-race differentiable, not a reserved scalar (derive-vs-author). Subsequent calibration segments set more derive-audited values (engine-units anchors, engine representability bounds, geometric/degenerate world data, social category-b data, category-b language curves, and the reviewer-approved coarse climate-productivity scaffold with the abstract-model limit noted), each verified against source and the floor registry. A climate segment reconciled the temperature-units seam: the worldgen temperature axis is normalised `[0,1]` but the physics needs absolute Kelvin (Stefan-Boltzmann `T^4`), so `Field::from_map_absolute` now maps the field to absolute temperature `T = mean + range*(normalised - 1/2)` from two RESERVED owner climate values (`climate.mean_surface_temperature`, `climate.latitude_temperature_range`), the dev fixture carrying the identity (byte-neutral, all four pins hold); this unblocks `saturation_t_ref` and makes the Kelvin thresholds consistent. With that in place, `hydrology.saturation_t_ref` was RETIRED: the Clausius-Clapeyron tangent's reference temperature now DERIVES from `climate.mean_surface_temperature` (the habitable-band midpoint the field is centred on), read by `EnvironCalib::from_manifest`. A third derive-correction RETIRED the GLOBAL `metabolism.medium_convective_coefficient` scalar in favour of per-medium DATA: the metabolism now reads the convective coefficient `h` from the being's own MEDIUM (`medium.{air,water,dense_toxic}` `convective_coefficient`, the `fluid.convective_coefficient` floor axis) at the being's cell via `MediumField::convective_at`, byte-neutral (uniform dev h, the medium field is not hashed). HONEST STATUS (from the §9 arc audit): `h` is flow-dependent (`h = Nu*k/L`), so the per-medium value is a LUMPED interim, not a full derive; the flow-dependent Nusselt derivation is DEFERRED (still flagged in the reserved bases), and two honest limits are documented (the drain reads current-cell `h` while the cached exchange rate reads spawn-cell `h`; the off-field zero is defensive). The per-medium differentiation is exercised by two new tests. The units-mechanism wiring for Coulomb k / mu_0 / R is DEFERRED (reviewer call): no live sim consumer, so it is forward-looking, and the sigma-precision fix would re-pin all four hashes for ~0.22% on a constant the code already factors around; flagged for the Mirror gate. FINAL Arc-2 segment IN PROGRESS: the owner signed off the Mirror dial-set (`docs/working/MIRROR_SIGNOFF_DECISIONS.md`) under a STANDING DERIVE-GATE (check "can this derive further?" before authoring anything). Batch 1 applied (owner values): `climate.mean_surface_temperature` 287 (pre-industrial), `climate.latitude_temperature_range` 60, `world.orbital_period_seconds` 31556952 (TROPICAL year, superseding the Julian 31536000), `physiology.thermal_half_band` 8 (owner resolution: attunement emerges via evolvable morphology, not the band), `metabolism.body_mass_kg_scale` 70 (Mirror human). The gated values were derive-gate-checked: `discovery.exploration_floor`/`surprise_gain` AUTHOR (the per-being disposition already derives from `Channel::Exploration`); `felt_conviction.retention` AUTHOR baseline + flag the bounded per-being memory-derivation; `behavior.controller_*` and `inst.crystallization_*` AUTHOR-NOW-DERIVE-LATER (mutator-allele channel / strength-scaling flagged); `genome.allele_presence_threshold` AUTHOR-IRREDUCIBLE. MIRROR BOOTS under `Profile::Calibrated`: the 21 keys on Mirror's disembodied boot path are set under the derive-gate (physical Clausius-Clapeyron hydrology tangent at the owner's 287 K; engine bounds generous; `medium.air` real-air; `evidence_ring_curve`/`field.body_exchange` as the authored curve/fallback with the per-being part deriving; `field.cell_size` flagged for the owner's map-scale intent), and a new test (`crates/sim/tests/mirror_calibrated_boot.rs`) proves Mirror builds and runs under Calibrated, deterministic and worker-invariant (population 7, bit-identical across widths 2/3/8). The boot is now EMBODIED: the founders carry body plans and an `EmbodimentGenesis` (air emergent, water submerged), so the metabolism/physiology path runs; `metabolism.respiration_transfer_coefficient` is set as a flagged INTERIM (a per-membrane gas permeability, the owner's real value surfaced) and `medium.water` as Mirror's real-water ocean profile. The embodied Mirror boots under Calibrated, deterministic and worker-invariant (population 7). Tempest's cranked dials select the `.high`/`.low` stress magnitudes the owner DEFERRED (Group A), so Tempest's boot is a Tempest-calibration follow-on (surfaced). The 5 group-E AUDIT-CATCHES are resolved (Prime Directive 2, corrected from their real bases, never the research-tagged value): `transmission.loss_rate` (stale evidence.decay_rate pin removed, re-based on its own generational erosion timescale), `axiom.group_aggregation_rule` (owner ruling recorded: equal-weighting now, member-variance derive flagged), `axiom.stubbornness_dogmatism_weight` (key-vs-wiring mismatch flagged: the live split is not fed from the manifest key, reconcile before setting), `transmission.loss_practitioner_floor` and `compose.emergent_proxy_weights` (bases already sound in reserved.toml; the wrong value was only in the research doc). Remaining: the Tempest boot (its cranked `.high` dials, per the reviewer). Calibration manifest: 117 set / 103 reserved.
- PARTIAL: the scenario name shaping world STRUCTURE. DONE (Arc 1 slice 1): worldgen + biomes are now scenario-SELECTED through a data-driven `WorldStructure` registry (`crates/world/src/structure.rs`), resolved from the scenario's `structure` name (`ScenarioResolution::world_structure`) and built into the map by the loader instead of `dev_default` regardless of scenario; Mirror/Tempest/Crucible declare `structure = "earth"` (the grounded triad), byte-identical. REMAINING: magic-structure (the scenario's `magic.laws` still shapes nothing, blocked on Part 34); exotic terrain modes (Crucible's patchy basins, Europa's z-stack) are registry entries added when their substrate arcs land; and the Earth structure's biome bands + worldgen params are still dev fixtures, not yet owner-reserved manifest values (an Arc-2-sibling follow-on).
- RUNS (Development) but design unbuilt: **Crucible** the substrate (salt, greenwell food, discovery, controller, convictions) runs; Crucible-the-design needs a patchy-basin terrain worldgen mode + the war mechanism above.

**The remaining canonical worlds (flagged substrate arcs, do NOT fabricate):**
- NOT DONE (Venus): the greenhouse/radiative energy-balance wiring (the Stefan-Boltzmann/Wien/optical-depth kernels exist in the physics floor but have zero consumers, so a planet's temperature is never derived from its atmosphere), and the corrosive-medium-to-tissue harm coupling.
- NOT DONE (Europa, blockers): a volumetric z-stacked medium/terrain (ice-over-ocean-over-seafloor; today one medium per (x,y) column, z=0 only), and a tidal-heating law + orbital/tidal axes (the ocean is liquid because of tidal heating). Plus a redox-couple chemosynthetic energy mechanism and a located depth/pressure caller.
- IN PROGRESS (Arc 5, Agent C, PR #112): the data-defined **AbioticField field-kind registry**. Segment 1 DONE (byte-neutral, `crates/sim/src/environ.rs`): the closed enum {Light, Water, Soil} gained a `DataScalar(u16)` OPEN arm that reads a world-declared `ScalarField` from the new `EnvironFields::data_fields` collection, plus a `ReadShape::Point` dimension on the binding, so an alien energy field (chemosynthetic/geothermal/mana) is a data row (`a_world_declared_energy_field_is_a_pure_data_row_that_caps_and_depletes`). The blind panel caught that the read-at-cell interface authored point-locality. Segment 2 was REFRAMED by its own blind panel (verified against source): co-limitation (needs both donor and acceptor, limited by the scarcer) is ALREADY the engine's Liebig-min, which segment 1 delivers, so a co-limited chemolithotroph is already a data row; the bespoke pairwise-difference operator is dropped. Segment 2 DONE (byte-neutral): the stock->biomass conversion (`AbioticBinding::biomass_per_stock`) and the stoichiometric drawdown (`AbioticBinding::stock_per_biomass`) are now PER-SOURCE `Option<Fixed>` defaulting to the registry-global (fixing the confirmed soil-global-conversion and implicit-1:1 seams; each reactant a separate source id, Liebig-min co-limited, so no role enum), proven by `a_source_converts_stock_to_biomass_by_its_own_per_source_rate_not_the_global` and `a_donor_and_acceptor_are_co_limited_and_drawn_by_their_own_stoichiometry`. Segment 3 CORE DONE (byte-neutral, the depth-independent floor-EMF yield): a redox source's conversion DERIVES from its couple's galvanic EMF (`AbioticBinding::redox_emf`, the two floor `chem.standard_potential` values through `civsim_physics::laws::battery_emf` = `E_acceptor - E_donor`, clamped at zero) times the RESERVED `AbioticSourceRegistry::emf_to_biomass` coupling (fail-loud sentinel, the owner's value, surfaced with the `dG = -nF*EMF` basis, never fabricated), proven by `a_redox_source_derives_its_yield_from_the_floor_emf_not_a_declared_number`, `a_non_spontaneous_redox_couple_powers_no_life`, and `a_redox_source_fails_loud_when_the_coupling_is_unset`. The enum-opening STRUCTURE (the DataScalar open arm, ReadShape, the registry, per-source conversion, stoichiometry, the redox-EMF core) is now COMPLETE and gate-signed-off; the T3 food-value derivation it feeds is complete and signed off (see the world-liveliness PARTIAL item). The one remaining piece, the corrected full-Nernst YIELD, is at FRAMING-DONE and HELD on the OWNER: the blind framing (verified against source) reshaped it well past the four seams. The gate ruled B out (folding per-lineage efficiency into the per-cell environmental yield breaks observer-independence P10) and ruled IN the physics/hygiene/determinism corrections (derive the thermal term from Boltzmann k_B and a per-couple carrier-charge axis not an authored molar R and Faraday F; KEEP the energy-to-biomass bridge since nFE is joules and the yield is biomass; clamp on NET free energy across coupled reactions not per-couple; a data-defined gamma activity registry with a non-Terran default; add dE0/dT; rename the electrochemical Faraday off the law.faraday_emf collision; derive R_s from a universal R and molar mass; guard ln(Q) and pin the Q/T field-step order). HELD for the owner: the efficiency ARCHITECTURE (A, a downstream consumer scalar with a FLOOR conservation cap efficiency<=1 and a modeled antagonistic cost, versus C, throughput and its functional form EMERGING from the being's modeled metabolic machinery with no authored scalar), which the yield's kinetic-rate term couples to. The corrections are built into whichever architecture the owner picks; the floor growth would collide with A's active floor-reconciliation sweep. PHASE-1 PARTIAL (Agent C, PR #112, commit 16df1d5): the two PURE physics laws are built and unit-proven as byte-neutral additive functions with no caller yet (`physics/laws.rs`): `nernst_emf` (E = E_standard + (RT/nF)(ln a_donor + ln a_acceptor), reduces to standard at unit activity, collapses when depleted) and `reversible_uptake_flux` (v = Vmax*Hill(S)*drive, drive = 1 - exp(-nE/(RT/F)), zero at/below equilibrium, min(v,S)); both are Fixed::ln/exp/powf, integer-only and pinned, so the ln-determinism (the gate's load-bearing check) holds. RECONCILIATION RESOLVED (audit-the-input catch, gate-confirmed): the catch was right and split cleanly, efficiency architecture C (throughput EMERGES from the being's own catalyst tissue, `Vmax = kcat*catalyst`, no authored efficiency scalar; the roadmap's "HELD" was stale, the owner's de-biaser resolved it to C) and the framing's ruled-IN EMF corrections RESTORED. Commit 05f3486 applies the LAW-LEVEL corrections: the thermal term is now the per-particle `k_B*T/q` (Boltzmann `k_B` a floor fundamental times a per-couple carrier-charge `n*e`, NOT molar R/Faraday, which also DISSOLVES the floor-constant collision, no new thermal constant), a new `standard_potential_at_temperature` law adds `dE0/dT`, and `reversible_uptake_flux` keeps `Vmax` a caller parameter so architecture C is wired by the caller not presumed. Three unit tests, physics lib 96 passed. PHASE 2 REMAINING (the wiring): the gamma activity-coefficient registry (non-Terran default), the catalyst-tissue `Vmax = kcat*catalyst` derivation, the per-source-class kinetics registry (kcat/Km/Hill h as data), the per-substance carrier-charge floor axis (sibling of `chem.electron_count`), the NET-free-energy clamp across coupled sources, and the two-site draw wiring into `effective_conversion`/`RedoxEmf` and the draw; byte-neutral by absence (Earth binds no redox source), verified against the four pins. Unblocks chemosynthetic (Venus/Europa), geothermal/redox (Europa), and mana (Arcanum) sources.

- DONE (Arc 2, Agent C, PR #112, byte-neutral opt-in): a surface with proper **day-night heating**. The owner ruled the third form (a general closed-form sun-angle law, not the interim-vs-substrate fork) and it is BUILT and WATCHABLE in `--scenario living` (a ~31 K equatorial diurnal surface swing that EMERGES from the field's relaxation-plus-diffusion, night side held on the back-radiation floor never 0 K). The light side (`environ.rs insolation_at`): the sun-angle `cos(zenith)` law with per-cell LONGITUDE and the SYNODIC hour angle over a data star-list, declination from per-world obliquity; the default is the zero-obliquity single-star REFERENCE world (labeled NOT-Mirror). A firewalled private phase counter (never exposed to any behavioural substrate, so a diurnal rhythm emerges from selection over the cycling fields, never a clock). The heat side (night-floor form (2), gate-ruled over the author's (1)): `solar_baseline = radiative_equilibrium(insolation*solar_constant + back_radiation, emissivity, sigma, t_max)`, the per-world back-radiation floor giving the Moon-Earth-Venus swing spectrum; `apply_diurnal_baseline` copies it into the temperature `Field` baseline (additive `Field::set_baseline_at`/`width`/`height`), the field's relaxation produces the emergent swing and lag. Opt-in: unarmed the light is the static latitude map and the four determinism pins hold bit-exact; armed only in `living`. The consumer-threshold audit CLEARED (the productivity Liebig satisfaction and the hydrology condensation/evaporation are graded/physical, no authored time-gate). Tests: the day-to-night swing, the warm-day/night-floor baseline. DONE (follow-on 1, Mirror's real tilt): `DiurnalSky::mirror` carries Earth's real 23.44-degree obliquity (0.4091 rad) as Mirror's per-world datum, so REAL SEASONS ride on the day-night through the declination term already in the law; the seasonal test proves the tilted pole takes the midnight sun at the summer solstice where the zero-tilt reference pole stays dark and reverses to polar night half a year on, the equator's diurnal swing surviving; the `living` demo now arms `mirror` and reads out the pole-vs-equator latitude structure with the warming pole reversing as the orbital phase advances; all four pins hold bit-exact. DONE (follow-on 2, per-material THERMAL INERTIA): a `SurfaceThermal` registry (fixed Rust, reserved per-material volumetric heat capacities `rho*c_p` for substrate/water/ice as data, dev-fixture Earth values, `environ.rs`) derives each armed cell's thermal-inertia factor from its OWN real state (soil moisture + standing-water saturation + freeze read, no lookup), and A's `Field::step` scales its relaxation-plus-diffusion by that per-cell factor (additive optional `Field::set_inertia_from`/`inertia`, `None` default so the step is byte-identical unarmed), so a water-laden or damp cell lags and swings less than dry land (the ocean/damp-soil effect), emergent from the cell's water content. The `living` readout shows dry-land vs water swing. Byte-neutral: armed only in `living`, the four pins hold bit-exact. Emissivity stays UNIFORM (per-cell ALBEDO is the real radiative differentiator, a separate follow-on flagged to A on the `radiative_equilibrium` floor law with the two-sided-Kirchhoff correctness question). FLAGGED follow-ons (remaining): per-cell ALBEDO + the two-sided emissivity on A's radiative law; the full per-column LITHOLOGY substrate (rock vs sand vs clay varying the dry substrate, the geodynamics arc); back_radiation deriving from atmospheric greenhouse optical depth. Then the corrected Nernst uptake-flux. PRE-MERGE: rebase onto current main and re-measure against the post-keystone pins (4bbf6b59 / 5aaa43f1 / c9d5cc17 / ad69f2bf).
- NOT DONE (Arcanum/Confluence): the **magic system Part 34** (MagicLaws / ManaSource / CostModel / LimitModel / MagicalTradition) is unbuilt pseudocode; "magic" is today a bool posture, a cosmetic mana-sac organ, and an unused genome channel. No mana field. Running `--scenario arcanum` produces a grounded non-magical world identical to Mirror.

**Engine fidelity and far horizon (tracked in Tiers C through E):**
- NOT DONE: temporal level-of-detail (R-TEMPORAL-LOD), event-driven agent execution (R-AGENT-EXEC), GPU offload of the hot loops, persistence/save-schema (R-SAVE-SCHEMA), the non-authoritative view elaboration (R-VIEW-ELAB).
- NOT DONE: the made world's deep tiers (Part 60 Stages 6 through 13) and the north star (emergent science, deep-tech, Stage 14).
- NOT DONE (owner-directed 2026-07-09, SUBSTRATE not authored): emergent pollution and climate change. Once industry emerges (the deep-tech tiers), its byproducts alter atmospheric composition, which through the greenhouse/radiative-balance wiring (the Venus item above) shifts the radiative balance and warms the world, all EMERGENT from the emergent industry plus the atmospheric/radiative floor, never an authored warming curve. Mirror starts at the pre-industrial mean (287 K) deliberately, so the warming has somewhere to go. Depends on: the radiative-balance wiring, a data-defined atmospheric-composition field, and emergent industry.
- NOT DONE (owner-flagged 2026-07-09): a genome-derived thermal SETPOINT (deeper attunement). Today the core setpoint is a fixed per-race baseline (310 K) and climate attunement emerges via evolvable body morphology (covering/mass/surface, the Bergmann/Allen path, which is how endotherms adapt). A drift-able genome setpoint would additionally let ectothermy, torpor, and variable-core-temperature lineages emerge; a substrate refinement, not needed for Mirror's endothermic humans.
