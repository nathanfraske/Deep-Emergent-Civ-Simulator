# Consensus Roadmap: the status board (is X done yet?)

This file is the LIVING status board and nothing else: the authoritative "has X been built" lookup for the whole project. A question like "can people chop down trees yet?" is answered by reading this, not by asking. It is read at every session START and edited IN PLACE on STOP whenever a segment makes progress (flip a status, tombstone a done feature where it stands and point to where the work landed, deprecate or re-classify an item, or add a newly-flagged one). It is never append-only and unrelated segments are never touched. The stop-gate hook enforces this.

The prior reconciliation narrative (the headline reconciliation, Tiers A through E, the derive-from-fundamentals audit, and the dated build-history sections) moved to `docs/working/ROADMAP_REFERENCE.md`, still useful for the deeper order-of-work reasoning and the history; this file stays lean so the board never gets buried.

## Feature status board: is X done yet? (the single lookup)

This is the authoritative "has X been built" table, kept current so a question like "can people chop down trees yet?" is answered by reading this rather than by asking. Status is one of: DONE (built and proven in code), PARTIAL (some of it built, a named piece missing), GATED (built but off pending an owner calibration/decision), IN PROGRESS (being built now), NOT DONE (planned, unbuilt, with the arc that would build it). Update this on every arc that changes what is built.

**World liveliness and agency (a being acting in and on the world):**
- DONE: a being forages and eats plants of its own volition, steered by its evolved controller (the run-path locomotion + intake, armed in `--scenario full`/`living`).
- DONE: the food cycle plant loop, grow producer -> graze -> die -> corpse tissue -> decompose -> soil nutrient -> grow (the matter cycle + decomposer life + corpse deposit, armed `full`/`living`).
- GATED: the full REAL biosphere thriving (real per-plant food value T3, and creature metabolic survival) awaits the biosphere-balance calibration (`worldbuild.rs` T3 owner-gate; creatures starve at small body scale). The loop turns; making it thrive is calibration.
- DONE: a being invents a novel action (the discovery/ideation loop samples a candidate from its afforded primitives x perceived targets, armed `discovery`/`viability`).
- DONE: a being makes and uses tools, and cuts/crushes/extracts/digs/strikes matter with them (the made-world arc; CUT via shear, wear, breakage, craft).
- DONE: a being's convictions are shaped by its own lived experience (hardship erodes or hardens belief), and its convictions bias its behaviour (the experiential-conviction substrate; armed `--convictions`, founder-zero).
- DONE: language emerges and drifts; belief forms from evidence; gossip, enculturation, and move-by-move dialogue run; reproduction, inheritance, genetic drift, speciation, aging, and mortality all run.
- PARTIAL: a being picks a plant for its PARTS (not just grazes it) is unproven; the grazing path is done, the pick-for-resources seam is the biosphere-meets-made-world integration. FOUNDATION building: corrected-T3 (Agent C) makes whole-plant food value DERIVE from the plant's own material composition (its substances' `bio.energy_density` floor axis), so a woody plant is less nutritious than a fruity one and an alien plant is a data row. The PER-PART follow-on (owner-ratified 2026-07-09) is the same derivation per part: a plant's parts (bark edible, woody core not, fruiting bodies as distinct organs) carry different compositions and a being chooses which part to eat. FULLER VISION (owner-articulated 2026-07-09, "a plant is a growing, depleting, multi-part matter body"): the derive-first spine is that a plant is a BODY of matter with parts, the same substrate a creature body uses (promoted parts, tissues-as-composition, located matter), so almost none of it is new mechanism. Already built and reusable: WHICH creature can eat WHICH part is the consumer's own metabolism (`physical_intake` keys on the eater's assimilation, so a cellulose-digester banks the woody stem and a fruit-eater does not, no plant-side edibility tag); the whole-plant value derives from composition (corrected-T3); and eating REMOVES matter (grazing depletes the cell producer supply, mass-conserving, and the matter cycle regrows it), though today at the cell not bite-by-bite from a part. The ONE piece that is new is the plant as a GROWING, DEPLETING INDIVIDUAL: it accumulates matter and its composition shifts as it ages (a sapling is not a mature tree), a bite drops its remaining value, picking a fruit removes that part; this turns a plant from a field supply into an individual body with a growth-and-depletion trajectory, and couples to the MULTI-CELL spatial-extent item below (a big plant spanning cells). Scope-as-one-arc when the owner wants it built.
- NOT DONE (owner-directed 2026-07-09): MULTI-CELL and sub-cell spatial granularity. A plant or creature that SPANS multiple cells, and a creature SIZED relative to a cell (larger than one cell) rather than a point-in-a-cell. This is the substrate the per-part food above and larger-bodied creatures both need: a multi-cell plant's parts sit in different cells, a creature bigger than a cell occupies a region. Couples to the coordinate/field model (today one entity per place tag) and the reach wire's `Coord3` separation; a spatial-extent datum on the entity, not yet built.
- DONE (Agent B sibling piece, PR #110): a world DATA-declares which affordance percepts its beings SELECT and in what order, by naming the fixed kernels (`AffordancePerceptRegistry::from_names`, `crates/sim/src/affordance_percept.rs`), with a tested opaque-slot invariant (the name resolves once and is never read downstream, so it cannot become a semantic hook). HONEST SCOPE (the blind framing panel corrected the prior overclaim): this makes the SELECTION among the existing kernels data; it does NOT make the affordance space or the kernels' CONTENTS world-declared or alien-clean. The kernel SET stays fixed Rust. Byte-neutral (no live caller; the world-build wire that reads scenario/manifest data into `from_names` is a coordinated follow-on on Agent A's `runner.rs`/`scenario.rs` surface).
- NOT DONE (Tier C, gated piece; affordance-percept contents): the `Sharpness` kernel is a purpose-laden COMPOSITE (`CapabilityKernel::Pierce` over tool geometry and hardness), so "piercing is a salient affordance" is AUTHORED as a percept rather than EMERGING. The framing (PR #111) fixed the approach: multi-axis composition must EMERGE from a learner over the single-axis transduction primitives under selection, routed through the discovery loop (`discovery.rs:61-63`, "NO coded primitive-to-affordance pairing"), so a composite is a DESCRIPTION of what emerged and a declared tree is at most a seed. Touches the discovery/controller surface (may intersect Agent A's percept-wiring); frame blind on its own PR when Tier B's live wire lands.
- PARTIAL (Tier B MECHANISM landed, PR #111): the `FracturePotential` reference stress was PERCEIVER-INDEPENDENT (one embodiment-wide scale, `emb.affordance_refs`), so a mite and a giant sensed identical breakability. Fixed at the mechanism level by the single-axis TRANSDUCTION form (`SingleAxisTransduction` / `ReferenceSource` / `TransductionRegistry`, `affordance_percept.rs`), which keys the reference on the PERCEIVER's own body axis (or a cited floor constant) through a typed `ReferenceSource` with NO literal field (the value-authoring line enforced by the type). Byte-neutral, no live caller, tested (a small vs large body reads the same matter differently; no literal terminal is representable). REMAINING (deferred, coordinated with Agent A): the live run-path wire feeding the being's `BodyPlan` into `perceive` at `runner.rs:4966`/`5126` (a signature and call-site change on A's surface, behaviour-changing, not byte-neutral).
- NOT DONE (flagged seam, R-XXX candidate; affordance-percept ceilings, framed PR #111): two honest bounds the composition work does NOT close, tracked so they do not go invisible. (i) The READABLE-AXIS ceiling: alien-admission is gated by floor-axis coverage, so a being whose native affordance needs an axis the floor does not model still forces the develop-the-floor path (the transduction substrate does nothing for it). (ii) The COMBINATOR-SET ceiling: the closed transduction/combinator kernel set (`TransductionKernel`, one kernel today) is fixed Rust, so a transform not in the set is a code change. Both are the honest limit.
- PARTIAL (framed PR #111, fork b; Tier B mechanism landed): the affordance composition substrate, corrected by the blind panel from an overclaim to an honest split. Tier B (single-axis transduction as data, a P9/P11 "sensor" keyed on the perceiver's own body, hard-schema value-line) is BUILT at the mechanism level (byte-neutral; live wire deferred, above). Tier C (multi-axis composition must EMERGE through the discovery loop under selection, never authored trees) is the deeper P8 fix, deferred as its own gated piece (above). HONEST HEADLINE: a P11 win over EXISTING axes plus an emergent-composition path for the P8 fix, with alien-admission gated by floor-axis coverage (the ceilings above). The "a NEW affordance formula is data" framing is RETIRED: a new fold over existing axes is data, a new AXIS is floor development, and multi-axis bundling must emerge.
- DONE (the being-percept KEYSTONE, live in every non-living run_world scenario, re-pinned `251f79d`; the four pins moved to default `4bbf6b59` / full `5aaa43f1` / discovery `c9d5cc17` / viability `ad69f2bf`, each deterministic, the re-pin founder-zero so foraging is unchanged and only the being-signal belief state moves): **predation and fleeing STEERING on the run path**. A being perceives another at a distance through its own thermal signature (`being_signal_emission`), learns from its own reserve outcomes whether that other predicts harm (predator) or reward (prey), and its founder-zero freely-signed controller weight decides approach or avoid, so predation, hunting, and fleeing are an emergent capability with nothing about the behaviour authored. (Was GATED-then-flipped this session; the PHYSICAL predation loop below (strike a live being -> butcher) is the follow-on.) The keystone that unblocks it (framed blind, gate-approved scope A both poles; `OWNER_DECISIONS_LOG` "FRAMED being-percept keystone") is built on `claude/being-percept-keystone` (#116): step 1 the being-signal subject band (`learn::being_signal_subject`), step 2 the reward-frame core, step 3 the being-directed perception (`perceive_being_signal` applying Beer-Lambert occlusion + threshold gating) and the two gradients (`being_avoidance_gradient`/`being_attraction_gradient`), step 4 the harm-path eligibility trace, and step 6 the LIVE WIRE (`step_embodiment` computes each perceiver's emission -> `resolve_reach` -> percept -> gradients into the controller through founder-zero FREELY-SIGNED weights; `advance_eligibility_traces`/`couple_conversation` learn HARMS/BENIGN [same-tick + lagged predator cue] and a same-tick REWARDS/NEUTRAL predation belief; `physiology::being_signal_emission = radiant_emission(body_temp) * reserved coefficient` is the emitter side). Armable by scenario data (`ScenarioFeatures.being_percept`, read fail-loud at the arming in `build_dawn_runner`); a `world_build` test proves arming wires the being block, changes the run, and replays bit-for-bit on a co-located band. So predation/fleeing STEERING can emerge from selection over the freely-signed weight. Byte-neutral for the four run_world pins (they declare no `being_percept`). REMAINING: (a) the ONE intended run_world re-pin (arming being_percept in the tracked scenarios so perception goes live there; needs the being block threaded through run_world's bespoke `dawn_layout` genome-seeding; posted to the gate for scope+sequencing, #116 comment 4932372907); (b) step 5, the per-being likelihood estimator, HELD for Agent B's shared build; (c) the PHYSICAL predation loop (strike a live being -> butcher), the follow-on below.
- DONE (MERGED via #109; the substrate now lives on `main` at `08a7cc1`): the **perception-substrate arc** (reframed Arc 3, the prerequisite the being-percept keystone consumes). Slice 1, the REACH WIRE, is BUILT end to end, section-9 audited, and GATE-SIGNED-OFF as a byte-neutral opt-in substrate (no live caller yet; all four run_world pins hold): the general dimensionality-parameterized `geometric_spread` kernel (`crates/physics/src/laws.rs`, byte-identical to inverse-square at D=3), the channel reach registry (`perception_reach.rs`, dispatch by named id, never channel identity), the reach read (`received_reach`: 3D `Coord3` distance including the vertical z, structural dimensionality read, medium-sampled optical depth with emergent occlusion), and the run-path resolver (`resolve_reach`/`absorption_along`: reads the row, samples the medium's own absorption along the `Coord3` line, dispatches by kernel id). The section-9 five-lens audit (6 blind panelists) hardened one major (a frequency-dependent row now fails loud rather than silently reading the optical axis; the frequency-dependent absorption path is a reserved follow-on) plus nits (the representability bound derived from the `i32` cast, overflow-safe optical-depth accumulation). Flagged follow-ons: the medium-confinement substrate that sets D<3 for surface/duct signals (overlaps the Europa z-stacked-medium item below), the non-geometric propagation-law-as-data and the medium-aggregation-kernel-as-data (overlaps the AbioticField field-kind registry item below), the frequency-dependent acoustic absorption path plus its body-resonance frequency source, and the acoustic absorption axis the floor does not yet carry. Slice 2, the SENSORIUM-GATED MAGNITUDE PERCEPT, is BUILT (4 segments) and section-9 audited byte-neutral (all four pins hold), pending the gate's completion ruling: the transduction+discrimination floor family (`ResponseLaw`/`transduce`, `DiscriminationLaw`/`discriminate`, Linear/AbsoluteStep byte-identical to the existing linear-mul/`feature_bucket`), the percept module (`perception_percept.rs`: `sense` transduce->threshold->discriminate, `perceive` folding the read/not-read gate into one being-derived threshold), the optical-gain-derived / non-optical-reserved-fail-loud derivation, and the class-string-keyed percept-channel binding. The section-9 audit hardened two majors (the binding re-keyed off the positional `PerceptId` onto the stable class string; the optical derivation reworded to the honest lens-eye limit with lensless light detection a data row) plus flagged follow-ons (saturating/non-monotone response laws, the class->channel binding deriving from the substance's floor coupling, a light-absorption capability for lensless sensing, the per-channel anatomy-transduction kernel). The gate ruled fork (a) for the keystone: an explicit anatomy transduction-KIND marker retiring the `opt.refractive_index` placeholder borrow. Slice 3, the RECEIVER-SIDE VALENCE LEARNER core, is BUILT and section-9 audited byte-neutral (all four pins hold), pending the gate's completion ruling: `learn::being_signal_observation` mints a HARMS/BENIGN observation for a perceived being-signal through the SHARED minting the environmental-feature learner uses, so a signal's valence emerges receiver-side from the receiver's own correlated outcomes, never stamped at the emitter. The section-9 audit (6 blind panelists, core clean across all five lenses) hardened 8 minor/nit findings (a shared `observation_toward` helper so identity is structural; the reserved likelihood basis corrected to the empirical co-occurrence, since a being-signal is a harm predictor not a cause; the subject-namespace collision flagged as a keystone-wiring seam). Reserved for the keystone or shared A+B builds: the per-being likelihood estimator (shared with Agent B's composer), the per-axis noise floor and the harm-path eligibility trace (live-learner behaviour changes that move the pins), referential meaning (flagged limit), and the subject-namespace offset / `SenseChannelId`-fit (owner-held `SEQ_FIELD_BITS` packing). With slices 1-3 built and audited, the perception-substrate arc is COMPLETE and MERGED (#109); the being-percept keystone that wires the percept live (consuming the reach, the sensorium-gated percept, and this valence learner, and doing the reserved estimator/eligibility/packing, sequenced with Agent B) is now IN PROGRESS on #116, the payoff follow-on that unblocks predation on the run path (the predation item above).
- DONE (MERGED #117 `0cee3a0`): **people hunt creatures and people, and people conflict with people, OBSERVABLY.** The being-perception percept is DONE (keystone merged #116, predation/fleeing STEERING live), and emission is species-blind so a person perceives and pursues a creature or another person identically. The WOUND on contact (making it observable, not latent) is Agent A's hunt-kill strike (#117): pieces 1 (the contact-transfer registry), 2 (the contact wound resolve), 3a (the STRIKE controller-consumption routing), and 3b (`Embodiment::strike_occupant`, the strike primitive over the target's largest-presented Segment) are BUILT, byte-neutral, and gate-signed, and the section-9 six-lens audit ran and is hardened (`df52889`; its two derive-first follow-ons are the flagged item below). Piece 4, the `Segment.damage` write, LANDED and MERGED: the wound writes B's accumulator in the exact aging convention (delivered energy vs `failure_tolerance = fracture_energy * contact_area`, no `* 1000`), one currency for wounds and aging, death through the one unified INTEGRITY cull. Gate-verified byte-neutral both sides, and the four pins are UNCHANGED post-merge (`4bbf6b59` / `1db633b3` / `c9d5cc17` / `ad69f2bf`, the strike armed only for a PIERCE-bearing predator absent from the four scenarios). A predator wounds the pursued prey on the run path: predation OBSERVABLE, not latent. The watchable predation demo defers to the creatures-react arc (#118, which builds co-located predator and prey). The swing-velocity derive-vs-author seam ships on the inherited interim; its stroke-rate/limb-biomechanics substrate is the flagged follow-on (#119, frame-blind in progress), plus the wound-side resistance-kernel registry for non-Griffith bodies.
- PARTIAL (1: steps 1 and 1b DONE and MERGED, step 2 STAGED) / NOT DONE (2) (derive-first follow-ons surfaced by the strike arc's section-9 audit, 2026-07-10): (1) the **stroke-rate / limb-biomechanics substrate** (branch `claude/stroke-rate-substrate` / #119, #123): `swing_velocity` was a per-being kinematic quantity authored as one world-global scalar (its basis names limb-length and stroke rate), so a mite and a giant swung at identical tip speed and the dominant velocity limb lost the per-being physics. The frame-blind resolved it: the section-10 panel proved the naive `v = sqrt(2 F L / m)` form is an identity (fed to `1/2 m v^2` the mass cancels to `F L`, and that reduction hides an authored acting-distance-to-length ratio of one, a per-body geometry value outside the floor), so the gate approved the direct work form. **STEP 1 MERGED to main (PR #119, merge `80e5ec1`) and byte-neutral** (the four pins hold `4bbf6b59` / `1db633b3` / `c9d5cc17` / `ad69f2bf`): the delivered kinetic energy is now the ACTUATOR WORK `F d` (`laws::actuator_work`), the actuating force (strength over cross-section, promoted to newtons by `laws::stress_force`, the section-9 MPa-to-N catch) times the acting part's own grown stroke, read per-body off the channel row's axes through one `acting_force_and_stroke` seam shaped so the owner-ruled tool-geometry payoff (follow-on b) drops in additively; both strike paths (`strike_occupant`, `strike_underfoot`) and the capability IMPACT read derive it from the being's own body, the world-global `swing_velocity` / `reference_swing_velocity` are retired, and the `mech.stroke_length` floor axis is declared. **STEP 1b MERGED to main (`bcd7d66`, PR #123), section-9-hardened, byte-neutral.** Current `main` (`959598a`, the byte-neutral #121 R-SOURCE-VECTOR lift) was merged into the branch non-destructively (`09baaae`, clean auto-merge), the four pins RE-CONFIRMED against main's values (`4bbf6b59` / `1db633b3` / `c9d5cc17` / `ad69f2bf`), and the gate merged it. The body-development program grows `mech.cross_section_area` and `mech.stroke_length` per-segment (`MorphogenProgram::actuator_axes`, their params appended AFTER the composition block at the end of the param layout, so no existing geometry/material/branch/spawn/composition index shifts), so GROWN bodies now deliver a non-zero `F d` blow; the four pins hold and the full sim suite is green. The byte-neutral guarantee (corrected by the step-1b section-9 second pass): no run_world scenario installs a morphogen program, so `grow()` never runs on the pinned path, and a grown body's effect reaches `state_hash` only through the dynamic state it drives (position, reserves), never a structure fold (the runner folds no `walker.structure`; `Structure::hash_into` has no run-path caller). Because the actuator block is now last, the composition energy/water axes are addressed through the program's own `pub composition_param` accessor: three founder-seeding fixtures (`aging_race`, `fully_grown_race`, the `aging_demo` example) that had reached the composition block by `param_count()-N` were re-pointed to `composition_param(...)` (that hand-rolled offset, not the mechanism, was the drift the actuator append exposed; the aging test starved its founders at zero energy/water under the stale addressing until repaired). **STEP 2 IN PROGRESS on #124 (the actuation-kind substrate, frame-blind DONE and GATE-SIGNED-OFF, owner-decisions R15).** The frame-blind (section-11 smoke then section-10 panel, unanimous) REJECTED the gate's first sketch (a discrete grown actuation-kind selector picking a kernel) as the template case that would reintroduce the retired `MorphCategory` gate, and the gate signed off the continuous-axis reframe, build shape (b): NO grown categorical selector; extend `contact_transfer`'s `TransferKernel` as an OPEN axis-id-keyed set with grounded delivered-energy laws each GATED TO ZERO by the segment's continuous grown axes, `F d` (`Kinetic`) the rigid limit, so a rigid lever, an elastic recoil, and a hydraulic jet are an emergent DESCRIPTION of which continuous axes are nonzero. NO new floor axes (all verified present: `mat.elastic_modulus`, `mat.yield_strength`, `mech.restitution`, `fluid.driving_pressure`, `fluid.immersed_volume`, `fluid.bulk_modulus`, `fluid.channel_radius`); one or two new grounded LAWS (elastic-recoil `1/2 k x^2`, hydraulic `integral P dV`), reserved-with-basis. SLICE 1 (the run-all-gate-to-zero substrate, byte-neutral): `contact_transfer::resolve_delivered_energy(geo, mat, row, cap)` reads the acting part's own grown axes through accessors and resolves the kernel set (Kinetic only today, `stress_force` then `actuator_work`), replacing the pre-derived `resolve_transfer` + `acting_force_and_stroke`; the strike wire passes the Segment's accessors, the tool-geometry follow-on (b) wraps them at the caller. Cross-kernel aggregation (max vs sum, the double-count that a spring's stored energy IS its loading work) is DEFERRED to the slice that lands kernel two, settled with the gate then. SLICE 1 gate-SIGNED-OFF (`7b8012b`, section-9-hardened). SLICE 2 BUILT (the `capability.rs` Impact axis-id-from-registry fix, byte-neutral): `FunctionLawDef` now carries `geometry_axes`/`material_axes` as DATA (defaulted to the kernel's contract via `FunctionLawDef::new`), the grade path's IMPACT kernel reads its strength/cross-section/stroke axis ids off those bindings (the delivery-path parallel, so an alien actuator names its own axes on both paths in lockstep), closing the parallel hole; the other capability kernels still read their hardcoded contract (flagged follow-on). SLICE 3 BUILT (the elastic-recoil kernel, byte-neutral, the gate RULED the aggregation): 3a the pure `laws::elastic_recoil_energy` (the modulus of resilience `yield^2 / (2 E)` over the strained volume, gating on absent material; `745266c`); 3b `TransferKernel::ElasticRecoil` wired into `resolve_delivered_energy` as the run-all-gate-to-zero MAX over the shared-metabolic-source mechanical family {rigid `F d`, elastic recoil}, each self-gating on the segment's grown axes (the gate CONFIRMED MAX over SUM: the two are alternative delivery paths for ONE metabolic source, a spring's stored recoil energy IS the muscle work that loaded it, so SUM double-counts; the independent-reserve additive case is flagged NOT folded in, compile-enforced by the exhaustive `Kinetic | ElasticRecoil` match); the strained volume is the SWEPT actuator volume `cross_section * stroke` (the two rigid-path geometry axes reused, no new axis), a PROXY for the elastic element's own volume with a dedicated `mech.elastic_element_volume` axis flagged as the reserved-with-basis refinement; 3c the capability IMPACT grade reads the SAME MAX (grade and delivery in lockstep, the gate's ruling iv). Four pins hold bit-identical. The COMPLETE byte-neutrality reason (the section-9 confirmation-bias lens caught that my first two justifications each named a secondary cause and missed the primary; verified against source): the delivery path is off all four pins by construction (strike opt-in); on the grade path, PRIMARILY, no pinned affordance registry (`dev_default` for default/discovery, `dev_geophage` for viability/full, run_world.rs:255/264/268) arms an IMPACT-gated affordance (POUND `requires ID_IMPACT` lives only in `dev_striker`, never installed by run_world), so `tool_capability(..., ID_IMPACT)` is never invoked and the IMPACT grade (with its new elastic term) never runs on any pinned scenario. Defense in depth: even if it were armed, `tool_capability`'s `geo` exposes `mech.contact_area`/`mech.mass` but never the `mech.cross_section_area`/`mech.stroke_length` the IMPACT kernel reads, so IMPACT-via-tool is pre-existingly zero (both rigid and elastic) regardless of the tool substance's `mat.elastic_modulus` (iron carries it); the grown-segment path (`morphogen::segment_capabilities`) supplies that geometry but grows no `mat.elastic_modulus` and its `grow()` runs on no pinned scenario; the catalog-part path scores only PIERCE/LOCOMOTE/REFRACT, never IMPACT. The grade/delivery LOCKSTEP the gate ruled (iv) holds at the SEGMENT level (a being's own actuator: `segment_capabilities` and `strike_*`'s `resolve_delivered_energy` read the same segment axes and the same MAX); the `tool_capability(ID_IMPACT)` path is pre-existingly zero (a separate step-1 seam, a tool exposes mass not cross_section/stroke), flagged not fixed here. Section-9 five-lens plus a mutation-verified springy-actuator probe on both paths. FLAGGED follow-ons (section-9, verified): (1) `capability.rs`'s `ENERGY_GUARD = 1e9` is a hardcoded representability cap where the file DERIVES its pressure/depth caps from the registry (pre-existing, extended here, nil practical impact; an energy-dimension cap from the floor is the derive-first fix); (2) the grade path binds axes POSITIONALLY while the delivery path uses NAMED fields, so a purely-springy alien binding that omits the absent rigid-strength slot is positionally mis-read (this slice extends the positional binding to three material axes, sharpening the gate-deferred named-vs-positional unification); (3) an independently-charged elastic actuator (a turgor/photosynthesis spring, not muscle-loaded) is under-counted by the shared-source MAX, the independent-reserve flag reframed by energy-SOURCE independence, not channel type. STILL STAGED: the hydraulic `integral P dV` slice (slice 4). The deep-`Body` wound path `body.rs:1108` still authors a swing velocity (sibling seam, coupled to the body-to-Structure bridge, a separate arc). Prime Directive 6: develop-the-substrate, not authored around. (2) the **resistance-kernel registry**: the wound-side resistance is a single mechanical Griffith fracture, NOT kernel-dispatched like piece 1's delivery law, so a non-Griffith integrity body (plasma, mana, field) needs a code change not a data row; the symmetric fix is a resistance-kernel registry (the sibling of the contact-transfer registry), coupled to the non-kinetic delivery kernels. The floor carries only Griffith and only kinetic today, so both are honest gap-flags. Candidates for a downtime agent.
- DONE (CAPACITY, MERGED #118 `373e0d8`; the emergent reaction awaits the creature-selection slice #120): **creatures hunt and react to people, and flee predators, the perceive-and-move CAPACITY.** The asymmetry today: a person perceives creatures (emission species-blind), but a creature EMITS yet cannot PERCEIVE, because the being-percept perceive loop is gated on `world.mind(w.id)` (`runner.rs:5287`) and a creature (a `Walker` tagged `CREATURE_ID_TAG`) has no mind by design. FRAME-BLIND DONE and it REVISED the premise (section-11 smoke, five rounds to CLEAR; section-10 panel, five of five returning convergent, verified against source; `docs/working/CREATURES_REACT_ARC_PLAN.md`, decisions R12/R13): the queued "creature forms a predator/prey BELIEF like the founder" is REJECTED as mechanism (i) on two grounds, that a within-life belief store is a rewrite of a definitionally non-learning tier (admit-the-alien) and that reading a committed labelled valence to produce the movement is the template-case authoring pattern plus a closed enum (P8). The resolved framing: base is mechanism (ii), the raw perceived signal (channel + the creature's OWN discrimination bucket, no valence, no belief) into a controller direction slot through a heritable freely-signed weight, the toward/away coupling set by cross-generational SELECTION (the template-case cure, native to the creature's genome-expressed controller); within-life plasticity (iii) folds in as a heritable genome-expressed plasticity coefficient on the same weight, defaulting to zero (reduces to ii), its direction a freely-signed selected parameter, so whether and which way a lineage learns is a selected emergent trait (P9). The decisive shared site (the magnitude bucket) is VERIFIED clean (it derives from the perceiver's own discrimination step, `perception_percept.rs:70,89`, not a global grid); build constraint: the creature carries its own transduction. Flagged: an inherited founder-path committed-valence question (R13, for a separate audit) and the `trophic_label` shortcut to avoid (`biosphere.rs:185`). GATE RULED the framing (adopt (ii)+folded-(iii), reject (i)), the build fork (sub-question 1 = (a) the belief-free creature branch; sub-question 2 = B3 magnitude-graded first, B2 per-bucket the follow-on, B1 rejected), and the transduction sourcing (declare per-species as data now, derive-from-anatomy a shared follow-on). BUILT + section-9-hardened + byte-neutral: the B3 SCAFFOLD (`learn::creature_being_direction`, the pure magnitude-graded toward-direction; section-9 caught a real distance double-count, fixed to magnitude-as-sole-distance-factor + a direction-preserving length-clamp) and the BLOCK-FILL WIRE (flag OFF: `Embodiment.creature_being_percept`, `learn::perceive_being_magnitude`, and the perceive-phase creature branch that fills the being block's attraction pair for a mind-less creature; section-9 clean, 14 clean-notes + 4 test/doc nits fixed). The branch is merged current with `main` (carries #115 hybrid key + #117 strike); all four pins HELD flag-off (default `4bbf6b59`, full `1db633b3`, discovery `c9d5cc17`, viability `ad69f2bf`). LIVE WIRE DONE and it surfaced a key finding, gate-accepted: arming `creature_being_percept` in run_world `full --creatures` is LIVE (all 131 spawned creatures perceive a being and get a non-zero magnitude-graded pull at tick 0, the branch fires end to end) but BYTE-NEUTRAL at FOUNDER-ZERO (`full --creatures` unchanged, `accc9fe6`; no re-pin), because the creature reuses the already-widened being block (no layout shift, unlike the founder keystone whose re-pin came only from widening the layout) and its being-weight is founder-zero (the approach/flee sign must emerge from selection, never authored, P9), so a real pull times a zero weight is zero movement. The observable REACTION emerges only when creature SELECTION lifts a being-weight (the reproduction/behaviour-selection slice, which Arc-7 flags "behaviour selection awaits the reproduction slice"). The (B) WATCHABLE DEMO (`crates/sim/examples/creatures_react_demo.rs`) proves the movement the founder-zero run cannot show: with a NON-ZERO weight injected test-only, a HUNTER (positive) steps toward the emitter (distance 12->0) and a FLEER (negative) steps away (12->26), both signs reachable from one wire, deterministic; a CI test (`controller.rs::a_being_attraction_weight_steers_...`) pins it. So the creatures-react CAPACITY is COMPLETE and byte-neutral (four canonical pins untouched; `full --creatures` unchanged at founder-zero). NEXT ARC (queued, Agent B): the CREATURE REPRODUCTION / BEHAVIOUR-SELECTION slice, the payoff that lifts the creature weights through selection so the reaction EMERGES and becomes observable in the shipped world and re-pins `full --creatures`; frame-blind it (emergence-critical) on its own PR off main. Then B2 (per-bucket discrimination) is the fuller follow-on. PR #118.
- NOT DONE / QUEUED (the CAPSTONE, owner-directed 2026-07-10, sequenced after day-night + strike + creatures-react land): **the integrated living-world scenario.** One watchable run arming day-night heating, seeded creatures, placed sentient bands, tool use, people-conflict, and creature-reaction together (extend `--scenario full` or a new scenario), so the whole living world can be watched at once. The owner's stated morning ideal; assigned to whichever agent frees up first.
- NOT DONE / RESEARCH (owner-directed 2026-07-10, a scoping workflow running overnight): **make the world's geology and geography come alive** (erosion, volcanism, biomes, rivers, lakes, continents, mountains). A derive-first arc proposal grounding each subsystem in the physics floor (what the floor carries, what it would grow, how it emerges rather than is authored, the owner-decisions surfaced), for the owner's morning review. Today's worldgen geography is largely static; this arc set makes it dynamic.
- NOT DONE (owner-directed 2026-07-09, the keystone follow-on to BUILD RIGHT AFTER the thermal keystone): MULTI-CHANNEL PERCEPTION (reflective VISION and acoustic HEARING). The reach wire is CHANNEL-GENERAL by design (verified: `ChannelReach` is a data row with `source_power_axis`, `absorption_axis`, `frequency_dependent`, in a `ChannelReachRegistry`), and the receiver, reach, occlusion, and valence learning are identical across channels, so vision and hearing are DATA-ROW extensions, not new mechanisms. The keystone builds THERMAL first only because it is self-sourced (no ambient light, no channel extension). VISION is a second emission MODE on the optical channel: a being is seen because it REFLECTS the ambient light field, which already runs live (`latitude_light`); the one new piece is the body's reflectance (albedo) as the source, `emission = ambient_light * reflectance`. HEARING is a new acoustic channel (a registry row); the acoustic physics already exists in the floor (`law.speed_of_sound`, `law.acoustic_absorption`, `law.tube_resonance`, built for the language substrate), so the new pieces are wiring the channel and a being's sound-emission source. The shared prerequisite for all three emission sources (emissivity, reflectance, sound-power) is the per-body COMPOSITION VECTOR on the run path (the plant-as-a-body / composition arc); once a being carries its own material, all three derive from it and each channel is a pure data row. For VISION specifically, the day-night lighting arc below makes the ambient light cycle.
- DONE (MERGED #112 `3c497f9`, the owner's overnight priority): DAY-NIGHT SURFACE HEATING, LIVE on main and watchable in `--scenario living` (a diurnal insolation-to-temperature swing ~19-24 K, Mirror's real 23.4-degree obliquity giving seasons, per-material thermal inertia so water lags rock, a back-radiation night floor so night never hits 0 K), gate-verified byte-neutral (opt-in, the four pins hold). The de-biased third form: a diurnal phase from the existing rotation period, one general sun-angle law `insolation = sum over the data star-list of L_s*max(0, cos theta_s(t))` with per-cell longitude and the synodic hour angle (so a tidally-locked or binary world is a data row), heat EMERGING on the existing diffusing Field by relaxation-plus-diffusion. Flagged follow-ons: per-cell albedo and the two-sided-Kirchhoff `radiative_equilibrium` fix (sequenced with A's floor law), the per-column lithology substrate. Original wiring notes: mostly WIRING existing pieces: the rotation data ALREADY EXISTS (`celestial.rs` `rotation_period_seconds`, "the length of a day", Earth default 86400), the radiative-balance kernels exist but are DEAD (no consumer derives surface temperature from insolation), and `latitude_light` varies by latitude but takes NO time argument. The arc CONSUMES the existing rotation to drive a diurnal cycle: the sun's position sweeps by rotation, giving local insolation (latitude times time-of-day), which feeds local LIGHTING (give `latitude_light` a sun-angle/time argument so it cycles day-night) and SURFACE HEAT (wire the dead `radiative_equilibrium` kernel so temperature RISES in daylight and falls at night, a diurnal swing, replacing the read-off-the-axis baseline). DAY-NIGHT then emerges. This feeds VISION (the ambient light cycles, so a being is more visible by day) and closes part of the "no temporal weather" gap. The next free agent runs a derive-first scope pass (what wires vs what is new, reserved values surfaced-with-basis) then builds it or reports what it would take.
- NOT DONE: **chop down a tree for wood/resources**. The discovery loop, CUT, and tools all exist; exposing a biosphere producer (a tree) as a cuttable affordance-target with a resource yield is the biosphere-meets-made-world seam, not yet driven end to end.
- NOT DONE: **war/raiding/violence as an emergent outcome**. `conflict_pressure` is computed and tested but has ZERO run-loop consumers; Crucible's "war emerges" has no mechanism. Arc: a group/territory/raiding system consuming `conflict_pressure`.
- DONE (MERGED to main, #113 `0c11e77`): lifespan DERIVED from anatomy, R-AGING resolved as owner-signed option (c): operational lifespan is the first-passage time of a per-part damage-energy accumulator against each part's own material tolerance, an emergent OUTPUT (no authored size-to-duration law). Plan `docs/working/LIFESPAN_C_FIRSTPASSAGE_BUILD.md`; five slices on branch `claude/lifespan-from-anatomy` (#113). SLICE 1 built + gate-signed (byte-neutral, inert, section-9 audited): the data-defined insult registry + energy-damage accumulator + `derive_integrity` + the wear->energy floor identity `wear_energy`. SLICE 2 built + pushed + section-9 audited (byte-neutral, inert): the tissue-turnover REPAIR flux (`repair_energy` = turnover_rate * failure_energy_tolerance * funded_fraction, the crux-3 conversion through the fracture scale), the maintenance funding gate (`maintenance_funded_fraction`, sign-aware overflow), and the signed `net_damage_delta`, all in `crates/sim/src/insult.rs`; 25 tests; the per-tissue turnover rate is a reserved-with-basis value (real tissue-turnover data, the gating dependency, grounded not authored); no mass-neutrality engineered, the slope emerges (mass-dependence deferred to the re-pin wiring). The turnover-fraction as energy-fraction identification is a flagged owner-review modeling assumption. SLICE 3 built + section-9 audited-and-hardened (the death-path switch, RE-SCOPED after the original `body.rs` Option-B target was proven test-only and its energy-reroute proven not bit-for-bit): aging now lands on the RUN-PATH grown `Structure` body (`crates/sim/src/morphogen.rs`), not the test-only `body::Body`. A per-segment normalized-fraction `Segment.damage` accumulator; `Segment::failure_tolerance` = `mat.fracture_energy * mech.contact_area` (the segment's own data) and an absence-guarded `Segment::integrity`; `whole_body_viability_aged` scaling each segment's capability by its integrity so a worn body reads a falling viability and dies through the EXISTING reserve-floor cull (route (i), no vital-part predicate, P8); `maintenance_demand` (extensive) and the pure `accrue_aging` (Archard wear on the floor kJ scale commensurate with the tolerance, no conversion factor, minus funded tissue-turnover repair, normalized once, clamped [0,ONE]). Wired opt-in inside the INTEGRITY-axis block in `runner.rs` `step_embodiment` (gated on physiology + wear caps + material registry; energy budget keyed on the `ENERGY_DENSITY`-backed axis, not a hardcoded id). Byte-neutral against the four pins; the section-9 six-lens audit hardened the confirmed defects (removed an erroneous kJ->J *1000, sign-aware overflow saturation, the material-registry scale gate, the alien metabolic-axis selection, a zero-hardness wear skip, a damage clamp). Merged onto `main` (now carries #116 predation); all four moved pins (default `4bbf6b59`, full `5aaa43f1`, discovery `c9d5cc17`, viability `ad69f2bf`) reproduce bit-identical with slice 3 applied, so aging is byte-neutral and armed only by a race that grows fracture-energy tissue. FUNCTIONAL CHECK done and passing (`crates/sim/tests/world_build.rs` `aging_kills_by_first_passage_and_a_tougher_body_lives_longer`): a world-backed `build_dawn_runner` grown-body race arms aging, its founders die of first-passage per-part damage (INTEGRITY, the existing reserve-floor cull, not a metabolic confound), and a race with 8x the grown fracture energy ages ~8x slower and suffers strictly fewer aging deaths, the size-longevity slope a pure output; replays bit for bit (the deterministic armed-aging run hash). A watchable `run_world --scenario aging` was STOP-reported and gate-accepted (run_world founders never grow a `Structure`); the clean watch artifact `crates/sim/examples/aging_demo.rs` (on `build_dawn_runner`, byte-neutral) is BUILT and gate-signed: it prints a founder's INTEGRITY falling tick by tick as its tissue wears and the first-passage deaths, contrasting a frail race (dies of aging at tick 7) with an 8x-tougher one (ages ~8x slower, outlives the window). The (c) aging arc is gate-signed PROVEN (the gate ran the functional-check test and verified the P8 core and byte-neutrality at source). Remaining: the pool projection (R-TIER-CONSIST), retiring the authored `Race.lifespan_years`. MERGED to main (`0c11e77`, #113); the gate ran the functional check and the `aging_demo` on the merged main and both pass. The `Segment.damage` accumulator on main now unblocks Agent A's hunt-kill strike damage-write (one shared damage currency for wounds and aging). The fed-state oxidative insult and the whole-body-load-per-segment and repair energy-draw questions are held owner/gate review items.
- DONE (Agent B, MERGED #115 `0c2c7aa`, R-DEEPTECH-COMPOSE): the affordance composer + belief-subject HYBRID key. The belief-subject key re-encoding is BUILT (`crates/sim/src/learn.rs` `sequence_subject` / new `sequence_subject_hashed`): the gate-ruled HYBRID that dissolves the old 4-bit/16-value over-merge cap, an exact widened pack for in-envelope sequences (primitive 6 bits, target/param 3 bits each, 4 steps, bit 60 clear) plus a `StateHasher` hash sub-band on overflow (bit 60 set), the four belief-subject bands partitioning `{60,61}` under bit 62 (feature, being-signal, sequence-exact, sequence-hash), Agent A's being-signal band untouched. This is the ONE sanctioned not-byte-neutral re-pin (it changes every sequence-belief subject): default `4bbf6b59` and discovery `c9d5cc17` HELD (no sequence beliefs there), full re-pinned `5aaa43f1` -> `1db633b3`, viability pending; the field widths are reserved-with-basis (gate-set interim). Section-9 audited and hardened; the re-pin coordinates with Agent A through the gate (its keystone consumes the re-encoded key). The byte-neutral COMPOSER LEAF is now BUILT and section-9 hardened (`aa09b6c`, gate-ruled option C, the opaque canonical representation): `crates/compose/src/node.rs` carries `NodeBody::Transduction { canonical: Vec<u8> }`, an OPAQUE domain-serialized primitive that `compute_content_id` tags `3` (tags 1/2 byte-identical) and folds length-prefixed whole, never parsing it, so `compose` stays domain-agnostic; `crates/sim/src/affordance_percept.rs` adds `SingleAxisTransduction::canonical_bytes` (fixed field order, u64-length-prefixed strings, discriminants by explicit match, no map iteration, the `state_hash` determinism discipline) and `to_composition_node`, so a sensed affordance and a designed object share ONE composer library and ONE selection process and flow through promote/fold/discovery alike. The section-9 six-lens audit caught a real MAJOR derive-vs-author defect (the leaf's structural viability was `point(ZERO)` on a false contentless-leaf equivalence, sitting exactly on the promotion gate so any positive owner safety margin would have rejected every discovered sensor); FIXED to the DERIVED zero-load maximum (`point(ONE)`, a utilization of zero gives safety one), with two regression tests through `promote()` under a positive margin, plus the u32->u64 length-prefix widening, the hybrid-key target/param overflow coverage, and softened docstrings (the Tier-C emergent bundling is the substrate here, not overclaimed as built). Byte-neutral: all four pins HELD after the fix (default `4bbf6b59`, full `1db633b3`, discovery `c9d5cc17`, viability `ad69f2bf`); compose 32 tests, sim 826 lib tests, fmt+clippy clean. MERGED to main (`0c2c7aa`, the one sanctioned re-pin: `full` -> `1db633b3`, the other three held, gate-verified all four). B is now on the CREATURES-REACT being-percept arc (#118, framing gate-ruled: a raw-percept weight with a SELECTED sign and no belief store, plasticity folded into a selected coefficient; build fork ruled B3-first, magnitude-graded). The multi-axis affordance COMPOSITION (bundling transduction leaves into a purpose-laden percept) remains the Tier-C emergent follow-on through the discovery loop.

**World build and calibration (standing a world up under `Profile::Calibrated`):**
- IN PROGRESS: **Mirror** (Earth 1:1, the owner-gated control) and **Tempest** (Mirror + `.high` dials). The loader is mostly present (`build_dawn_runner` takes a Profile + ScenarioResolution and arms the social/belief/language/reproduction/biosphere-supply layers fail-loud). The 6-domain Earth-value research is done (`docs/working/MIRROR_CALIBRATION_RESEARCH.md`, 162 cited proposals). DONE (Arc 1 slice 2): the opt-in learning/discovery/conviction/tool features moved onto the canonical `build_dawn_runner` path, armed fail-loud from the manifest and GATED by a scenario `[features]` block (`ScenarioFeatures`); `from_manifest` threaded for `DiscoveryCalib` and `FeltConvictionCalib` (the reward one already existed), with dev-fixture and reserved-with-basis manifest entries. Proven: the loader refuses to build under `Profile::Calibrated` while its values are reserved and builds under Development (the fail-loud calibration gate that maps Arc 2); the FEATURE-block fail-loud is proven end to end separately (a feature armed with only its own calib reserved refuses at the feature arming). Remaining: set the reserved values and boot Mirror/Tempest under Calibrated (Arc 2).
- DONE (assessment): the world substrate-readiness map (`docs/working/WORLD_SUBSTRATE_READINESS.md`), verified against source.
- PARTIAL: the four universal physical constants (Stefan-Boltzmann sigma, Coulomb k, vacuum permeability mu_0, gas constant R) to CODATA, coupled to the R-UNITS-PIN per-quantity fixed-point scales; big G to be forward-declared (no consumer until orbital mechanics; little g stays per-world). DONE (Arc 2 seg 1): Stefan-Boltzmann set to CODATA (the one constant with a live sim consumer, plus six other derive-audited floor/world scalars); Coulomb k, mu_0, R await the units-scale wiring (`crates/units`). Arc 2 also RETIRED `metabolism.surface_emissivity`: the radiant thermoregulatory term now DERIVES emissivity from the being's covering `opt.emissivity` floor axis (`physiology::covering_emissivity`, `crates/sim/src/physiology.rs`), per-race differentiable, not a reserved scalar (derive-vs-author). Subsequent calibration segments set more derive-audited values (engine-units anchors, engine representability bounds, geometric/degenerate world data, social category-b data, category-b language curves, and the reviewer-approved coarse climate-productivity scaffold with the abstract-model limit noted), each verified against source and the floor registry. A climate segment reconciled the temperature-units seam: the worldgen temperature axis is normalised `[0,1]` but the physics needs absolute Kelvin (Stefan-Boltzmann `T^4`), so `Field::from_map_absolute` now maps the field to absolute temperature `T = mean + range*(normalised - 1/2)` from two RESERVED owner climate values (`climate.mean_surface_temperature`, `climate.latitude_temperature_range`), the dev fixture carrying the identity (byte-neutral, all four pins hold); this unblocks `saturation_t_ref` and makes the Kelvin thresholds consistent. With that in place, `hydrology.saturation_t_ref` was RETIRED: the Clausius-Clapeyron tangent's reference temperature now DERIVES from `climate.mean_surface_temperature` (the habitable-band midpoint the field is centred on), read by `EnvironCalib::from_manifest`. A third derive-correction RETIRED the GLOBAL `metabolism.medium_convective_coefficient` scalar in favour of per-medium DATA: the metabolism now reads the convective coefficient `h` from the being's own MEDIUM (`medium.{air,water,dense_toxic}` `convective_coefficient`, the `fluid.convective_coefficient` floor axis) at the being's cell via `MediumField::convective_at`, byte-neutral (uniform dev h, the medium field is not hashed). HONEST STATUS (from the §9 arc audit): `h` is flow-dependent (`h = Nu*k/L`), so the per-medium value is a LUMPED interim, not a full derive; the flow-dependent Nusselt derivation is DEFERRED (still flagged in the reserved bases), and two honest limits are documented (the drain reads current-cell `h` while the cached exchange rate reads spawn-cell `h`; the off-field zero is defensive). The per-medium differentiation is exercised by two new tests. The units-mechanism wiring for Coulomb k / mu_0 / R is DEFERRED (reviewer call): no live sim consumer, so it is forward-looking, and the sigma-precision fix would re-pin all four hashes for ~0.22% on a constant the code already factors around; flagged for the Mirror gate. FINAL Arc-2 segment IN PROGRESS: the owner signed off the Mirror dial-set (`docs/working/MIRROR_SIGNOFF_DECISIONS.md`) under a STANDING DERIVE-GATE (check "can this derive further?" before authoring anything). Batch 1 applied (owner values): `climate.mean_surface_temperature` 287 (pre-industrial), `climate.latitude_temperature_range` 60, `world.orbital_period_seconds` 31556952 (TROPICAL year, superseding the Julian 31536000), `physiology.thermal_half_band` 8 (owner resolution: attunement emerges via evolvable morphology, not the band), `metabolism.body_mass_kg_scale` 70 (Mirror human). The gated values were derive-gate-checked: `discovery.exploration_floor`/`surprise_gain` AUTHOR (the per-being disposition already derives from `Channel::Exploration`); `felt_conviction.retention` AUTHOR baseline + flag the bounded per-being memory-derivation; `behavior.controller_*` and `inst.crystallization_*` AUTHOR-NOW-DERIVE-LATER (mutator-allele channel / strength-scaling flagged); `genome.allele_presence_threshold` AUTHOR-IRREDUCIBLE. MIRROR BOOTS under `Profile::Calibrated`: the 21 keys on Mirror's disembodied boot path are set under the derive-gate (physical Clausius-Clapeyron hydrology tangent at the owner's 287 K; engine bounds generous; `medium.air` real-air; `evidence_ring_curve`/`field.body_exchange` as the authored curve/fallback with the per-being part deriving; `field.cell_size` flagged for the owner's map-scale intent), and a new test (`crates/sim/tests/mirror_calibrated_boot.rs`) proves Mirror builds and runs under Calibrated, deterministic and worker-invariant (population 7, bit-identical across widths 2/3/8). The boot is now EMBODIED: the founders carry body plans and an `EmbodimentGenesis` (air emergent, water submerged), so the metabolism/physiology path runs; `metabolism.respiration_transfer_coefficient` is set as a flagged INTERIM (a per-membrane gas permeability, the owner's real value surfaced) and `medium.water` as Mirror's real-water ocean profile. The embodied Mirror boots under Calibrated, deterministic and worker-invariant (population 7). Tempest's cranked dials select the `.high`/`.low` stress magnitudes the owner DEFERRED (Group A), so Tempest's boot is a Tempest-calibration follow-on (surfaced). The 5 group-E AUDIT-CATCHES are resolved (Prime Directive 2, corrected from their real bases, never the research-tagged value): `transmission.loss_rate` (stale evidence.decay_rate pin removed, re-based on its own generational erosion timescale), `axiom.group_aggregation_rule` (owner ruling recorded: equal-weighting now, member-variance derive flagged), `axiom.stubbornness_dogmatism_weight` (key-vs-wiring mismatch flagged: the live split is not fed from the manifest key, reconcile before setting), `transmission.loss_practitioner_floor` and `compose.emergent_proxy_weights` (bases already sound in reserved.toml; the wrong value was only in the research doc). Remaining: the Tempest boot (its cranked `.high` dials, per the reviewer). Calibration manifest: 117 set / 103 reserved.
- PARTIAL: the scenario name shaping world STRUCTURE. DONE (Arc 1 slice 1): worldgen + biomes are now scenario-SELECTED through a data-driven `WorldStructure` registry (`crates/world/src/structure.rs`), resolved from the scenario's `structure` name (`ScenarioResolution::world_structure`) and built into the map by the loader instead of `dev_default` regardless of scenario; Mirror/Tempest/Crucible declare `structure = "earth"` (the grounded triad), byte-identical. REMAINING: magic-structure (the scenario's `magic.laws` still shapes nothing, blocked on Part 34); exotic terrain modes (Crucible's patchy basins, Europa's z-stack) are registry entries added when their substrate arcs land; and the Earth structure's biome bands + worldgen params are still dev fixtures, not yet owner-reserved manifest values (an Arc-2-sibling follow-on).
- RUNS (Development) but design unbuilt: **Crucible** the substrate (salt, greenwell food, discovery, controller, convictions) runs; Crucible-the-design needs a patchy-basin terrain worldgen mode + the war mechanism above.

**The remaining canonical worlds (flagged substrate arcs, do NOT fabricate):**
- NOT DONE (Venus): the greenhouse/radiative energy-balance wiring (the Stefan-Boltzmann/Wien/optical-depth kernels exist in the physics floor but have zero consumers, so a planet's temperature is never derived from its atmosphere), and the corrosive-medium-to-tissue harm coupling.
- NOT DONE (Europa, blockers): a volumetric z-stacked medium/terrain (ice-over-ocean-over-seafloor; today one medium per (x,y) column, z=0 only), and a tidal-heating law + orbital/tidal axes (the ocean is liquid because of tidal heating). Plus a redox-couple chemosynthetic energy mechanism and a located depth/pressure caller.
- DONE (Arc 5, MERGED #112 `3c497f9`): the data-defined **AbioticField field-kind registry** plus the corrected Nernst uptake-flux, LIVE on main (byte-neutral, four pins hold). Unblocks chemosynthetic (Venus/Europa), geothermal/redox (Europa), and mana (Arcanum) sources; the fuller catalyst axis and the coupled-set net-free-energy clamp are flagged R-SOURCE-VECTOR follow-ons (#121). Segment 1 DONE (byte-neutral, `crates/sim/src/environ.rs`): the closed enum {Light, Water, Soil} gained a `DataScalar(u16)` OPEN arm that reads a world-declared `ScalarField` from the new `EnvironFields::data_fields` collection, plus a `ReadShape::Point` dimension on the binding, so an alien energy field (chemosynthetic/geothermal/mana) is a data row (`a_world_declared_energy_field_is_a_pure_data_row_that_caps_and_depletes`). The blind panel caught that the read-at-cell interface authored point-locality. Segment 2 was REFRAMED by its own blind panel (verified against source): co-limitation (needs both donor and acceptor, limited by the scarcer) is ALREADY the engine's Liebig-min, which segment 1 delivers, so a co-limited chemolithotroph is already a data row; the bespoke pairwise-difference operator is dropped. Segment 2 DONE (byte-neutral): the stock->biomass conversion (`AbioticBinding::biomass_per_stock`) and the stoichiometric drawdown (`AbioticBinding::stock_per_biomass`) are now PER-SOURCE `Option<Fixed>` defaulting to the registry-global (fixing the confirmed soil-global-conversion and implicit-1:1 seams; each reactant a separate source id, Liebig-min co-limited, so no role enum), proven by `a_source_converts_stock_to_biomass_by_its_own_per_source_rate_not_the_global` and `a_donor_and_acceptor_are_co_limited_and_drawn_by_their_own_stoichiometry`. Segment 3 CORE DONE (byte-neutral, the depth-independent floor-EMF yield): a redox source's conversion DERIVES from its couple's galvanic EMF (`AbioticBinding::redox_emf`, the two floor `chem.standard_potential` values through `civsim_physics::laws::battery_emf` = `E_acceptor - E_donor`, clamped at zero) times the RESERVED `AbioticSourceRegistry::emf_to_biomass` coupling (fail-loud sentinel, the owner's value, surfaced with the `dG = -nF*EMF` basis, never fabricated), proven by `a_redox_source_derives_its_yield_from_the_floor_emf_not_a_declared_number`, `a_non_spontaneous_redox_couple_powers_no_life`, and `a_redox_source_fails_loud_when_the_coupling_is_unset`. The enum-opening STRUCTURE (the DataScalar open arm, ReadShape, the registry, per-source conversion, stoichiometry, the redox-EMF core) is now COMPLETE and gate-signed-off; the T3 food-value derivation it feeds is complete and signed off (see the world-liveliness PARTIAL item). The one remaining piece, the corrected full-Nernst YIELD, is at FRAMING-DONE and HELD on the OWNER: the blind framing (verified against source) reshaped it well past the four seams. The gate ruled B out (folding per-lineage efficiency into the per-cell environmental yield breaks observer-independence P10) and ruled IN the physics/hygiene/determinism corrections (derive the thermal term from Boltzmann k_B and a per-couple carrier-charge axis not an authored molar R and Faraday F; KEEP the energy-to-biomass bridge since nFE is joules and the yield is biomass; clamp on NET free energy across coupled reactions not per-couple; a data-defined gamma activity registry with a non-Terran default; add dE0/dT; rename the electrochemical Faraday off the law.faraday_emf collision; derive R_s from a universal R and molar mass; guard ln(Q) and pin the Q/T field-step order). HELD for the owner: the efficiency ARCHITECTURE (A, a downstream consumer scalar with a FLOOR conservation cap efficiency<=1 and a modeled antagonistic cost, versus C, throughput and its functional form EMERGING from the being's modeled metabolic machinery with no authored scalar), which the yield's kinetic-rate term couples to. The corrections are built into whichever architecture the owner picks; the floor growth would collide with A's active floor-reconciliation sweep. PHASE-1 PARTIAL (Agent C, PR #112, commit 16df1d5): the two PURE physics laws are built and unit-proven as byte-neutral additive functions with no caller yet (`physics/laws.rs`): `nernst_emf` (E = E_standard + (RT/nF)(ln a_donor + ln a_acceptor), reduces to standard at unit activity, collapses when depleted) and `reversible_uptake_flux` (v = Vmax*Hill(S)*drive, drive = 1 - exp(-nE/(RT/F)), zero at/below equilibrium, min(v,S)); both are Fixed::ln/exp/powf, integer-only and pinned, so the ln-determinism (the gate's load-bearing check) holds. RECONCILIATION RESOLVED (audit-the-input catch, gate-confirmed): the catch was right and split cleanly, efficiency architecture C (throughput EMERGES from the being's own catalyst tissue, `Vmax = kcat*catalyst`, no authored efficiency scalar; the roadmap's "HELD" was stale, the owner's de-biaser resolved it to C) and the framing's ruled-IN EMF corrections RESTORED. Commit 05f3486 applies the LAW-LEVEL corrections: the thermal term is now the per-particle `k_B*T/q` (Boltzmann `k_B` a floor fundamental times a per-couple carrier-charge `n*e`, NOT molar R/Faraday, which also DISSOLVES the floor-constant collision, no new thermal constant), a new `standard_potential_at_temperature` law adds `dE0/dT`, and `reversible_uptake_flux` keeps `Vmax` a caller parameter so architecture C is wired by the caller not presumed. Three unit tests, physics lib 96 passed. PHASE 2 INCREMENT 1 DONE (Agent C, the Nernst YIELD wired, byte-neutral opt-in): `RedoxEmf` gained the concentration term (`carrier_charge` q=n*e on `elec.charge`, an `acceptor_field`, donor/acceptor `gamma` activity coefficients, `dE0/dT` + `t_ref`), the registry gained the reserved `boltzmann_k` (option B: a calibration value not a floor axis, so it does not race A's floor sweep; k_B graduates to a floor fundamental with that sweep later), and `redox_conversion_at` reads the couple's ACTUAL cell concentrations through `standard_potential_at_temperature` + `nernst_emf`, wired into `extract_producers` pass 1 (temperature threaded in) so an ARMED redox source's yield FALLS as the couple depletes and crosses zero at its own equilibrium. OPT-IN and byte-neutral: `carrier_charge = 0` (the default, all existing redox sources) reduces exactly to the standard EMF, and Earth binds no redox source, so the four pins hold. Proven by `a_nernst_armed_redox_source_yield_falls_as_the_couple_depletes` (reduces to standard at unit activity, falls below it when depleted). Two floor-collision catches resolved WITHOUT growing the floor (audit-the-input, both verified against source): k_B is NOT on the floor (the gate's premise was wrong, only composite sigma exists), resolved as a reserved value (option B, gate-ruled); and there is NO catalytic-activity axis, so the catalyst-tissue `Vmax` will proxy by the existing `bio.protein_fraction` (enzymes are proteins, Principle 4's causal-primitive-plus-proxy, no new axis). PHASE 2 INCREMENT 2 DONE (Agent C, the reversible-flux DRAW, byte-neutral opt-in): a per-source `RedoxKinetics { kcat, km, hill, catalyst_class }` registry (data on the binding, `set_source_kinetics`), and `redox_draw_flux` draws an armed redox source's pass-2 uptake as `reversible_uptake_flux` (Hill-saturating, driven by the shared `redox_nernst_emf`, `min(v,S)` conserved) with `Vmax = kcat * the being's OWN catalyst tissue` read directly from `producer_food[i][catalyst_class]` at the cell (no PhysicsRegistry). The catalyst class is a per-source DATUM defaulting to `bio.protein` (admit-alien NOW: a silicon/mineral-catalyst alien names its own class as a data row, the mechanism reads a world-named class not a hardcoded protein; enzymes-are-proteins is the default, Principle 4), the fuller catalyst AXIS flagged as coupled to R-SOURCE-VECTOR. Opt-in and byte-neutral: a source with no kinetics keeps the segment-2 capacity-proportional draw exactly, and Earth binds no redox source, so the four pins hold. Proven by `a_nernst_redox_source_with_kinetics_draws_the_reversible_flux_from_its_catalyst_tissue` (catalyst tissue drives the draw, no catalyst = no draw, min(v,S) caps it). NET-FREE-ENERGY CLAMP: the gate CORRECTED the interpretation, the per-couple clamp (increment 1's Nernst EMF zeroed below each couple's own equilibrium) is CORRECT for these INDEPENDENT couples; a producer-level sum over uncoupled couples would be unphysical (it would zero a releasing couple because an unrelated non-releasing one's negative EMF dominates the raw sum). The true net-across-a-thermodynamically-coupled-set clamp (shared-intermediate coupling, an individually-uphill reaction powered by a coupled exergonic one) is FLAGGED as a follow-on for when the coupling substrate exists. The corrected Nernst uptake-flux (yield + draw) is now COMPLETE. Unblocks chemosynthetic (Venus/Europa), geothermal/redox (Europa), and mana (Arcanum) sources. PRE-MERGE: rebase onto current main and re-measure the CURRENT pins (`4bbf6b59` / `1db633b3` / `c9d5cc17` / `ad69f2bf`).

- DONE (Arc 2, Agent C, PR #112, byte-neutral opt-in): a surface with proper **day-night heating**. The owner ruled the third form (a general closed-form sun-angle law, not the interim-vs-substrate fork) and it is BUILT and WATCHABLE in `--scenario living` (a ~31 K equatorial diurnal surface swing that EMERGES from the field's relaxation-plus-diffusion, night side held on the back-radiation floor never 0 K). The light side (`environ.rs insolation_at`): the sun-angle `cos(zenith)` law with per-cell LONGITUDE and the SYNODIC hour angle over a data star-list, declination from per-world obliquity; the default is the zero-obliquity single-star REFERENCE world (labeled NOT-Mirror). A firewalled private phase counter (never exposed to any behavioural substrate, so a diurnal rhythm emerges from selection over the cycling fields, never a clock). The heat side (night-floor form (2), gate-ruled over the author's (1)): `solar_baseline = radiative_equilibrium(insolation*solar_constant + back_radiation, emissivity, sigma, t_max)`, the per-world back-radiation floor giving the Moon-Earth-Venus swing spectrum; `apply_diurnal_baseline` copies it into the temperature `Field` baseline (additive `Field::set_baseline_at`/`width`/`height`), the field's relaxation produces the emergent swing and lag. Opt-in: unarmed the light is the static latitude map and the four determinism pins hold bit-exact; armed only in `living`. The consumer-threshold audit CLEARED (the productivity Liebig satisfaction and the hydrology condensation/evaporation are graded/physical, no authored time-gate). Tests: the day-to-night swing, the warm-day/night-floor baseline. DONE (follow-on 1, Mirror's real tilt): `DiurnalSky::mirror` carries Earth's real 23.44-degree obliquity (0.4091 rad) as Mirror's per-world datum, so REAL SEASONS ride on the day-night through the declination term already in the law; the seasonal test proves the tilted pole takes the midnight sun at the summer solstice where the zero-tilt reference pole stays dark and reverses to polar night half a year on, the equator's diurnal swing surviving; the `living` demo now arms `mirror` and reads out the pole-vs-equator latitude structure with the warming pole reversing as the orbital phase advances; all four pins hold bit-exact. DONE (follow-on 2, per-material THERMAL INERTIA): a `SurfaceThermal` registry (fixed Rust, reserved per-material volumetric heat capacities `rho*c_p` for substrate/water/ice as data, dev-fixture Earth values, `environ.rs`) derives each armed cell's thermal-inertia factor from its OWN real state (soil moisture + standing-water saturation + freeze read, no lookup), and A's `Field::step` scales its relaxation-plus-diffusion by that per-cell factor (additive optional `Field::set_inertia_from`/`inertia`, `None` default so the step is byte-identical unarmed), so a water-laden or damp cell lags and swings less than dry land (the ocean/damp-soil effect), emergent from the cell's water content. The `living` readout shows dry-land vs water swing. Byte-neutral: armed only in `living`, the four pins hold bit-exact. Emissivity stays UNIFORM (per-cell ALBEDO is the real radiative differentiator, a separate follow-on flagged to A on the `radiative_equilibrium` floor law with the two-sided-Kirchhoff correctness question). FLAGGED follow-ons (remaining): per-cell ALBEDO + the two-sided emissivity on A's radiative law; the full per-column LITHOLOGY substrate (rock vs sand vs clay varying the dry substrate, the geodynamics arc); back_radiation deriving from atmospheric greenhouse optical depth. Then the corrected Nernst uptake-flux. PRE-MERGE: rebase onto current main and re-measure against the post-keystone pins (4bbf6b59 / 5aaa43f1 / c9d5cc17 / ad69f2bf).
- NOT DONE (Arcanum/Confluence): the **magic system Part 34** (MagicLaws / ManaSource / CostModel / LimitModel / MagicalTradition) is unbuilt pseudocode; "magic" is today a bool posture, a cosmetic mana-sac organ, and an unused genome channel. No mana field. Running `--scenario arcanum` produces a grounded non-magical world identical to Mirror.

**Engine fidelity and far horizon (tracked in Tiers C through E):**
- NOT DONE: temporal level-of-detail (R-TEMPORAL-LOD), event-driven agent execution (R-AGENT-EXEC), GPU offload of the hot loops, persistence/save-schema (R-SAVE-SCHEMA), the non-authoritative view elaboration (R-VIEW-ELAB).
- NOT DONE: the made world's deep tiers (Part 60 Stages 6 through 13) and the north star (emergent science, deep-tech, Stage 14).
- NOT DONE (owner-directed 2026-07-09, SUBSTRATE not authored): emergent pollution and climate change. Once industry emerges (the deep-tech tiers), its byproducts alter atmospheric composition, which through the greenhouse/radiative-balance wiring (the Venus item above) shifts the radiative balance and warms the world, all EMERGENT from the emergent industry plus the atmospheric/radiative floor, never an authored warming curve. Mirror starts at the pre-industrial mean (287 K) deliberately, so the warming has somewhere to go. Depends on: the radiative-balance wiring, a data-defined atmospheric-composition field, and emergent industry.
- NOT DONE (owner-flagged 2026-07-09): a genome-derived thermal SETPOINT (deeper attunement). Today the core setpoint is a fixed per-race baseline (310 K) and climate attunement emerges via evolvable body morphology (covering/mass/surface, the Bergmann/Allen path, which is how endotherms adapt). A drift-able genome setpoint would additionally let ectothermy, torpor, and variable-core-temperature lineages emerge; a substrate refinement, not needed for Mirror's endothermic humans.
