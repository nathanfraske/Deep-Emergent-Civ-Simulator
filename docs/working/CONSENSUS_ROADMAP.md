# Consensus Roadmap: the ground-truth order of things (for sign-off)

This reconciles the three ordering artifacts in the repo against what is built in code, into one authoritative sequence, each item cited to where its gate lives. It is a synthesis for the owner to sign off on, not a maintained document; nothing here is committed until you approve it.

The three sources it reconciles:

- **`ROADMAP.md`**, the near-term critical path to a living, talking world (milestones M0 through M4).
- **`docs/design.md` Part 60**, the full implementation staging (Stage 0 through Stage 14).
- **`docs/audit.md` Section 3**, the research backlog: every open item tagged `R-XXX` with its site (`Part N`), its prerequisites, and its couplings, plus the resolved-item record (Part 62).

The reconciliation was checked against a direct read of every crate's modules, tests, and the calibration manifest, so "done" below means built and proven in code, not merely planned.

## The headline reconciliation (read this first)

The build did not follow Part 60's stage numbers in order. It followed the research-gate dependency order instead, front-loading the determinism bedrock, the reasoning-and-being core, the physics substrate, and the embodiment stack, because those are the pieces the emergent design cannot be proven without. The consequence is that the actual frontier is best read as a research-gated dependency graph plus the `ROADMAP.md` M-slice, and Part 60's stage numbers are a topic index rather than a build order.

Three divergences the ground-truth read exposes, stated plainly so the sign-off is honest:

- **The code is ahead of `ROADMAP.md`'s prose.** `ROADMAP.md` still says the deep being model is "absent" and the map is "entirely unbuilt." Neither holds: the being model (genome, intrinsic beliefs, axioms, value profile) is wired end to end through `seed_dawn_populations`, and the map generates, classifies biomes, builds a level-of-detail quadtree, and renders in a windowed viewer, proven bit-identical by `crates/world/tests/zoom.rs`. `ROADMAP.md`'s "Where we are" section is stale and should be refreshed.
- **The code is ahead of the audit's bookkeeping in the determinism cluster.** The audit still lists `R-CMD-ORDER` among the open determinism items, but the mechanism is built (`crates/core/src/command.rs`: `CommandBuffer`, `EventQueue`, the total ordering key). This is the same "mechanism built, item open until consolidated" state that `R-LANG-TYPOLOGY` is in. The audit's resolved count (28) undercounts what code proves; a consolidation pass is owed for `R-CMD-ORDER`, `R-LANG-TYPOLOGY`, and the canonical-walk and reduce helpers.
- **The physics substrate and embodiment stack are built ahead of their nominal Part 60 stage.** Five physics floors (biology, mechanical, fluids, chem-optics, electricity-and-magnetism) and the full homeostasis-to-controller-to-evolve embodiment chain exist and are tested, though Part 60 places the physics substrate as a Stage 14 concern. They are substrate, proven in isolation, not yet integrated into a running world tick.

So the single biggest truth for sequencing: the engine is a deterministic, serial, CPU, promoted-tier core with a viewable generated map, a deep cognition-and-language core, and a broad shelf of tested-but-not-yet-integrated substrate modules. What stands between it and a running world is not more substrate. It is **owner calibration**, **integration into the canonical runner**, and then the **parallel, persistence, and made-world** tracks.

## Tier A: done (built and proven, research resolved)

Each of these is implemented in code with a determinism or behavioural test, and its research question is resolved in Part 62 unless noted.

- **The determinism bedrock.** Q32.32 `Fixed`, the per-entity counter RNG and the canonical draw-keying schema (`R-RNG-COORD`, site Part 3.2, record 62.15, `ROADMAP.md` M0), the canonical-state boundary that makes a float in canon a compile error (Part 58), the state hash (Part 3.5), the id registry with save-side id continuity, and the append-only event log. Gate met: `crates/core/tests/determinism.rs` holds one-seed-one-world across thread counts.
- **The command and event-queue substrate.** `CommandBuffer` and `EventQueue` with the total ordering key (`R-CMD-ORDER`, site Part 4.3 and 57). Mechanism built; the audit item awaits consolidation (see the headline divergence).
- **The GPU canonical-arithmetic and transcendental path** (`R-GPU-CANON-PIN`, resolved, record 62.23, Part 3.4/5.4). Fixed-point multiply and divide and the full transcendental set (exp, ln, powf, powi, and the CORDIC sin, cos, atan, asin) are built as CubeCL `#[cube]` kernels in `crates/gpu` and proven bit-identical to the `crates/core` `Fixed` oracle on the RTX 5090 (`crates/gpu/tests/stage0_gate.rs`, `transcendental_gate.rs`), alongside the fixed-point diffusion field (tile-invariant, matching the CPU bench at about 22x, `diffusion_gate.rs`), all with a no-float source guard (`canon_guard.rs`). The cross-backend determinism gate proves the Stage 0 arithmetic (multiply and divide) bit-identical across three independent CubeCL codegen paths, CUDA (NVRTC), the CPU backend (MLIR/LLVM), and the wgpu Vulkan/SPIR-V backend (cubecl-spirv, run here on lavapipe), all matching the oracle (`cross_backend.rs`). The transcendentals do not yet cross-backend on the non-CUDA paths: on the CPU backend they trip an upstream `cubecl-cpu` constant-propagation panic, and on wgpu the box's only Vulkan adapter is lavapipe (software), too slow for the large unrolled transcendental shaders (though SPIR-V Int64 is supported, proven by a native-i64 probe that ran in milliseconds), so their cross-backend confirmation awaits a cubecl-cpu fix or a hardware Vulkan device. The general u128 `Fixed::sqrt` GPU primitive is now built (a 96-bit limb isqrt in `crates/gpu/src/prim.rs`, bit-identical to the oracle over the full magnitude sweep; asin keeps its faster domain-limited u64 isqrt), and the shared limb primitives (multiply, divide, isqrt) are consolidated in `prim.rs` instead of copied per kernel. Gate remaining: the transcendental cross-backend confirmation (a hardware Vulkan device or a cubecl-cpu fix) and a true multi-vendor hardware device run (a discrete GPU across Metal, DX12, ROCm).
- **GPU-vs-CPU offload map (scoped, `docs/working/GPU_CPU_OFFLOAD_MAP.md`).** A subsystem survey classified every engine compute workload GPU / HYBRID / CPU-parallel / CPU-serial against a five-property framework (domain size, flattenability, control-flow uniformity, arithmetic intensity vs transfer, order-independence). Landed: the diffusion kernel, the perceive notice-roll spike, the runner field-step kernel (clamped-Neumann + relaxation, matching `Field::step`), and worldgen noise + fused biome classify, all bit-identical on the 5090 (`crates/gpu`). Prioritised offload roadmap: finish the temperature field kernel (matches `diffusion_bench`, NOT the runner `Field::step` yet: needs a clamped-Neumann + relaxation variant), then worldgen noise + fused biome (the cleanest standalone win), then a fused resident being-kernel (controller + homeostasis + body-thermal, unlocks the field offload). BEING-KERNEL ARITHMETIC COMPLETE (`crates/gpu/src/being.rs`): `gpu_body_thermal` (#72), `gpu_sat_mul` (#75, a blind-audit-caught overflow bug fixed), `gpu_activate` (#76, the controller activation with a two-limb 128-bit saturating accumulator), `gpu_metabolize` (#78, the homeostasis drain), all bit-identical on the 5090 and each blind-audited. Remaining on the GPU track: fuse the four over one resident being buffer + the reaction-norm matvec + wire into `World::tick`, all SCALE-GATED (the dev map sits below the launch threshold, so these pay only on a large world, which is gated on the reserved-value calibration above)., then pool-tier aggregate mortality and Wright-Fisher drift, then perceive to production. Gate: every GPU verdict is contingent on residency (Part 5.6) AND scale, routed per-deployment by a profile, with bit-identity (R-GPU-CANON-PIN) the price of a canonical offload; Archard wear and wide-i128 EM need a new wide-limb primitive first. The branchy cognition (converse, gossip, belief update, theory-of-mind) and the canonical merges (CommandBuffer, state-hash, naming game, scheduler) stay CPU by contract.
- **Intra-phase per-being parallelism (first phase parallelised).** The throughput lever for the cognition tick: not phase-level scheduling (the minds/belief hub serialises the expensive phases), but parallelising the per-being work WITHIN a phase, over the population N. The `converse` phase already proved the two-pass pattern (a pure read across `workers` threads, then a canonical-order apply through the `CommandKey` barrier); `World::perceive` now takes the same shape (`crates/sim/src/world.rs`, `gather_trace` across `std::thread::scope` workers), proven bit-identical to the serial gather at 2/3/8/16 workers. Remaining candidates: gossip, decide, drift, mortality (all draw-keyed gather-then-apply). The gather uses dynamic load balancing (workers pull work units from an atomic counter, re-canonicalised by a cheap work-unit sort), so it adapts to a hybrid CPU (Intel P and E cores, AMD X3D CCDs) with no topology query; determinism decouples correctness from scheduling, so this cannot change a result. Measured on the reference 265K, perceive scales about 1.8x and plateaus (memory-bound, capped by bandwidth); the compute-bound phases are where the core-heterogeneity win appears. For the memory-bound work the other lever is the GPU (an order of magnitude more bandwidth): the perceive notice roll is proven bit-identical on the GPU (`crates/gpu/src/perceive.rs`, `gpu_notice`, gated over 7200 pairs on the 5090), a canonical offload made possible by the draw-keyed RNG and the pinned fixed-point arithmetic; the full offload (SoA extraction, a resident cross-tick buffer per Part 5.6, hit compaction) is scoped, gated on a profile. Heterogeneous-scheduling design in `docs/deterministic_scheduler_design.md`. Gate: each phase's per-being rolls are already draw-keyed (R-RNG-COORD), so the parallel gather is a pure function of state; width-invariance is the proof.
- **Visual projection (scoped, first slice built).** How the deep world is shown: appearance derived from the canonical physics/material/anatomy data the sim already computes, never authored, with the renderer a non-canonical consumer (Principle 10). A data-driven `VisualProjection` registry (Principle 11) maps optical axes to a material palette, the anatomy part graph to a procgen silhouette, and state to overlays, with a two-tier glyph-then-procgen-sprite view and a content-hash sprite atlas. Scoped in `docs/working/VISUAL_PROJECTION_SCOPED_PROPOSAL.md`; the first slice is built (`physics_terrain_color` in `crates/viewer`, terrain colour from a tile's elevation/moisture/temperature). Awaits sign-off on the substrate shape; the reserved palette values are the owner's aesthetic call.
- **The generated map.** Topology and the 2.5D coordinate model (Part 56), fixed-point fractal worldgen with elevation, moisture, and temperature, biome classification by data-defined ranges, the level-of-detail quadtree (Part 6), and the camera-and-glyph view plus a windowed viewer (Part 14). This is `ROADMAP.md` M1 and Part 60 Stage 2 worldgen, proven by `crates/world/tests/zoom.rs`.
- **The reasoning-and-being core** (Part 60 Stage 3, the most mature area). The evidence engine (`R-EVIDENCE`, 62.7), recursive theory of mind (`R-TOM-UPDATE`, 62.11), the value-distance metric (`R-VALUE-METRIC`, 62.3), axiomatic belief (`R-AXIOM`, 62.4), the genome and genotype-to-phenotype map (`R-GENOME`, 62.5), being representation (`R-BEING-REP`), and the utility-AI decision layer, wired end to end and exercised by `dawn_seed.rs`, `dawn_lifecycle.rs`, and the false-belief-and-deception battery `tom_battery.rs`.
- **Language and dialogue.** The naming game over the roughly sixty-five semantic primes (`ROADMAP.md` M3, `sim/tests/primes.rs`), the emergent lexicon and articulation substrate (`R-LANG-DET` 62.12, `R-LANG-MODALITY` 62.13), the typological substrate (`R-LANG-TYPOLOGY`, built in `crates/sim/src/typology.rs`, item open pending consolidation), and modelled dialogue as a speech-act substrate (`R-CONVERSE` 62.14, `ROADMAP.md` M4, `dialogue_steering.rs`).
- **The physics substrate floors** (`R-PHYS-BIO`, `R-PHYS-MECH`, `R-PHYS-W2`, `R-PHYS-W3`; records 62.16 onward): five data floors and their closed-form fixed-point law kernels, built and tested, the reach-bounding authored layer.
- **The living-substrate and deep-time modules.** Stocks and flows (Part 15), biosphere generation, the pre-dawn radiation epoch, and the genesis sequence (`R-BIOSPHERE`), the species-lineage tree, and edibility as a measured relation.
- **The embodiment and behaviour stack.** Homeostatic physiology and affordances, structured anatomy, the per-part body with wounds and fluids (`R-BUILD-PHYS`, `R-WOUND`, `R-FLUID`, 62.20), emergent locomotion, and the evolved controller with homeostatic-survival selection (`R-BEHAVIOR-EVOLVE`, 62.19), including the eastward-scorer steering fix landed this session.
- **Tier consistency.** The two-tier individual-and-pool model and the conserved-projection registry (`R-TIER-CONSIST`, 62.9), and the resolved institution-function substrate (`R-INST`, 62.8).
- **The institution and ADICO-norm substrate** (`R-INST`, 62.8, Part 36), now built in code (`crates/sim/src/institution.rs`). An institution carries no authored kind: its identity is its structure (roles, ADICO-grammar norms, a blend over an open `FunctionRegistry` of function axes, legitimacy, lineage), and any type is recovered only as a derived, non-authoritative `EticDescriptor` (a fixed-point weighted-Tanimoto match against a `TemplateLibrary` that may be empty, in which case every institution reads generic and the engine runs). `Deontic` is a closed `{May, Must, MustNot}` (a logical primitive), the function axes are an open owner-set registry with a labelled `dev_seed`, and ADICO conditions key off `decision.rs`'s open `InputId` world-fact registry; the norm type (strategy/norm/rule) is derived from which optional fields are present, never a stored tag. `tier.decision_propensity` derives through the `DecisionPropensity` accumulator and `emit_undertaking` gate; the compact `AggregateInstitution` rides on `Pool` (`lod.rs`), its feature vector and legitimacy mass registered as conserved projections and proven conserved across promote/demote/merge/split (`crates/sim/tests/invariants.rs`), with promotion materializing an explicit institution whose signature reproduces the pool's compact vector. The mechanism is proven against a synthetic coordination-pattern stream, with the non-steering test (a race's exotic function axis, carried only as data, crystallizes an institution in a region other races cannot reach, distance-dominating), gloss-blindness (label/column permutation leaves metrics bit-identical), the empty-library fallback, norm-type emergence, order-independence, cross-thread determinism, and descriptor-out-of-hash (`Phase::CRYSTALLIZE = 0x1A`). Gate: mechanism built and tested; the LIVE crystallization detector wiring into the running agent loop (feeding it the decision layer's real coordinated undertakings and minting `InstId`s on the tick) is a NAMED FOLLOW-ON, and the reserved `inst.*` and `tier.decision_propensity` values (`calibration/reserved.toml`) are the owner's to set before a calibrated world crystallizes institutions.

## Tier B: the near-term frontier (what to do next), with gates

This is where the consensus order matters most, because the substrate shelf is deep but the running world is thin. Recommended order:

1. **Owner calibration of the reserved values.** The manifest carries 153 entries; 76 are set and 77 remain fail-loud sentinels (`calibration/reserved.toml`, RUNBOOK section 4). Under the Calibrated profile the engine refuses to start on any unset value, so the real (non-dev-fixture) world cannot run until these are set. This is the single largest unblock and gates everything that runs a world rather than a test. Gate: each value is reserved-with-basis at its mechanism site; you set it, the manifest graduates it to `set`. Progress (2026-07-03): Batches 1 through 11 graduated through the whole-batch fully-blind audit (worksheet §7), plus the three semantic design-changes (§8), the differentiated language-floor resolution (record 62.24), and the structured-value format (`require_map`) with the compound entries graduated (§10, the drift operator set, calcification rate and cap, the conformity and prestige strengths); the remaining reserved include the components the design leaves without a magnitude (calcification brittleness, the fission and deviation thresholds, the per-axis physiology drains, burn scale from the physics floor), the dawn-floor values, the environmental levers (thermal band and medium promotion), and the physics-floor batch.
2. **Wire the built core into the canonical runner.** `crates/sim/src/runner.rs` carries the physics field layer (Part 5.4, 5.5) with zero authored numbers. The being-and-cognition core is now composed onto that spine. Gate: integration, not research.
   - DONE (composition increment): the runner owns an optional cognition `World` and runs `World::tick` as a fixed sub-phase after the field phases, in a pinned within-tick order (field first). `Runner::with_world` (`crates/sim/src/runner.rs`), composite `state_hash` folds the field-side hash then the world's, and the determinism triad plus a clock-lockstep invariant is proven in `crates/sim/tests/determinism_harness.rs` (`composed_runner_tick_*`, worker widths [1,2,3,8]). No new authored number, RNG draw, or `Phase`. Honest limit: no field-to-cognition data flow yet, and the world hash does not yet fold the being lifecycle (genomes, ages, affect) or the dialogue log.
   - STEERING BOUNDARY (Principle 9, guiding-star audit): the canonical-emergent runner's behaviour source is the evolved controller (Part 8.4), never an authored policy. `World::decide`'s authored drive-and-action repertoire (the sentient deliberative tier, Part 8.1) is steering at the level of behaviour, so `Runner::with_world` fail-loud rejects a world that carries one (`World::has_behaviour`), quarantining the authored path off the canonical spine (proven by `the_canonical_runner_refuses_an_authored_behaviour_repertoire`). The composed cognition phases the runner does drive (belief, dialogue, gossip, language) are emergent and data-driven.
   - DONE (field-to-behaviour coupling, evolved-controller substrate, the guiding-star path chosen over the original sketch below): a runner built with `Runner::with_embodiment` owns an `Embodiment`, a located population of evolved-controller beings. Each tick, after the field step and the body-thermal exchange, a pure comfort-band map (`comfort_fraction`) turns each being's field-driven core temperature into a `TEMPERATURE` homeostatic reserve, per being from its reserved comfort band (Part 20), the being's evolved controller reads the reserve and moves-or-rests, and the beings' new coordinates re-sync `LocationIndex` for the next tick's exchange (`crates/sim/src/runner.rs`, `crates/sim/src/homeostasis.rs::dev_thermal`). The first increment is KINESIS: no directional thermal percept, so an uncomfortable being explores (`Phase::EXPLORE`, no new `Phase`) and directed thermotaxis emerges under survival rather than being authored. The composite `state_hash` folds each being's position, reserves, and controller state after the world fold; proven in `crates/sim/tests/thermal_coupling.rs` (the comfort map is even, a comfortable being rests and an uncomfortable one moves, the controller authors no heading, replay bit-identity and seed divergence, death a full half-band past the set point, and the R-EVOLVE-STEER anti-steering check that the warmth-west and warmth-east advantages match with bit-identical idle baselines). The behaviour source is the evolved controller only; no authored repertoire rides this path (Principle 9).
   - DONE (in-situ thermal selection, the guiding-star retirement of the hand-built fixture): `evolve_thermal` scores a controller by running it through the field-to-behaviour coupling (`Runner::with_embodiment`) in a frozen warm-border, cold-centre field and counting how long it keeps its temperature reserve off the floor. From random controllers, thermal homeostatic-survival selection produces a kinesis (move while cold, rest while warm) that keeps its body in the viable band, retiring the hand-built thermotaxis fixture the coupling increment used: selection produces it. `evolve` was generalized to `evolve_with(layout, params, seed, scorer)` so the scoring environment is data, not hardcoded (Principle 11), with `evolve` the water scorer and `evolve_thermal` the thermal one. Proven in `crates/sim/src/evolve.rs` tests (`thermotaxis_evolves_under_thermal_survival_selection` mean survival 129 to the 200 cap; the scoring field is invariant under a 90-degree rotation, the R-EVOLVE-STEER anti-steering check; a mover outlives an idle being that dies at the cold centre; the scorer is deterministic). No authored heading and no authored objective: survival is what is counted.
   - DONE (directional thermal percept, raw physics, sensed taxis): `Field::gradient_at` is a pure integer central-difference stencil, the raw temperature gradient (the GPU-portable counterpart of `Field::step` and `crates/gpu`'s field kernel); `Runner::step_embodiment` reads it per being and feeds it as the `TEMPERATURE` axis's directional percept through `locomotion::step_with_field_dirs`, an axis-generic per-being field-percept channel (a moisture or wind field plugs in the same way), merged alongside the known-source percept with no controller-layout change. The percept is raw physics (what a thermoreceptor senses), not comfort: no physiology is folded in, and the being's comfort reserve stays a separate input, so the evolved controller combines "am I uncomfortable" with "which way is warmer" and selection wires it. Proven in `crates/sim/src/evolve.rs` and `crates/sim/tests/thermal_coupling.rs`: a smooth rotation-invariant thermal bowl with a balanced set of four off-centre starts (`thermal_sensed_survival`, `evolve_thermal_sensed`); sensed taxis reaches the warm ring from every start (survives to the cap) while undirected kinesis random-walks and often dies (200 vs about 66 to 153); a fixed heading has a directional blind spot the percept-follower does not; the bowl is 90-degree rotation invariant (R-EVOLVE-STEER); directed thermotaxis evolves to near the cap; and `Field::gradient_at` points toward warmer and is flat on a uniform field. Honest limit: raw gradient plus the even comfort reserve lets a linear controller learn warmth-seeking in a cold world, but fleeing lethal heat needs a recurrent controller reading a signed thermoreceptive percept (a warm-versus-cold receptor, a new input channel) so selection can evolve the hot-versus-cold gating. GATE (owner-confirmed): this is deferred and rides in with the medium substrate, since lethal-hot environments (lava, desert) are what the medium work introduces; bidirectional thermotaxis lands under `R-MEDIUM`, not before there is heat to flee.
   - DONE (FieldCalib manifest loader): `FieldCalib::from_manifest` reads `field.diffusion`, `field.relaxation`, and `field.body_exchange` from the calibration manifest, fail-loud if any is still reserved (Principle 11), so a canonical run obtains its field calibrations from the owner's manifest rather than a fabricated default (the three entries ship reserved-with-basis in `calibration/reserved.toml`). Tests in `crates/sim/src/runner.rs`: it reads a set manifest exactly and refuses a reserved one.
   - REMAINING sub-increments, ordered: (a) map-backed terrain (real biomes and elevation) replacing the bounded open plane; (b) fold the being-lifecycle path (`seed_dawn_populations`, enculturate, birth, age, mortality) into the composed tick and the hash; (c) the cognition-`World` coupling (temperature as a percept to the deliberative tier, and a chosen action moving a `Coord3` against a unified `PlaceId`/`LocationIndex`) is DEFERRED behind rebuilding that deliberative tier (Part 8.1) on the evolved substrate, because its current `World::decide` path is the quarantined authored repertoire (Principle 9), not a clean coupling target.
   - OWNER-DIRECTED, the emergent-physiology direction (signed off; the organ abstraction was red-teamed and reframed, see below). First, DERIVE THE HOMEOSTATIC RESERVES FROM ANATOMY. Today reserve capacity scales with `body_mass` and only integrity derives from the per-part body. Mass-scaling is wrong for a huge, mostly-armored creature with few organs (it would be handed reserves it has no internal systems to justify): a being carries a reserve because it bears the organ that maintains it, and reserve size follows the organs, not total mass, so "no organs, no reserves" is expressible and armor and integrity decouple from metabolic reserves. RED-TEAM OUTCOME (owner-steered): the organ's function is NOT an authored tag. The first-cut design gave each organ kind an authored `OrganCategory` (fat-body to Metabolic, gills to Respiratory); red-teamed against Principle 8 that tag is a template, a closed function-per-kind authored one level down. The reframe: an organ is a tissue-composition structure (the exact pattern the body's fluids already use, a composition over the biology floor), and which reserve it backs is DERIVED from its composition (an energy-dense tissue is an energy store, a water-rich one a water store) against the physics-floor material axes (`bio.energy_density`, `bio.water_fraction`, `therm.specific_heat`). We author the floor physics and the tissue-composition vocabulary; the organ's role emerges. The first-cut authored-kind build is set aside for this composition-derived rework. Second, THE MEDIUM SUBSTRATE, generalized so aquatic life is one instance: a medium is a `Substance` in the physics substrate (`crates/physics`, content-addressed, not a label), defined by its physics-axis values (`mech.density`, `fluid.dynamic_viscosity`, `therm.temperature`, and the rest); a per-cell medium field of the GPU-portable stencil shape. The FLOOR TO AUTHOR here is a gas-exchange and respirable-medium physics law the floors do not yet carry (a medium's respirable content, dissolved-gas concentration or partial pressure, and Fick's-law diffusion across a membrane by area, permeability, and gradient), the one new authored piece, tracked as `R-MEDIUM`. Medium affinity is then EMERGENT: a high-surface-area membrane organ in contact with water respires water (a gill), one in contact with air respires air (a lung), from the organ's composition and geometry against that law plus selection, never a hardcoded "aquatic" flag, so a lava creature, a magical-fluid creature, a rooted autotroph drawing from its medium, and a sentient aquatic race all emerge. Full aquatic physiology is the chosen depth: medium, respiration, buoyancy (from body density), in-medium thermal exchange, and amphibious both-medium bands. The bidirectional-thermotaxis gate above rides in here. Both are substantial; the anatomy-derived reserves come first (composition-derived), then the medium substrate on top.
   - DONE (composition-derived anatomy-derived reserves, the first half of the owner-directed item, signed off as "Derived from tissue composition"): a homeostatic reserve's capacity now derives from the being's ORGANS, not its body mass. `anatomy.rs` gains a tissue-composition organ substrate: `TissueComposition` (a value on each biology-floor material axis, `bio.energy_density` and `bio.water_fraction`), `TissueComponent` (the floor-axis vocabulary a reserve reads), and `OrganKindDef` (an organ kind as data: id, name, fantasy gate, and composition, with NO function tag), plus an `organs: Vec<OrganKindDef>` registry, `organ_composition`, and a body-plan organ draw (`pick_organs`) keyed off `base + 100` with no coupling to body mass, covering, or trophic layer (the anti-steering invariant). `homeostasis.rs`: `HomeostaticAxisDef` gains `backing_component: Option<TissueComponent>`, and the canonical `Homeostasis::new(reg, plan, organs)` sets each backed axis's capacity to the development-weighted sum over the being's organs of their composition on that component (a derived, non-backed axis such as integrity or temperature keeps unit capacity). The mass-scaling path is retained only as the labelled `Homeostasis::from_mass` development fallback (fixtures and tests keep running); no production caller sources a reserve from mass. Which reserve an organ backs is DERIVED from its composition against the floor, never a tag (Principle 8): an energy-dense tissue backs energy, a water-rich one water. Proven in `crates/sim/src/homeostasis.rs` and `crates/sim/src/anatomy.rs`: capacity derives from composition; an energy-dense organ backs energy not water; a huge, mostly-armored giant with one tiny organ holds a SMALLER reserve than a small, organ-rich body (the owner's armored-giant case); no organs means no metabolic reserve and non-viability; capacity sums over organs and scales with development; a derived axis holds unit capacity regardless of organs; determinism; and the organ draw is registry-gated, fantasy-gated, and deterministic. The first-cut authored-`OrganCategory` build is retired by this rework. REMAINING on this item: the medium substrate (below), whose `R-MEDIUM` floor adds the respiratory-surface composition component the organ substrate is built to extend.
   - DONE (red-team hardening of the above, merged as #28): a red-team pass found the `TissueComponent` enum was a closed vocabulary sitting where the physics biology floor already models composition as string-keyed data (`Substance::vector`), a Principle 11 seam that capped what reserves could exist without a code change. Hardened: `TissueComposition` is now a `BTreeMap<String, Fixed>` over biology-floor axis ids, `HomeostaticAxisDef.backing_component` is an `Option<String>` floor axis id, and the enum is removed, so a reserve backed by protein or the R-MEDIUM respiratory-surface axis is a pure data edit (proven by a `bio.protein_fraction` test). The line 54 note's `TissueComponent`/`Option<TissueComponent>` is superseded by this floor-axis-keyed form.
   - DONE (the F3 birth-nonviability decision, owner-directed: lean on the existing closure cull, and prove it does not steer). The owner's call on a species whose organ roll gives it no metabolic reserve (born nonviable on energy) is to LEAN ON THE EXISTING CLOSURE CULL rather than add a seed-time viability filter, and to prove that leaning on it steers no outcome. Proven in `crates/sim/tests/biosphere_steering.rs` (Principle 9): the cull is physics-only (the seed-time least-fixed-point `grounded` walk reads only the food web, the runtime Part 15 `Stock` collapse reads only supply and demand, neither reads morphology, shown by a body-swap invariance test and a collapse test); birth-viability (`homeostasis::birth_viable`, a documented query, NOT a generator gate) is a pure function of the organ set, invariant to body mass, armor, and weapons; the armored giant with even one small energy organ survives while only the truly reserveless body is culled; and over 300 generated species the verdict is invariant to the drawn mass forced to each extreme, so the cull selects no mass class, and the culled are exactly those with an empty energy reserve. The birth-nonviability leans on the runtime half: a reserveless creature dies at once, its pool draws no sustaining return, and the existing over-harvest cull collapses it. The end-to-end wiring (reserve-viability feeding the pool persistence) lands with the being-spawn loop in the medium-substrate work; the invariants it must preserve are the ones proven here.
   - APPROVED, R-MEDIUM (owner: "as long as it is emergent on physics kernels, I approve"). The scoped proposal is `docs/working/MEDIUM_SUBSTRATE_SCOPED_PROPOSAL.md` (draft PR #33). The binding condition is emergence on physics kernels: the one new authored piece is a Fick's-law membrane gas-exchange kernel plus a respirable-content medium axis and an ambient-medium field, and medium affinity (a gill respires water, a lung air, a lava creature its lethal-hot medium) is emergent from organ composition against the medium through those kernels, never a flag. This moves the medium substrate (task #20) to the active build; the bidirectional-thermotaxis signed-percept gate rides in with it. Next build increment: the physics floor (the gas-exchange kernel and axes in `crates/physics`) under the emergence constraint, then the ambient-medium field and the respiration reserve (a data extension of the now floor-axis-keyed organ substrate).
   - DONE (R-MEDIUM increment 1, the physics floor, merged as #39): `law.membrane_gas_flux`, Fick's law in the mass-transfer form `J = k*A*(c_medium - c_internal)` (signed: uptake positive, off-gassing negative), plus the `fluid.respirable_content` and `fluid.gas_transfer_coefficient` axes, each reserved-with-basis. The kernel reads only the medium's respirable content, so a gill in water and a lung in air are the same kernel over different concentrations, equilibrium is zero flux, and no exchange happens without a surface (proven in `crates/physics/tests/fluids.rs`).
   - DONE (R-MEDIUM increment 2a, the respiration coupling, the organism side): `crates/sim/src/medium.rs` couples the gas-exchange kernel to a being's physiology. A `RESPIRATION` homeostatic reserve (a unit-capacity oxygen buffer, `dev_respiration`) drains by metabolism and is replenished each tick by `medium::respire`, which reads the being's exchange area (`exchange_area`, the development-weighted sum over its organs of the new `bio.respiratory_surface` biology-floor axis, the same composition-derived shape the metabolic reserves use) and the ambient medium's respirable content, and applies the signed Fick flux via a new `Homeostasis::adjust`. Proven: a gilled being breathes and survives in a rich medium; the same body suffocates in a poor one (outcome from the medium's content, not a label); a body with no respiratory organ suffocates even in the richest medium (the no-surface-no-breath consequence, the respiratory sibling of the F3 no-energy-organ case); a larger surface takes up more; determinism. REMAINING (increment 2b): the per-cell ambient-medium field that supplies each being's medium (the coupling takes it as input now, as the thermal coupling took a Field), buoyancy from body density against the medium (the existing `law.buoyant_force`), and the signed thermoreceptor for lethal-hot media.
   - DONE (R-MEDIUM increment 2b, the ambient-medium field, the world side): `crate::medium::MediumField`, a per-cell medium (respirable content and density) in the row-major layout the temperature field uses, and `medium::respire_at`, the located respiration that reads a being's cell and exchanges gas with it. Proven: a being in a rich cell breathes and survives while the same body in a poor cell suffocates (respiration is located, the medium is the cell's, not a label); off the field there is no medium and a being suffocates; the field reads its medium back and zero off the edge; determinism. The field carries the medium density for the buoyancy increment. STILL REMAINING (increment 2c): buoyancy, which needs a being's body density derived from its tissue composition against `mat.density` (no body density exists yet), against the field's medium density through `law.buoyant_force`; and the signed thermoreceptor for lethal-hot media.
   - DONE (R-MEDIUM increment 2c, buoyancy and amphibious emergence): `medium::body_density` derives a being's density as the development-weighted average over its organs of their `mat.density` composition (the water baseline when none declare one, the first-cut organ-composition proxy), and `medium::buoyancy`/`buoyancy_at` returns the signed net buoyant acceleration through the resolved `law.buoyant_force` and `law.weight` kernels, so the sign is the density difference. Proven: a light body floats and a dense one sinks in water; the same dense body floats on lava and falls in air (buoyancy is the cell's medium, not a label); body density is the composition average; determinism. And the amphibious case is proven emergent: a body whose respiratory organ exchanges in two respirable-rich media is viable in both cells, with no both-medium flag. STILL REMAINING to finish R-MEDIUM (increment 2d, lethal-hot media): in-medium thermal exchange (the medium field carrying a temperature the being's thermal coupling reads) and the signed thermoreceptor (a warm-versus-cold controller input so selection can evolve hot-fleeing), the deferred bidirectional-thermotaxis gate.
   - DONE (R-MEDIUM increment 2d part A, in-medium thermal exchange): `MediumField` now carries a per-cell temperature (the same row-major layout as its respirable content and density), and `medium::in_medium_temperature`/`in_medium_temperature_at` relaxes a being's core temperature toward the medium of its cell, `new = prev + rate * (medium_temp - prev)`, the discrete form of Newton's law of cooling over `Fixed` (checked, cap-on-overflow). Physics in, death out: a body held in a lava-hot medium heats past its comfort band (`runner::comfort_fraction` falls to zero) and a body in a lethal-cold medium cools past it, both lethal, while a temperate medium holds the body in band, with no authored rule that lava is lethal. The anti-steer is proven: the exchange is a scalar relaxation toward the medium temperature carrying no spatial heading, and an equal gap above and below the body drives an equal and opposite step (`a_hot_and_a_cold_medium_are_mirror_image_treatments`: `up == -down`), so the mechanism bakes in no direction (Principle 9). Proven in `crates/sim/src/medium.rs` (19 medium tests): heats toward hot and cools toward cold, equilibrium at the medium temperature, lethal-hot and lethal-cold both kill while temperate holds, the located form reads the cell and holds off the field, determinism. STILL REMAINING to finish R-MEDIUM (increment 2d part B, the deferred bidirectional-thermotaxis gate): the signed thermoreceptor, a raw warm-versus-cold percept delivered as its own controller input (not folded into a "which way to safety" heading, which would be steering), so selection can evolve hot-fleeing under the lethal-hot media this increment makes real.
   - DONE (R-MEDIUM increment 2d part B, the signed thermoreceptor and bidirectional thermotaxis: R-MEDIUM is now COMPLETE): the controller gains a fifth per-axis input, the signed setpoint-deviation percept (`controller.rs` `INPUTS_PER_AXIS` 4 to 5, the general substrate: every homeostatic axis exposes both its even comfort level and its signed deviation, so the mechanism authors no "temperature is special"). The runner supplies it for the TEMPERATURE axis from the new odd `runner::signed_deviation` (too hot positive, too cold negative, saturating at the band edges), threaded through `locomotion::step_with_field_dirs` alongside the gradient percept. It is a raw percept, not a heading: it says the body is too hot, not which way to flee, so the controller must combine it with the gradient itself (Principle 9). The proof (`crates/sim/src/evolve.rs`): a bidirectional scorer scores survival in BOTH a lethal-cold bowl and a lethal-hot bowl (mirror images across the set point, proven cell-for-cell); NECESSITY, seed-robust: no single gradient polarity survives both (the warmth-seeker climbs to the warm rim in the cold bowl but walks into the lethal-hot centre, 200 vs 14 ticks; the heat-fleer the mirror), because telling hot from cold is the bit the even comfort reserve cannot carry; EMERGENCE: from random controllers, homeostatic-survival selection with the signed percept and a recurrent controller produces a being that seeks warmth when cold and flees heat when hot, surviving both bowls to the cap (bi 200), without any heading or hot-versus-cold rule authored; and the percept is LOAD-BEARING by ablation (zeroing only the signed input drops the same evolved controller from 200 to 131). Anti-steer proven throughout: `signed_deviation` is exactly odd about the set point (the signed counterpart of the even `comfort_fraction`), the two bowls are mirror images, and the start sets are balanced, so the mechanism favours neither hot nor cold and no compass direction. Honest limit: the bidirectional gating is a product of two percepts, so it needs the recurrent controller (a reaction norm cannot form it); the reaction norm still cannot solve a both-dangers world. This retires the bidirectional-thermotaxis gate the directed-thermal-percept increment deferred, so full aquatic-and-thermal physiology (medium, respiration, buoyancy, in-medium thermal exchange, amphibious bands, and now heat-fleeing) is emergent on physics kernels with no authored steering. R-MEDIUM is COMPLETE.
   - IN PROGRESS (base-level liveliness arc, `docs/working/BASE_LIVELINESS_HANDOFF.md`; step 1 DONE, on branch `claude/base-liveliness`): the running world was dead (static dawn blobs) because the founding race `GeneSet` carried no `Channel::Controller` locus, so every expressed controller weight was zero and the MOVE arm never fired. Step 1 (field-coupled movement, pure data, no tick change) closes it: `controller::taxis_move_weights` names the nonzero founding thermotaxis weights (MOVE activation from the bias input, MOVE heading from the temperature axis's source-direction percept), and `genome::append_controller_block` appends the full controller substrate (a gene per `weight_count` weight, matching `evolve::controller_gene_set`) and seeds the pool additive spine so a founder expresses them deterministically (frequency one, zero within-locus variance, the effect `target/ploidy`). The run harness (`crates/sim/examples/run_world.rs`) wires it into the founding race and opens the selection dials off zero (`mutation_rate`, `additive_mutation_step`, `environment_variance`), and a migration reader reports dispersal. Gate: the milestone (founders leave their single dawn cell and disperse along the temperature gradient) is proven in `crates/sim/tests/world_build.rs` (`founders_with_a_taxis_block_disperse_and_a_blank_race_does_not`: a taxis race disperses, a blank-controller race stays put, and the run replays bit-for-bit) plus a unit test on the weight indices, and observed live in the harness (all founders off their dawn cell on the first run). The taxis weight magnitudes are reserved-with-basis (`controller.taxis.move_bias`, `controller.taxis.heading_gain`) for a canonical race genesis; the dev harness stands up fixtures. Principle 9: the seeds are the same for every race, no `RaceId` branch. Determinism: mutation is counter-keyed, the controller float-free, positions in `state_hash`.
   - IN PROGRESS (base-level liveliness step 2 DONE, the environmental field stack): a new `crates/sim/src/environ.rs` generalises the field layer into a data-driven stack of pinned-stencil scalar fields, stepped in canonical order after the temperature field (`Runner::step_field`, run in both the pinned and scheduled orders) and folded into `state_hash`. HYDROLOGY: a per-cell water-depth field, sourced by precipitation (moisture crossing `laws::saturation_vapor_pressure`, so cold humid cells rain), sunk by evaporation (`laws::evaporation_rate`, so hot cells dry), transported downhill over the frozen worldgen elevation (a deterministic lowest-neighbour advection, ties broken in a fixed order, so water pools in basins), and capped at a reserved basin holding capacity so a basin fills to a lake rather than growing without bound. PRODUCTIVITY: a per-cell standing producer biomass, the Liebig minimum (`laws::net_nutrition`) over water, light, temperature, and soil, written into the embodiment's `ResourceField` as the `bio.energy_density` supply, NOT a dead-zone cutoff (the limiting factor sets the continuous productivity, Principle 8). BIOSPHERE-READY (owner directive): the productivity is the default abstract source of the per-cell producer biomass (`biomass_from` is the seam), so the living-biosphere addendum swaps real producer occupants in with no rewrite. `build_dawn_runner` arms the stack fail-loud (`EnvironCalib::from_manifest`), a field-state reader lands in the run harness, and the whole step replays bit-for-bit and matches the scheduled order (`crates/sim/src/environ.rs` tests: the Liebig minimum, downhill routing pools into a basin conserving water, latitude light, determinism; `world_build`/`determinism_harness` green). Sixteen forcing constants reserved-with-basis (`hydrology.*`, `productivity.*`). GPU note: the new advection stencil is written GPU-portable (the same pinned-integer shape as `Field::step`); its CubeCL twin and 5090 oracle gate are a mechanical escape-hatch follow-on, not a merge blocker, since the canonical run is CPU and the gpu crate's oracle is opt-in per kernel.
   - IN PROGRESS (base-level liveliness step 3 DONE, the living resource loop where carrying capacity bites): the standing food is now a persistent, grazable, logistically regrowing stock, so the Malthusian explosion is bounded. `EnvironFields.capacity` holds the primary productivity (the Liebig ceiling), and the standing food lives in the embodiment's `ResourceField` as the `bio.energy_density` supply: `EnvironFields::regrow_supply` reads back last tick's grazed amount and regrows it toward the capacity as a Part-15 logistic `Stock` (regrowth and over-harvest collapse for free), with a reserved colonization propagule floor so a viable-but-empty cell bootstraps (a dry-start world greens as the hydrology fills, a grazed-out patch recovers rather than dying forever). The INGEST arm of `step_with_field_dirs` now takes a `&mut ResourceField` and DEPLETES the tile: the satisfaction-measured net gain is bounded by the reserve room, grossed up by a reserved trophic efficiency to the biomass the bite removes, taken from the standing stock, and the assimilated part deposited, so a being's gain is conservation-honest against the tile's loss (the `stocks::flow` trophic step) and a grazed-out tile feeds the next id-ordered being less (Principle 8, no stock-empty gate). The embodiment registry swaps `dev_thermal` for `dev_grazer` (the ENERGY and WATER metabolic reserves plus the required TEMPERATURE axis), the founder body carries organs backing those reserves, and the founder controller is seeded with a full FORAGE reaction norm (`controller::forage_taxis_weights`: move toward known food and water, stop and ingest on a source underfoot, steer along the temperature comfort gradient). Water is a drinkable mirror of the standing water depth, refreshed each tick (the honest limit: the hydrology field is not itself a drinking sink at this scale). The `ResourceField` folds into `state_hash` (a divergence in the regrow-and-graze loop cannot pass replay while hiding), and the field-less `step` stays non-depleting (the scoring and fixture path grazes a throwaway copy). Gate: the milestone (population settles around what the land regrows, a grazed tile empties and beings move on, the reader shows scarcity for the first time) is proven in `crates/sim/src/environ.rs` (the food stock regrows toward capacity and sustained over-harvest collapses it) and `crates/sim/src/locomotion.rs` (grazing depletes the tile and competition is the deterministic id-sorted walk), and observed live in the harness (the population is bounded in a predator-prey oscillation around ~60 to 80 rather than the old explosion to 4090, and the new carrying-capacity reader shows 95 to 100 percent LOCAL grazing pressure where beings graze while the wilderness stays abundant); it replays bit-for-bit, matches the scheduled order, and is worker-count invariant (`world_build`, `determinism_harness` green). Three reserved-with-basis values (`productivity.regen_rate` and `productivity.colonization` fail-loud from the manifest through `EnvironCalib::from_manifest`; the trophic `locomotion.ingest_efficiency` reserved-with-basis on the `LocomotionParams` struct until the locomotion parameters read the manifest) plus two forage taxis gains (`controller.taxis.here_suppress`, `controller.taxis.ingest_drive`). Principle 9: the ingest, regrow, and forage path keys off floor-axis-id class strings (`bio.energy_density`, `bio.water_fraction`), never a race, species, or biome id, so a world's alien fluid enters as data; the only concrete seam is the source-field-to-class binding (which physical field feeds which consumable class, two string constants), the same binding the living-biosphere addendum generalizes. Still to come: conversation liveliness and the dynamic readers (step 5), the generous arc-scoped promotion policy, and the living-biosphere addendum arc.
   - IN PROGRESS (base-level liveliness step 4 DONE for its tolerance-adaptation half; the conflict-coefficient feed and the wind/dust fields DEFERRED with cause): the salt-flat gradient is live and a lineage adapts to it by heritable selection. SALINITY: `EnvironFields.salt` is a per-cell salt mass, weathered into every cell and advected downhill with the water routing (the same lowest-neighbour targets), capped, so salt accumulates in endorheic basins (which route nowhere) and washes from well-drained cells; the toxin DOSE is the concentration `salt/(water + reference)` scaled, high on a dry evaporated flat and diluted on a wet cell, written as the `bio.salinity` toxin on the cell `Composition.toxins`, and the salt field folds into `state_hash`. HERITABLE TOLERANCE: a new `Channel::Tolerance(ToleranceAxisId)` genome channel and a data-defined `ToleranceRegistry` (sibling to the controller and composition registries, keyed off the floor toxin-class id) let `Physiology::express` express a per-toxin-class tolerance from the genome, the same expression path the controller uses, so a founder carries its own salt resistance and a newborn inherits it (armed at both the dawn founder step and the lifecycle-pairing beat). THE HARM SINK: `step_with_field_dirs` reads the cell's toxin dose against the being's OWN tolerances through `laws::net_harm`, and nets a reserved recovery against it on a new non-draining `CONDITION` reserve (`Homeostasis::adjust`), so environmental harm is a race between damage and healing: a halophile (harm below the recovery) heals faster than it is worn and lives on the flat, a naive lineage (harm above it) declines through its condition floor to death, the emergent reserve-through-floor cull, never a fixed-dose exclusion gate (Principle 8). Gate: the milestone half (a salt flat lethal to a naive lineage and livable to a halophile) is PROVEN in `crates/sim/src/locomotion.rs` (`a_salt_flat_is_lethal_to_a_naive_lineage_and_livable_to_a_halophile`: two beings on one salt cell differing only in tolerance, the naive dies and the halophile lives the whole run) and `crates/sim/src/environ.rs` (salt concentrates in an endorheic basin), and observed live in the harness (12 percent of cells are salt flats, the population carries a heritable mean salt tolerance the reader reports); replays bit-for-bit, worker-count invariant (`world_build`, `determinism_harness` green). Four reserved-with-basis salinity constants fail-loud from the manifest (`salinity.weathering_rate`, `salinity.salt_cap`, `salinity.dose_scale`, `salinity.reference_water`) plus the `LocomotionParams` `harm_caps` and `condition_recovery` and the race-genesis `tolerance.salinity_baseline` (reserved in their struct docs until the locomotion parameters and a canonical race genesis read the manifest). Principle 9: tolerance is per-toxin-class heritable data keyed off the floor class id, never a `RaceId` and never one scalar; salinity is a `bio.*` class the same harm path serves for a future dust or alkalinity, so a world's alien toxin enters as data. DEFERRED WITH CAUSE (audit of the handoff's input, Prime directive 2): (a) the `conflict_coefficient` DERIVATION is blocked, not skipped: the handoff's premise that a caller supplies the authored dial in the run path is false. `value.rs::conflict_pressure` has NO production caller and nothing reads `value_metric.conflict_coefficient` into the run path (the dial lives only in scenario TOML and a scenario test), so there is no authored dial to swap for a measured one; the derivable half, the measured scarcity, IS on screen (the step-3 carrying-capacity reader, global and local), and feeding it to `conflict_pressure` awaits that function gaining a run-path caller, which is the Crucible-war arc (recorded below), not this one. (b) the WIND and DUST vector fields are deferred: they are a large vector-advection sub-arc (a `thermal_buoyancy`/pressure vector field advecting heat, moisture, and a dust scalar, each its own pinned stencil and GPU gate) that the salt-flat milestone does not require; salinity, the milestone-critical field, is built without them (salt concentrates by evaporation and drainage, no wind needed). Both deferrals are honest scope calls, buildable follow-ons, not defects. Still to come as before.
3. **Close the M1 tail.** Add the hydrology pass `ROADMAP.md` M1 lists as light-or-stubbed, and move biome membership from the in-code `BiomeSet::dev_default` fixture to a data loader (Principle 11). Gate: `R-WEATHER` (site Part 18) is deferred and not required for the hydrology pass; the biome loader is a data-driven-by-default cleanup.
4. **The open determinism cluster, before any parallelism or persistence.** The audit's seven remaining determinism items, each cited to its site: `R-CANON-WALK` (Part 3.5, primitive built; the enforcement landed on branch `claude/determinism-onramp`: a clippy disallowed-types deny of `HashMap`/`HashSet` scoped to the canonical crates core, sim, and world, the `EventLog::by_entity_sorted` ordered accessor for the one named provenance index, and a test-time source-scan backstop, with consolidation to resolved owed), `R-REDUCE-ORDER` (Part 57, built for the combines touched, full form open), `R-SAVE-SCHEMA` (Part 58, only the id-continuity half built; the rkyv-and-bincode versioned persistence and migration path are not), `R-PROJ-REGISTER` (Part 58, only population, wealth, genome, and axiom register exact projections; belief, ecology, language, and institutions still fold as lossy means; the first aggregate-tier age conserved projection is now built (`AgeHistogram::total`) and adopted into the two-tier world (`crates/sim/src/lod.rs`: `Pool` carries an age distribution, `Individual` an age), with the `age_population` projection registered in the `invariants` harness and proven conserved across the promote/demote/merge tier crossings and an exact sink across mortality, a concrete de-risking increment), `R-UNITS-PIN` (Part 55, mechanism built, pin open), `R-HARNESS-COVER` (Part 58, extended, the full coarse-versus-fine proof open), and `R-CMD-ORDER` (built, plus a duplicate-`CommandKey` uniqueness guard added on `claude/determinism-onramp` so the barrier surfaces a key collision rather than leaking the worker schedule, consolidation owed). `R-GPU-CANON-PIN` is resolved and out of this cluster (record 62.23, built in `crates/gpu`, cross-backend arithmetic proof landed; Tier A above). The load-bearing one is `R-PROJ-REGISTER`: it is the hard prerequisite for temporal level of detail. Gate for the whole cluster: `ROADMAP.md`'s "stay serial" risk, the moment a phase is parallelised these become live blockers at once.
   - DONE (determinism-cluster on-ramp: `R-HARNESS-COVER` Phase-0, and `R-REDUCE-ORDER` for the two live combines). The sim determinism harness worker sweep was vacuous: it ticked the promoted beings on empty input, so the converse phase emitted no dialogue moves and the `CommandKey` barrier re-ordered an empty set, proving nothing. It now drives the beings with an observation schedule (`crates/sim/tests/determinism_harness.rs`: `seed_observations`, wired through a new additive `Runner::step_with_world_inputs` for the composed sibling), so the converse phase produces a non-empty, thread-scrambled move set (1650 moves in the direct sweep, 1476 in the composed), and a move-count guard (`MIN_EXPECTED_MOVES`) asserts the run can never silently go vacuous again; the widths 1/2/3/8 bit-identity and the move-count width-invariance both hold. Separately, the two live `R-REDUCE-ORDER` combines were analysed rather than rewritten (`crates/sim/tests/reduce_order.rs`) and an adversarial determinism audit corrected one claim. The typology weighted pick is order-independent by construction, pinned by the value-id-sorted walk `TypologyPrior::set` guarantees (value id is the total key that recovers the canonical weight vector from any permutation). The gossip conflict apply is now order-independent: an adversarial audit found the earlier first-writer-wins candidate set made it order-dependent when competing assertions carried different candidate sets (`add_evidence` silently dropped evidence toward a value absent from the frozen `hyps`), so on the owner's north-star call the candidate set is now the sorted UNION of every asserted hypothesis (`InferenceFrame::merge_hyps`, `evidence.rs`), applied in both the first-order belief (`consider`) and the theory-of-mind (`model`) paths. A belief's hypothesis space emerges from what the world asserts rather than from who spoke first, no evidence is dropped by arrival order, and the apply is safe to parallelise. Both R-REDUCE-ORDER sites are order-independent, proven across an arrival-order shuffle (`crates/sim/tests/reduce_order.rs`). Gate unchanged: the full coarse-versus-fine harness proof and the full `R-REDUCE-ORDER` form stay open, and the cluster stays serial until parallelism.
## Tier C: engine fidelity in time and breadth (gated on Tier B)

- **`R-TEMPORAL-LOD`** (site Part 32), the adaptive per-region clock, scoped in `docs/temporal_lod_research.md`. Hard prerequisites, cited: `R-PROJ-REGISTER` (load-bearing), `R-HARNESS-COVER`, `R-GPU-CANON-PIN` (resolved; the fixed-point-sampler primitives are built in `crates/gpu`), and the inter-region order items `R-CMD-ORDER`, `R-REDUCE-ORDER`, `R-SAVE-SCHEMA`.
- **`R-AGENT-EXEC`** (site Part 57), event-driven agent execution, scoped in `docs/event_driven_execution_research.md`. Prerequisites: `R-CMD-ORDER` (built) and the deterministic scheduler (signed off, core built in `crates/core/src/schedule.rs`, and its first real tick integration landed: the runner's tick phases are declared as scheduler systems and run through the serial executor bit-identically to the hand-pinned order (`Runner::step_scheduled`, `Runner::tick_systems`, proven in `crates/sim/tests/determinism_harness.rs`), with the scheduler auto-discovering that the cognition world parallelises with the field phases; the hotter `World::tick` cognition phases are the next surface, then the Rayon executor when profiled). It shares one event-queue substrate with `R-TEMPORAL-LOD`, so the two must be built to a single design.
- **`R-VIEW-ELAB`** (site Part 54), the non-authoritative view elaboration; sits with the level-of-detail foundations and is partly present as the pure-read superfine render.

## Tier D: the made world (Part 60 Stages 6 through 13), the largest new work

Gated on the focus-scale local settlement layer (Part 60 Stage 11), which Part 60 names the keystone and the heaviest single capability, on the scale of Dwarf Fortress's own core loop. The order within this tier follows Part 60: the living world (Stage 6, ecology, climate, economy, geology, with the stocks and biosphere substrate already built), peoples and conflict and deep history (Stage 7), the observation and dramaturg layer (Stage 8), emergent society (Stage 9, institutions and governance and economy already resolved as substrate, plus language drift, magic, combat, and divinity), presentation and modding and adaptive time (Stage 10, which folds in `R-TEMPORAL-LOD`), the local settlement layer (Stage 11), the civilizational physical layer (Stage 12, mechanical power, infrastructure, transport, agriculture), and knowledge and material culture and the social atom (Stage 13).

Content-registry gates that must open for world content to emerge, each a data registry rather than a closed enum (Principle 11), cited to site: `R-EVENT` (Part 7), `R-RELATION` (Part 10), `R-WEATHER` (Part 18), `R-CONTACT` (Part 21), `R-CATASTROPHE` (Part 26), `R-DOMAIN` (Part 38), `R-INFRA` (Part 46), and `R-COMMS` (Part 41).

The gaps-and-holes items ride this tier, several with a buildable half already in code: `R-EMOTION` (transient affect, buildable half in `affect.rs`), `R-SENSORIUM` (channel-gated perception, buildable half in `sensorium.rs`), `R-AGING` (the individual-tier life-process loop built in `world.rs` and now WIRED into `World::tick` as a pinned `life_cadence` sub-phase that ages and rolls mortality on the life-cadence period, per-world overridable, mortality gated on an installed owner hazard, proven to replay bit-for-bit; the canonical period now DERIVES from the world's orbit (`World::from_manifest_with_orbital` over `clock::ticks_from_seconds`, R-CELESTIAL) rather than the hardcoded `LIFE_CADENCE_TICKS`, which is demoted to a labelled dev/test fallback; the pool-tier age distribution and mortality built in `crates/sim/src/demography.rs` (`AgeHistogram`) and adopted into the two-tier world (`lod.rs`: `Pool.ages`, `age_pools`, age-carrying promote/demote, the `age_population` conserved projection), item open pending the age-tracked split rule and wiring the pool-tier demography into the tick), `R-REST`, `R-REPRO` (mate-choice direction researched and recommended emergent, cited in `docs/working/REPRO_MATE_CHOICE_SCOPED_PROPOSAL.md`; the load-bearing claim that direction follows the offspring fitness landscape and the mechanism authors none is proven prototype-in-isolation in `crates/sim/src/mate_choice.rs`, with the genome-derived fitness, the selection loop, the feature-weighted version over both axes (`evolve_featured_preference`), and the `World::birth` choose site (`World::choose_mate`, a per-being heritable preference seeded with unbiased variation and inherited at birth) all built; the shared-`evolve_with` folding and a Dobzhansky-Muller table on the World choose path are the remaining follow-ons; the SEX / BREEDING-SYSTEM substrate and the reproductive census now BUILT (gate: `crates/sim/src/breeding.rs` `SexClass`/`BreedingSystem`/`BreedingSystemRegistry`, a data-driven mating-type registry with dev fixtures for binary anisogamy, N mating types, and hermaphroditism, and the Fisherian `fisher_select_step`; `crates/sim/src/genome.rs` new `Channel::SexDetermination`, so sex is a gene-fed phenotype read through `express()` off a data-chosen locus with no `Phase::SEX_DETERMINE` and no drawn ratio; `crates/sim/src/race.rs` `Race.breeding`; `crates/sim/src/census.rs` `ReproductiveCensus` plus `ReproductiveMoments` and the Ne kernel: Wright 1931 `effective_size_sex` with its k-class harmonic generalization `effective_size_classes`, Crow-Kimura 1970 `effective_size_var`, composed by one race-blind `ReproductiveMoments::effective_size` feeding `GenePool::effective_size`; `crates/sim/src/world.rs` credits the census in `World::birth` and stamps founder sex at the dawn, window-stamped and RNG-free; pool tier via `AgeHistogram::add_births` and `Pool.repro`/`Pool::effective_size`, tier-consistent with the individual census in expectation (record 62.9); tests in `census.rs`, `breeding.rs`, `demography.rs`, and `tests/breeding_census.rs`. Non-steering (Principle 9): two races diverge in Ne only through their census data through the one kernel, swapping the census inputs swaps the Ne, a symmetric census yields N, and a 1:1 sex ratio EMERGES from the sex-determination locus under equal-investment selection rather than being hardcoded, shifting off 1:1 under unequal investment. Manifest: `genome.effective_population_size` RE-SCOPED to the pre-dawn founder seed (the canonical Ne now derives from the census), the value kept as the world-dialing lever. Remaining: reset the census window onto the generation cadence and route mate compatibility through `World::choose_mate`), `R-CHILDHOOD`, `R-JUSTICE`, `R-LIVELINESS`, `R-TECTONICS`.

## Tier E: the north star (Part 60 Stage 14; Parts 41, 61), the far horizon

The emergent technology design space, gated on the made world (Stages 11 through 13) and the physics substrate being in place. Its first gate is the one still-unimplemented test in the whole workspace: `convergence_without_a_target_is_forced_by_physics` (`crates/sim/tests/steering.rs`, an ignored placeholder whose own guard says the subject "does not exist yet"). Stage 14 must first prove convergence without a target, two isolated cultures reaching the same physical attractor from different seeds, with the convergence explained by physics and not authored bias, checked by the steering audit.

The `R-DEEPTECH` cluster: composition is resolved (`R-DEEPTECH-COMPOSE`, 62.10); the four deeper questions remain open and are, by the owner's standing instruction, to be taken super-deep when reached rather than scoped down now: `R-DEEPTECH-SCIENCE`, `R-DEEPTECH-PHYSICS`, `R-DEEPTECH-DEPTH`, and `R-DEEPTECH-SCALE` (sites Parts 41 and 61). The new steering item `R-EVOLVE-STEER` (site Part 8.4, flagged this session) rides here as well, since the pool-tier controller-evolution wiring it constrains is itself gated on the quantitative genome tier (25.10) and temporal level of detail (Part 32).

## Cross-cutting gates (true at every tier)

- **Owner calibration.** Eighty-two reserved values remain fail-loud. Nothing that runs a real world, as opposed to a dev-fixture test, is unblocked until the values on its path are set. This is the most pervasive gate in the project.
- **Stay serial.** Every determinism item in Tier B is dormant only while the tick is a single stream. The first parallelised phase makes the whole cluster a live blocker at once.
- **Tier consistency.** Any kernel that runs on both a promoted individual and a pool representative (the axiom kernel, the controller expression) must run identically on both, the invariant `R-TIER-CONSIST` guarantees and `R-PROJ-REGISTER` extends.

## What I recommend you sign off on

The order above is my synthesis of the consensus. The one genuine fork worth your explicit call is the head of Tier B: whether the immediate next work is (a) owner calibration so a real world can run, (b) wiring the cognition tick into the canonical runner so the built core drives a world at all, or (c) the determinism cluster so the world can later parallelise and persist. My recommendation is (b) then (a) then (c): make the built core run under the canonical runner first, calibrate against it, and hold the determinism cluster until just before parallelism, since the serial tick already replays bit for bit. The three consolidation debts (`R-CMD-ORDER`, `R-LANG-TYPOLOGY`, and the canonical-walk and reduce helpers) and the stale `ROADMAP.md` "Where we are" section are bookkeeping to reconcile whenever convenient, not blockers.

## Reserved-value derive-from-fundamentals audit (2026-07-03, applied)

Gate: the derive-vs-fundamentals audit of the full calibration manifest (`docs/working/RESERVED_VALUES_WORKSHEET.md` §13 through §17), applied to `calibration/reserved.toml` on `claude/reserved-values-consolidation`. Seventeen HIGH human/terran steers were found among graduated values; eight reverted set to reserved (fail-loud derive sentinels), five kept set with a flagged basis (hard readers or dial bases), twelve still-reserved steers de-anchored in basis. The manifest moved 76 set to 68 set of 153. The derivations themselves (h² readout, field diffusivity from thermal axes, damage caps from floor maxima, the behaviour-controller scalings, the ToM single-source-of-truth aliases, per-pair projection loss, enculturation from belief plasticity, the genome law constants) are the buildable-today implementation batch now in design; the blocked ones await their prerequisite build (the metabolic-rate kernel and body-surface-area axis, the edibility `ResourceField` composition, the parse-cost and attenuation laws, tier-consistency, the per-race maturation and memory datum).

R-CELESTIAL orbital elements: BUILT (gate: `crates/world/src/celestial.rs` `OrbitalElements` plus `crates/sim/src/clock.rs` `ticks_from_seconds`, `orbital_from_manifest`, `base_tick_seconds_fixed`; tests in `clock.rs`, `genesis.rs`, `world.rs`). A world's year and day are now fixed-point world-seconds carried through `GenesisParams`/`LivingWorld` (folded into `LivingWorld::state_hash` at a pinned position) and read from two new reserved scalars `world.orbital_period_seconds` and `world.rotation_period_seconds` (owner-set per world, fail-loud; Earth is the labelled `OrbitalElements::dev_earth` fixture, one option among many). The canonical life-cadence now derives as `ticks_from_seconds(orbital_period_seconds, time.base_tick_seconds)` in `World::from_manifest_with_orbital` rather than the hardcoded 31,536,000, with `LIFE_CADENCE_TICKS` demoted to a labelled dev/test fallback. The maturation half of `language.generation_ticks` (`Race::maturity_years` times the orbital year in ticks) is now BUILT (Wave-2 derive build-out below): `Language` carries its race, `World::drift_languages` derives each lineage's drift cadence as `race.maturity_years * World::life_cadence_ticks()`, and the `language.generation_ticks` scalar is RETIRED.

R-AXIOM evidence-ring capacity: BUILT (gate: `crates/sim/src/axiom.rs` `RingCapacityLaw::capacity_for` and `::from_manifest`, `crates/sim/src/calibration.rs` `require_curve`, wired at the dawn and at birth in `crates/sim/src/world.rs`; tests in `axiom.rs`, `calibration.rs`, `dawn_seed.rs`, `birth.rs`, `inheritance.rs`). The evidence-ring capacity now DERIVES per being from its own expressed `Mind.memory` through a reserved memory-to-slots curve (curve read, round half-up, clamp to a reserved hard ceiling), read by the same `Curve` mechanism the entrenchment gate uses. This supersedes the interim `ring_capacity_from_memory` (a `to_int`-clamped linear form, worksheet §13b), whose two-point case the curve reproduces. The single reserved `axiom.evidence_ring_capacity` is retired and split into `axiom.evidence_ring_curve` (the memory-phenotype-to-slots map, Miller's 7 plus or minus 2 kept only as a candidate point on the curve) and `axiom.evidence_ring_hard_cap` (the per-tick and per-save state-hash and replay budget), both reserved-with-basis (the manifest moved to 154 entries). The birth path is reordered so the child's genome and mind are expressed before its beliefs are inherited, fixing a bug that copied the first parent's ring cap onto the child regardless of the child's own recombined memory; the non-steering test (`dawn_seed.rs`) proves two races diverge in ring capacity purely from their memory-gene data through the one law, with no `RaceId` branch (Principle 9).

Maturation-datum: BUILT (gate: `crates/sim/src/race.rs` `lifespan_years`/`maturity_years` + `maturation_fraction`/`life_fraction`/`is_mature`; `crates/sim/src/world.rs` `race_of` map + `apply_mortality_by_race`; tests in `race.rs`/`life_stage.rs`). Adds the per-race maturation data the blocked steer-derivations (`plasticity_by_age`, `maturity_targets`, `l2_acquisition`) read, restores the missing per-being `StableId`-to-`RaceId` map so it is reachable, and makes mortality read a race-normalized `life_fraction` so one shared hazard curve means the same for an 80-year and a 700-year race. Non-steering: two races diverge in maturity and mortality purely from their data through the identical code path.

Edibility-composition: BUILT (gate: `crates/sim/src/edibility.rs` axis-id-keyed `Composition`/`Physiology` + the `net_nutrition` argument-order fix; `crates/sim/src/locomotion.rs` `ResourceField` per-tile `matter` + ingest derived from `laws::satisfaction`). Ingest now derives from the tile's measured composition against each walker's own physiology, retiring the flat `physiology.intake_yield` (tombstoned). Fixes a live bug (net_nutrition was always 1.0) and a Principle-11 fabricated-index defect (positional vectors re-keyed to biology-floor axis ids). Non-steering: two physiologies diverge on one identical tile, no race branch.

Env-variance: BUILT (gate: `crates/core/src/keys.rs` `Phase::DEVELOPMENT` 0x16; `crates/sim/src/race.rs` `environment_variance`; `crates/sim/src/world.rs` `env_offset`; tests in `development_env.rs`). A mean-zero symmetric per-being developmental offset makes `express()` vary per individual, so V_E is positive across a cohort and the phenotype variance exceeds the genotypic variance (V_P > V_A, h² < 1), the environmental half of narrow-sense heritability. The new reserved `genome.environment_variance` is fail-loud, and `Fixed::ZERO` reproduces today's homogeneous world bit-for-bit. Non-steering: V_E widens each race's spread without eroding the between-race difference; mean-preserving. Pairs with the still-walled breeding-value tier (V_A half) to fully derive `narrow_sense_heritability`.

Breeding-value tier: BUILT (gate: `crates/core/src/gauss.rs` `GaussApprox`/`gaussian_unit`/`gaussian`, exported in `crates/core/src/lib.rs`, tests in `crates/core/tests/gauss.rs`; `crates/sim/src/genome.rs` `GenePool` per-locus `effects` plus stamped `gauss`, `additive_variance`, `narrow_sense_heritability`, `hash_into`, the rewritten `promote`/`demote`/`found`, and `GeneticScheme.additive_mutation_step`/`gauss` with the `gamete` continuous step at `SLOT_MUTATE_STEP`; tests in `crates/sim/tests/breeding_value.rs`). On the owner's three stamped decisions: the integer-Gaussian is sum-of-uniforms at k=12 (the unit-variance scale `sqrt(12/k)` is exactly one, tails bounded to +/- 6, an honest limit), the `GenePool` representation carries per-locus additive effects alpha_i, and `narrow_sense_heritability` GRADUATES from an authored constant to the derived read `V_A/(V_A+V_E)` with V_A = `additive_variance()` = sum `2*p*(1-p)*alpha^2` and V_E = `genome.environment_variance`. A promoted individual now carries a continuous additive spine: per locus the pool's additive mean plus a mean-zero within-locus deviation scaled to the locus additive std `sqrt(2p(1-p))|alpha|` (one `Fixed::sqrt` per locus, never in a sub-draw loop), drawn on a slot distinct from the state draw; across a cohort its variance reconstructs `additive_variance()` (tier consistency, `R-TIER-CONSIST`), while its mean matches the pool so promote-then-demote is unbiased (the `2Ne`-weighted additive-mean fold in `demote`). The continuous mutation step (`GeneticScheme.additive_mutation_step`, reserved `genome.additive_mutation_step`, fail-loud) is the sole lever that grows the additive variance; a zero step freezes the spine. Locked representation: `effects` and the stamped `gauss` are folded into `GenePool::hash_into`, so the change is visible in identity. Manifest: `genome.gauss_approx` SET to `SumOfUniforms{k=12}` (a world-identity measure re-rolling every quantitative lineage if changed); `genome.additive_mutation_step` added reserved fail-loud; `genome.narrow_sense_heritability` and `being.heritable_fraction` retired to tombstones (they now derive; the effect vector alpha_i is per-race genome data, dev fixtures, never a manifest scalar). Non-steering (Principle 9, `breeding_value.rs`): two pools with identical frequencies and Ne but different alpha vectors (A dense small, B few large) diverge in V_A, heritability, and promoted spread; a relabelling swaps the outputs identically; a same-alpha, different-id case gives identical V_A; the kernels are race-blind and key off the effect data alone. The V_A==V_E case gives exactly one half (the human twin-study midpoint) as an emergent consequence, not an input. Consistency: `evolve::mutate` (behaviour-controller step) and `axiom::inherit_seed` (axiom-seed mutation, via the `world.rs` caller) now draw the same `core::gaussian_unit` k=12 primitive in place of their former bounded-uniform steps, keeping their existing keys; two seed-tuned property tests were re-tuned to the new deterministic trajectory (the thermotaxis dev-fixture seed `0x1234` to `0x1007`, the axiom mutation-bound assertion to the Gaussian +/- 6-std tail). Deferred as before: the infinitesimal segregation noise on gamete formation, multi-allele loci, the large-Ne Wright-Fisher approximation, and the distance-measure and speciation calibrations.

Technique-transmission: BUILT (gate: `crates/sim/src/transmission.rs` `Knowledge`/`TransmissionParams`, the transmit/erode/`is_stabilised` laws; `crates/core/src/keys.rs` `Phase::TRANSMIT` 0x17 and `Phase::KNOW_LOSS` 0x18; tests in `transmission.rs`). A knowledge-transmission substrate over an OPAQUE content-addressed `DesignId = u64` (never an authored Technique enum, the generalization catch that dissolves half of the Part 23/41 inconsistency structurally), with a copy-drift transmit law whose fidelity is a FUNCTION of the copier's per-race memory and perception (never an authored per-race table) and a below-floor erosion-and-cull loss law with free rediscovery. Exposes the reserved `transmission.drift_rate`, `transmission.loss_rate`, and `transmission.loss_practitioner_floor`, from which `compose.transmission_stability` now derives set-equal (`stability_span := ceil(ONE/loss_rate)`, `drift_similarity_radius := 2*drift_rate`, a local proficiency-convergence metric until the Part 41 evaluator's morphospace metric lands). Non-steering: a fidelity-swap dual invariant proves the outcome keys off derived per-race data, not race identity. The holder-count conserved projection is deferred (Knowledge is not attached to the tier boundary yet).

Trace-kind + race-base-rate substrate: BUILT (gate: `crates/sim/src/trace.rs` `TraceKindRegistry`/`TraceKindDef`/`DecayLaw` and the derivations `mortality_implication_weight`, `organic_salience`, `corroding_salience`; `crates/sim/src/base_rates.rs` `RaceBaseRateRegistry`/`RaceBaseRates`; `crates/sim/src/absence.rs` `absence_window`/`characteristic_lifespan`; `crates/sim/src/evidence.rs` `good_weight`; tests in each of those modules). Three authored evidence numbers now DERIVE through cited primitives. `evidence.implication_weights` retires: a trace's belief weight is I.J. Good's weight of evidence `good_weight` (ln P(E|H) - ln P(E|not H)) over the trace kind's own reliability and the race's own natural-mortality base rate, so a high-background-mortality race finds a corpse less diagnostic. `evidence.trace_decay_curves` retires: a trace's salience decays through the physics reaction kernel (thermal-activation gate) and `Fixed::exp` for organic remains scaled by the race's `decay_multiplier`, and through the physics corrosion kernel integrated over elapsed time for corroding traces. `evidence.absence_windows` retires: the presumed-dead window derives from the race's visibility, capped at `characteristic_lifespan` of the race's own hazard (a `LIFESPAN_HAZARD_THRESHOLD` percentile convention, not an authored biology value). The reserved inputs (per-kind reliability and `DecayLaw` parameters, per-race visibility and `decay_multiplier`, universal absence-stage multipliers) live as typed data on the registries (labelled dev fixtures), not manifest entries; the manifest drops three sentinels. Non-steering (Principle 9, swap-tested): the higher-background-mortality race gets the strictly lower implication weight, the lower-visibility race the strictly longer absence window, and the more corrosion-prone kind the strictly faster decay, each swapping when the data swaps, with a static check that no derivation branches on a `RaceId`/`TraceKindId` literal. `DecayLaw` is a CLOSED enum over the fixed physics kernels (an owner call, leaning enum since `physics::laws` is itself a fixed hand-authored kernel set); it is flagged in the type doc. Two integrations are deferred as named follow-ons and NOT done here: the global `World::mortality_hazard`/`apply_mortality` path is untouched (the two mortality sources are not yet unified), and the public `Trace` struct is untouched (no time-varying salience field, no `created_tick` sweep, no `PlaceId`-to-temperature wiring); the derivations are pure and standalone so the build stays additive.

R-METABOLIZE (derived resting metabolism and body-to-medium exchange): BUILT (gate: `crates/physics/src/laws.rs` `basal_metabolic_rate`/`resting_heat_loss`/`metabolic_drain_fraction`; `crates/physics/data/biology_floor.toml` new `bio.convective_surface` axis; `crates/sim/src/physiology.rs` (new) `whole_body_surface`/`whole_body_specific_heat`/`whole_body_energy_density`/`derive_base_drain`/`derive_exertion_coupling`/`derive_body_exchange_rate`/`MetabolicAnchors`; `crates/sim/src/homeostasis.rs` `DerivedDrain`/`metabolize_derived`; `crates/sim/src/runner.rs` per-being `body_exchange_rate` map + `set_body_exchange_rate`, `phase_body_exchange` rewired; tests in `laws.rs`, `physiology.rs`, `homeostasis.rs`, `runner.rs`, and `crates/sim/tests/metabolism_steering.rs`; closes the worksheet §102 blocked-on-the-metabolic-kernel derivation). Three authored scalars now DERIVE from a body's mass and tissue against the physics. `physiology.base_metabolic_drain` retires to a DERIVE sentinel: the resting drain is Kleiber's law `P = a*m^(3/4)` (`basal_metabolic_rate`, the exponent an authored universal physics affordance per West/Brown/Enquist, evaluated by the EXACT two-square-root fixed-point identity `m^(3/4) = sqrt(m*sqrt(m))` so no exp/ln is touched) plus the resolved convective and radiant thermoregulatory heat loss over the whole-body `bio.convective_surface` (`resting_heat_loss` reusing `convective_flux` + `radiant_emission`), bridged to a fraction of the reserve's stored energy (`metabolic_drain_fraction`). `physiology.exertion_drain_coupling` retires: the exertion drain derives from the mechanical work power `laws::power(force*velocity)`, scaled per tick by the being's exertion in the new `Homeostasis::metabolize_derived` (the scalar-field `metabolize` kept as the fixture path). `field.body_exchange` becomes a per-being derived rate `h*A/(m*c)` (`derive_body_exchange_rate`: `h` the medium coefficient, `A` the whole-body convective surface, `m*c` the body's thermal mass over `therm.specific_heat`), the runner using the derived per-being rate when supplied and the `FieldCalib.exchange` scalar only as a labelled fixture-fallback for a being placed without a body. New owner anchors, all reserved fail-loud with basis: `metabolism.kleiber_coefficient` (the coefficient a), `metabolism.body_mass_kg_scale` (the normalized-body-mass-to-kilograms R-UNITS-PIN bridge, NOT derivable), `metabolism.medium_convective_coefficient` (h), plus `metabolism.surface_emissivity` and `metabolism.stefan_boltzmann` for the radiant term. Determinism: the two-sqrt path is exact, the whole-body reads sum organs order-independently (`saturating_add`), `metabolize_derived` walks `reg.axes` canonically and `phase_body_exchange` walks `body_temp` in `StableId` order. Non-steering (Principle 9, `metabolism_steering.rs`): a large low-surface energy-dense body and a small high-surface water-rich body diverge in derived base drain and body-exchange rate from composition and mass alone; a hot-set-point body in a cold medium and its mirror diverge in derived drain from temperature alone; identical physics derive identical results and replay bit-for-bit; and a static source scan proves the kernels read no `RaceId`. Larger-body-drains-a-smaller-fraction is proven (basal grows as `m^(3/4)` while the reserve grows with size, the Kleiber signature). Honest limits: the exact kJ/g-to-joule reconciliation of the reserve's stored energy is the R-UNITS-PIN owner units bridge (the mechanism derives, the absolute scale is the owner anchors); the base drain (watts) and exertion drain (`laws::power` kilowatts) live on different power scales pending the same bridge; and, TIER-CONSISTENCY, the base drain is NONLINEAR in mass (`m^(3/4)`) and temperature (`T^4`), so a pool-tier drain over a size distribution is NOT the drain of the mean size (a Jensen gap), the honest cross-tier difference `docs/design.md:2803` already declares for a nonlinear law output over additive quantities, with an R-TIER-CONSIST reconciliation (a size-distribution moment or an accepted honest gap) named as the follow-on.

R-LANGMOD-PERCEPTUAL-GEOMETRY (perceptual geometry derives from channel physics): BUILT (gate: `crates/physics/src/laws.rs` `acoustic_absorption`/`tube_resonance` and the `F_ABS_MAX` overflow ceiling; `crates/physics/src/graph.rs` two `KernelContract` arms; `crates/physics/data/fluids_floor.toml` new `acoustic.absorption_reference`/`acoustic.resonator_length`/`acoustic.formant_frequency` axes and `law.acoustic_absorption`/`law.tube_resonance`; `crates/sim/src/langmod.rs` (new) `perceptual_geometry`/`PerceptualGeometry`/`PerceptualParams`; tests in `crates/physics/tests/fluids.rs` and `crates/sim/src/langmod.rs`; re-bases `calibration/reserved.toml` `langmod.perceptual_geometry`). Two authored universal physics affordances land on the fluids floor (Principle 9 permits authored physics): thermoviscous frequency-squared absorption `alpha = reference*f^2` (Stokes), overflow-staged as `reference*f` then `*f` (the poiseuille grow/shrink discipline) with a frequency past the `sqrt(2^31)` ceiling routed to the cap; and quarter-wave closed-open tube resonance `f_n = (2n-1)*c/(4L)` (the source-filter formant model), staged `c/L` first so the large intermediate never forms, a near-zero length routed to the cap and a zero sound speed to zero. Both report the linear indicator (alpha, alpha*path through the EXISTING `optical_depth`), deferring the exp transform, and both are pure closed-form `Fixed`, caller-cap-passed, and total on adversarial inputs. DEVIATION from the task sketch, on a codebase-grounded audit: the task proposed Asserted `KernelContract` arms, but both monomials CLOSE over integer exponents (`reference^1 * frequency^2` reduces to the absorption coefficient `1/m` once the reference carries its true residual dimension `1/(m*Hz^2)`; `c^1 * L^-1` reduces to a frequency), so under the closed-monomial discipline `graph.rs` states (Asserted is "never a lazy skip of a checkable monomial", and the explicit-rate pass made poiseuille a verified Monomial for exactly this reason) both are Monomial, dimensionally VERIFIED at load rather than waived (speed_of_sound stays Asserted because its square root halves the exponents to non-integers, the honest Asserted case). The three new floor axes are surfaced RESERVED-with-basis (cited acoustics envelopes: Kinsler, Fant, Stevens), the owner's to set, never fabricated; the absorption reference's sub-epsilon SI envelope reserves a per-quantity windowed scale (R-UNITS-PIN). The sim-side `langmod::perceptual_geometry` DERIVES the per-modality confusability table: per feature value a resonator length maps to a formant vector via `tube_resonance`, pairwise formant distances are weighted by `acoustic_absorption` over a typical path (through `optical_depth`) and by the being's sensorium per-channel just-noticeable difference (the channel gate: a being blind to the channel derives nothing), and the medium-blind contrast budget is the usable-span-over-just-noticeable-difference. `langmod.perceptual_geometry` is re-based off the human auditory dispersion target to derive through this. NON-STEERING RESULT (Principle 9, proven both floors): the SAME resonator lengths in two real media (air c ~340, water c ~1480, each c from `speed_of_sound` over its floor bulk modulus and density) give two DIFFERENT formant vectors and two different confusability tables, with no authored per-race table and no `RaceId` (the function takes none); the equal-capacity invariant holds (medium B at twice the sound speed and twice the just-noticeable difference shares A's contrast budget though its confusable pairs differ, because frequency-squared absorption blurs high formants more, so a physics difference does not force a richness difference). Honest limit: the raw-`Fixed` kernel path works in a representable scale, so the true sub-epsilon SI absorption reference is exercised through a scale-bridged value in tests and its exact bridge is the reserved R-UNITS-PIN anchor; the read-out models the acoustic modality (the manual and chromatic modalities plug in the same shape once their channel-physics laws land).

R-LANG-TYPOLOGY word-order harmony tilt (derived from parse cost): BUILT (gate: `crates/physics/src/laws.rs` `parse_cost`/`harmony_tilt` plus their unit tests; `crates/sim/src/typology.rs` `linearization_domain`/`derived_tilt`/`grammar_parse_cost`/`TiltParams`, the `HarmonyBias.structural_weight` field replacing `weight`, the rewired `tilted_weights`/`sample_profile`, and the `TypologyParams` temperature read; `crates/sim/src/language.rs` new `Linearization` enum and `ProductionModalityDef.linearization` field; tests in `laws.rs`, `typology.rs`, and `crates/sim/tests/reduce_order.rs`; retires `calibration/reserved.toml` `lang.typology_harmony_strong`/`lang.typology_harmony_weak` to tombstones and adds `lang.typology_temperature`). The authored WALS/Dryer harmony tilts stop being numbers and become DATA plus one derivation. On the owner's SPLIT: a direction-NEUTRAL processing-cost floor lands in `laws.rs` (`parse_cost(domain_extent, memory_capacity, cost_max)`, the Hawkins 1983/2004 dependency-integration cost as the integer-Hill saturating form `extent/(extent+memory)` scaled by the cap, softened by working memory; `harmony_tilt(cost_reduction, temperature, tilt_max)`, the softmax weight `exp(cost_reduction/temperature)` floored at one and saturating at the cap through the canon-pinned deterministic `Fixed::exp`), and the branching-consistency mapping lives in `typology.rs`. Both kernels are LABEL-BLIND and DIRECTION-BLIND: they read only a scalar extent and a scalar reduction, never a word-order value, so they author no attractor (Principle 9). The `HarmonyBias` row now carries a data-defined `structural_weight` (how many linearization decisions the pairing constrains: the two-sided fully-phrasal adposition and relative-clause pairs get 2, the one-sided partly-non-phrasal genitive pair gets 1, grounded in Dryer's Branching Direction Theory), so strong-vs-weak is DATA and the tilt DERIVES: `derived_tilt` folds the firing rows' structural weights by the order-independent `Fixed::saturating_sum`, runs the folded extent through `parse_cost` then `harmony_tilt`, and `tilted_weights` multiplies the prior count by that derived tilt in place of the retired constant. The single reserved manifest entry is now the softmax `lang.typology_temperature` (the two tier tilts tombstoned; the disharmony gate kept). NON-STEERING RESULT (Principle 9, proven): two races identical but for a permutation swapping which adposition value id is head-initial produce an IDENTICAL derived tilt, an IDENTICAL grammar parse cost, and an IDENTICAL (exact, weight-over-total) sampled harmonic proportion, with the non-permuted parameters drawing bit-identically and the empirical proportions matching within tolerance; the DIVERGENT-MODALITY test shows a `Simultaneous` modality suppresses the tilt to the untilted marginal (about 0.49 postpositions given OV) while the `Sequential` one shows a strong tilt (about 0.96), the two grammars diverging; a per-race test shows a larger working memory softens the parse cost and weakens (never removes) the harmony pressure; and the sampler replays bit-for-bit across repeats and call order (observer independence, identical at every LOD tier). HUMAN-DATA-ROW gate (validation): with the single dev-fixture temperature at the human calibration (and the human race's working memory), the sampled harmonic proportion reconstructs WALS 95A on the adposition axis (about 0.96, in the 94 to 97 percent band) and Dryer 1992 on the OV genitive side (about 0.89), proving the one free scale is validated AGAINST the human row rather than authored as the tiers. DEVIATIONS, surfaced: (1) `cost_max` is passed as `ONE` so `parse_cost` reports a normalized cost FRACTION and the temperature is the single free scale (no second magnitude hides in the cap); (2) the genitive VO side sits at the WALS-language-count marginal (about 0.55), not Dryer's genera-based 0.45, since the one-sided bias does not tilt that side, the model's honest one-sided prediction stated in the test and the temperature basis; (3) `sample_profile` takes `#[allow(clippy::too_many_arguments)]` at nine args (substrate, calibration, modality flag, draw coordinate), the same `#[allow]` the `laws.rs` kernels take; (4) `grammar_parse_cost` was added as a companion pub fn so the grammar-scope monotonicity test (a harmonic grammar costs strictly less than a mixed one) runs the real code path; (5) the direction-neutrality is exact at the proportion and weight level, while individual per-draw picks differ because the cumulative walk is value-id-ordered, so the test asserts the exact tilted proportion and derived tilt and grammar-cost identity plus a Monte-Carlo cross-check. Determinism: pure `Fixed` kernels, canon-pinned `exp`, checked-and-capped arithmetic, order-independent folds, no new `DrawKey` (only the weights fed to the existing keyed walk change), and the modality gate a deterministic conditional on the data flag. The design-document consolidation (the Part 33.4 blockquote, the Part 62 record, the Part 63 bibliography, the audit-log Section 1 block and counts) is NOT done here and stays the follow-on, on the owner's sign-off, as the scoped proposal directs.

Belief facet-strength + prevailing-belief substrate (the Part 54 belief lift/restrict operator): BUILT (gate: `crates/sim/src/belief.rs` `FacetStrength`/`BeliefKey`/`PrevailingBelief`/`BeliefPool`/`BeliefParams` and the `instantiate_strength` lift, the `advance_diffusion`/`lift_one`/`lift_cohort`/`fold_one`/`fold_back` operators; `crates/core/src/keys.rs` `Phase::BELIEF_LIFT` 0x19; `crates/sim/src/evidence.rs` `aggregate_diffusion_rate`; `crates/sim/src/lod.rs` the `BeliefPool` on `Pool`, the `Vec<(BeliefKey, FacetStrength)>` on `Individual`, `promote_lifting`, the belief restriction folded into `demote`, `belief_mass`, and the belief folding in `state_hash`; `crates/sim/tests/invariants.rs` the `aggregate_belief_mass` projection with the round-trip and leak tests; tests in `belief.rs`, `keys.rs`, `invariants.rs`). The belief keystone deferred from the evidence work (design.md Part 54) lands on the owner's decision: a prevailing belief carries an INTENSIVE `knowledge_level` (a `Fixed` in `[0,1]`, the derived mean conviction) and an EXTENSIVE belief `mass` (the raw Q32.32 bit-sum of member facet strengths, an exact-associative `i128` like `total_wealth` at `lod.rs`), reconciling the Part 9.14 integer diffusion with the Part 54 monotone fixed-point curve and plugging into the conserved-projection registry. Lifting (`promote_lifting`) mints each promoting mind's facet strength from the pool level through the reserved monotone curve plus a counter-keyed per-mind dispersion (keyed on being, belief-content hash, tick, and `Phase::BELIEF_LIFT`), subtracting exactly the minted bits; restriction (`demote`) folds the id-ordered strengths back by adding exactly their bits, so total belief mass is conserved BOTH directions by construction and registers alongside population and wealth. CONTENT-BLIND (Principle 9, proven): the diffusion and the lift key on no belief's identity, so two beliefs differing only in their `BeliefKey` diffuse and lift identically and a permutation of belief ids leaves belief-mass conservation and the canonical state hash invariant; two races diverge only through their per-race curve DATA, a pure function of the curve, with no belief-kind branch. Manifest: the two former mappings `evidence.knowledge_to_strength` and `tier.belief_level_to_strength` now DERIVE through the one `belief::instantiate_strength` (the reserved inputs are the `tier.belief_level_to_strength` curve, the new split-out `tier.belief_dispersion` half-width, and the `evidence.aggregate_diffusion_rate` rate); `evidence.knowledge_to_strength` is retired to a tombstone (a duplicate curve) and the former curve-plus-dispersion bundle is split, all reserved fail-loud (net manifest change zero: one retired, one added). HONEST LIMIT, documented: because the curve may be nonlinear and the dispersion is a finite sample, the intensive level drifts slightly on a lift while the extensive mass stays exact; that the population mean reconstructs the pool level within a fixed-point tolerance is a CALIBRATION TARGET on the curve and dispersion (proven within 0.01 on the identity-curve fixture), not a mechanism-enforced identity, the deliberately-lossy lift direction of Part 54 corrected on the next re-summarize. Determinism: mass is a raw i128 bit-sum (exact, associative), the dispersion draw is bit-identical and independent of cohort iteration order, and the fold-back mean is order-independent; any level-to-mass remainder rides the existing lowest-id rule (`tier.partition_remainder_rule`). NOT done here (follow-ons): the design-document consolidation (the Part 9/54 blockquotes, the Part 62 record, the Part 63 bibliography, the audit-log Section 1 block and counts) on the owner's sign-off; the belief state is not yet in the `WorldSnapshot` schema (an R-SAVE-SCHEMA follow-on, so a reloaded tier starts belief-free); and the substrate is standalone, not yet wired into the canonical `World::tick` belief phase.

R-DEEPTECH-COMPOSE (the Part 41 technology-composition evaluator): BUILT (gate: a NEW crate `crates/compose` (`civsim-compose`), depending only on `civsim-core` and `civsim-physics` and added to the workspace members and default-members; `crates/sim` now depends on it. Modules `interval.rs` (the fixed-point `Interval` with saturating, bound-propagating `Add`/`Sub`/`Mul` and `min_with`), `form.rs` (the FORM/JOIN floor: `FormRegistry`/`FormId` over the wave-1 geometric axes `mech.contact_area`/`section_modulus`/`arm_length`/`edge_radius` and `JoinRegistry`/`JoinId`, labelled dev seeds), `node.rs` (`CompositionNode`/`NodeBody::Leaf`/`::Composite`/`ComponentRef`/`IntentRef` with a PURE-CONTENT `content_id`), `interface.rs` (the open `InterfaceRegistry` of `InterfaceAxisDef` bound to physics axes with `Direction` and role, the fixed-width `PortVector`), `combinator.rs` (the `CombinatorRegistry` keyed by topology, dev-seeded with the four kernels `LimitingMin`/`SaturatingSum`/`ConservedBudget`/`EfficiencyProduct`), `proxy.rs` (the open `ProxyRegistry` of closed-form whole-system proxies plus `ProxyWeights`), `eval.rs` (`evaluate_node`/`evaluate_uncached`/`Memo`/`NodeEval`/`PenaltyCurve`/`EvalParams`), `promote.rs` (the three-gate `promote`/`promoted_library`); tests in `crates/compose/tests/evaluator.rs` (15) plus the fmt/clippy gates; re-bases `calibration/reserved.toml` `compose.viability_threshold`, `compose.interface_penalty_curve`, `compose.emergent_proxy_weights`). The Part 41 evaluator is the EMERGENT side of the technology substrate, kept in its own crate OUTSIDE `crates/physics` on purpose (Principle 9, the authored-physics boundary made a crate boundary): nothing in physics depends on compose, and the kernel path takes only `Fixed`, never a material or race id. A leaf runs the physics law kernels (`bend_stress`, `fracture_onset`) over form geometry and a material `Substance` vector through an axis-presence dispatch; a composite recurses (children resolved by content id from the `Memo` store, cache-keyed by that id), folds child vectors by each interface axis's combinator, applies the proxies, and charges a graded interface-mismatch penalty through a `decision::Curve`-shaped `PenaltyCurve` (graded inside the adaptable range, FLOORED beyond, so a mismatch is softened by an emergent adapter rather than hard-rejected). Three of the six `compose.*` values now route through the evaluator: `compose.viability_threshold` DERIVES to the physics collapse boundary (viability is a DIMENSIONLESS safety fraction `1 - utilization`, so the boundary is a safety of ZERO, defined by the material's own yield and fracture data; only an owner safety factor above zero stays reserved-with-basis); `compose.interface_penalty_curve` is reserved-with-basis THROUGH the evaluator (the adaptable range derives from the interface axis's physics range width, the penalty depths reserved); `compose.emergent_proxy_weights` is reserved-with-basis THROUGH the evaluator (the proxy membership is data, each proxy's criticality weight reserved and FAIL-LOUD at `Memo::new`); `compose.transmission_stability` already derives set-equal to the transmission substrate rates (unchanged). NON-STEERING RESULT (Principle 9, proven): the SAME structural design (identical form, join, and PortVector contract) over two materials with divergent physics (a hard-brittle stone, high strength and low toughness, versus a tough-ductile bronze) DIVERGES in viability and aggregated vector purely from the material vectors, with the stone winning the stress margin and the bronze the toughness margin (neither dominates), and the kernel path taking no id (a relabelled substance with identical physics evaluates identically). NON-TEMPLATEDNESS (Principle 8): two peoples with different interface substrates (one exposing an exotic stiffness-resonance axis) get DIFFERENT promoted-library shapes under ONE physics (the exotic people's resonance proxy rejects a heavy design the base people promote). MEMOISATION: evaluate-with-cache equals evaluate-without-cache bit-for-bit. CONTENT-ID: same node maps to the same u128 across builds, permuted children canonicalize equal, and the intent is not folded. TIER-CONSISTENCY: the additive envelope-mass slot (ConservedBudget) satisfies parent equals the sum of children EXACTLY (an `i128` bit-sum), while a nonlinear proxy over that additive mass (a square-root resonance) is tier-resolution-dependent BY CONSTRUCTION and is NOT asserted invariant (the honest coarser-physics gap). DEVIATIONS, surfaced: (1) the node `content_id` uses PURE-CONTENT addressing (a `StateHasher` digest over the canonical body, master seed and intent OMITTED), matching the physics `Substance::content_id` pattern for cross-world dedup, flagged for owner ratification; (2) the interface-mismatch `PenaltyCurve` is a compose-local mirror of `civsim-sim`'s `decision::Curve` (compose sits below sim in the crate graph, so it cannot depend on it), same piecewise-linear clamp-at-ends shape; (3) the leaf structural dispatch consumes contact_area, section_modulus, and arm_length; edge_radius is carried by the form floor for the cutting dispatch, not the structural one; (4) the physics caps the leaf routes out-of-range results to derive from the substrate's axis-range upper bounds by dimension, with `Fixed::MAX` (the representable ceiling, not an owner value) the fallback when a dimension has no set axis. Design-document consolidation (Part 41 blockquote, the Part 62.10 record, Part 63 bibliography, the audit-log Section 1 block and counts) is NOT done here and stays the follow-on on the owner's sign-off; the sim-side wiring of the manifest into the evaluator and the canonical library into a culture is the named next increment (compose is standalone and additive today).

- **R-ORGAN-FLUX (flagged future derivation).** The inter-organ flux graph: circulation and respiration wired between named organs so a localized organ wound cascades physiologically, a level below the current two-tier aggregate-reserve (`anatomy.rs`) plus promoted-part-wound (`body.rs`) model. Gate: worksheet §18; owner-flagged, not scheduled; the binding constraint is the derive-not-tag rule (a flux law reads organ geometry and fluid properties, never an organ tag).

Audit-fix pass (2026-07-03, APPLIED): the blind adversarial audit of the sixteen built substrates confirmed 26 defects (1 critical, 8 major, 17 minor), zero surviving refutations, with the architecture sound and the defects concentrated in wiring gaps, bounds seams, and fail-loud lapses. The critical: the celestial substrate was not wired into the production `World::from_manifest`, so the Earth-year `LIFE_CADENCE_TICKS` was still live (a Principle-9 breach the co-designed tests missed by exercising only the test-only constructor). The fix pass is now applied. Every must-fix (the 1 critical + 8 major) landed with a REGRESSION TEST exercising the production path: (1, critical) `World::from_manifest` gates on the two per-world orbital scalars plus the base tick and derives the life cadence from the manifest orbit inside the shipping constructor, and a calibrated world with a reserved orbit now fails loud (the dev fixtures declare an Earth orbit; the reserved manifest's orbit stays owner-set per world, so the two calibrated determinism tests supply a labelled fixture orbit through `from_manifest_with_orbital`); (2) the terran-water specific-heat baseline (`4186`) is retired for the ZERO-absence convention its siblings use; (3) the composition content-id sorts composite children on a full per-child digest so overrides ride with the ordering; (4) `World::state_hash` folds `life_cadence_ticks` and the mortality-hazard curve (a golden hash refreshed); (5) `apply_mortality_by_race` fails loud (`UnraceableBeing`) on an untracked being rather than reading a life-fraction curve at raw age; (6) `mortality_implication_weight` is Good's log-likelihood ratio of two LIKELIHOODS (a new per-kind `false_attribution` denominator), the base rate kept to the prior; (7) `signature_distance` squares in i128 so large extensive-count differences no longer wrap negative to a zero distance; (8) the norm dedup and sort key fold attribute and condition (full identity) and count a rule as deontic AND sanction; (9) `genome.mutation_step` and its `.high`/`.low` are tombstoned and the seven scenarios repoint to `genome.additive_mutation_step` (with new reserved `.high`/`.low` siblings). The minors landed too: the exertion coupling reconciled to the watt scale (`laws::power_watts`); `acoustic_absorption`'s frequency-alone early return dropped; the transmission draw folds the learner; typology's parse-cost integration unified through one helper; edibility's dose/tolerance zipped over paired classes; the k=12 Gaussian threaded through a world-identity stamp and `EvolveParams`; the ULP-bias, retired-key, and scan-ceiling docs corrected; `Memo::new` validates every combinator key; the two whole-system proxy margins (control-loop, resonance) subtract reserved floors so a below-floor design registers. DEVIATION, surfaced: audit minor 23 (assign a census-derived Ne into the pre-dawn drift) is a NAMED SEAM, not a local rewire: the aggregate pre-dawn tier carries a suitability-scaled carrying-capacity Stock, not the reproductive census the Ne kernel reduces, so feeding a real per-species census into the drift is a design addition (recorded in-code at `epoch.rs` and reported), and the authored `pool_size` stays the honest aggregate-tier Ne until it lands. Gates: `cargo build`, the five-crate test suite, `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests`, `scripts/verify.sh`, and the manifest/scenario resolution all green. The tail of about eighteen derive-pending values (phoneme priors, l2-acquisition, the value-metric floors, the personality curves, `field.diffusion`, the ToM aliases) is scoped for the build-out that closes the derive column, after which the manifest is only levers, constants, and budgets.

Wave-1 derive build-out (eight validated derivations applied): BUILT (gate: `crates/sim/src/body.rs` `Body::function_integrity` + the `loss_knee` helper; `crates/sim/src/langmod.rs` `CapabilityGate`/`capability_halves`/`capability_gate` added alongside the existing `perceptual_geometry`; `crates/sim/src/value.rs` `project_to_etic_with_loss`, the `etic_euclidean`-to-`etic_sq_distance` split, and the flat incommensurability floor removed from `cross_race_distance`; `crates/sim/src/axiom.rs` `enculturation_pull_rate`; `crates/sim/src/language.rs` `SalienceDecayLaw`; `crates/sim/src/world.rs` `band_mean_memory` + `build_etic_substrate` + the derived ToM path in `from_manifest_gated`; `crates/sim/src/typology.rs` `information_weights`; `crates/sim/src/tom.rs` `meta_params_from_evidence` (replacing `meta_params_from_manifest`) + `AccessWeights::from_evidence_and_manifest`; `crates/sim/src/genome.rs` `IncompatibilityTable::active_between` rewritten to the joint Hardy-Weinberg product + the `state_freq` helper; `crates/sim/src/lib.rs` additive re-exports; tests ported/added in every touched module; re-bases/retires in `calibration/reserved.toml` `value_metric.enculturation_pull_rate`, `value_metric.incommensurability_floor`, `value_metric.etic_substrate_axes` (+ new `value_metric.etic_recurrence_min`), `lang.typology_distance_weights`, `langdet.salience_decay_rate` (re-scoped to a curve, + new `langdet.salience_decay_floor`), `langmod.capability_gates` (+ new `body.function_loss_threshold`), `genome.allele_presence_threshold` (re-based, re-reserved), and the six ToM aliases removed). Eight `value_metric.*`/`lang.*`/`langdet.*`/`langmod.*`/`genome.*`/`tom.*` values move from authored numbers to fundamentals. (1) A body function's produce capacity is the mean structural integrity of its intact bearers, kneed at the wound function-loss threshold, so redundant limbs degrade a manual channel gracefully and a bearer worn past the threshold loses the function; (2) the language capability gate is the Liebig minimum of that produce half and the sensorium's perceive acuity, two races diverging only through body and sensorium data run through one kernel; (3) the cross-race value incommensurability is measured per race-pair as the etic projection loss (one untranslatable axis of stance s raises the distance by exactly the magnitude of s) rather than a flat floor, which is removed from `cross_race_distance` entirely; (4) enculturation pull is the Friedkin-Johnsen complement `1 - effective_stubbornness`; (5) concept-salience decay reads a decreasing memory-to-rate curve at the founding band's mean memory (`band_mean_memory`, an i128 sorted fold) floored by an underflow bound, so a sharp-memoried culture leaks its lexicon slower; (6) the shared etic substrate grows bottom-up, one axis per emic value axis that recurs across at least `etic_recurrence_min` races, an idiosyncratic axis staying private; (7) typology distance weights are the order-2 diversity `N^2 / sum(c_i^2)` of each parameter's prior counts; (8) the Dobzhansky-Muller cross counts by the joint Hardy-Weinberg product of the two pools' allele frequencies gated by the same presence, retiring the marginal-AND. The ToM meta params derive as the first-order `evidence.*` params themselves and the witnessed/told/said ladder derives from them by the offsets `commit + margin`, `commit`, `commit - margin`, so a witnessed access out-ranks a contrary telling by exactly the runner-up margin the commit test uses and a lie is seen through by construction; `reachable`, `absence`, and `denied` stay independent reserved levers. Every kernel is fixed-point, race-blind (no `RaceId` branch, Principle 9), and carries ported non-steering tests. RECONCILIATIONS: `langmod.rs` already existed on this branch (the `perceptual_geometry` read-out), so the capability-gate struct and functions were ADDED to it with their own test submodule rather than overwriting; the branch's diverged `lib.rs`, `axiom.rs`, `value.rs`, `language.rs`, `typology.rs`, and `world.rs` re-exports and imports were extended additively; and `World::from_manifest_gated` was repointed from the manifest ToM reads to the derived path, dropping the three `tom.meta_*` gate requirements. NEW RESERVED INPUTS surfaced fail-loud with basis (never fabricated): `value_metric.etic_recurrence_min` (the recurrence at which a value axis is shared not idiosyncratic), `langdet.salience_decay_floor` (the salience-scale resolution floor `ceil(2^32 / usage_max_bits)`), `body.function_loss_threshold` (the wound function-loss knee, set equal to the wound model's boundary per record 62.13), and `langdet.salience_decay_rate` re-scoped from a scalar rate to a decreasing memory-to-rate curve. DEVIATION, surfaced: `genome.allele_presence_threshold` was a SET owner value (0.9) for the marginal meaning; the joint Hardy-Weinberg gate is a materially different scale, so it is re-based and RE-RESERVED (fail-loud) for the owner to re-set under the joint-probability meaning rather than silently reusing a number calibrated for the retired mechanism. Gates: `cargo build`, `cargo test -p civsim-sim` (511 lib + integration green), the core/physics/compose/world suites, `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests`, `scripts/verify.sh`, and the real-manifest load/scenario resolution all green. Not done here (follow-ons): the design-document Part 62 consolidation of these derivations and the audit-log Section 1 blocks stay owner-sign-off follow-ons; the derived salience law, capability gate, and etic substrate are standalone kernels, not yet wired into the canonical `World::tick`.

Wave-2 derive build-out (five related language-substrate derivations applied): BUILT (gate: `crates/sim/src/langmod.rs` `acquisition_split` + `phoneme_priors`; `crates/sim/src/language.rs` a `resonator_length` field on `FeatureValueDef`, `Language::race` (set at `Language::new`, carried through `fork`), the retired `DriftParams.generation_ticks`, and the `L2AcquisitionLaw` + `LangKnowledge` L2 kernel; `crates/sim/src/agent.rs` `RetentionLaw`; `crates/sim/src/world.rs` a `races` registry field (populated at `seed_dawn_populations`, settable with `set_races`), `set_form_system_for`, and `drift_languages` taking a `races` param with a per-lineage derived cadence (the `drift_step` move-out-and-back wrapper on the tick, `lang.race` folded into `state_hash`); `crates/sim/src/lib.rs` additive re-exports; tests in `langmod.rs`, `language.rs`, `agent.rs`, the reworked `crates/sim/tests/drift.rs`, and the new `crates/sim/tests/l2_acquisition.rs`; re-bases in `calibration/reserved.toml` of `langmod.acquisition_split`, `lang.phoneme_priors`, `lang.l2_acquisition` (to a maturation curve), and `langdet.usage_recency_window` (+ new `langdet.retention_memory_scale`), the RETIREMENT of `language.generation_ticks` in both `reserved.toml` and `calibration/profiles/dev-fixtures.toml`). Five values move from authored numbers to fundamentals, all on the substrate the Wave-1 gate and the celestial/maturation data already built. (1) `langmod::acquisition_split` is the being's perceive minus produce capability, read straight from the two `capability_halves`, so the receptive-bilingual asymmetry falls out of the capability gap: a race strong in perception weak in production splits positive, the swap mirrors it exactly, equal halves split zero, no `RaceId`. (2) `langmod::phoneme_priors` is a spread-maximising dispersion prior over the race's own `perceptual_geometry` confusability (each value's prior the sum over the others of `ONE / (ONE + confusability)`, higher where a value is more distinguishable), masked by the per-value capability gate so a value the race cannot produce or perceive gets zero prior; UPSID/PHOIBLE become the human race's DATA ROW (its resonator lengths, now carried per feature value on `FeatureValueDef.resonator_length`, and its sensorium acuity) rather than the global prior. Two media give two priors from the channel physics alone, no authored inventory, no `RaceId`. (3) the L2 kernel (`L2AcquisitionLaw::increment_for` + the per-being-per-language `LangKnowledge`) derives the age-of-acquisition breakpoint from each race's own maturation (`Race::maturation_fraction`, off `maturity_years`), the increment a decreasing data curve read at the maturation fraction, so two races with different `maturity_years` cross the critical period at different raw ages from one kernel and the increment slows past each race's own breakpoint, never the human late teens, no `RaceId`. (4) the drift cadence derives PER LINEAGE: `Language` carries its race, and `World::drift_languages` reads each lineage's race maturity against the world's `life_cadence_ticks` (the orbital year in ticks) so the generation length is `race.maturity_years * life_cadence_ticks`, two lineages of races with different maturities drifting on different cadences (proven by change-log count over a fixed span) with the one global `generation_ticks` scalar retired; a lineage whose race is absent does not drift on a fabricated cadence. (5) `RetentionLaw::window_ticks_for` scales a base recency window (the kept `langdet.usage_recency_window`) by a data curve read at a mind's memory, so a sharper memory holds a longer window and a flat curve reproduces one window, keying on the memory scalar, no `RaceId`. Every kernel is fixed-point, deterministic, race-blind (Principle 9), and carries a non-steering test. NEW RESERVED INPUT surfaced fail-loud with basis (never fabricated): `langdet.retention_memory_scale` (the increasing memory-to-window-multiplier curve the retention law reads). RE-BASED to DERIVE (kept reserved, fail-loud, value cleared): `langmod.acquisition_split` (no input remains, a pure capability subtraction), `lang.phoneme_priors` (the physics-and-sensorium data plus the new `resonator_length`), `lang.l2_acquisition` (re-scoped from a scalar rate to a maturation-fraction increment curve), and `langdet.usage_recency_window` (re-scoped to the BASE window the retention curve scales). RETIRED (comment tombstone, engine no longer reads it): `language.generation_ticks`, the same treatment as `genome.mutation_step`. DEVIATIONS, surfaced: (a) `drift_languages` needs the `races` registry, but threading it through `World::tick` would touch about eighty call sites, so the world now stores a `races` field (populated at `seed_dawn_populations`, settable with `set_races`, moved out and back on the tick's `drift_step` to satisfy the borrow checker), consistent with how `life_cadence` reads stored fields rather than per-call race maps; the field is owner config, not folded into `state_hash` (the lineage's `race` id is, mirroring how `race_of` is unfolded), so the observable divergence is the drift the derived cadence produces over time. (b) The retention law is placed in `agent.rs` (co-located with `Mind.memory`, the belief-deterioration consumer it names), not `language.rs`; the task allowed either. (c) `phoneme_priors` returns priors in feature-value order paired with the index as the id (the geometry is built index-ordered from the values), so a caller with arbitrary value ids zips the returned priors with its own ordered value list; documented at the function. Gates: `cargo build`, `cargo test -p civsim-sim` (515 lib + integration green), `cargo test -p civsim-core`, `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh`, and the real-manifest parse all green. Not done here (follow-ons): the Part 62 consolidation and audit-log Section 1 blocks stay owner-sign-off follow-ons; the five kernels are standalone (like the Wave-1 salience law and capability gate), not yet wired into the canonical `World::tick`.

Wave-3 derive build-out (three semantics-and-distance derivations applied): BUILT (gate: `crates/sim/src/value.rs` `GroundMetric::resolution` (the grid separability floor) and `incommensurability_ceiling` (a pure alias of the per-race-pair etic projection loss `project_to_etic_with_loss` sums); new `crates/sim/src/semantics.rs` (`Concept`, `SemanticSubstrate`, `ConceptThresholds`, `concept_thresholds`, `substrate_quantization`, `QUANTIZATION_DIVISOR`); new `crates/sim/src/langdist.rs` (`lexical_distance`, `phonological_distance`, `language_distance`, `ComponentWeights`, `distance_component_weights`); a `FormSystem::inventory` accessor on `crates/sim/src/language.rs`; `crates/sim/src/lib.rs` module registration and additive re-exports; per-module non-steering tests; re-bases in `calibration/reserved.toml` of `lang.concept_thresholds`, `langdet.substrate_quantization`, `langdet.incommensurability_ceiling`, and `lang.distance_component_weights` to DERIVE). Three reserved values move from authored numbers to fundamentals on the substrate Wave-1 (the etic projection loss and `information_weights`) and Wave-2 (`perceptual_geometry`) already built. (1) `value::incommensurability_ceiling` is a pure ALIAS of the summed measured etic projection loss of a race pair (the same two loss terms `cross_race_distance` folds in), surfaced on its own so the language-distance no-shared-form ceiling and per-cell one-sided-coverage contribution read the measured loss rather than a separate constant; symmetric, no `RaceId`. In `langdist` those two cases are the normalised language image: a concept one side lacks counts as fully non-cognate and a form-disjoint or cross-modality pair reaches the ceiling of one, both emerging from the non-cognate share, no separate authored constant (`langdet.incommensurability_ceiling` re-based to DERIVE). (2) the Part 33.1 semantic substrate: `Concept` is a `prototype: Vec<Fixed>` point over a meaning `GroundMetric`, `SemanticSubstrate` a `ConceptId`-to-`Concept` map, and `GroundMetric::resolution` the grid separability floor (the lightest positive finite structural distance, defaulting to `ONE` for independent axes). The discrimination threshold DERIVES as the minimum positive inter-meaning distance (Euclidean over prototypes, the pinned independent form, structured meaning metrics left as the same flagged seam `value_distance` returns `None` for) floored by the perceptual just-noticeable difference; the lexicalisation threshold as the occupied meaning span spread over the contrast budget, floored by discrimination. `substrate_quantization` is a fixed integer fraction (`QUANTIZATION_DIVISOR = 2`, the Nyquist separability bound) of the discrimination threshold, the one knob reused for the walk, the walk key, and the correspondence tie window, consistent with `GroundMetric::resolution`. Two races with different meaning prototypes get different thresholds from the one kernel, no authored human concept set, no `RaceId` (`lang.concept_thresholds` re-based to DERIVE, its concept-drift-rate the one remaining reserved input; `langdet.substrate_quantization` re-based to DERIVE, no input remaining). (3) `langdist::distance_component_weights` derives the three component weights as each layer's own order-2 diversity (the Hill number `N^2 / sum(c_i^2)`, the same measure `information_weights` uses) across a corpus, the lexical layer over pooled word-forms, the phonological over pooled producible primitives, the grammatical over pooled `(parameter, value)` settings, each over the total and normalised to sum to exactly one by deterministic residual absorption into the lexical component (R-LANG-DET); lexical dominance EMERGES from the entropy of real lexicons rather than the authored 0.6/0.25/0.15, the weights invariant to permuting which language is which and reading no `RaceId` (`lang.distance_component_weights` re-based to DERIVE, no input remaining). Every kernel is fixed-point, deterministic, race-blind (Principle 9), and carries a non-steering test. DEVIATIONS, surfaced: (a) the task names the ceiling `value_metric.incommensurability_ceiling`, and the alias lives in `value.rs`, but the manifest entry whose basis is verbatim the no-shared-form-and-one-sided-coverage ceiling is `langdet.incommensurability_ceiling`, so that is the entry re-based (no redundant `value_metric.incommensurability_ceiling` entry added, since the alias introduces no new reserved input beyond the already-derived projection loss). (b) the language-distance ceiling and one-sided contribution are the top of the normalised `[0, ONE]` share (`ONE`), the language image of the value-space projection-loss ceiling, rather than a literal cross-crate call into `value.rs` (which would need value profiles the language layer does not hold); the two are the same untranslatable-magnitude concept at two scales, documented at both sites. (c) the `jnd` and contrast budget flow into `concept_thresholds` as `Fixed` parameters (from `perceptual_geometry` / `PerceptualGeometry::contrast_budget`) rather than recomputing the perceptual geometry inside `semantics.rs`, keeping the module a pure geometry-and-threshold kernel. (d) semantics-and-langdist are non-trivial builds (each a new module with its own test submodule), larger than Task 1's small value.rs alias, as the task anticipated. Gates: `cargo build`, `cargo test -p civsim-sim` (530 lib + integration green), `cargo test -p civsim-core`, `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh`, and the real-manifest parse all green. Not done here (follow-ons): the Part 62 consolidation and audit-log Section 1 blocks stay owner-sign-off follow-ons; the semantic substrate, the concept thresholds, and the language-distance layer are standalone kernels (like the Wave-1 and Wave-2 derivations), not yet wired into the canonical `World::tick`.

Wave-4 derive build-out (the FINAL four derivations, closing the derive column): BUILT (gate: NEW `crates/sim/src/personality.rs` (`TraitAxisId`/`TraitDef`/`PersonalityProfile`/`PersonalityRegistry`/`TraitInstance`/`plasticity_at`/`age_personality`), registered in `crates/sim/src/lib.rs` and wired into `crates/sim/src/world.rs` (a `personality` registry field and per-being `traits` map, `set_personality_registry`/`install_personality`/`trait_value`, birth-neutral seeding at the dawn and at birth, pruning in `remove_being`, and a `drift_personalities` beat inside `life_cadence` after aging); `crates/physics/src/laws.rs` `thermal_diffusivity`; `crates/sim/src/runner.rs` `derive_field_diffusion`/`FieldCalib::from_manifest_with_medium` and the `STENCIL_STABILITY_BOUND`/`DIFFUSIVITY_MAX` constants; `crates/sim/src/evolve.rs` `EvolveParams::from_layout`/`from_manifest_and_layout` and the `ACTIVATION_CLAMP_WIDTH` constant; `crates/sim/src/evidence.rs` `derive_aggregate_diffusion_rate` and the rewritten `aggregate_diffusion_rate`; tests in each module plus the new `crates/sim/tests/personality.rs`; re-bases/retires in `calibration/reserved.toml` of `being.plasticity_by_age`, `being.maturity_targets`, `field.diffusion` (+ new `field.cell_size`), the three `behavior.controller_*` scalars (+ new `behavior.controller_target_mutations`/`behavior.controller_mutation_step_fraction`), the three `medium.*` profiles (thermal axes added), and `evidence.aggregate_diffusion_rate`). Four derivations close the derive column the audit-fix tail named. (1) AGE-PERSONALITY: `being.plasticity_by_age` and `being.maturity_targets` stay per-race `TraitDef` data on a new `PersonalityRegistry`, and one `age_personality` kernel, read at `World::life_cadence`, drifts each being's trait toward its race's maturity target at `plasticity_curve.eval(Race::maturation_fraction(age))`, high in youth and low at the maturity plateau; two races diverge (one trait rising, one falling) purely from their curve and target data, no `RaceId` branch, and the trait state rides the aging cadence deterministically (not folded into `state_hash`, matching how `ages` is treated). The human maturity principle (rising rank-order stability, rising conscientiousness, falling novelty-seeking) is one race's data row. (2) FIELD.DIFFUSION derives from the SELECTED medium: `laws::thermal_diffusivity` is the medium's `alpha = k/(rho*c)`, and `runner::derive_field_diffusion` scales it by the base tick over the squared cell size and clamps to the four-neighbour stencil's 1/4 von Neumann stability bound, so air and water get different coefficients from `k/(rho*c)` alone; the medium is the lever and the diffusivity is physics. The three medium profiles gain reserved-with-basis `conductivity` and `specific_heat` axes (real air/water/CO2 values from Incropera and DeWitt), and a new reserved `field.cell_size` is the scale bound. (3) BEHAVIOR.CONTROLLER trio: `EvolveParams::from_layout` derives `init_spread = 1/sqrt(n_in)` (Xavier/He fan-in scaling, no new scalar), `mutation_rate = target_mutations/weight_count` (so the per-locus rate falls as the controller grows at fixed expected mutations), and `mutation_step = mutation_step_fraction * clamp_width` (a fraction of the activation's [-1, 1] range); the two new reserved fractions `behavior.controller_target_mutations` and `behavior.controller_mutation_step_fraction` are fail-loud with basis, and the three old `behavior.controller_init_spread`/`_mutation_rate`/`_mutation_step` scalars retire to a tombstone. (4) EVIDENCE.AGGREGATE_DIFFUSION_RATE derives as the mean field of the individual-tier gossip loop `told_weight * trust_baseline * contact_density` (`evidence::derive_aggregate_diffusion_rate`); `aggregate_diffusion_rate` reads the two gossip parameters at unit contact density, with the per-belief contact density entering downstream as the `PrevailingBelief::advance_diffusion` distance coupling, so no independent Earth-village rate is authored, and a tier-consistency test validates the aggregate logistic run at the derived rate against the mean of a stochastic individual-tier gossip ensemble within a sampling tolerance. Every kernel is fixed-point, deterministic, race-blind (Principle 9), and carries a non-steering test; each new reserved input is fail-loud with basis (never fabricated). DEVIATIONS, surfaced: (a) the age-personality substrate is a full new module with world wiring (a birth-neutral trait seed at the dawn and at birth, and a `drift_personalities` life-cadence beat), larger than a small alias, as the task anticipated; the birth value is a neutral zero the drift moves away from (a being's inherited starting personality is a named follow-on), so a maturity target above zero reads as a rising trait and one below as a falling one. (b) field.diffusion carried more than a reserved add: it needed the physics `thermal_diffusivity` law, the `conductivity` and `specific_heat` medium axes, and a new `field.cell_size` scale constant, and its HONEST LIMIT stands: at a one-second base tick and a map-scale cell the physical coefficient is far below the stencil bound and even below Q32.32 resolution, so the derivation reproduces heat that barely conducts per tick (physically correct), and a scale-appropriate cell size or a coarser field sub-step is where a visible diffusion rate comes from (recorded in the `field.diffusion` and `field.cell_size` bases). (c) the aggregate rate keeps `evidence.aggregate_diffusion_rate` as a reserved pointer (a DERIVES basis, no scalar) rather than a tombstone, because `advance_diffusion` still takes the per-belief contact density as its `distance` argument, so `rate * distance` is the full mean-field product. Gates: `cargo build`, `cargo test -p civsim-sim -p civsim-physics -p civsim-core` (542 sim lib plus every integration binary green), `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh`, and the real-manifest parse and scenario resolution all green. Not done here (follow-ons): the Part 62 consolidation and audit-log Section 1 blocks stay owner-sign-off follow-ons; the personality substrate is wired into the life cadence but its trait values are not yet read by a downstream decision or belief kernel, and the medium-derived field diffusion is available through `FieldCalib::from_manifest_with_medium` while the canonical runner constructor is not yet repointed to it.

Blind-audit correctness-fix tail (the CONFIRMED defects a blind audit found in the derive-column tails): BUILT (gate: `crates/sim/src/evidence.rs` `derive_aggregate_diffusion_rate` rewritten to map the per-contact gossip LOG-ODDS through a `logistic` (sigmoid) transform to a probability before scaling by contact density, and the circular tier-consistency test replaced by one that validates the aggregate against the REAL log-odds threshold-gated gossip loop (`Mind::consider`/`Mind::belief`); `crates/sim/src/belief.rs` `PrevailingBelief::advance_diffusion` restored to the SI logistic `rate * distance * level * (1 - level)`; `crates/sim/src/tom.rs` the witnessed/told/said assertion ladder re-derived from a new `AccessChannelDef.margin_steps` data offset instead of an English-name `match`, with a strictly-positive `LADDER_WEIGHT_FLOOR` on the derived rung; `crates/sim/src/axiom.rs` `EpistemicStance::effective_stubbornness` and `enculturation_pull_rate` taking the reserved dogmatism/freezing split weight as a parameter, plus the `stubbornness_dogmatism_weight` manifest reader; `crates/sim/src/world.rs` a `stubbornness_split` field + `set_stubbornness_split` feeding the two enculturation-band beats (no-op until installed), and `state_hash` now folding `ages`, `traits`, and each lineage's race `maturity_years`/`lifespan_years`; `crates/sim/src/langdist.rs` `distance_component_weights` normalising each layer's diversity by its own type-space ceiling before combining and absorbing the rounding residual into the largest measured layer, and the shared `order2_diversity` guarding the shift value; `crates/sim/src/typology.rs` `information_weights` reusing that saturating helper so an unvalidated prior cannot wrap to a negative weight; `crates/sim/src/agent.rs` `RetentionLaw::window_ticks_for` saturating at `u64::MAX`; `crates/sim/src/personality.rs` a `TraitInstance::entries` accessor and the corrected `maturation_fraction` doc; `crates/sim/src/runner.rs` the inclusive `[0, 0.25]` stencil-bound doc; registry-construction sites in tests and examples carrying the new `margin_steps` datum; a regression test for each must-fix; and in `calibration/reserved.toml` the new reserved `axiom.stubbornness_dogmatism_weight` (fail-loud with basis), the RE-RESERVED `value_metric.enculturation_pull_rate` (a fabricated "no reserved input remains" resolution reopened: it had been derived-through the Part 28 axiom pull, a different domain, while the Part 21 ValueProfile pull it governs reads a per-race `belief_plasticity` that exists in no `.rs` file), and the corrected `evidence.aggregate_diffusion_rate` basis. Every fix keeps determinism (fixed-point, counter-keyed, canonical order), Principle 9 (no `RaceId`/label branch: the ToM ladder now keys off a data offset, not a channel name), and Principle 11 (the split weight is surfaced reserved fail-loud, never fabricated). WIRE-PENDING (the next arc, deliberately NOT wired here, correct-in-isolation, and no doc or manifest basis claims them live): (1) `genome::IncompatibilityTable::active_between` (the joint Hardy-Weinberg cross count) into epoch speciation, where `epoch.rs` still uses `rng.flip(0)`; (2, WIRED 2026-07-03, see the world-wiring increment 1 entry below) `runner::FieldCalib::from_manifest_with_medium` into the world-build path, retiring the free scalar `field.diffusion` on that path; (3) a `RetentionLaw` consumer (`agent.rs` `window_ticks_for` has no live caller yet); (4) a `personality` trait-value consumer (`world.rs` `trait_value` is read by no downstream decision or belief kernel yet); (5) the `Sensorium` acuity-versus-resolution split for the phoneme-wiring increment (`langmod::capability_halves` reads `sensorium.reads(channel)` as a `[0, ONE]` acuity while `langmod::perceptual_geometry` reads the SAME field as a Hz-scale just-noticeable difference; latent today since no path feeds one sensorium into both, but `phoneme_priors` combines exactly those two, so wiring it must first separate the acuity capability from the resolution/JND on `Sensorium` rather than conflate them on one field; the re-audit's finding, the acquisition-split clamp and the per-value-gate doc are already fixed). These get WIRED next. Gates: `cargo build`, `cargo test -p civsim-sim -p civsim-physics -p civsim-core`, `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests`, `scripts/verify.sh`, and the real-manifest parse and scenario resolution all green. Not done here (follow-ons): the Part 62 consolidation and audit-log blocks stay owner-sign-off follow-ons; the four WIRE-PENDING consumers above are the named next arc.

World-wiring increment 1 (WP2 cleared, field diffusion from the medium): WIRED (gate: `crates/sim/src/scenario.rs` a `DEFAULT_MEDIUM_ID` const (`medium.air`, the documented default temperate air) and `ScenarioResolution::medium_manifest_id` returning the resolved medium's profile id or that default; `crates/sim/src/runner.rs` `FieldCalib::from_resolution`, the world-build path's field constructor, routing through the already-built `from_manifest_with_medium` on `medium_manifest_id` so the field's diffusion coefficient DERIVES from the scenario's ambient medium's `k/(rho*c)`; a new observable test `medium_derived_field_diffusion_is_wired_through_the_world_build_path`). The built-but-dormant `from_manifest_with_medium` derivation is now LIVE on the construction path, giving it its first non-test caller: a world of air and a world of water resolve to different `medium.{name}` profiles, get different diffusion coefficients from their thermal axes alone, and their temperature-field state hashes DIVERGE after stepping a hot spot, with the field bit-identical under the scheduler variant (pinned order equals the scheduled step, the field-only runner's worker-width invariance). The free-scalar `field.diffusion` is retired on the world-build path: `from_resolution` reads no diffusion scalar and derives every world's coefficient (the medium-named worlds and the air-default worlds alike) from a medium's physics, so the manifest's `field.diffusion` survives only as the labelled test fixture and the `.high` sibling baseline, exactly as its reserved basis already anticipates. Fail-loud preserved: a reserved or missing medium profile, cell size, or timestep refuses to build (the `from_manifest_with_medium` fail-loud proof stands). No field kernel change, no new authored number, no RNG, pure Q32.32, and the coefficient reads no medium LABEL (only the three thermal axes), so replay coverage is free through the existing `state_hash` field fold (Principles 3, 9, 10, 11). NO golden-hash refresh was needed: no prior production path consumed `field.diffusion` (its only callers were tests), so adding the derived constructor shifted no existing fixture, and all 681 sim tests pass unchanged. DEVIATION, surfaced (the flagged `field.cell_size` limit): at a map-scale cell and a one-second base tick the physical coefficient underflows Q32.32, so the observable test uses a one-centimetre fixture cell where air's coefficient lands near the stencil stability rail and water's far below it; a scale-appropriate `field.cell_size` or a coarser field sub-step is where a visible map-scale diffusion rate comes from, recorded already in the `field.diffusion` and `field.cell_size` reserved bases. Gates: `cargo build`, `cargo test -p civsim-sim -p civsim-physics -p civsim-core` (681 sim tests green), `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh`, and the real-manifest parse and scenario resolution all green. Not done here (follow-ons): the remaining WIRE-PENDING consumers (1, 3, 4, 5 above) are the continuing arc; `from_resolution` is the field-calib constructor for the world-build path, and the top-level scenario-to-`Runner` assembly that calls it on a fully resolved world is the next world-wiring increment, since no production caller assembles a whole `Runner` from a `ScenarioResolution` yet.

World-wiring increment 2 (the production scenario-to-`Runner` assembly, handoff section 4): BUILT (gate: `crates/sim/src/worldbuild.rs`, a new module carrying `DawnPeoples` (a world's declared races, founding bands, breeding registry, personality registry, and optional mortality hazard) and `build_dawn_runner`, the top-level world-build function that assembles a whole `Runner` from a resolved scenario: it builds the `World` from the manifest with the life cadence derived from the orbit, installs the breeding and personality registries before seeding, seeds the dawn population through `seed_dawn_populations` (sizing each founder's evidence ring from expressed memory through `RingCapacityLaw::from_manifest`), arms the language and drift calibrations (concepts from the NSM primes, `LanguageParams`/`DriftParams` fail-loud from the manifest), optionally arms a mortality hazard, derives the field from the resolved medium through `FieldCalib::from_resolution` (the increment-1 seam, now with a top-level caller), and composes the armed world onto the field runner through `Runner::with_world`; `calibration/profiles/dev-fixtures.toml` extended with the world-build path's fixtures (`medium.air`, `medium.water`, `field.cell_size`, `field.relaxation`, `field.body_exchange`, `axiom.evidence_ring_curve`, `axiom.evidence_ring_hard_cap`), each a labelled placeholder with its basis while the reserved fail-loud entries stay in `reserved.toml`; a new integration test `crates/sim/tests/world_build.rs` (4 tests) proving the assembly seeds a genome-real dawn, replays bit for bit and is seed-sensitive, advances its composed cognition world every step (closing the open question: a `Runner` wrapping a dawn world DOES tick the inner cognition beats), derives the field from the medium end to end (an air world and a water world build a bit-identical dawn but diverging fields), and keeps the scheduled order bit-identical to the pinned order). This closes the handoff section-4 prerequisite: `from_resolution` and `seed_dawn_populations` now have a top-level production caller, and the running world exists that increments 5 (reproduction) and 6 (enculturation) hang their beats on. Fail-loud (Principle 11), determinism-clean (Principle 3, the seed keys before any minting and the field runner adds no RNG phase), and steering-clean (Principle 9, the assembly installs no authored decision repertoire, so `with_world`'s refusal boundary holds by construction). DEVIATION, surfaced: the assembly runs under `Profile::Development` on the dev-fixtures profile (the `Profile::Calibrated` world still awaits the owner's reserved-value calibration, Tier B item 1, so nothing here graduates a reserved value); the races and founding bands are a caller-supplied input, not a product of the path, since the race-genesis that would grow sentient races from the pre-dawn biosphere (design Part 28) is a later arc; and the per-race articulation substrate that would make the naming game and drift LIVE is the language-substrate increment below, so the language calibrations are armed but inert until a lineage carries a form system. Gates: `cargo build`, `cargo test -p civsim-sim -p civsim-physics -p civsim-core` (all green), `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh` clean. Not done here (follow-ons): the derived language substrate (2a-2e below), reproduction (increment 5), enculturation (increment 6).

The language-substrate increment (handoff increment 2, "arm language from `phoneme_priors`, no authored lexicon"), decomposed and audit-confirmed to be an L not an S-M: `phoneme_priors` returns dispersion WEIGHTS with no consumer that builds a `FormSystem` (the bridge from weights to producible feature-values to `FormSegment`s to `FormSystem::new` does not exist), `Race` carries NO acoustic/articulation/sensorium data (so a per-race derived form system has no owner-data home), and the WP5 `Sensorium` conflation is load-bearing (`langmod::capability_halves` reads `sensorium.reads(channel)` as a `[0, ONE]` acuity while `langmod::perceptual_geometry` reads the SAME field as a Hz-scale just-noticeable difference). Ordered sub-increments, each independently testable and gated: 2a, land WP5 first (split `Sensorium` into a per-channel acuity read by `capability_halves` and a per-channel resolution/JND in Hz read by `perceptual_geometry`, distinct fields or accessors, fixing the latent bug regardless of the rest); 2b (the L core), give per-race articulation and resolution a data home (fill `FeatureValueDef.resonator_length` with real base per-feature-value geometry reserved-with-basis, replacing the degenerate ZERO, add a per-race vocal-tract scale to `Race` that scales those base lengths in `perceptual_geometry` plus a per-race sensorium resolution seed, following the per-race registry pattern with no `RaceId` branch, and flag the vocal-tract scale as authored-now-derivable-later from the body plan / R-ORGAN-FLUX); 2c, add the reserved producibility threshold (the prior-and-gate level below which a feature value does not enter the inventory, the selection folding in canonical `FeatureValueId` order); 2d, bridge the thresholded inventory to a `FormSystem` (`FormSystem::new(modality, inventory, min_len, max_len)` per race, `min_len`/`max_len` reserved-with-basis or derived via a contrast-budget argument, fail-loud on an empty inventory); 2e, arm the per-race `FormSystem` at the founder step built above via `set_form_system_for`, so the already-live naming game and drift run (milestone: bands coin and converge one word per concept, separated lineages drift into sister languages on their race cadences). WP5 in the WIRE-PENDING list above is subsumed by 2a.

The language substrate (2a-2e): BUILT (2026-07-04). 2a (`crates/sim/src/sensorium.rs`, `crates/sim/src/langmod.rs`): `Sensorium` split into a per-channel acuity map (read by the perceive gate and `capability_halves`) and a sibling resolution/JND map (read by `perceptual_geometry`), fixing the WP5 conflation where a valid acuity of one read as an implausible one-hertz JND; `reads()`/acuity unchanged so the perception gate is untouched, no golden-hash refresh. 2b (`crates/sim/src/race.rs`, `crates/sim/src/langmod.rs`, `crates/sim/src/language.rs`): `Articulation { vocal_tract_scale, hearing_resolution }` as an `Option` on `Race` with a `with_articulation` builder, `articulated_geometry` scaling the shared base lengths by the race's vocal tract and reading its hearing resolution as the sensorium JND, and `ArticulationSubstrate::phonetic` carrying real base resonator lengths (replacing the syllabic zero) with a `resonator_lengths` accessor; the vocal-tract scale flagged authored-now-derivable-later (body plan / R-ORGAN-FLUX). 2c (`crates/sim/src/langmod.rs`): `producible_values` selecting the values whose dispersion-and-capability prior clears the reserved threshold in canonical `FeatureValueId` order, a gate-masked (zero-prior) value never entering. 2d (`crates/sim/src/langmod.rs`): `form_system_from_values`, the previously-missing bridge from thresholded producible values to `FormSegment`s to a coinable `FormSystem`, fail-loud (`FormSystemError::EmptyInventory`) rather than coining empty words. 2e (`crates/sim/src/worldbuild.rs`, `crates/sim/src/world.rs`): `LanguageGenesis` and `arm_dawn_languages` deriving each articulating race's form system through the 2b-2d pipeline (cached per race) and installing a per-band lineage, wired into `build_dawn_runner` via the optional `DawnPeoples.language`, plus a `World::being_ids()` accessor. Reserved-with-basis, surfaced never fabricated (`calibration/reserved.toml`): `articulation.base_resonator_lengths`, `articulation.vocal_tract_scale`, `articulation.hearing_resolution`, `articulation.producibility_threshold`, `articulation.word_min_len`, `articulation.word_max_len`. Determinism (Principle 3) and no-`RaceId`-branch (Principle 9) throughout: two races diverge in their sounds only through their `Articulation` data, one pipeline. Proven in `crates/sim/tests/world_build.rs` and the `langmod`/`language`/`sensorium`/`race` unit tests: a sharper ear yields a strictly richer producible inventory (a full one versus a fail-loud empty), bands coin and converge words from their derived inventory while separated bands and the two races diverge, a genesis-less build stays inert, and the language-armed build replays bit for bit. HONEST LIMITS: the founder step's `LanguageGenesis` is a caller-supplied bundle (its medium acoustics need the medium's bulk modulus, a reserved axis not yet on the medium profile, so a full `from_manifest` for it is a follow-on); the capability gate is a channel-wide scalar broadcast per value (a per-value producibility model is a follow-on); the vocal-tract scale and the word-length range are interim levers flagged for their eventual derivations. Gates green: build, test (sim/physics/core), fmt, clippy (zero warnings), verify.sh, real-manifest parse.

World-wiring increment 10 Part B (the anatomy-derived physiology producers LIVE in the embodiment step): BUILT (2026-07-04). The R-METABOLIZE and R-MEDIUM producers, built and unit-tested but dormant on the running tick, now drive an embodied being's survival from its body plan rather than the authored per-axis scalars. `crates/sim/src/runner.rs` gains an `EmbodiedPhysiology` config (the organ `BodyPlanRegistry`, the `MetabolicAnchors`, a uniform ambient medium's respirable content, the reserved Fick transfer coefficient, and the base tick), sourced fail-loud through `EmbodiedPhysiology::from_manifest` (anchors from `MetabolicAnchors::from_manifest`, respirable content from the resolved `medium.{name}` profile, the transfer coefficient and base tick from the manifest) or a labelled `dev_fixture`; it installs on an `Embodiment` through `set_physiology`, opt-in so the evolve harness and the thermal fixtures keep the scalar path untouched. When installed: `step_embodiment` builds each being's per-axis `DerivedDrain`, deriving the metabolic (energy-density-backed, keyed off the floor axis id not a hardcoded constant) axis's drain through `derive_base_drain` (read against the being's LIVE post-exchange core temperature as the effective ambient and its own comfort set point, so the base drain tracks the medium each tick) plus a work-derived `derive_exertion_coupling`, while every other axis keeps its authored per-axis rate, then drains through `Homeostasis::metabolize_derived` inside `locomotion::step_with_field_dirs` (a new `drains` parameter reconciles metabolism with locomotion cleanly: the being's own exertion signal scales the coupling where it is computed, and the scalar `metabolize` is retired on the embodied+physiology path while kept for the fixture callers); a respiration sub-phase runs `medium::respire` per being in canonical id order (breathe in before the tick's metabolic draw) where the registry carries a `RESPIRATION` axis; and `with_embodiment` seeds each being's static derived `h*A/(m*c)` body-to-medium exchange rate through `set_body_exchange_rate`, so `phase_body_exchange` couples at the anatomy-derived rate rather than the flat `FieldCalib.exchange`. One new reserved-with-basis value, surfaced never fabricated (`calibration/reserved.toml`, dev-fixtures set): `metabolism.respiration_transfer_coefficient` (the Fick membrane gas-transfer coefficient); the respiration flux representability cap is an engine-mechanics const (`RESPIRATION_FLUX_MAX`), not an owner value. Determinism (Principle 3): the derived drains are recomputed each tick and the exchange rate is static config, so no new canonical state escapes the existing `state_hash` fold (reserves and body temperatures are already folded); the walks are canonical id order and draw no RNG. No-`RaceId`-branch (Principle 9): the divergence keys off body-plan, tissue-composition, medium, and temperature data, never a label. Proven in `crates/sim/tests/physiology_embodiment.rs`: two beings differing ONLY in body plan diverge in per-tick energy drain and time-to-death through the live step, with the larger, denser body draining a SMALLER fraction per tick and outliving the small one (the Kleiber signature, end to end); a body lives in a rich medium and suffocates in a poor one from the medium's content; the coupled physiology runner replays bit for bit and is bit-identical under the scheduler variant (`step` equals `step_scheduled`); and `EmbodiedPhysiology::from_manifest` reads a set manifest and fails loud on a reserved input. HONEST LIMITS and DEVIATIONS, surfaced: the ambient medium is a single uniform value this increment (a spatially-varying per-cell `MediumField` a being reads at its coordinate is the named refinement, the respiratory sibling of the per-cell temperature field); the exertion coupling's work force is the being's normalized body mass as a proxy pending the Part 35 work-force datum, its velocity the body's full-exertion ground speed; the R-METABOLIZE unit limits stand (the kJ/g-to-joule R-UNITS-PIN bridge, the base-versus-exertion power-scale gap, and the nonlinear-mass pool-tier Jensen gap); and the physiology is installed on the `Embodiment` (evolved-controller) path, not on the cognition-world `build_dawn_runner` path, since no production builder yet unifies the two populations. Gates: `cargo build`, `cargo test -p civsim-sim -p civsim-physics -p civsim-core` (all green), `cargo fmt --all -- --check`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `scripts/verify.sh` clean, and the real-manifest parse and scenario resolution green. This completes the world-wiring arc (increments 1 through 10, Parts A and B).

Real-world unification arc (one runner carrying minds and bodies): BUILT (2026-07-04), `docs/working/REAL_WORLD_UNIFICATION_HANDOFF.md`, on branch `claude/real-world-unification`. The wired substrate now runs as one deterministic world: a founder's `StableId` owns at once a `World` mind and an `Embodiment` walker on the field, and the runner ticks N generations. Five steps plus the lifecycle half, each determinism-audited and tested. Step 1, census-Ne close (retiring audit deviation 23 for the post-dawn tier): `build_dawn_runner` arms `World::set_reproduction` and `arm_generational_drift`, and `World::life_cadence` resets the reproductive census window once per generation after `drift_pools` reads it, so each generation's drift keys off its own census-derived Ne, not the authored founder size (`crates/sim/src/world.rs`, `crates/sim/src/worldbuild.rs`; R-REPRO). Step 2, run-path unification (the crux): `Runner::with_world_and_embodiment` carries both Options under one shared id space (the world's `Registry` is the sole minting authority; walkers reuse founder ids, never a second mint), and a `RES_BEING` resource declared by both `SYS_EMBODIMENT` and `SYS_WORLD` forces the deterministic scheduler to serialize the two systems in the pinned `SYS_EMBODIMENT` < `SYS_WORLD` order once a being is shared, so the composite stays bit-identical across worker widths with the shared being present. Step 3, the dawn embodiment: a `body: Option<BodyPlan>` data home on `Race` (sibling to articulation), and `assemble_dawn_embodiment` gives each founder whose race carries a body plan a located, metabolizing body sharing its mind id, its controller expressed from its genome and its comfort band the reserved thermal set point and half-band; a bodiless-race mind stays a mind with no walker (owner ruling: the pairing is optional, so a future disembodied mind is a clean extension). Step 3c, lifecycle pairing: a new `reconcile_lifecycle` beat runs after `world.tick` on both the pinned and scheduled paths so a `World` birth mints a paired body (expressed from the newborn's race and genome, reusing the mind id), a locomotion death ends its mind in the world, and a world-mortality death retires the paired body (walker, comfort band, body temperature, exchange rate, and located-index entry all dropped); it is a pure deterministic function of post-tick state (no RNG, canonical id order, worker-count independent). Step 4, the per-cell medium field (closing the increment-10B uniform-medium limit): `MediumField::from_map` folds the worldgen map into a per-cell medium by physical elevation (below the reserved `medium.submersion_elevation` a cell holds the submerged medium, else the emergent medium, no biome-label branch, owner ruling), `EmbodiedPhysiology` carries the field, and `step_embodiment` swaps the scalar `medium::respire` for `medium::respire_at` at the being's coordinate; only respiration is wired (the field's temperature vector is sourced from the worldgen tile but unread, the canonical diffused `Field` stays authoritative for body temperature, so the G5 temperature desync cannot manifest), with buoyancy and in-medium thermal exchange deferred until that desync is resolved (R-MEDIUM). Step 5, the Part 35 muscle-force datum (closing the increment-10B body-mass-proxy limit): `physiology::whole_body_muscle_force(plan, organs, anchors)` is the development-weighted sum over the organs of `mat.fracture_strength` times body mass, mirroring the individual-tier `Body::strength` (muscle mass times material strength) so the two tiers stay dimensionally consistent (owner ruling), and the runner exertion coupling reads it in place of the raw `body_mass` proxy, so two bodies of equal mass but different muscle endowment now drain differently under exertion and a body with no muscle tissue exerts no force (the absence convention). Two new reserved-with-basis values, surfaced never fabricated (`calibration/reserved.toml`, dev-fixtures set): `reproduction.mutation_spread` (the one spread the belief-seed and mate-preference inheritance share) and `medium.submersion_elevation` (the waterline on the normalized elevation field the biome classifier reads); the muscle-force axis reuses the existing `mat.fracture_strength` figure. Determinism (Principle 3): proven bit-identical across worker widths [1,2,3,8] and replay, and `step` equals `step_scheduled`, for the shared-id dawn runner, the multi-generation reproducing-and-drifting runner, and the birth-and-death lifecycle runner. Principle 9: every divergence keys off data (genome, body plan, tissue composition, census, elevation), never a `RaceId` or label; the `has_behaviour` refusal survives into `with_world_and_embodiment`. Principle 11: every reserved value is fail-loud from the manifest. Proven in `crates/sim/tests/world_build.rs` (census-Ne growth, embodied founders, newborns paired to bodies across generations, culled minds' bodies retired), `crates/sim/tests/determinism_harness.rs` (the shared-id path across widths, `step` versus `step_scheduled`, the authored-repertoire refusal), `crates/sim/tests/physiology_embodiment.rs` (per-cell respiration through the runner, the muscle-force datum with mass scaling), and `crates/sim/src/medium.rs` (the elevation fold and its single-medium regression). TOMBSTONES, the increment-10B honest limits this arc closes: the uniform ambient medium is now the per-cell `MediumField` (Step 4); the exertion work force is now the Part 35 muscle-force datum, not the normalized-body-mass proxy (Step 5); the physiology and the cognition-world `build_dawn_runner` path are now one unified population (Steps 2, 3). HONEST LIMITS that stand: `PlaceId` stays frozen at the dawn band (deriving it from the cell is research-gated), so movement is thermoregulation-only while social geography stays at the dawn band; buoyancy and in-medium thermal exchange are deferred behind the medium-temperature desync; the edibility physiology on the dawn embodiment is the temperature-only dev fixture; and the R-METABOLIZE unit limits (the R-UNITS-PIN bridge, the base-versus-exertion power-scale gap, the pool-tier Jensen gap) stand. Gates: `cargo fmt --all`, `cargo clippy --workspace --exclude civsim-gpu --lib --tests` (zero warnings), `bash scripts/verify.sh` clean, the full `civsim-sim` suite green, the real-manifest parse and scenarios resolve. GPU oracle out of scope for this arc (verified separately on the 5090). The arc was fully-blind audited before merge (the muscle-force datum's missing body-mass factor was caught by that lens and fixed before the panel ran).


## World-wiring stood up and first real runs (2026-07-04)

The run harness `crates/sim/examples/run_world.rs` drives `build_dawn_runner` into one unified runner (minds and located, metabolizing bodies), ticks N life-cadence generations, and emits emergence readers (population and lineages, per-race census Ne, language consensus and cross-band cognate overlap, per-band belief stance, births/deaths, mean body temperature) plus a `state_hash`. Runs are bit-identical on repeat and seed-sensitive (different seed, different world); the field derives from the medium end to end (air and water hash differently). Dev run on the dev-fixtures manifest, not a Calibrated run.

Remaining-wiring fix applied: `World::birth` now inherits the child's NATIVE LANGUAGE (its first parent's `lang_of` lineage and a copy of its lexicon), so a tongue is transmitted across generations and drifts through the naming game and the drift step rather than each newborn starting speechless. Folded into `state_hash` through the per-mind lexicon fold; a deterministic copy, no draw. Imperfect acquisition (per-birth transmission loss) is a refinement.

FIRST-RUNS FINDINGS (Crucible, Confluence, Mirror, 50 generations, several seeds): the dominant dynamic is an unchecked Malthusian population explosion (Crucible 24 to 4090 in 50 generations, births far outpacing deaths) because there is NO carrying capacity yet. Language consensus collapses in lockstep (70 percent to 18 percent) as the naming game is swamped by the flood of new speakers, while per-race belief stances stay distinct and stable through the churn (inherited culture persists; actively-negotiated culture fragments under population pressure). Per-race census Ne is tracked and drifts. This is the vivid case for the environmental-fields arc below: without carrying capacity, life multiplies without limit and negotiated culture cannot keep up. NOT-YET-OBSERVABLE: a canonical speciation reader, design origination (knowledge reads zero), and institution crystallization on the run path.

Priority after the real-world unification stands up and runs: (1) the ENVIRONMENTAL-FIELDS arc, UP NEXT, foundational and gating a true culture and society (owner call 2026-07-04); (2) the CELESTIAL / ASTRONOMY arc, later, the deep-end fun.

======================================================================
ARC A (UP NEXT): Environmental fields, the basal physical substrate
======================================================================

Today there is exactly one live field, temperature (runner.rs:44 names moisture, wind, and resource as the fields that follow the same pattern), plus static per-cell worldgen elevation/moisture/temperature (worldgen.rs:32) and biome classification. A world's habitability contrast (the Crucible's fertile greenwells in a lethal waste) is currently PAINTED by the biome classifier, not emergent from physics. This arc builds the sibling fields so scarcity, habitability, and adaptation emerge:

- Hydrology: water moves over the elevation map, pools in basins, evaporates faster where hot, so the greenwells fall out as where water collects in an arid world. A dynamic field on the temperature diffusion/advection stencil (GPU-ready, bit-identity-gated the same way).
- Productivity / primary production: the biomass a cell can support, a physical potential from water, light, temperature, and soil nutrients. NOT an authored dead-zone scalar.
- Wind and a particulate (dust/ash) field: advects heat and carries a dust load that couples into respiration and the per-cell medium, and produces rain shadow so aridity emerges behind mountains.
- Soil chemistry (salinity, nutrients): a per-cell physical property.

GOVERNING PRINCIPLE (Principle 8, the owner-directed correction 2026-07-04): every environmental field is a GRADIENT organisms adapt to, NEVER a hard gate. A salt flat is not dead by fiat. Salinity is a toxin-class DOSE on the biology-floor substrate (a per-cell field value), and an organism's harm from a salt cell is its dose measured against its OWN tolerance through the EXISTING edibility mechanism (edibility.rs: Composition carries the dose per toxin class at :75, Physiology carries a per-organism tolerance and integer-Hill exponent per class at :142-151, harm is dose versus tolerance). Tolerance is per-organism and heritable through the genome, so the homeostatic-survival and selection loop ADAPTS it: a salt flat is lethal to the intolerant and a NICHE for halophile lineages that evolve to tolerate it. Heat, drought, dust, and low-oxygen tolerance are the same shape, all heritable, all selected, so extremophiles and specialists emerge and the waste is a mosaic of niches rather than a dead zone. Carrying capacity is therefore organism-relative: a salty-but-wet cell has high capacity for a halophyte and near-zero for the intolerant, so scarcity is measured per people, not an authored per-cell scalar. A lineage that adapts to the waste can cross and contest it, so the killing ground itself becomes contested range and the emergent conflict deepens. No per-cell dead-zone template, no per-race branch (Principle 9): the environment supplies the gradient, selection supplies the adaptation.

CONFLICT-COEFFICIENT DERIVATION (closing the last steer, coupled to this arc): once the productivity/carrying-capacity field exists, retire the free authored value_metric.conflict_coefficient dial (crucible.toml sets it high) into a DERIVATION from measured scarcity (population pressure over what the land supports per lineage). value.rs:525 conflict_pressure already reads emergent pairwise inputs (value distance times the coefficient times one-plus-measured-niche-overlap, plus grievance, minus trust); only the coefficient is authored, and crucible.toml's own comment states the intent that its regime key on the generated scarcity physics, not the world's name. The carrying-capacity field is what makes that derivation possible. Then the Crucible's war is fully bottom-up: physics, then water and fertility, then per-lineage carrying capacity, then measured scarcity, then conflict-proneness, then the pairwise pressure, then war, with nothing telling the world to fight.

Not Crucible-only: these fields serve every world (a Confluence's rivers, a Tempest's storms), and they pair with the celestial arc's insolation (insolation drives temperature, which drives evaporation, which drives hydrology). Gate: determinism (the fields on the pinned diffusion/advection stencil, folded into state_hash, GPU oracle bit-identity verified on the 5090), Principle 8 (gradient not gate), Principle 9 (organisms key off their own heritable tolerances), Principle 11 (the physical constants reserved-with-basis).

======================================================================
ARC B (LATER, deep-end fun): Celestial / astronomy
======================================================================

The whole celestial model today is two scalars, orbital_period_seconds and rotation_period_seconds (crates/world/src/celestial.rs), feeding only the calendar; nothing positional is mapped (no star, no moon, no bodies, no sky). The unlock is a deterministic celestial-mechanics layer: a set of bodies (star(s), planet, moon(s), maybe wanderers) with orbital elements whose positions are pure functions of the tick. From positions, two cascades derive, on the authored-physics / emergent-culture line: a PHYSICS cascade (insolation, eclipses, moon phases, tides) and a CULTURE cascade (calendars, constellations, navigation, omens, astrology, all emergent through the belief/language/institution substrate, never authored).

- Insolation (the first increment, already flagged): today the surface temperature field is diffusion plus relaxation toward a STATIC per-cell baseline (Field::step, Field.baseline, runner.rs:262), the baseline the worldgen tile temperature (a standing latitude gradient, blend_latitude, worldgen.rs:113) the doc calls the solar and biome forcing (runner.rs:137). The orbital and rotation periods feed only the clock, never the heat, so there is no diurnal cycle, no seasons (no obliquity datum), and no distance-to-star or luminosity term. The increment: a time-varying baseline or an explicit insolation source term in Field::step, the diurnal term from the rotation phase, the seasonal term from the orbital phase and a reserved obliquity datum, the mean level from orbital radius and stellar luminosity. Determinism (the phase is a pure function of the tick and the periods), Principle 9 (forcing from the world's own star and orbit, never a terran constant or hardcoded day length), Principle 11 (obliquity, orbital radius, luminosity reserved-with-basis).
- Eclipses: geometry from the bodies' positions (star-planet-moon alignment inside a shadow-cone threshold), deterministic and predictable, so the astronomer-priest and the eclipse-omen emerge as knowledge and institutions. Needs a moon body plus the star.
- Moons and phases, tides (moon plus sun gravity), comets (bodies on eccentric orbits with periodic, derivable apparitions; fixed-point Kepler via deterministic eccentric-anomaly iteration), meteor showers (a comet's debris trail crossing the orbit, deterministic; sporadics counter-keyed RNG), a generated deterministic star field with EMERGENT constellations (a race groups salient stars by its own sensorium and semantics, so two races draw different skies), a pole star and axial precession, the star's own properties (luminosity, color, mass) shaping insolation and sky color and the habitable band, planets as wanderers feeding astrology, and the emergent calendar (day, lunar month, year, eclipse and comet cycles derived from the observable cycles, so a two-moon race invents a different calendar).

Sequence, once the unification lands: (1) celestial-mechanics layer, (2) insolation, (3) moon/phases/eclipses/tides, (4) star field with emergent constellations and navigation, (5) comets and meteor showers, (6) the emergent calendar/astrology/omen cultural layer. Owner call when scheduled: whether the first real runs want a living sky or a static climate.


The base-level liveliness arc is now fully scoped with an ordered, feasibility-grounded build plan in `docs/working/BASE_LIVELINESS_HANDOFF.md` (the five steps, the promotion policy, the observability additions, and the gates). The first runs proved its case: the world is dead for three concrete reasons (blank dawn controllers so no movement, no energy/water axis or depletable food so no carrying capacity, conversation half-wired and decoupled from movement). Recommended first step: field-coupled movement (pure data, no Rust change), the keystone the rest rides on.
