# Abiotic evidence and derivation debt

Status: active migration record for the canonical planet and stellar-system arc.

This document preserves evidence and unresolved derivation obligations without granting admission. The
sealed absolute physics floor is the only source of physical causal values in the canonical runner. Exact SI
coordinate definitions remain noncausal representation metadata. A citation, vendored
artifact, candidate status, old fixture, viewer choice, or historical owner ruling cannot authorize a
magnitude.

## Authority and present admission state

The exact provenance marks are `[D]` derived, `[M]` measured, `[E]` estimator, `[C]` closure, `[A]`
authored, `[W]` written state, and `[X]` contingency. The accounting tiers are Universal, Reference,
Residue, and Contingency.

`docs/working/CANONICAL_LEDGER_INVENTORY.txt` is generated from the repository-owned catalog. At this
writing the physical floor contains three Universal `[M]` invariant coordinates: `alpha`, `G`, and `m_e`.
Every other cell and tier is zero. Seven exact SI definitions are untagged representation metadata, and the
execution values `eps_0`, `sigma`, `R`, and `A3_per_cm3_mol` derive from sealed relations rather than entering
as additional floor leaves. Every item below is therefore unadmitted.

The broad candidate audit in `crates/physics/data/floor_provenance.toml` is evidence custody and migration
work only. Its `unverified_measurement_candidate` value is a fail-closed sentinel, not an eighth mark.

## Required admission receipt

Before any non-derived leaf at any tier can enter the absolute floor, its record must establish all of the
following:

1. the candidate identity, phenomenon, proposed tier, and one of the seven marks;
2. the held source, checksum, exact claim anchor, domain, uncertainty, and conditioning;
3. every attempted first-principles and composition-level derivation, including why it failed;
4. the Buckingham-Pi group budget for the phenomenon;
5. Gap Law evidence showing why known mechanisms and measurements leave a gap,
   including the typed Chaos Protocol branch for dynamical sensitivity;
6. Residual Law evidence for conservation, disequilibrium, fluctuation-dissipation, and dimensional
   analysis;
7. one unique residual slot that is not already occupied by another candidate;
8. explicit out-of-domain and missing-evidence refusal behavior;
9. an admit-the-alien falsifier showing that the representation does not depend on a Terran identity,
   named regime, authored world profile, or viewer selection.

The executable receipt type is `DerivationExhaustionReceipt` in `crates/ledger/src/admission.rs`. Evidence
fields must be machine-resolvable before a candidate may render `[M]` or `[E]` in the admitted catalog.

## Crust optics and redox

### Charge-transfer optics

The retained candidate data is in `crates/physics/data/crystal_field.toml` and the reader is in
`crates/physics/src/crystal_field.rs`.

Open derivation work:

- obtain machine-resolvable evidence for `chi_opt(O2-)`, `chi_opt(Fe3+)`, and `chi_opt(Fe2+)`;
- test the Jorgensen relation `E_CT = 30000 cm^-1 * (chi_opt(ligand) - chi_opt(metal))` across compounds;
- retain the hematite 3.1 eV LMCT and magnetite 0.6 eV IVCT observations as candidate evidence;
- obtain per-feature oscillator strengths or derive the transition matrix elements;
- refuse spectral synthesis when the energy or intensity evidence is absent.

Direct observations and optical-electronegativity rows are proposed Reference `[M]` candidates only after
their evidence links and domains are complete. A cross-compound Jorgensen prediction is Residue `[E]`, not
`[D]` merely because arithmetic evaluates it. The old viewer weight `3.0` is `[A]` or `[C]` and is barred
from canonical physics. Transferring a ferric-oxide observation to another phase retains estimator
provenance and must declare its conditioning.

### Oxide thermochemistry

Open evidence work:

- obtain independent witnesses for Fe0.947O wustite, NiO, and MnO;
- preserve the distinction between real nonstoichiometric wustite and fictive stoichiometric FeO;
- reconcile the Fe3O4 compilation split rather than averaging it: JANAF is recorded as 4.7 kJ/mol more
  negative in Gibbs energy and about 5.2 kJ/mol more negative in enthalpy than Robie;
- verify the Robie and Barin anchors and identify scope or systematic differences.

Thermochemical curves are proposed Reference `[M]` candidates. Redox equilibria computed from admitted
curves are `[D]`. Compilation preference is not a physical law. If source scope and systematics do not
resolve the gap, the redox-dependent stage refuses.

## Condensation, differentiation, and melting

### Full-element condensation and generated crust

The retired 30 km crust fallback, fixed surface temperature or pressure, solar-only identity, and Earth
isotope mix are not inputs. Surface temperature and pressure must come from the generated system state.
Crust inventory and thermal history are `[W]`; thickness, pressure, gravity, differentiation, and
assemblage consequences are `[D]`.

The present full Al/Ca condensation minimizer does not converge, and anorthite support is incomplete.
Excluding Al or Ca to obtain a result is not an admissible fallback. The general minimizer must close over
the data-defined element and phase population or issue a structured refusal. A special path keyed to the
solar major-element set would violate admit-the-alien.

### McKenzie-Bickle obligations

The pre-migration `ReservedMeltParams` bundle in `crates/materials/src/surface_composition.rs` is not a
canonical input contract. Its four quantities must be replaced by typed upstream receipts:

- mantle potential temperature is generated `[W]` by young-planet and deep-time thermal evolution;
- surface gravity is `[D]` from generated mass and radius;
- the adiabatic slope is `[D]` from assemblage thermal expansion, density, temperature, and heat capacity;
- melt productivity derives from fusion entropy and heat capacity, or becomes a Residue `[E]` candidate
  only after complete derivation exhaustion.

The old 1588 K, 15.5 K/GPa, 0.12/GPa, and 9.8 m/s2 values remain validation archaeology. They cannot bind
the runner. Missing closure means refusal.

### Endmember melting evidence

The candidate table is `crates/physics/data/melting_endmembers.toml`. Preserve these literature splits and
missing fields as explicit gaps:

- forsterite fusion enthalpy: 89 to 142 kJ/mol;
- corundum fusion enthalpy: 111 versus 120 kJ/mol;
- anorthite fusion enthalpy: 135.6 versus 133 kJ/mol;
- diopside fusion volume: 13.2 cm3/mol versus the old 5.2 fixture;
- absent fusion volumes for anorthite, fayalite, spinel, and corundum;
- incongruent or metastable behavior for enstatite and fayalite.

Direct quantities are proposed Reference `[M]` candidates. Metastable-congruent proxies are Residue `[E]`.
Ideal-solution behavior is `[C]` and cannot enter the initial floor. Zero is not a value for a missing fusion
volume; pressure-dependent evaluation must refuse when the field is absent. Literature splits are Gap Law
evidence, not an owner-choice menu.

## Solid-lid rheology and flexure

### Cohesive-energy witnesses and the Miedema estimator

`crates/physics/src/materials_oracle.rs::phase_cohesive_energy` derives a phase cohesive energy by Hess
arithmetic over elemental atomization enthalpies and a phase formation enthalpy. That arithmetic is `[D]`,
but its result inherits the admission state of every input. The Rose equation-of-state route is not an
independent witness: it consumes the same cohesive energy and reproduces the well depth at equilibrium by
construction. Agreement between those paths cannot validate the shared atomization column.

The only independent atomization checks presently identified are the diatomic per-atom checks for oxygen,
nitrogen, and hydrogen. The rock-forming Mg, Si, Al, Ca, Ti, and Fe rows still lack independent witnesses.
The Miedema-class formation-enthalpy estimator for compounds without direct rows is also unbuilt and
unadmitted. Its `phi*`, electron-density, metallization, phase-domain, and uncertainty obligations must first
survive the full derivation hunt. If a unique irreducible estimator remains, it is a proposed Residue `[E]`
candidate with a complete receipt and overlap sentinel against independent measured compound rows. Neither a
metal identity nor a familiar silicate roster may select or validate it.

### Creep activation-volume chords

`docs/working/HK2003_VERIFICATION.md` establishes that Hirth and Kohlstedt Table 2 contains nine
activation-volume determinations conditioned on different pressure intervals. The lowest interval endpoint is
`1e-4 GPa`, not `0.3 GPa`, and the table supplies no single dry-dislocation `V*`. A bare activation volume has
dropped the pressure chord that gives it meaning.

At exact lithostatic pressure `P = 0`, the product `P V*` vanishes, so the exponent derives without selecting
an activation volume. At positive pressure outside every supported chord, a canonical consumer must derive a
pressure-dependent relation, exhaust that route and admit one unique residual, or refuse. Taking the table's
extrema where no determination applies does not create a physical bound. A downstream result that happens to
be insensitive to those extrema does not admit the unsupported inputs or prove that the extrema exhaust alien
materials. The retained private geodynamics path already demonstrates the required coverage refusal; a future
typed canonical adapter must preserve it.

### Brittle friction

Byerlee 1978 supports the candidate high-stress rock relation with a 50 MPa intercept, while McNutt and Menard
use 80 MPa and cite both Byerlee 1968 and 1978. The friction coefficient agrees and the intercept does not.
Byerlee 1968 remains unread, so the 30 MPa gap cannot be assigned to a frame conversion, a different regime,
or a source error. At low normal stress, roughness and cohesion matter, which is especially load-bearing for
thin or low-gravity lids that never reach the high-stress rock-independent regime. The ice relations recorded
in the older fetch are summary-only and leave a pressure-domain gap.

These are proposed Reference `[M]` candidates only after source custody, domains, and uncertainties are
complete. No owner-selected scatter band, material-class default, or citation grade can enter the runner. A
lid whose local stress state lacks a derived or admitted branch receives a structured refusal.

### Flexure validation evidence

`docs/working/SEAMOUNT_TREATMENT_FETCH.md` preserves the primary evidence that an isolated seamount is an
axisymmetric load, that Calmant's target rows are uniform-elastic rigidity fits rather than yield-envelope
outputs, and that a comparison belongs in rigidity space. Diagnostic rows must retain their load class,
method, modulus pair, `Te(elastic)` versus `Te(YSE)` kind, source domain, uncertainty, and evidence role. These
fields condition a validation row; they do not supply a generated planet with a load, modulus, or thickness.

The factor-of-3.6 load-class penalty is second-hand to Watts et al. 1988, Calmant's printed Poisson ratio is
unresolved, and the external cost of a biaxial fibre state is unmeasured in the reached corpus. The internal
`Te(YSE)/Te(elastic)` test is a conditioned `[D]` sensitivity inside one uniaxial fixture, not `[M]` evidence of
uniaxial-versus-biaxial error. The forward Stage 6 path still owes generated distributed loads, finite-footprint
geometry, conservation and moment residuals, and validity through the `D -> 0` limit. It must remain general
over load fields and body properties rather than dispatch on a seamount, Earth, or other familiar identity.

## Surface mechanics

### One general transfer integrator

Retain one conservative transfer integrator. No event or state may select a process-named kernel. Ejecta,
runout, and fluvial behavior are observer descriptions of regimes that emerge from a general force and
resistance law.

Parcel initial conditions are generated `[W]`. Realization choices are internally generated `[X]` only
after a physical measure exists. Kinetic energy, dissipation, and their dimensionless relationship are
`[D]`. A breakpoint must derive, pass complete admission, or cause refusal. An alien force requires an
admitted floor identity and a general law term, not a caller-authored world row.

### Continuous geodynamics

Rayleigh number determines whether convection occurs. Local convective stress divided by local yield
strength determines whether the lid mobilizes. Rayleigh number, stress, and mobilization margin are `[D]`;
damage and lid history are `[W]`; generated loads may be `[X]`.

Named mobile-lid, stagnant-lid, or tectonic labels are snapshot-only descriptions. They may not dispatch
causal behavior or enter canonical state. Yield strength must derive from material and damage state or use
a fully admitted reference; presence in a TOML candidate table is insufficient.

## Stellar evidence

### Stellar-birth joint measure

The active structural contract is
`docs/working/STELLAR_BIRTH_MEASURE_CONTRACT.md`. It preserves one opaque joint
physical measure plus a distinct realization-coordinate law. The older epoch,
environment-class, metallicity, core-temperature, angular-momentum, rotation,
and disk-radius root census is historical dependency discovery, not an input
schema. Those axes may identify conditional evidence or derivations within the
joint measure, but none can be inserted as a caller draw or independent
marginal.

The current floor closes neither obligation. No `[X]` may be generated until
the joint measure is normalized over its admitted support and the coordinate
law is independently approved. Missing either obligation retains the single
`stellar_birth.realization_measure` refusal.

### BHAC15 Hayashi grid

Held bytes and checksums are under `crates/physics/data/bhac15/`. The extracted grid is
`crates/physics/data/hayashi_wall.toml`, with its reader in `crates/physics/src/hayashi_wall.rs`.

BHAC15 is theory-model output and therefore a proposed Residue `[E]`, not `[M]`. It remains unadmitted.
Its conditioning is solar `[M/H]=0`, 0.010 to 1.400 solar masses, earliest tabulated age near 0.49 Myr,
and the temperature drift over the first 2 Myr. Interpolation is `[D]` arithmetic over `[E]` evidence and
retains estimator ancestry. Reads outside the mass or composition domain must refuse or enter a separately
derived branch.

Sensitivity statements must declare what is held fixed. Along the fixed-age contraction trajectory,
luminosity scales as `T_eff^(4/3)`, giving about 13 percent for the recorded correction. The 46 percent
figure applies only at fixed radius. The observer may display the result but cannot choose the row,
interpolation, or physical outcome.

## Active evidence locations and backlink map

- Charge-transfer optics: `crates/physics/src/crystal_field.rs` and
  `crates/physics/data/crystal_field.toml`.
- Endmember melting: `crates/physics/data/melting_endmembers.toml`.
- Condensation and crust migration seam: `crates/materials/src/surface_composition.rs`.
- Cohesive-energy and estimator witnesses: `crates/physics/src/materials_oracle.rs`,
  `crates/materials/src/metallic.rs`, and `scripts/diamond_gate.py`.
- Creep pressure chords: `docs/working/HK2003_VERIFICATION.md`,
  `crates/physics/src/creep_rows.rs`, and the retained private planet geodynamics path.
- Brittle-strength evidence: `docs/working/GEOTHERM_FETCHES.md` and
  `docs/working/TE_CONSTRUCTION_FETCH.md`.
- Flexure and seamount evidence: `docs/working/SEAMOUNT_TREATMENT_FETCH.md`,
  `crates/physics/src/elastic_thickness_rows.rs`, and `crates/physics/src/moment_equivalence.rs`.
- Surface transfer research: `docs/working/NONLOCAL_REDISTRIBUTION_KICKOFF.md`.
- Hydrosphere migration debt: `docs/working/OCEAN_HYDROSPHERE_TO_RESEARCH.md`.
- Legacy authored terrain source: `parked/crates/world/src/structure.rs`; the active world crate no longer
  exposes that scenario-authored structure.
- Solid-planet research map: `docs/working/GEODYNAMICS_RESEARCH_SURFACE.md`.
- Stellar model evidence: `crates/physics/data/bhac15/`,
  `crates/physics/data/hayashi_wall.toml`, and `crates/physics/src/hayashi_wall.rs`.

## Excluded legacy permissions

Do not restore authored fit profiles, Mirror choices, viewer steering, pins as physical evidence,
reserved-with-basis permission, provisional cited inputs, named-regime dispatch, or arbitrary per-world
magnitudes. Historical documents may explain how evidence was found, but the only forward path is derive,
complete a structured admission, or refuse.
