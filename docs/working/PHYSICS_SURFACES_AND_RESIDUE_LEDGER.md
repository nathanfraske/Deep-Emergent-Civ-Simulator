# Superseded physics-surfaces residue map: historical research inventory

Status: superseded by the canonical absolute-floor ruling of 2026-07-20.

The physical relations below remain derivation leads. Their earlier authored
residue and authored-input language grants no canonical permission. Buckingham
Pi bounds the number of dimensionless residual slots that still require proof;
it does not authorize values for those slots. Each irreducible survivor must be
an eligible `[M]` or `[E]` value with complete derivation-exhaustion, Gap Law,
Residual Law, unique-slot, evidence, and admit-the-alien receipts. Otherwise
the consumer refuses. No per-class, per-world, familiar-reference, or viewer
value enters merely because this historical map names it.

Use `docs/working/ABIOTIC_EVIDENCE_DEBT.md` and the executable ledger admission
types for the active rule. Every reference to an authored residue or budget
below is historical terminology only.

Owner-supplied derive-first batch (2026-07-11) carrying the physics surface from ocean waves through material strength to the energy technologies and the electric age, each a derived skeleton with a short authored residue. Captured as a living map; spot-checked numbers noted. The meta-tool at the end, Buckingham Pi, bounds the authored surface of every phenomenon before it is built.

## Water surfaces

Deep-water dispersion `omega^2 = g*k` and shallow-water speed `c = sqrt(g*h)` are exact potential flow (a 4 km ocean gives 198 m/s tsunamis, confirmed). The capillary length `sqrt(gamma/(rho*g))` sets the maximum raindrop size at about 2.7 mm (confirmed). Surface tension is order-of-magnitude derivable from the cohesive-energy anchor as about `E_b/a^2` per exposed area, but it overshoots by a factor of a few through surface relaxation (the same relaxation-overshoot class as the vacancy-formation energy in the creep block), so it is authored or the error band accepted.

## Materials and structures

Stiffness is derivable in scale: `E ~ E_coh/a^3`, and one electron-volt per cubic angstrom is 160 GPa, the metal-and-rock range (confirmed). Strength is not thermodynamics but history: Griffith's `sigma = sqrt(2*E*gamma_s/(pi*c))` is a derived form in which the crack size `c` is a defect statistic that is authored, which is why real materials sit 100 to 1000 times below ideal strength; one strength anchor per material class. The derived cap `h_max ~ sigma/(rho*g)` limits mountains and towers (Mars at 0.38 g allows about 2.63 times taller relief, against the Olympus-Mons-over-Everest ratio of 2.5, confirmed). Melting via Lindemann (a vibration amplitude near a tenth of the spacing) gives `T_m` scaling with `E_coh` up to a fitted Lindemann constant (flagged). Friction coefficients are pure empiricism, dimensionless in the 0.1 to 1 range, authored.

## The energy technologies (derived ceilings)

Each technology has a derived ceiling from a conservation law or a detailed-balance argument, with at most a small residue. Carnot `eta = 1 - T_c/T_h` is exact and gates every heat engine. Betz `16/27` for wind follows from momentum conservation alone; the wind resource `(1/2)*rho*A*v^3` and hydro `rho*g*Q*h` are exact. Chemical fuels are about an electron-volt per bond, so the 10 to 50 MJ/kg scale derives from the same cohesive anchors as the latent heats (about 5 MJ/kg for a bond over a few tens of atomic-mass units, confirmed). The photovoltaic Shockley-Queisser ceiling near 33 percent for a single junction is detailed balance plus the Planck spectrum, one bandgap in and the ceiling out. Nuclear energy is `E = m*c^2` exact, and the semi-empirical mass formula has a fully derived liquid-drop form (volume, surface, Coulomb, asymmetry) with five megaelectron-volt-scale fitted coefficients, predicting the iron peak and fission's roughly millionfold advantage over chemistry, a ratio that is itself about the nuclear megaelectron-volt scale over the `alpha^2*m_e*c^2` chemistry scale, constants all the way down. Radioactive decay `N_0*exp(-lambda*t)` is derived (memoryless tunneling) and the Geiger-Nuttall relation is Gamow's tunneling integral, but the half-life values are exponentially sensitive, so they are authored per isotope (see the reclassification below). The authored radiogenic inventory times the decay law is the geothermal budget and the tectonic clock.

## The electric age

Maxwell's equations are exact and `c = 1/sqrt(mu_0*eps_0)`. The gem is Wiedemann-Franz: `kappa/(sigma*T) = pi^2*k_B^2/(3*e^2) ~ 2.445e-8 W*ohm/K^2`, a universal derived number for any metal on any world (confirmed). Drude conductivity `sigma = n*e^2*tau/m` splits cleanly: the carrier density `n` comes from valence, which is the periodic table again, while the scattering time `tau` is the fitted residue, one resistivity anchor per metal. The refractive index follows from Clausius-Mossotti and the polarizability (about `a^3`, and derivable from the first ionization energy per the earlier map), and once `n(lambda)` exists the 42-degree rainbow is geometry.

## The honest-residue ledger (complete and short)

Across the whole surface above, the authored residue is a short list: turbulence coefficients, defect and crack sizes, friction coefficients, isotope half-lives, the exospheric temperature, the Lindemann constant, and a few many-body-accident booleans such as water's ice-floats anomaly (authored as a boolean; it is a many-body accident). Everything else is skeleton.

## Two reclassifications (added in review)

- Strength is a STATE variable, not a per-class constant. Griffith's crack size `c` is authored per class, but the defect population evolves with the material's stress and thermal history, which is the same crack-and-damage substrate as the geodynamics yield block's Bercovici-Ricard grain damage. So the per-class anchor is the pristine ceiling and the actual strength is that ceiling degraded by accumulated damage, a written state variable like the lithology-as-history point. The mountain cap `sigma/(rho*g)` and the plate-boundary yield share one damage substrate, so strength carries a trajectory rather than a fixed number.
- Half-lives belong in the AUTHORED TABLE, not the per-world residue. They are measured per isotope because the Gamow integral makes them exponentially sensitive, but they are the same on every world, so they are a periodic-table-adjacent universal column (layer 2 of the taxonomy, beside `Z`, atomic mass, and the first ionization energy), not a per-world switch. The per-world part is the isotope inventory in the composition vector. The geothermal clock is universal-table half-lives times a per-world inventory times the derived decay law.

## The meta-tool: Buckingham Pi (the authored-input budget)

Before implementing any phenomenon, count its Buckingham-Pi dimensionless groups; that count is the maximum number of authored inputs the phenomenon may legally demand before it is fabricating. This is the design-review ceiling on the authored surface, and it pairs with the per-value derivation-hunter: Pi caps how many authored values can exist at all, and the hunter proves each survivor cannot derive. It is adopted as a standing gate discipline: every new physics substrate declares its Pi-group count as its authored-input budget, and a design that reaches past that count fails the gate.
