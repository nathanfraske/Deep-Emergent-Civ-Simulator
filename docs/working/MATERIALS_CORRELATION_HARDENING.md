# Materials substrate: the correlation-classifier hardening (owner pre-build audit, twenty-second audit)

Owner-authored (2026-07-12). A PRE-BUILD adversarial audit of the first four model-breakers (Mott insulators, cerium, plutonium, water) against the materials substrate DESIGN, found before the sweep was run. Verdict: all four live on a single axis the substrate never laid, and the stage-3 dispatch as specified WOULD misroute them. The fix and the case resolutions are below. Provenance inline per the seven-tag register.

## The single missing axis: itinerant W vs on-site Coulomb U
All four breakers are the competition between itinerant kinetic energy `W` (bandwidth) and on-site Coulomb repulsion `U`. The stage-3 dispatch as built ("metallicity from conduction columns") is BAND-FILLING logic, and band filling calls NiO a METAL (the famous failure). So the substrate as specified misroutes the correlated materials. The clean fix: BOTH axes of the missing classifier are already banked columns.
- **U** = the hardness ladder, successive ionization differences `U_atomic = IE_{n+1} - IE_n` (the same IE ladder the buffer machinery reads), SCREENED in-crystal by a class factor ~0.4 to 0.5 `[E, declared band]`.
- **W** = Harrison's `V ~ hbar^2 r_d^3 / (m d^5)` on the banked radius columns.
So the LOCATION of the correlated regime DERIVES at estimator grade (`U/W` from columns). What does NOT derive is BEHAVIOUR inside it, and the honest architecture is a CLASSIFIER that dispatches AWAY from estimators exactly there (to measured / compute-once). The substrate's job in the correlated regime is to REFUSE correctly, and it can now derive WHERE to refuse: the Mars-obliquity refusal transplanted to condensed matter.

## Case-by-case resolutions
**Mott insulators (classifier fix + a retroactive vindication):** Ni2+ has `IE3 - IE2 = 35.2 - 18.2 = 17 eV` atomic, screened to the textbook 7-8; Harrison `W` for 3d oxides is a few eV; `U/W ~ 2` -> insulator, correct where band filling fails. The Zaanen-Sawatzky-Allen (ZSA 1985) refinement adds a SECOND axis free: the charge-transfer energy `Delta` from the banked chi columns, splitting Mott-Hubbard (V2O3) from charge-transfer (NiO, cuprate parents) insulators `[D-in-form]`. This closes a loop open for ten turns: the banked magnetics machinery (Hund, 10Dq, Goodenough-Kanamori) presupposes LOCALIZED electrons and worked on NiO/magnetite precisely because they sit on the localized side; the classifier now makes that a ROUTED decision, not a lucky one. Gap values stay `[M]`/compute-once; cuprate superconductivity is a named wall.

**Cerium gamma->alpha collapse (two-branch disposer, most of it derived):** a single Rose EOS is single-welled, but cerium's f electron gives TWO free-energy branches, localized (larger volume, 4f1 moment) and itinerant (bonded, ~15% smaller), crossing at ~0.8 GPa with a critical point near 550-600 K, 2 GPa (the only solid-solid isostructural line ending in a critical point, `[M]` Koskenmaki-Gschneidner). Resolution: the disposer already competes PHASES; extend it to compete ELECTRONIC BRANCHES of the same structure, each with its own EOS. The thermodynamic skeleton derives: the localized branch carries magnetic entropy with a counted ceiling `R ln(2J+1) = R ln 6` for `J=5/2` `[D, Kondo screening eats part, flag]`, so heating favours localization, `dP/dT = dS/dV > 0` with both signs positive, matching the phase diagram, and the branch merger IS the critical point (van der Waals on electrons). 3He's Pomeranchuk minimum (solid entropy exceeds liquid below 0.3 K, melting-curve slope flips) validates the sign logic, same theorem. Crossing position and collapse magnitude: compute-once (DMFT) or `[M]`.

**Plutonium (refuse correctly, and derive where):** the worst case in nature (six allotropes below a 913 K melt, delta 25% larger than alpha, negative thermal expansion, fluctuating-valence moments, Shim-Haule-Kotliar 2007). No estimator survives meV allotrope spacings. But the ACTINIDE ROW POSITION derives: `r_f` contracts with Z, so `W ~ r_f^3/d^5` falls while `U` holds, `U/W` crosses 1 at Pu-Am (the famous volume-kink plot), and the classifier flags the window FROM COLUMNS ALONE. Inside the window: measured rows for real elements (Pu is exhaustively measured); for INVENTED f-elements near the line, the generator outputs the FLAG, the branch structure, and bands, NEVER a fabricated allotrope count. Real derived structure survives at the edges: Ga-stabilized delta is Hume-Rothery; self-irradiation aging is the radiogenic column writing the damage state (~2 mW/g for Pu-239, He bubbles and swelling `[W]`, a cross-module echo). Invar's near-zero expansion joins delta-Pu's NTE as the same moment-volume class accident `[M]`.

**Water (the boolean derives its SIGN, the accident relocates one level down):** with the H-bond Keating term the competition is priceable from columns: open tetrahedral ice keeps ~2 net H-bonds/molecule (~42 kJ/mol on the E_HB ladder, vs measured 51 sublimation), while close-packing's consolation is ~8 extra London pairs at `(3/4) alpha^2 IE/r^6 ~ 1.2 kJ/mol` each ~10 kJ/mol. Directionality wins by ~4x: ICE FLOATS is now `[D-in-form]` IN SIGN, authored only in decimals, and the consumer web that hung on the boolean (top-down lake freezing, under-ice refugia, 4C dimictic turnover, frost weathering) inherits the upgrade. Bonus closure: Pauling's proton-disorder entropy `R ln(3/2) = 3.37 J/mol.K` against Giauque-Stout's calorimetric ~3.4, a 1% derived match. Beyond estimators (compute-once by name, MB-pol lineage): the quantitative anomaly set (density max at 4C, ~20 ice polymorphs, melting-curve reentrance) needs many-body polarization plus nuclear quantum effects; the two-liquid LDL/HDL picture with its hypothesized critical point is the cerium two-branch machinery again (open vs collapsed network as branches) `[flag, active research]`. NQE forces one new flag: hydrogen's zero-point energy is not classical (D2O shifts the density maximum 4.0 -> 11.2 C and melting by 3.8 K `[M]`), so LIGHT-ELEMENT PHASES (H, He) carry a path-integral compute-once tag.

## The three consolidated additions to the substrate
1. **The CORRELATION CLASSIFIER** `[E, column-native, ZSA axes, wide band from the screening factor]`, inserted in STAGE-3 dispatch AHEAD of the metallic route: `U/W` (and the ZSA `Delta`) dispatches itinerant -> banked metallic machinery, localized -> banked Hund/CF/superexchange, WINDOW -> estimators FORBIDDEN, measured/compute-once only. VO2's 340 K metal-insulator transition is the boundary's poster child and standing validation (Peierls-vs-Mott flagged field-unsolved).
2. **TWO-BRANCH FREE ENERGIES** as engine spec under the existing solver law: branch crossings are FOLDS (the banked continuation machinery), hysteresis is WRITTEN STATE, seeded draws at sub-resolution gaps. One mechanism serves cerium, water's two liquids, and mixed-valence systems (SmB6 class).
3. **The NQE FLAG:** light-element phases (H, He) carry a path-integral compute-once tag; ZPE is not classical bookkeeping.

## Validation battery gains
The monoxide series classification (NiO/CoO/FeO insulators, not metals); the actinide U/W kink location (Pu-Am); cerium's positive dP/dT and the critical-point existence; the ice-floats SIGN derivation; the Pauling proton-disorder entropy closure (3.37 vs 3.4); the D2O density-max and melting shifts; 3He's Pomeranchuk slope flip; VO2 340 K as the classifier-boundary poster child.

## Ledger delta (twenty-second audit)
Layer 3 gains the U screening factor `[E class]`, the classifier and ZSA axes `[D-in-form on banked columns]`, the NQE flag, and moment-volume accidents booked `[M]`; the ice boolean retags `[D-in-form sign, authored decimals]`; stage-3 dispatch gains the `U/W` check AHEAD of the metallic route; named walls grow by cuprates and quantitative Pu; layer 4 gains nothing.

Verify Mott 1949/1968, Hubbard 1963, Zaanen-Sawatzky-Allen 1985, Imada-Fujimori-Tokura 1998, Koskenmaki & Gschneidner 1978, Shim-Haule-Kotliar Nature 2007, Pauling 1935 vs Giauque & Stout 1936, Babin-Medders-Paesani (MB-pol), Poole 1992 and Kim 2020, Morin 1959 (VO2).

---

# SECOND AUDIT BATCH (owner, twenty-third audit): floor / exotic / edge

Owner-authored. The rest of the adversarial sweep, in three shapes: the FLOOR batch tests whether the existing tags already contain each breaker (4 of 5 yes, 1 own-goal); the EXOTIC batch tests reach (the substrate extends, with two named handoffs); the EDGE batch is computability hygiene, where the answer is ENGINE LAW, not physics.

## Floor audits
- **LDA/GGA band gaps:** provenance ROUTING, not new physics. Total-energy quantities (formation energies, EOS, the disposer's diet) stay PBE-grade; SPECTRAL quantities (gaps, band edges, what the semiconductor and opacity consumers read) escalate one rung (hybrid/GW class or the `[M]` column). Rule: EIGENVALUES ARE NOT OBSERVABLES, route by consumer. ("semiconductor gaps [M]" already obeyed this before the reason was stated.)
- **Invar:** upgrade from moment-volume accident to the TWO-BRANCH machinery (high-spin-large-volume vs low-spin-small competing within kT, thermal population of the small-moment branch cancels lattice expansion, Weiss 1963 / DMFT). Retag `[D-in-form mechanism, M magnitude]`; Invar clusters near the itinerant-moment instability line the classifier draws (Fe-Ni, Fe-Pt, delta-Pu NTE all sit there).
- **Silica 3 kJ/mol mis-ordering:** already legislated by the resolution-ladder rule + sub-kT seeded draw. Battery addition: quartz->coesite->stishovite PRESSURE ordering must come out right (large dV, PdV dominates, estimator-safe), while quartz-vs-cristobalite at ambient is a DECLARED DRAW; a future change that silently asserts it must FAIL the audit as overconfidence.
- **Gold vs silicon Cauchy:** promote the SIGN to a classifier OUTPUT: `C12 - C44 > 0` flags embedding-class (many-body volume energy), `< 0` flags angular-class, computed BEFORE dispatch, so the litmus ROUTES rather than merely diagnoses.
- **Tin pest (not a breaker):** the machinery owns it. beta-Sn (metallic, dense) vs alpha-Sn (gray, diamond-structure, 27% larger, semiconducting) is a two-branch crossing at 13C, CNT-plus-interface kinetics (incubation, autocatalytic spread) inheriting the prefactor flag, the freezer writing damage into artifacts (Napoleonic buttons, organ pipes). Worked: 13C, dH~2 kJ/mol -> dS~7 J/mol.K, the right electronic-entropy scale [M].

## Exotic audits (the substrate extends, two handoffs)
- **Carbide worlds:** the disposer already returned moissanite-graphite mantles (Pitch); SiC POLYTYPISM (200+ stackings, meV/bilayer) is the sub-kT draw's poster child; consumers healthy (SiC stiffness/conductivity run the banked chains, carbide-world geodynamics gets its distinct regime honestly).
- **Superionic ice:** a PARTIAL two-branch state (oxygen lattice solid while protons melt into a conducting fluid), carrying SPLIT TRANSPORT tags (lattice kappa solid-like, ionic sigma liquid-like ~100 S/cm [M, Millot 2018-19]). HANDOFF: it hands the dynamo module a new derived branch (ice-giant fields generated in a superionic shell, multipolar and offset, observed at Uranus and Neptune, a banked validation waiting).
- **Metallic hydrogen:** the classifier's own axes predict the insulator-metal crossing (Herzberg/band-closure ~400-500 GPa [M contested, flag], Wigner-Huntington 1935 the oldest two-branch prediction in the file). Jupiter consumes the liquid-metallic EOS [compute-once] and its dynamo the conductivity; the NQE flag is load-bearing (proton ZPE shifts boundaries tens of GPa).
- **Non-Terran isotope mixes:** almost free (mass columns are per-isotope): reduced-mass shifts move every vibrational frequency (sqrt(mu)), opacities, NQE phase shifts, diffusion, heavy-isotope self-shielding. RULE: isotope abundances are layer-4 contingency (GCE + fractionation), and only the sim's OWN fractionation (Rayleigh distillation on p_sat, gravitational settling, crossover-mass escape writing D/H) may write them, NO authored ratios downstream of genesis.
- **Degenerate matter (the refusal line is the deliverable):** the substrate derives its own basement (P ~ n^(5/3), Zapolsky-Salpeter). Name the ladder: electron degeneracy + Coulomb -> white-dwarf EOS + the Chandrasekhar mass [D]; one-component-plasma freezing at Gamma~175 -> crystallizing white dwarfs [D-in-form, Gaia cooling sequences]; BEYOND NEUTRON DRIP the engine REFUSES (nuclear-matter EOS is QCD's, the same sovereignty as layer-1 nuclear variation), so neutron stars enter as [M] mass-radius envelopes (GW170817-class) consumed, never derived.

## Edge cases, as ENGINE LAW (not physics)
- **Unbound anions:** free-ion reference states with negative binding are FORBIDDEN as inputs, the stabilized-density representation mandatory (the QEq constraint clause already half-said this).
- **Single-element "compounds":** the proposer emits ELEMENTAL phases as first-class candidates (allotropes are polymorphs of a one-element assemblage, graphite/diamond load-bearing). A unit test, not a feature.
- **Vacuum:** a legal E-bucket, mu undefined, the disposer returns the empty assemblage + surface phases at the boundary; sublimation into vacuum is p_sat with zero back-pressure (airless-world consumers assume it).
- **One-cell grids:** legal but flagged; every gradient-driven module degenerates to its 0-D balance form (the documented reduction, a Tozer box, NOT a silent divide-by-zero). Spec rule: every field module declares its 0-D limit.
- **Negative / imaginary computed quantities, split by meaning:** imaginary phonon/sound speed = the lattice INSTABILITY signal (route to the disposer, discard or follow the soft mode) [D]; negative bulk modulus mid-continuation = a SPINODAL fold flag; negative absolute temperature FORBIDDEN except the declared population-inversion (laser) exception; anything else non-physical (negative mass, NaN) is a HARD INVARIANT VIOLATION that halts the cell and surfaces, NEVER clamps (clamping is silent fabrication).
- **Q32.32 T/P limits:** real and quantifiable (2^31 K fine for planets, overflows at white-dwarf/neutron-star pressure; 2^-32 K insufficient at the superfluid/BEC end). LAW: per-domain fixed-point scales (log-binned or per-module Q-formats) chosen so each module's dynamic range sits centered, the lattice-quantization theorem (spacing below error band) as the selection criterion; any cross-domain quantity passes an exact rescaling with declared rounding. Overflow surfaces as a COMPUTABILITY FLAG, the arithmetic sibling of the neutron-drip refusal: the engine states where its NUMBER SYSTEM ends exactly as it states where its PHYSICS does.

## Two recurring meta-patterns (worth naming)
1. **Observables, not artifacts, route by consumer.** "Eigenvalues are not observables" is the charge-not-an-observable lesson for spectral quantities: total-energy consumers get PBE, spectral consumers escalate. The provenance grade required is consumer-driven (pairs with the escalation rule and the resolution ladder).
2. **The REFUSAL LINE is a first-class deliverable.** The substrate derives down to its basement and then REFUSES with an honest boundary: neutron-drip -> QCD sovereignty ([M] envelopes), the correlated window -> estimators-forbidden (measured/compute-once), Q32.32 overflow -> computability flag. Knowing where you stop, and saying so, is part of the substrate, the Mars-obliquity refusal made structural.

## Ledger delta (twenty-third audit)
Layer 3 gains the eigenvalue-routing rule, the Cauchy-sign classifier output, Invar's mechanism retag, superionic split-transport tags, the OCP Gamma~175 line, the isotope-writer exclusivity rule. Named walls gain neutron-drip sovereignty and the metallic-hydrogen contested band. Engine law gains the six edge rulings. Validation battery gains tin's 13C, Uranus-Neptune multipolar fields, Gaia white-dwarf crystallization, silica pressure-ordering-with-declared-draw. Layer 4 gains nothing.

Verify Perdew 1983 and Sham-Schluter (the discontinuity), Weiss 1963, Wigner-Huntington 1935, Millot 2018/2019, Zapolsky-Salpeter 1969, Tremblay 2019, Busch-Kern (tin), Soderlind (Pu-adjacent DMFT).
