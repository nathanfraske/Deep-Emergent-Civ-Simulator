# Next-arc bridge kickoff (doc-only): what should the next arc be?

This is a doc-only bridge opened off current `main` (`30304b8`, which now carries the merged R-UNITS-PIN continuation, #133). Its only purpose is to ask the gate what the next arc should be. No mechanism is authored and no value is moved.

## Where the last arc left things

The R-UNITS-PIN continuation (B, floor-invariant population; A, the consumer-lift survey; C, the design consolidation) is merged. The two-tier number system, the load-time `LawExpr` scale planner, the divide-and-log physical-floor invariant on the run-path sites, the CODATA float-free composite compute, and the flagship byte-neutral sigma lift are all on main. R-UNITS-PIN is recorded as a stated partial advance (record 62.25, audit block 1aa) and stays open. All four canonical pins are byte-identical.

R-UNITS-PIN keeps two named residuals, which are the only pieces the item still owes:
- The emic-unit conversion is a hash map with one multiply-and-divide, so an emic to absolute to emic round trip is lossy by up to one epsilon for a non-power-of-two factor. The item asks for a round-trip exactness rule.
- The sorted unit store for an ordered canonical walk is unbuilt.

A third, explicitly deferred item is on record from the survey: the sky solar-baseline (`radiative_equilibrium` via `sky.sigma`) is the one remaining sigma sibling, attenuated four-fold through a quarter power below the flagship's already-byte-neutral correction, flagged to lift only if a future scenario surfaces it as material.

## The candidates for the next arc, for the gate to pick

**Option 1: close R-UNITS-PIN by finishing its two residuals.** Build the emic round-trip exactness rule (so an emic to absolute to emic round trip returns the original for the representable cases, with the honest lossy boundary stated for the rest) and the sorted unit store (so any canonical walk over the unit registry is ordered, the R-CANON-WALK sibling for units). This is the smallest, most self-contained next step and it would let R-UNITS-PIN move from partial advance to resolved. It couples to the determinism cluster (an ordered walk is a determinism guarantee).

**Option 2: take an adjacent open determinism-cluster item.** The red-and-green audit's cluster still has six open siblings (R-CANON-WALK, R-CMD-ORDER, R-REDUCE-ORDER, R-HARNESS-COVER, R-SAVE-SCHEMA, R-PROJ-REGISTER). The sorted-unit-store residual above is a small instance of R-CANON-WALK, so the gate may prefer to fold that residual into a broader canonical-walk pass rather than solve it unit-locally.

**Option 3: a different open arc the gate has in view.** The backlog holds thirty-six open items across the world-content registries, the R-DEEPTECH deeper questions, the language typology substrate, long-range communication, the anti-steering hardening items (R-DIM-HOMO, R-EVOLVE-STEER), and the gaps cluster. If the gate wants to advance one of these instead, name it and I ground the scope.

## The ask

Gate, what should the next arc be: option 1 (close R-UNITS-PIN's residuals), option 2 (fold the ordered-walk residual into a broader determinism-cluster pass), option 3 (a different open item you name), or something else. On your direction I ground the scope in the actual parts, run the input-audit for a generalization seam, and bring back a slice plan before building. The cost directive still holds unless you lift it: I self-audit and ask you to gate, no spawned panels or fleets.
