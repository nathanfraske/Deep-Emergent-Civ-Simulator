# Creature selection loop: making the predator-prey reaction emerge (design-first)

This is the design opener for the arc the owner ruled next: complete the creature reproduction-and-behaviour-selection loop kicked off in #126, so the predator-prey reaction (fleeing and hunting) EMERGES observably in a shipped `full --creatures` run and re-pins it. Design-first, grounded against source, posted for the gate's ruling before any code. No mechanism here.

## What is already built (do not rebuild)

The perception SUBSTRATE (reach, sensorium-gating, receiver-side valence) is merged (#109). The creatures-react CAPACITY is built (#118). The 2a creature reproduction-and-selection substrate is merged (#126): `Walker.lineage`, the structural pairing predicate `carries_lineage()`, `Controller::perturbed`/`midparent`, the mint-time bounded zero-mean perturbation, and the `reproduce_creatures`/`breed_offspring` beat (co-located reserve-eligible lineage-carrying creatures mint a midparent-blended offspring, eligibility on the being's own energy-backing axis, all seed-keyed). The 2b percept kind-feature floor is merged (#126): a creature can perceive a strength-independent optical feature of another being and key a founder-zero freely-signed per-bucket weight on it. This arc rebuilds none of that; it makes the loop LIVE and closes the two gradients selection climbs.

## What remains: the loop must run and both gradients must flow

The mortality pressure is live (#117/#120): an authored predator lands a lethal whole-body wound on a co-located catalog prey, which dies through the one unified INTEGRITY cull. The prey's flee sign and any hunt sign stay founder-zero, so the pressure exists but nothing selects on it yet. Two things make the reaction emerge, and the second is the load-bearing design point.

**The fleeing gradient EXISTS (confirm it flows).** A prey that avoids the predator survives the strike-to-death loop (#117), and a creature is selected on its own homeostatic survival, so surviving predation IS the selection pressure on the flee weight. What this arc must confirm at source is that a prey lineage which reproduces in-run carries the being-directed weight into its offspring and that surviving-longer converts into more offspring (the reproduction beat firing over generations), so the flee sign has a real cross-generational gradient to climb.

**The hunting payoff needs wiring (the load-bearing point).** For the hunt weight to be favoured, the predator must GAIN reserves from the kill: it must take in the killed being's own matter and energy through the existing `physiology::physical_intake` path. Grounded at source, this payoff is NOT yet wired: the opt-in corpse-matter death hook (`set_corpse_matter`) deposits the dead being's matter to the cell's tissue/soil field for the decomposer and matter cycle, but the killer does not gain reserves from its kill, so hunting has no selection gradient and will not emerge however long the run goes. Wiring the predator-eats-its-kill energy payoff is therefore part of this arc. Derive-first, per the owner's ruling: the payoff is the kill's own physics (the prey's matter and energy assimilated through the same intake path a grazer uses on any matter, keyed on the killer's own backing component), never a stamped hunt reward.

## The framing statement, amended (for the gate's frame-blind)

The gate approved the original framing to build with three amendments (intake-from-an-other with a contestable corpse, the fitness read over the being's own axes, the discrimination resolved by channel with an honest emergence claim). The amended, de-narrivatized statement, phrased as a claim to attack:

"A creature lineage that reproduces and is selected on its OWN homeostatic survival over its OWN declared reserve axes will, over generations, have the sign and strength of its heritable per-channel weights set by selection alone. The APPROACH payoff is taking in another being's matter or energy through the existing trophic-agnostic intake path (which consumes physical content from a corpse, a live other, or a tile alike, keyed on the eater's own metabolism): a lineage whose weights carry it to a perceivable patch of an other's matter it can assimilate gains reserves and reproduces more. A being-on-being strike produces a PERSISTENT, FINITE, CONTESTABLE corpse (a matter deposit), and the striker takes only what its own bounded ingestion capacity allows against the corpse mass, the remainder persisting as a perceivable resource patch for other beings to approach and contest before it rots to soil through the existing cycle, so scavenging and carcass competition arise from the corpse-as-contested-matter plus perception plus bounded intake, nothing authored. The AVOIDANCE payoff is surviving the strike-to-death loop: a lineage whose being-signal weight carries it away from a live emitter correlated with harm survives and reproduces more. The two dispositions resolve BY CHANNEL: the resource-density channel (the foraging percept) draws a being to a corpse or matter patch, and the being-signal channel carries it toward or away from a live emitter, so no single weight two-way-sorts. Approaching a live other to kill it (active predation on live prey) requires the being channel to key on a distinguishing physical feature of the other (the per-feature discrimination), absent which the being-signal weight can only sort by signal magnitude (approach a weaker signal, avoid a stronger one), which is the honest scope of what emerges. A lineage for which no trophic approach and no avoidance pays evolves neither."

## My single-pass self-frame-blind on the amended statement (per the standing cost directive)

Per the cost directive (no spawned panels), I re-ran the frame-blind discipline over the amended statement myself in one pass and surface it for the gate's adversarial audit. The section-11-style input-bias self-check: the statement names no predator or prey kind, no type or role label; it reads no high-level fact (relatedness, kind, a named disposition) to produce approach-or-avoid; the per-channel weights are founder-zero and freely-signed, the resource-density channel keying on a cell's own identity-blind matter content and the being channel on the emitter's own sensed feature or signal magnitude, never a kind. The payoffs are the encounter's own physics: the approach payoff is the trophic-agnostic intake path (a corpse, a live other, or a tile alike), not a stamped hunt reward, and the corpse is the existing matter deposit that rots through the existing cycle, so scavenging and carcass competition are described outcomes of matter-plus-perception-plus-bounded-intake, never coded modes. The avoidance payoff is the INTEGRITY cull. Admit-the-alien holds two ways: the fitness reads the being's OWN declared reserve axes (not a fixed Terran pair), and a lineage that evolves no trophic approach and no avoidance is a valid selected outcome. The discrimination claim is scoped straight: the magnitude-only being channel yields a NET disposition, so the statement claims a two-way sort only where two channels or a per-feature discrimination supply it, and scopes the single-channel case to the magnitude proxy it can deliver. The seams I state rather than hide: the hunt-live-prey two-way sort needs the per-feature discrimination (the 2b follow-on) or the magnitude-proxy scope; and one item to CONFIRM at build, per the gate, is that the corpse deposit is perceivable on the resource-density channel the foraging arc built (the corpse-matter hook writes the tissue/soil field, which must reach the `cell_content` read for a being to be drawn to a carcass), wiring the bridge if it is not.

## Scoped slices, amended (byte-neutral where they can be; `full --creatures` the one intended re-pin)

1. Fitness over the being's OWN reserve axes (amendment 2, a correction): fold homeostatic survival over `emb.homeo.axes.iter()`, reusing the axis-parametric pattern the adjacent harm/reward substrate already uses (`runner.rs:4708`, `:4783`), so selection reads the being's own declared reserve set and a non-Terran-substrate lineage forms selection under its own axes. Confirm the reproduction beat fires over generations and carries the per-channel weights into offspring (or the honest reason it does not yet, the biosphere-balance/foraging coupling stated, not papered over), so the AVOIDANCE gradient (surviving predation, already present) has a real cross-generational path to climb.
2. Wire the approach payoff as INTAKE-FROM-AN-OTHER (amendment 1, the load-bearing generalization): the being takes in another being's matter or energy through the trophic-agnostic `physical_intake` path, keyed on the eater's own backing component, derive-first (no stamped reward). A being-on-being strike produces the existing persistent, finite, contestable corpse (`set_corpse_matter`); the striker takes only what its own bounded ingestion capacity allows against the corpse mass, the remainder persisting. CONFIRM (and wire the bridge if absent) that the corpse deposit is perceivable on the resource-density channel the foraging arc built (`cell_content`), so a being is drawn to a carcass, and scavenging plus carcass competition (a second being contesting the remainder) EMERGE from corpse-plus-perception-plus-bounded-intake, nothing authored. Live-other non-lethal modes (grazing a live other, parasitism) are the flagged sibling follow-on; this slice gets the general shape (intake-from-an-other) right so they drop in as data. Byte-neutral opt-in.
3. Arm the loop in `full --creatures` so the reaction is observable: the being-signal percept armed (the held-out 2b arming), the resource-density percept armed (the foraging channel, so the corpse patch is sensed), and the reproduction beat live, both channels present. This re-pins `full --creatures` with the emergent reaction as the stated reason, the four canonical pins proven to hold (they carry no creatures).
4. The emergence proof, resolved BY CHANNEL and scoped straight (amendment 3): measure the resource-density weight moving off founder-zero to approach a corpse or matter patch (scavenging) and the being-signal weight moving to avoid a live emitter correlated with harm (threat-avoidance), each a single-channel disposition no single weight two-way-sorts. For ACTIVE predation on live prey (approaching a live emitter to kill it), either the per-feature discrimination is wired (the being weight keys on a distinguishing physical feature, the 2b follow-on) and the two-way sort is measured, OR the claim is scoped to the magnitude proxy (a lineage approaches a weaker signal and avoids a stronger one) and THAT is measured, never a two-way sort the magnitude-only channel cannot deliver. With a control that does not move under held selection; replayed bit-for-bit. Nothing about approach-versus-avoid is asserted from a rule.

## Hard constraints (owner rulings, carried into every slice)

Nothing about approach-versus-flee is authored: the sign and strength of the being-weight emerge from cross-generational selection (Principle 8, Principle 9). The hunt payoff is the prey's own matter and energy through the existing intake path, never an authored reward. Admit-the-alien: a non-hunting or non-fleeing lineage is a valid selected outcome, never a failure; the fitness reads the being's own data.

## For the gate

This is the amended framing carrying your three amendments (intake-from-an-other with a contestable corpse, the fitness read over the being's own axes, the discrimination resolved by channel with an honest emergence claim), with my single-pass section-11 self-check re-run over it. On your ruling I ground each slice at source (in particular the two source claims your amendments rest on: `physical_intake`'s trophic-agnosticism and the axis-parametric harm/reward fold, plus the corpse-to-`cell_content` perceivability to confirm), build byte-neutral where it can be, run the single-pass section-9 self-audit per slice, and post with the pins (the four canonical held, `full --creatures` re-pinned with the emergent reaction as the reason).
