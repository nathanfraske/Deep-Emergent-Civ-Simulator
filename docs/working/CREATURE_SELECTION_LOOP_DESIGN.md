# Creature selection loop: making the predator-prey reaction emerge (design-first)

This is the design opener for the arc the owner ruled next: complete the creature reproduction-and-behaviour-selection loop kicked off in #126, so the predator-prey reaction (fleeing and hunting) EMERGES observably in a shipped `full --creatures` run and re-pins it. Design-first, grounded against source, posted for the gate's ruling before any code. No mechanism here.

## What is already built (do not rebuild)

The perception SUBSTRATE (reach, sensorium-gating, receiver-side valence) is merged (#109). The creatures-react CAPACITY is built (#118). The 2a creature reproduction-and-selection substrate is merged (#126): `Walker.lineage`, the structural pairing predicate `carries_lineage()`, `Controller::perturbed`/`midparent`, the mint-time bounded zero-mean perturbation, and the `reproduce_creatures`/`breed_offspring` beat (co-located reserve-eligible lineage-carrying creatures mint a midparent-blended offspring, eligibility on the being's own energy-backing axis, all seed-keyed). The 2b percept kind-feature floor is merged (#126): a creature can perceive a strength-independent optical feature of another being and key a founder-zero freely-signed per-bucket weight on it. This arc rebuilds none of that; it makes the loop LIVE and closes the two gradients selection climbs.

## What remains: the loop must run and both gradients must flow

The mortality pressure is live (#117/#120): an authored predator lands a lethal whole-body wound on a co-located catalog prey, which dies through the one unified INTEGRITY cull. The prey's flee sign and any hunt sign stay founder-zero, so the pressure exists but nothing selects on it yet. Two things make the reaction emerge, and the second is the load-bearing design point.

**The fleeing gradient EXISTS (confirm it flows).** A prey that avoids the predator survives the strike-to-death loop (#117), and a creature is selected on its own homeostatic survival, so surviving predation IS the selection pressure on the flee weight. What this arc must confirm at source is that a prey lineage which reproduces in-run carries the being-directed weight into its offspring and that surviving-longer converts into more offspring (the reproduction beat firing over generations), so the flee sign has a real cross-generational gradient to climb.

**The hunting payoff needs wiring (the load-bearing point).** For the hunt weight to be favoured, the predator must GAIN reserves from the kill: it must take in the killed being's own matter and energy through the existing `physiology::physical_intake` path. Grounded at source, this payoff is NOT yet wired: the opt-in corpse-matter death hook (`set_corpse_matter`) deposits the dead being's matter to the cell's tissue/soil field for the decomposer and matter cycle, but the killer does not gain reserves from its kill, so hunting has no selection gradient and will not emerge however long the run goes. Wiring the predator-eats-its-kill energy payoff is therefore part of this arc. Derive-first, per the owner's ruling: the payoff is the kill's own physics (the prey's matter and energy assimilated through the same intake path a grazer uses on any matter, keyed on the killer's own backing component), never a stamped hunt reward.

## The framing statement (for the gate's frame-blind)

De-narrivatized, phrased as a claim to attack: "A creature lineage that reproduces and is selected on its own homeostatic survival in the running world will, over generations, have the sign and strength of its heritable being-directed weight (the weight that turns a perceived other's sensed direction into approach or avoidance) set by selection alone. A lineage whose weight avoids an other that is lethal to approach survives and reproduces more; a lineage whose weight approaches an other whose matter it can take in for a reserve gain survives and reproduces more. So avoidance and approach each emerge only where the physics of the encounter makes them pay: surviving the strike-to-death loop is the avoidance payoff, and taking in the killed other's own matter and energy through the existing intake path is the approach payoff. A lineage for which neither pays evolves neither."

## My single-pass self-frame-blind (per the standing cost directive)

Per the cost directive (no spawned panels), I ran the frame-blind discipline myself in one pass and surface it for the gate's adversarial audit rather than an N-panelist workflow. The section-11-style input-bias self-check on the statement: it names no predator or prey kind, no type or role label; it reads no high-level fact (relatedness, kind, a named disposition) to produce the approach-or-avoid; the being-directed weight is founder-zero and freely-signed and keys on the emitter's own sensed feature (2b), never a kind; the two payoffs are the encounter's own physics (the INTEGRITY cull and the intake path), not stamped rewards; and a lineage that evolves neither is an admissible selected outcome. The one seam the self-check surfaces, which I state rather than hide: the hunt payoff must physically EXIST for its gradient, and today it does not (the kill enriches the soil, it does not feed the killer), so the honest form of this arc WIRES that payoff from the kill's own matter, and until it is wired the hunt sign has no gradient and correctly does not emerge. A second flagged limit, inherited not introduced: the fitness read is through fixed Terran reserve axes (ENERGY/WATER), so a non-Terran-substrate lineage forms no selection under either gradient (the named sibling generalization, not silently assumed here).

## Scoped slices (byte-neutral where they can be; `full --creatures` the one intended re-pin)

1. Confirm the fleeing gradient flows at source: the reproduction beat fires over generations in `full --creatures` (or the honest reason it does not yet, the biosphere-balance/foraging coupling), the being-directed weight is carried into offspring, and surviving-predation converts into differential reproduction. Where the beat does not fire because creatures starve, that coupling is stated (it ties to the foraging arc just merged and the GATED biosphere balance), not papered over.
2. Wire the hunting payoff: the killer takes in the killed being's own matter and energy through `physical_intake`, keyed on the killer's own backing component, derive-first (the kill's physics, no authored reward). Byte-neutral opt-in.
3. Arm the loop in `full --creatures` so the reaction is observable: the being-feature percept armed (the held-out 2b arming), the reproduction beat live, both gradients present. This re-pins `full --creatures` with the emergent reaction as the stated reason, and the four canonical pins are proven to hold (they carry no creatures).
4. The emergence proof: over a `full --creatures` run, the flee sign (and, where the payoff pays, the hunt sign) is measured moving off founder-zero under selection, with a control that does not; replayed bit-for-bit. Nothing about approach-versus-flee is asserted from a rule.

## Hard constraints (owner rulings, carried into every slice)

Nothing about approach-versus-flee is authored: the sign and strength of the being-weight emerge from cross-generational selection (Principle 8, Principle 9). The hunt payoff is the prey's own matter and energy through the existing intake path, never an authored reward. Admit-the-alien: a non-hunting or non-fleeing lineage is a valid selected outcome, never a failure; the fitness reads the being's own data.

## For the gate

Please run your adversarial audit over this framing and rule it, and in particular confirm the load-bearing point: the hunting payoff needs wiring (the predator gains reserves from the kill's own matter through `physical_intake`), and that wiring is in-scope for this arc. On your ruling I ground each slice, build byte-neutral where it can be, run the single-pass section-9 self-audit per slice, and post with the pins (the four canonical held, `full --creatures` re-pinned with the emergent reaction as the reason).
