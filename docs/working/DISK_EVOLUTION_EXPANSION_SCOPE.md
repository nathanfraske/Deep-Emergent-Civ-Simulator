# Disk-evolution expansion scope: the chain over non-main-sequence stars

Status: scoped and gate-ruled, held at its own gate after slice 3. This records the spine so the expansion stands ready. The load-bearing piece (the pre-main-sequence contraction luminosity) already jumped this gate and is BUILT in the base arc, because the race's dropped-multiplicand catch gave it a consumer inside the current arc; everything else here waits.

The working principle the whole expansion rests on: EVERY `None` IS A DOOR. The base arc's mechanisms are parametric over state variables and guarded at their fit domains, so the domain guards that refuse today are the dispatch seams tomorrow. The expansion is wires and branches, not rewrites, and every guard keeps its exact semantics while gaining a second exit. Total cost: two wires, one derived branch with a band, two conditioning fields, one named axis, one derived power law (the contraction luminosity, already built), and zero authored scalars.

## Move one: feed the star's own track, and the saturation blindness

Disk-hosting stars are not main-sequence objects: they are pre-main-sequence stars descending the Hayashi track, brighter and fully convective. The main-sequence bias in the current chain lives entirely in what callers feed, so it is a wire, not a rewrite.

- THE `L_bol` WIRE (built). `L_bol` wires to the pre-main-sequence contraction luminosity (`pre_main_sequence_luminosity_lsun`, built in the base arc) instead of a main-sequence instance. This is fully derived from the `n = 3/2` polytrope at the H-minus wall temperature (`L ~ t^(-2/3)`), reading the Hayashi attractor the #77 ruling already built (the same wall serves both directions of travel). The premise correction that produced it: the #77 ruling built the temperature ATTRACTOR (the wall), not the pre-main-sequence LUMINOSITY LAW; the wall was ruled and the law was needed, so the law was derived. `L_bol` precision matters here (unlike `tau_conv`, below), since in saturation `L_X = plateau * L_bol` and a pre-main-sequence star is brighter than its main-sequence instance.
- THE FORMATION-TEMPERATURE `L_bol` SITE (LANDED in slice 3b-ii; the finding is recorded as it was found, the resolution appended). The main-sequence-instance sweep found a THIRD site reading a main-sequence `L_bol`: `formation_midplane_temperature`'s irradiation term takes a mass-luminosity slope (`alpha`), and its live caller (the `--derived` globe render) fed the main-sequence slope `35/10`, so the formation-epoch midplane temperature, hence the condensation staircase the crust reads, was computed with a main-sequence brightness where the star is pre-main-sequence and brighter. The physics is the same defect class as the `tau_conv` and `L_bol` fixes (a pre-main-sequence star is brighter, its disk warmer, `t_formation` earlier). It was NOT dormant, correcting my own premise: `formation_midplane_temperature` has a live consumer on the VISIBLE derived-globe render, so the fix changes that byte surface. It does NOT move the two canonical pins, `run_world` (default and living) never reaching `formation_midplane_temperature`, verified. The lesson recorded: dormant-in-the-disk-chain is not dormant-in-the-engine, and the whole-engine caller check is the premise the sweep's third site required. RESOLUTION (traced against code 2026-07-19): the fix LANDED. The production caller `derive_formation_condensation_temperature` (`crates/viewer/src/main.rs:1763`) now passes `Some(pre_ms_l_bol)`, and inside `formation_midplane_temperature` the supplied-luminosity arm fully short-circuits the slope, so the globe renders on the pre-main-sequence luminosity. The owner hold DECOMPOSED to nothing rather than being released by fiat: the pre-main-sequence luminosity is `derive_pre_ms_bolometric_luminosity(star_mass, kappa_ref, abundances)`, which derives the Hayashi wall `T_eff` from the vendored BHAC15 grid (Baraffe et al. 2015, retiring the 4000 K wall interim) and the formation epoch from `derive_formation_epoch_myr` (retiring the reserved formation-epoch age), so BOTH reserved values that gated the hold are now derived and there is no owner scalar left to ratify. One hygiene remnant, HELD off my hands (the sequencer's viewer-file lane): the caller still passes `Fixed::from_ratio(35, 10)` as the now-DEAD `alpha` argument (never read on the supplied-luminosity path), an authored constant reading as live and positioned to be miscopied; its retirement (or a signature collapse that drops the dead parameter) is handed to the sequencer's pass on `viewer/main.rs`.
- THE SATURATION BLINDNESS (declared, with its consumer named). During the disk era stars are rotationally saturated: rotation against a long convective turnover puts `Ro` below the saturation knee, so the activity fraction sits on the saturated plateau and the disk arc is insensitive to `tau_conv`'s precision. The full Rossby machinery's precision matters where stars LEAVE saturation, gigayears in, which is the atmosphere-escape arc's domain, not this one's.

  FINDING (RIDER 1, surfaced to the gate, pending its ruling). The gate required this blindness be PROVEN by a margin exceeding the polynomial's error, not assumed, and the proof does NOT hold as originally justified. Two corrections fall out. First, disk-hosting stars are not fast rotators: star-disk coupling LOCKS them near `P_rot ~ 8` days, and with the main-sequence Wright polynomial `Ro = 8/tau_MS` sits ABOVE the knee for `M >~ 0.5 M_sun` (solar `Ro ~ 0.55`, four times above; `1.36 M_sun` `Ro ~ 0.9`). Second, and this is the resolution, the multiplicand is the same pre-main-sequence nature Move 1 already invoked for `L_bol`: a disk-hosting star is fully convective with a turnover roughly an ORDER OF MAGNITUDE longer than its main-sequence instance, so the true `Ro = 8/tau_preMS` sits BELOW the knee (saturated) after all. So the blindness describes true physics, but the gap that decides it is the ~1 dex main-sequence-versus-pre-main-sequence SYSTEMATIC, not the 0.028 dex fitting error, and the claim "a main-sequence-calibrated polynomial gives right answers" is FALSE: the main-sequence polynomial computes unsaturated where the star is saturated. The fix is a PRE-MAIN-SEQUENCE convective turnover time (the same pre-main-sequence substrate the contraction luminosity already stands on), which the expansion must carry; the disk arc IS sensitive to `tau_conv`, specifically to using the pre-main-sequence value, which is a finding in this arc rather than the escape arc.

  RESOLVED (gate-ruled, built `12e07c6`). The gate ruled the pre-main-sequence turnover lands, and `pre_main_sequence_convective_turnover_days` is built on the Hayashi substrate: global mixing-length theory gives `tau ~ C (R^2 M / L)^(1/3)`, and on the Hayashi track `L = 4 pi sigma T_H^4 R^2` cancels `R` to `C (M / (4 pi sigma T_H^4))^(1/3)`, Hayashi-specific (it does not misfire on a main-sequence star) with the mass dependence correctly INCREASING where the main-sequence polynomial decreased. The saturation assertion is mutation-proven (a 10x-shorter turnover breaks it) across the disk-locked rotation range (8 d with a factor-two margin and the slow 15 d end below the knee, over the mass range). The spin-down doc's consumer split is no longer flagged: the disk-era consumer reads the pre-main-sequence value and saturates by a several-fold margin. The only reserved value is `C` (the mixing-length coefficient, basis the MLT calibration anchorable to a pre-main-sequence model `tau ~ 250 to 400` d), and its precision does not decide the answer.

## Move two: the high-mass domain guard becomes a two-branch dispatch (the second branch DERIVED)

The convective-turnover domain guard's high-mass refusal (`convective_turnover_time_days` returning `None` above its fit range) is the dispatch seam. Two branches, keyed on the star's structural state:

- CONVECTIVE-ENVELOPE stars drive winds through the dynamo (X-rays), the branch built.
- RADIATIVE-ENVELOPE stars (Herbig Ae/Be and up) have no dynamo, which the guard correctly refuses today (now via the typed [`TurnoverRefusal::AboveFitDomain`] door), but they photoevaporate HARDER: a hot photosphere is intrinsically EUV-bright without a corona, so the ionizing luminosity is the spectrum's own high-energy tail, DERIVED from `T_eff` and `L` (both owned by the star module) with an atmosphere-model band. Same race, same gravitational radius (the EUV wind temperature is already a banded caller value), a different wind-rate source.

  RIDER 2 (gate ruling): the band width IS the finding. A real hot photosphere's EUV tail departs from a blackbody at that `T_eff` by ORDERS OF MAGNITUDE (line blanketing, NLTE, wind blanketing), and the departure is the whole quantity, not a correction. So if the atmosphere-model band spans orders of magnitude, THE BRANCH'S OUTPUT IS A BRACKET, not a value, and it must ship as one and say so (the way the `V*` gap ships a bracket tagged unconstrained-by-source). A `tau_disk` derived through it inherits that width, so the width is STATED before the consumer is built, because a bracket that reaches a race silently becomes a value.

  BUILT (dormant, byte-neutral). `radiative_euv_luminosity_bracket` returns an `EuvLuminosityBracket [lo, hi]` in `L_sun`, derived as `L_bol * f_BB(T_eff)` times the atmosphere-model departure band. The blackbody ionizing baseline `f_BB(T_eff)` (`blackbody_ionizing_fraction`) is the Wien-tail upper-incomplete integral above the 13.6 eV hydrogen edge, `(15/pi^4) exp(-x)(x^3+3x^2+6x+6)`, `x = T_ion/T_eff` with `T_ion = E_edge/k_B` a derived constant (~157821 K), computed in log space so the tiny tail stays representable, and oracle-tested against an external python computation (T_eff = 10000/20000/30000 K matched within two percent). Per RIDER 2 the output is the bracket and its width is machine-readable through `EuvLuminosityBracket::width_dex` (`log10(hi/lo)`) before any consumer reads the bounds. RESERVED-with-basis, unconstrained-by-source: the departure-band edges (the hot-star atmosphere-model ensemble spread relative to the LTE blackbody, orders of magnitude, to pin when a hot-star EUV model grid is fetched). The Wien-tail validity limit (exact for `x >~ 3`, so cooler than ~50000 K at the hydrogen edge) is now GUARDED, not merely documented (AUDIT-CORRECTED): the guard-symmetry sweep caught that the doc claimed "flag rather than extrapolate silently" while the code extrapolated silently, so `blackbody_ionizing_fraction` now REFUSES below `wien_x_min` (~3), the second edge of a once-one-ended domain, the full-Planck-denominator regime a separate door. The consumer wire into `gravitational_radius_au` and the dispersal race is the next slice.

REFINEMENT (gate-adopted): the dispatch key is the star's own STRUCTURAL STATE from the track (convective versus radiative envelope), not a mass cut, so the `1.4 M_sun` figure is demoted to the main-sequence instance of a structure-keyed line. That structural state is itself a derived quantity (fully convective on the pre-main-sequence, mass-dependent on the main sequence), a small derivation the branch needs, not a field that exists today. The convicting populations write themselves as predictions: Herbig stars clear disks faster, massive stars are diskless within a megayear (their main-sequence arrival overlaps their accretion), and planet occurrence collapses above a few solar masses. The observed disk-lifetime-versus-stellar-mass trend joins the hindcast rows (a flagged fetch target) and referees the branch the day it lands.

## Move three: conditioning fields and one named axis, priced not built

- `XrayWindFit` GAINS A METALLICITY-OF-SAMPLE FIELD AND ITS WIDENING (BUILT: `sample_metallicity` + `metallicity_domain` + `MetallicitySampleDomain` for the marker, `metallicity_rate_factor` + `RateFactorBracket` for the widening; dormant, byte-neutral). TWO ORTHOGONAL AXES, KEPT SEPARATE, and the separation is the load-bearing correction. The MODEL-STRUCTURE axis is which row the caller picks (Owen versus the Sellek 2024 thermochemistry revision, ~1 dex, BOTH at solar `Z`; Sellek is O-excitation-by-neutral-H physics, a solar-metallicity model that varied no metallicity, NOT a low-`Z` instance). The METALLICITY axis is one model evaluated across `Z`, the negative slope `Mdot ~ Z^s`, a SEPARATE ~1 dex that MULTIPLIES whatever base rate the row gives. `metallicity_rate_factor` returns `(Z/Z_sample)^s` as a `RateFactorBracket [lo, hi]`, a BAND because the slope is model-dependent, `s` in `-0.4` to `-0.8` dex/dex (Ercolano and Clarke 2010 `Z^-0.77`, Nakatani 2018 `Z^-0.6` with X-rays and `Z^-0.4` without), reserved-with-basis-and-cited. Sign FIRMLY NEGATIVE: a metal-poor draw runs a HIGHER rate, a metal-rich draw LOWER. DOMAIN (TWO-ENDED, AUDIT-CORRECTED): the slope holds between the ~0.03-solar FUV floor and the 2-solar Ercolano-Clarke fit ceiling; a draw outside EITHER edge REFUSES, the domain-stated discipline. The one-ended guard (floor only) was caught by the guard-symmetry sweep, the second instance of the half-guard class after the A-star turnover cut, and the standing rule is now two-ended-by-default. The vestigial `OnSample` exact-equality arm (unreachable by measure on a continuous draw) was removed; exactly-solar folds into `MetalRich` with a unit factor. CAUGHT INVERSION plus a caught WELD, both surfaced before any code shipped against them and gate-confirmed: the original line ("low-metallicity toward Sellek's edge") was sign-backwards AND welded the two axes (reading Sellek's low solar rate as a low-`Z` instance). Molecular and dust cooling scale WITH metallicity, so metal-rich runs the lower rate; the cross-check closes as a triangle (disc lifetime `t ~ Z^0.52`, tied by `t ~ Mdot^(-2/3)`, and `-0.77 * -2/3 ~ 0.51`), and both metallicity channels (the wind slope here and the formation-root thermal/opacity path) send metal-rich disks toward longer `tau_disk`, matching the observed trend. OPEN, not authored: a Sellek-generation slope across `Z` does not exist in the literature (Sellek ran only solar), plausibly steeper. Full fetch record: `docs/working/METALLICITY_PHOTOEVAP_FETCH.md` on seam4-deeptime, reaches this branch at the rebase.
- BINARITY (BUILT, dormant, byte-neutral; AUDIT-CORRECTED). Half of all stars; a companion truncates the disk tidally, shortening `tau_disk` through the same machinery untouched. `roche_lobe_radius_au` returns the Eggleton 1983 Roche-lobe radius of the host, `R_L/a = c_num q^(2/3) / (c_log q^(2/3) + ln(1 + q^(1/3)))`, `q = M_host/M_companion`; `tidally_capped_scale_radius_au` is `min(birth R_1, R_L)`, and since [`derive_viscous_time_myr`] gives `t_visc ~ sqrt(R_1)` the capped disk runs a shorter viscous time and `tau_disk` through the built path, no new machinery. Eggleton-oracle-tested (`q = 1/2/0.5` at 20 AU give 7.58/8.80/6.42 AU). MODALITY (the audit's headline, self-caught and pre-panel): `R_L` is the HARD UPPER EDGE on the disk radius, NOT the expected truncation radius. A circumstellar disk truncates INSIDE its Roche lobe at `R_t = f R_L` (Paczynski 1977, Papaloizou-Pringle 1977, Artymowicz-Lubow 1994), the resonance-truncation fraction `f` (~0.3 to 0.9) a FETCH TARGET conditioned on `(q, e, viscosity)`, a banded class row not an owner scalar (so ZERO new free values survives, the fraction fetched not authored). The interim ships `R_L` as the declared upper edge of a one-sided bracket: the cap is the CONSERVATIVE (least-truncating, disk-too-large, `tau_disk`-biased-long) bound, and `t_visc`/`tau_disk` inherit the roughly `sqrt(f)` band (a 10 to 20 percent class effect) when the fetch lands. The original name and doc called `R_L` "the derived truncation radius," a BOUND-AS-ESTIMATE defect (a hard limit circulating as a central value, sibling of modality laundering, first convicted in a Slack docstring, now caught by its author in fresh code); the name and modality are corrected. `c_num` (~0.49) / `c_log` (~0.6) are the fixed Roche-geometry fit (cited, universal, not an owner tunable). A layer-4 axis, entering as an upper-bound-tagged derived cap.

## The domain declaration (the "disk" vocabulary fence)

This arc governs the NATAL GAS DISK, birth to `tau_disk`. White-dwarf debris disks, Be-star decretion disks, and post-AGB circumbinary material are SECOND-GENERATION objects fed by the small-body reservoir and the tidal machinery, other arcs' physics. The word "disk" carries three mechanisms, and this project has a defect class named for exactly that (the same shape as the "wind" trap the base arc already fenced: photoevaporative winds disperse, MHD winds transport), so the arc doc earns this one-line lexical fence keeping them apart.

## Build order at the gate

The spine stands. The gate ruled the TYPED REFUSAL lands FIRST because it keys everything after it, and it is BUILT (`convective_turnover_time_days` now returns `Result<Fixed, TurnoverRefusal>`, the radiative dispatch keying on `AboveFitDomain` alone), so "every `None` is a door" holds by construction before the branch exists. When the expansion opens (after slice 3), the remaining order is: the structure-keyed dispatch state (it keys both the wind-branch dispatch and the `L_bol` track selection), then the radiative-envelope wind branch with its atmosphere-model band SHIPPED AS A BRACKET (RIDER 2), then the conditioning fields, then the binarity axis when priced. The PRE-MAIN-SEQUENCE convective turnover time (the RIDER 1 finding's fix) is now BUILT (`12e07c6`); the mass-keyed gyrochrone that lands the `P_ref` draw remains a sibling follow-on on the same pre-main-sequence substrate the whole chain reads. The formation-temperature `L_bol` third site has LANDED in slice 3b-ii (the visible-surface change through `viewer/main.rs:1763`, recorded under Move one): its owner hold decomposed because both gating reserved values, the Hayashi wall `T_eff` and the formation-epoch age, are now derived. Every piece is confirmed against code by the base-arc pass; none is a rewrite.

## The arc goes live: ratifications and the wiring sequence

The researcher ratified all four surfaced items and cleared the wrong-Ro trace on a test, so the first derived clock wires behind a flag. Recorded here as the design of record, riders included.

RATIFIED SOURCES (each entering with its riders, phi-star honored):
- The truncation fraction `f`: Manara et al. 2019 Table C.1 (all thirty rows re-verified against the primary PDF, in `docs/working/DISK_ARC_FETCH_VALUES.md`). RIDERS: `f` carries the source's viscosity assumption as a CONDITIONING FIELD, the residual Reynolds-number dependence its BAND rather than a silent inheritance; and it reads the DRAWN eccentricity from the binarity axis, never defaulted circular, since eccentricity is what moves truncation inside the lobe. Binarity's cap tightens from the `R_L` conservative edge to the `f * R_L` band the moment a consumer exists.
- The Kraft break: a BAND, not a point. The primary-read 6200 K classic edge and the search-grade ~6550 +/- 200 K modern edge (upgraded to full citizenship on its own primary read); below 6200 K the convective branch is certain, above the modern edge the radiative branch is certain, and draws inside the band take the near-degenerate dispatch per the Gap Law, carried not asserted. RIDERS: the break is the solar-metallicity main-sequence instance of the convective-envelope structural boundary, so it shifts with `Z` (a flagged field, fetch optional), and it DEMOTES to a hindcast row for the structural criterion once the track exposes envelope structure directly.
- The pre-main-sequence `tau_conv` anchor for `C`: Landin et al. 2010 (Table 1, `tau_g ~ 767 d` peak, primary-read). RIDER: the value is THEORY-GRADE (model-derived, not measured), carrying the source's mixing-length `alpha` as a conditioning field, sufficient here only because the saturation blindness is declared with its consumer named.
- The third-site pre-main-sequence `L_bol` fix: approved, with the ATOMICITY condition. The pre-main-sequence luminosity has three consumers (the formation epoch, the race wind-epoch band, the expansion `L_bol` wire); the switch lands atomically across every consumer that is ready, any unwired one listed pending with the same-fact-two-doors-in-time hazard named.

THE WRONG-RO TRACE: CLEARED BY MACHINERY (`26585e8`). The saturation-margin assertion, evaluated with the pre-main-sequence `tau_conv`, asserts disk-era Ro sits below the knee by more than the turnover's error band across the disk-era mass and rotation range; green means the coordinator's slice-3 gate is a CI fact rather than a clearance in anyone's head.

THE WIRING SEQUENCE (behind a flag, presented for audit before the flag flips, the granted eye condition): (1) compose the L_X activity chain (`tau_conv -> Ro -> activity_fraction * L_bol -> log10 L_X`, dormant); (2) wire `tau_disk` (the race output) into the #73 giant gate and the DiskGas opening behind the flag; (3) the third-site `L_bol` atomic switch across its three consumers; (4) the Kraft band and the ratified conditioning fields. Each a gated slice, panel-audited, presented before the flag flips. `Mdot_0` and `R_1` run on their solar interims until the disk-size-demographics draw lands.

### Tightening: the reorder, the oracle redesign, and the pin-by-diff rule

The researcher's tightening on the wiring sequence, ratified and recorded before the code lands, so the design of record precedes the design.

THE REORDER, 1 -> 3 -> 2. The original slice order (compose the clock, flip the flag, then land the third-site `L_bol` switch) creates the exact temporal hazard the atomicity ruling exists to prevent. Walk the state between the old slice 2 and slice 3: `tau_disk` is live on the run path deciding giant verdicts through the pre-main-sequence luminosity, while `formation_midplane_temperature` is live on the same run path deciding formation epochs through the main-sequence luminosity. One star, two brightnesses, both load-bearing at once, which is same-fact-two-doors in its temporal form, the precise hazard the atomic-landing condition names, and the third-site fix is already signed so nothing blocks doing it in the right order. The clean sequence: the Kraft band row with its ratified conditioning fields lands with or before the composed clock (the clock dispatches on it); the atomic `L_bol` switch lands across its ready consumers next, dormant-to-render-visible per its approval; and the consumer wire with its audited flag flip goes last, onto a codebase that already holds one truth about every star's brightness. So the build order is composed clock (with the Kraft dispatch it needs), then the atomic `L_bol` switch, then the flagged consumer wire.

THE ORACLE REDESIGN (the replacement-circularity rule, applied to CI asserts). This arc RETIRES the observed disk lifetime, and the standing ruling is that it never calibrates against the lifetime it retires: the observed disk-lifetime distribution validates the ENSEMBLE output later, never an input, never a median. A CI test asserting "a solar-analogue star gives `tau_disk ~ a few Myr`" is the Haisch-Lada range wearing a test's clothes, the calibration encoded where it can never be argued with, so an honest miss becomes unshippable and every upstream input feels quiet pressure toward the expected answer. That is the I_Fitting lens firing on a test constant, the class this project convicted twice in tolerances. The composed clock's tests assert instead four things that protect more and claim nothing the retiree owns: mechanism asserts (the race crosses, the result is positive and finite, `tau_disk` is monotone in the wind rate at fixed clock, each falsifiable mechanics); a units-catastrophe bracket orders of magnitude wider than any observational claim, which still catches a kilobar-class slip cold; a determinism pin of the computed solar-analogue value once it exists and is twin-checked, asserting the code keeps producing what it produced rather than that nature agrees; and the few-Myr comparison relocated to the ensemble validator where the ruling homed it, band-aware, out of CI's reach. The derived solar-analogue value on the tagged interims (the disk-locked `P_rot`, the hundredfold-over-wind peak accretion, the unit `t_visc`) comes out near 20.5 Myr, which is a Residual finding surfaced (not a red X to make green): it is not the real Sun's disk lifetime, it runs on placeholder draws, and it moves deliberately when the `Omega_star_0`, `Mdot_0`, and `R_1` draws land. It is inside the units bracket and it is pinned, so the code is reproducible while nature is left unasserted.

THE PIN-BY-DIFF RULE (the wire's re-pin is an empirical claim, not a label). The consumer wire is sim-side and touches the giant verdict, so the two canonical state-hash pins are EXPECTED to move when the flag flips; but expected and verified parted company publicly three days ago, in the other direction, so expected is not verified. The wire lands behind the flag, the state hash is diffed with the flag ON in a scratch run, and the re-pin event with Nathan's signature on the verified fact is scheduled, so neither arc ever carries a pin claim someone asserted rather than measured. The composed clock (slice 1) and the atomic `L_bol` switch that render-visibly precede it are checked the same way: slice 1 is dormant and MUST hold both pins byte-for-byte, the `L_bol` switch moves only the visible derived-globe render and MUST hold both canonical `run_world` pins, each verified by diff rather than assumed.

SLICE 3a BUILT (the Kraft band dispatch, dormant, byte-neutral, both pins hold `6b85d08` onward). `KraftBreakBand` carries the band edges as reserved-with-basis data (the classic `classic_edge_k` lower edge, the modern `modern_center_k` plus or minus `modern_halfwidth_k` upper reach) and a `metallicity_shift_k_per_dex` conditioning field, and `kraft_band_dispatch` returns a `KraftVerdict` of Convective (below the band, the X-ray dynamo branch certain), Radiative (above, the EUV branch certain), or NearDegenerate (inside, the Gap Law: a consumer must carry both branches as a bracket, never assert a side). The boundaries themselves fall inside the band, so a star exactly at a measured break reads NearDegenerate, which is precisely where certainty fails. The point `stellar_envelope_structure` is kept as the certain-cut classifier and the structural-criterion demotion target, its doc now pointing to the band successor. Edge values are caller-supplied fixtures in tests (6200, 6550, 200), NOT a loaded data column: the anchors await Nathan's graduation ratification (the coordinator's standing note) before any load, and the Z-shift sign is unauthored (the field defaults to zero, its probe coefficient in the test asserts only that a nonzero value moves the edges). The truncation-fraction `f` conditioning fields (viscosity plus the Reynolds band, and the drawn eccentricity) remain UNBUILT until `f` gains a consumer, since binarity still ships the `R_L` conservative upper edge and adding fields to an unconsumed value would be speculative; the Landin `alpha` stays a reserved-with-basis provenance note on `C`, not a loaded field. The dispatch is what the consumer wire (slice 2) keys on: a certainly-convective disk-hosting star takes the composed X-ray clock, while a radiative or near-degenerate star is a declared door until the EUV branch's own wind rate and race are built (the scope's Move-two next slice), so the wire stays honest rather than routing a Herbig star through a T-Tauri clock.

SLICE 3b SPLIT INTO THE MECHANISM (BUILT) AND THE RENDER-VISIBLE FLIP (HELD). The third-site `L_bol` fix is the atomic switch that gives every consumer ONE truth about a star's brightness (the pre-main-sequence luminosity), which the reorder puts before the consumer wire so `tau_disk` (on the pre-MS `L_bol`) and the formation midplane cannot go live on two different brightnesses at once. The mechanism the flip needs is byte-neutral and is BUILT (`3b-i`): `stellar_flux` now delegates to `stellar_flux_from_luminosity_lsun`, a flux from a directly-supplied bolometric luminosity, because the mass-luminosity power law `mass^exponent` is exactly one at the Mirror mass and cannot carry a pre-main-sequence star's several-fold brightness there; and `formation_midplane_temperature` gains a `bolometric_luminosity_lsun: Option<Fixed>` door that drives the irradiation term directly when supplied and falls back to the power law byte-for-byte when `None`. Every live caller passes `None`, so the delegation moved no bit and both canonical pins hold (verified by diff, and the run-path `stellar_flux` world insolation is unchanged). What is HELD for the owner's eye is the FLIP itself (`3b-ii`): the viewer's `derive_formation_condensation_temperature` passes `Some(pre_main_sequence_luminosity_lsun(star_mass, hayashi_wall_T_eff, formation_age))`, a RENDER-VISIBLE change (the warmer formation midplane shifts the condensation staircase on the --derived globe) that also introduces two inputs to surface with basis before it lands: the Hayashi wall `T_eff` (the #77 attractor, read not authored) and a reserved FORMATION-EPOCH AGE (the condensation epoch, `L ~ t^(-2/3)`, its basis the age at which the crust condenses against the disk). The dormant sibling consumers (the race wind-epoch band in the composed clock, the expansion `L_bol` wire) already read `pre_main_sequence_luminosity_lsun`, so the flip completes the atomicity rather than opening a split: after it, the one live consumer and the two dormant ones read the same brightness.

THE 3b-ii RENDER-DELTA, MEASURED (the owner ruled HOLD FOR AUDIT). Before flipping, the delta was measured with a throwaway probe at illustrative reserved values (formation age 1 Myr, Hayashi wall 4000 K, the pre-MS oracle's own fixtures), the pre-MS solar star coming out at 1.67 `L_sun`. The finding: the formation midplane is VISCOUS-DOMINATED in the inner disk (the reserved formation accretion rate carries it), so the pre-MS irradiation warming is small and grows outward, +0.37 K at 1 AU (1398.30 -> 1398.67), +0.75 K at 2 AU, +1.9 K at 5 AU, +3.8 K at 10 AU, with the inner disk (below ~0.5 AU) saturated at the bracket. After the viewer's 100 K condensation-grid snap, NONE of these deltas cross a grid node at the sampled orbits, so at a ~1 Myr formation epoch the flip is RENDER-IDENTICAL on the globe (the Mirror world at 1 AU snaps to 1400 either way). Two levers grow the delta: a YOUNGER epoch (the pre-MS brightness rises as `t^(-2/3)`) and a WIDER orbit (irradiation overtakes the falling viscous term), so a young enough epoch could cross a grid node near a boundary. The coupling to flag: the reserved formation accretion rate (0.19 `M_sun`/Myr, calibrated to land 1 AU at ~1400 K under `L_sun` irradiation) is essentially undisturbed, since the inner front moves +0.37 K. The owner holds the flip pending his ratification of the FORMATION-EPOCH AGE (the reserved value that controls the whole delta) and confirmation of the Hayashi wall `T_eff`; neither is fabricated here, and the measurement is preserved so the audit reasons from the real numbers. SLICE 2 (the flag-flipping wire) is held with it: landing the wire before 3b-ii would put `tau_disk` (on the pre-MS `L_bol`) live while the formation site still reads the MS `L_bol`, the two-brightness state the reorder exists to prevent. LANDED (2026-07-19): the flip is live and the hold decomposed, not by ratifying the two reserved values but by RETIRING both to derivation. The illustrative 1 Myr / 4000 K probe values above were superseded: `derive_pre_ms_bolometric_luminosity` now derives the formation epoch from `derive_formation_epoch_myr` and the Hayashi wall `T_eff` from the BHAC15 grid (the solar wall the corrected 4397 K, not 4000 K), so the pre-MS luminosity carries no owner scalar. The globe therefore renders on the pre-MS `L_bol` at every star, and the reorder's ordering held: the render-visible switch preceded the still-held consumer wire.

### The 3b audit response: the age dissolves into a root, three riders, and a non-circularity seam

The audit dissolved half the ask: the formation-epoch age is not a reserved value the owner picks, because it is the DERIVED ROOT of `T_mid(1 AU, t) = T_condensation`, and the bisection that solves it, `derive_formation_epoch_myr`, is already built and tested (the slice-2 dossier, `astro.rs:1636`). Input-audit confirmed against code before trusting the claim: the function exists, its leverage is the accretion-rate DECLINE with age (the strong term), not the weak irradiation term, so the root is well-conditioned. So the ask "what formation-epoch age" is answered by wiring the root, and 3b closes with one fewer reserved value than it opened with.

THE NON-CIRCULARITY SEAM (surfaced, an input-audit catch on the consistency check). The prediction is that the derived `t_formation` lands near the retired 1 Myr interim, so `Mdot(t_formation)` lands near the retired 0.19 formation rate, demoting the 0.19 from a landmark to a consistency check. But that check is REAL only if the clock's `Mdot_0` and `t_visc` come from INDEPENDENT bases (the pending layer-4 draws, or independently-motivated solar interims). Fitting `Mdot_0` and `t_visc` to reproduce 0.19 (for instance `Mdot_0 = 1`, `t_visc = 0.5` gives `Mdot(1 Myr) = 0.192`) makes the agreement true by construction, the replacement-circularity anti-pattern this arc convicts elsewhere, so it must NOT be done. The wire therefore rests on the clock's two draw-pending interims having independent bases, and the consistency check is meaningful only then; used with independently-motivated interims it reports whatever `t_formation` and `Mdot(t_formation)` fall out, a Residual to surface rather than a target to hit.

THE WIRE, ASSEMBLED AND RENDER-ROBUST. The viewer's `derive_formation_condensation_temperature` composes the already-built pieces: a `midplane_temp_at_rate` closure over `formation_midplane_temperature` at 1 AU, `derive_formation_epoch_myr` to the root `t_formation`, then `pre_main_sequence_luminosity_lsun(star_mass, hayashi_wall_T_eff, t_formation)` fed as the `Some` override (slice 3b-i's door) to the displayed midplane. It pulls the full LBP clock into the render path (`Mdot_0`, `t_visc` via `R_1`, `gamma`, plus the `alpha`, `mu`, disk `T` that `derive_viscous_time_myr` reads), all draw-pending or reserved-with-basis. The RENDER is robust: the measured flip is neutral through the 100 K JANAF grid for any `t_formation` in ~0.3 to 3 Myr, and that band holds across a wide range of interims, so the globe is byte-identical regardless of the exact interim, and the canonical `run_world` pins (which never reach `formation_midplane_temperature`) are untouched. The wire is READY; landing it live is the next focused build, deliberate rather than at a session's tail, because it introduces the clock into the render path and rests on the interim bases above.

THE HAYASHI-WALL RIDERS (ratified conditionally, the cited class row). The ~4000 K pre-main-sequence Hayashi wall enters on three riders. FIRST, the citation is named and the digit goes source-verbatim: the wall temperature is a pre-main-sequence evolutionary-track datum (Hayashi 1961 for the track; a modern pre-main-sequence grid, Baraffe et al. 2015 or Siess et al. 2000 class, for the solar-mass ~4000 K digit), and the exact digit with its anchor is a FETCH TARGET (a primary read, the vendoring protocol), not fabricated inline. SECOND, the composition-conditioning rider: this is the SOLAR-COMPOSITION instance, and the H-minus wall shifts with metallicity, metal-poor pre-main-sequence stars running hotter and bluer, the convicting population for anyone reading 4000 K as universal, so the row carries a metallicity field. THIRD, the destiny rider: per the #77 ruling the anchor demotes to the engine's own opacity-derived wall once that derivation lands.

THE 100 K CONDENSATION-GRID PROVENANCE (audited, an adjacent item, does not reopen 3b-ii). The grid is the JANAF tables' NATIVE tabulation sampling (verified against `crates/physics/data/janaf`: rows at 0, 100, 200, 298.15, 300, then 100 K steps), a DATA CEILING, not an authored coarseness. The equilibrium-condensation minimizer converges only at exactly-tabulated rows (`delta_f_g_at` returns `None` off-grid), so `snap_to_condensation_grid` evaluates at the nearest tabulated node, its own doc already naming this as a data ceiling that retires when the minimizer is hardened to interpolate between rows (a #54-class follow-on). The standard treatment is interpolation, which is DERIVED (no authored coarseness) and also converts the younger-epoch cliff hazard into a smooth gradient, retiring it rather than monitoring it. The render-identical conclusion survives either way (sub-4 K on 1400 is invisible at any honest resolution).

THE RENDER-IDENTICAL CLAIM IS CONDITIONED (the I_Terran rider). Render-identical is a (SOLAR MASS, ~1 Myr) statement, not a universal one. M-dwarf disks are irradiation-dominated much further in, and their pre-main-sequence-to-main-sequence brightness ratios are larger for longer, so their flips WILL be visible on the globe, and that visibility is the fix working on the population it matters for, not a surprise to absorb later. This conditioning rides the flip's notes so a neutral result on the solar Mirror is never read as a neutral result on the catalog.

### The non-circularity gate, BUILT (the trap made unconstructible in code)

The audit upgraded the seam into a mechanism, and it is now built (dormant, byte-neutral, both pins hold). `InterimBasis` grades a draw-pending interim as `DrawGrade` (a layer-4 draw), `CitedToPopulation` (a documented population basis, a birth-accretion band or a disk-size demographic), or `ChosenWithoutBasis`; only the first two `is_independent`. `ProvenancedInterim` pairs a value with its basis so an input carries its own provenance. `formation_rate_consistency` is the PROVENANCE-GATED check: it compares the derived epoch's `Mdot(t_formation)` against the retired 0.19 landmark and returns a `FormationRateConsistency` verdict, but REFUSES (`None`) unless every interim `is_independent`. So the exploit I constructed is unconstructible: `Mdot_0 = 1`, `t_visc = 0.5` lands `Mdot(1 Myr) = 3^(-3/2) = 0.192` exactly, but tagged `ChosenWithoutBasis` the check refuses to run, so a fitted agreement cannot be laundered as a passed check. The branch the ruling named holds: if `Mdot_0` and the `R_1` that sets `t_visc` carry documented population bases (the class-0/I accretion band, the disk-size demographics), they are `CitedToPopulation` and the check runs meaningfully BEFORE the draws land; chosen-without-basis interims never qualify. Tests: the fitted exploit refuses even though its value fits (and one non-independent interim is enough to refuse); independent interims report `Consistent` near the landmark and `Inconsistent` far from it (a Residual, surfaced not tuned); the basis grade and the fail-loud guards. This is the mechanism the wire's consistency check consumes, so the render-visible wire (sub-build B) rests on a gate that cannot be gamed rather than on a discipline anyone has to remember.
