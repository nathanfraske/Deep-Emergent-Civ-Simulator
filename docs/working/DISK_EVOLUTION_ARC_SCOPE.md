# Disk-evolution arc scope: the Mdot(t) clock and the wind-versus-accretion dispersal race

Status: design posted, build authorized, premise-check done, awaiting the reserved-value sign-off before slice-1 code. This is the separate authorized arc the owner named PIPELINE-STAGE DEBT WITH MULTIPLE CREDITORS, not a DiskGas feature. It is the mechanism the dispersal race needs, the gas-era feedback needs, and the one that turns `tau_disk` from a CONSULTED CLOCK into a DERIVED OUTPUT. The minimal DiskGas ledger (the account, the envelope-drain edge, both conservation gates) landed first and against a static account; this arc makes that account live.

## Check-outranks-order clearance (done before building)

The standing rule (the check outranks the order, no matter whose order it is) requires checking the project's prior rulings for a superseding form and the codebase for an existing one before building an ordered mechanism. Both checked. No prior ruling in the design records or the audit log supersedes the time-evolving disk (the disk-evolution physics is engine implementation, not spelled out in the design parts, and gate-G retired the free `DiskProfile` onto the static viscous-similarity form without ordering its time evolution). No `Mdot(t)` or `tau_disk` implementation exists in the codebase. The one adjacent thing is `giants.rs`'s reserved `disk_gas_lifetime_myr` (~1 to 10 Myr, the class-II median the Kelvin-Helmholtz time races against): that is the CONSUMER this arc's derived `tau_disk` replaces, not a duplicate of it. The order stands, and the arc is new work.

## The carried premise-check (done first, per the coordinator's instruction)

The instruction was to verify how the formation-era clock slice landed before trusting its outputs: if it consulted a clock that does not tick, its outputs are suspect, and that is a finding rather than a fix. Two formation-era slices read the disk; they land differently.

### Slice one, the 26Al young-thermal clock (R-YOUNG-TEMPERATURE): CLEAN

`young_thermal_verdict` reads `formation_time_myr` as the time from CAI to accretion, the short-lived-radionuclide decay clock. The run path (`crates/viewer/src/main.rs`) wires it from the reserved constant `FORMATION_TIME_MYR` (1 Myr, with a swept 0.5 to 4 Myr band), tagged `ThermalProvenance::Interim`. Its stated basis is the oligarchic ISOLATION-MASS growth time (Kokubo and Ida 1998; Lambrechts and Johansen 2012), which is a SNAPSHOT quantity: given the current solid surface density, how fast an embryo grows to isolation mass. That is readable from the static profile and does not require the gas disk to evolve. The module reads no `Mdot`, no gas lifetime, no dispersal time (verified: `young_thermal.rs` carries none of those terms). The reset epoch reads a seeded impact draw, not a disk clock. So the young-thermal outputs (the default world derives MELTED, the young temperature pinned at the magma-ocean handoff) are trustworthy under the static profile. No finding.

### Slice two, the formation-era condensation temperature (the SEAM-3 epoch join-law): THE FINDING

The crust condenses against the formation-era midplane temperature, and the finished planet's surface reads the mature irradiation equilibrium. These are two epochs, and the code carries two reserved accretion rates to separate them: `FORMATION_ACCRETION_RATE_MSUN_MYR` (0.19 solar masses per Myr, feeding `astro::formation_midplane_temperature` at `main.rs:1494`, pinned so the derived 1 AU midplane reconstructs the ~1400 K forsterite-enstatite condensation front) and `MATURE_ACCRETION_RATE_MSUN_MYR` (0.01, feeding the surface warmth at `main.rs:2592`, the observed class-II value). The formation rate is documented in the code as "the DECLINING-DISK accretion rate at the epoch the 1 AU midplane sits at that front."

That is a time-evolving-disk premise landed on a static profile. The two rates are two snapshots of one declining Mdot(t), a 19-to-1 ratio across two epochs, but they sit in the code as two independent reserved scalars with no clock tying them. Each is a reserved-with-basis value calibrated to its own landmark, so the OUTPUTS are honest as a snapshot pair, not wrong. The PREMISE is the suspect part: a formation epoch hotter than a mature epoch by a fixed Mdot ratio presupposes a decline curve that no clock produces. This is a clock that does not tick.

The finding is therefore a constraint the disk arc must satisfy, not an error to patch now. When the Mdot(t) clock lands, `FORMATION_ACCRETION_RATE` and `MATURE_ACCRETION_RATE` must RETIRE onto that one clock, evaluated at t_formation and t_mature respectively, rather than remain two free scalars. Otherwise the two rates plus the clock's own decline law over-parameterize the disk (the redundant-parameter class the owner already named, k-and-kappa's sibling): the decline law and the two epoch times already determine both rates, so carrying both as independent inputs is one degree of freedom too many. The formation-era temperature is the clock's FIRST CONSUMER, and reproducing both landmarks (0.19 at t_formation and 0.01 at t_mature) from a single Mdot_0 and a single viscous time is a gate this arc must pass before it can claim to have absorbed them. Until it does, the two independent rates are the honest interim, each hitting its landmark.

## The mechanism to build

### The Mdot(t) clock (the viscous-similarity decline)

The substrate is in place: `astro::viscous_similarity_surface_density` already takes `accretion_rate_msun_myr` as a parameter and scales the whole surface density linearly with it. The arc adds the time dependence the parameter has lacked: the Lynden-Bell-Pringle self-similar decline (Hartmann et al. 1998), `Mdot(t) = Mdot_0 * (1 + t / t_visc) ^ (-(5/2 - gamma) / (2 - gamma))`, with `gamma ~ 1` the same viscous-spreading exponent gate-G already retired the free profile onto. At `t = 0` the rate is `Mdot_0` (the hot accreting formation epoch); as `t` grows it declines toward the class-II mature value. This is arithmetic in the log domain over quantities the disk code already forms, so it is deterministic and adds no floating path.

### The wind-versus-accretion dispersal race (tau_disk as a derived output)

The disk does not fade smoothly to zero; it clears in a two-timescale race (Clarke, Gendrin and Sotomayor 2001; Alexander, Clarke and Pringle 2006, the UV-switch). While the viscous accretion rate exceeds the integrated photoevaporative wind loss, the disk drains through the star. When the declining accretion rate falls to the wind rate at the gap radius, the wind opens a gap, starves the inner disk, and clears it on the much shorter local viscous time. The disk lifetime `tau_disk` is the time this race tips, a DERIVED output of `Mdot_0`, `t_visc`, and the wind mass-loss rate, never a consulted constant. This is the piece that lets a giant's Kelvin-Helmholtz contraction (the #73 gate) race a REAL gas clock instead of a reserved lifetime, and it is what turns the DiskGas ledger's static account into a live one that feeds and disperses.

## The value line, re-ruled: ZERO owner-set scalars, each of the four already homed in the engine

The coordinator re-ruled the value line and it dissolves the sign-off request: none of the four surfaced numbers is the owner's to set, because each already has a home in the engine invisible from the out-of-loop view. The instinct to refuse authorship was right; the engine refused it four times over. Each source is premise-checked for BUILD STATUS below (the standing rule: a claimed machinery is verified against the repo, not taken on report), and two of the four are built while two are not yet, which conditions the slice order.

- `Mdot_0` is not a constant awaiting a signature: it is the audit-30 layer-4 addition, the per-disk accretion scalar priced into the contingency vector, DRAWN per system with the solar instance pinned for the Mirror. The draw carries a CONDITIONING LINE with a convicting population: accretion rate scales steeply with stellar mass in the measured relation, so `Mdot_0` draws conditioned on `M_star` from the population distribution (Manara-class accretion surveys as the fetch target), or every M dwarf accretes like a young Sun, which the sky contradicts. BUILD STATUS (premise-checked): the layer-4 contingency-draw machinery is built (`crates/materials/src/verdict.rs`, `SeededDraw` into a named contingency slot), but the accretion draw itself is NOT wired: `accretion_rate_msun_myr` is a constant field, never drawn, and neither the design doc nor the audit records the audit-30 accretion slot (it lives in the ruling channel). So it is DESIGN-ONLY, and the interim is the solar pin (0.19 formation, 0.01 mature) loudly tagged, with the destination the `M_star`-conditioned draw, never a global. Slice 1 builds the clock parametric over `Mdot_0`, with this interim source behind the signature.
- `t_visc` is not a second free scalar: it is what the Lynden-Bell-Pringle family itself defines, the viscous time at the scale radius, `R_1^2 / (3 * nu(R_1))`, which derives from the banked alpha closure and the disk's own thermal structure. BUILD STATUS: the dependencies are built (`alpha_viscosity` on the disk, the disk temperature through `disk_effective_temperature`, and the scale radius `r_c`), so `t_visc` derives now, not a new input.
- The photoevaporative wind mass-loss rate wires to the star's high-energy output: the Owen-class relation is a scaling on the star's own X-ray luminosity, so the rate is a wire through a measured-class coefficient (Owen, Ercolano and Clarke 2011), not a reserved number. BUILD STATUS (premise-checked across code AND the research docs, correcting a first-pass overreach): the source is DESIGN-COMPLETE but CODE-UNBUILT, the same status as `Mdot_0`, not the absent source a code-only search first suggested. The DESIGN is banked in full: `docs/working/PLANETARY_STELLAR_PHYSICS_DERIVE_MAP.md` derives `L_X` and `L_EUV(M_star, age)` from the star's magnetic activity through its Rossby number and spin-down (the saturated regime near `L_X / L_bol ~ 1e-3` for young fast rotators, then decaying as roughly `t^-1.5` by gyrochronology, with only the saturation ratio and the decay exponent as empirical coefficients), and the input that derivation needs, `Omega_star_0`, is already a named layer-4 contingency-vector axis for young systems (`docs/PROVENANCE_LEDGER.md`), erased for old ones by the same gyrochronology. The CODE is not there yet: the star module (`crates/sim/src/stellar.rs`, `stellar_evolution.rs`) computes bolometric luminosity, radius, and effective temperature only, no activity output. So the ruling's "already running" overstates the code status, but its deeper claim holds: the wind rate is homed in the engine's design, not a reserved number, and it reuses the same `L_X(M_star, age)` the atmospheric-escape arc already designs (a shared source, a second consumer). This is a SLICE-2 prerequisite (the dispersal race needs the wind rate; slice 1 does not): the `L_X` derivation is built first, or the wind rate takes the same interim-plus-destination treatment as `Mdot_0` until it lands.
- The gap radius (where the wind first wins) derives from `M_star` and the local sound speed as the gravitational radius, with the wind-physics prefactor carried as the banded class constant it is. BUILD STATUS: the dependencies are built (`M_star`, the disk temperature giving the sound speed), so it derives; a slice-2 quantity alongside the wind rate.

The dispersal band (the alpha-viscous-versus-wind-driven regime) the giant arc already flagged is the closure condition on which branch dominates, and it conditions Mdot_0's meaning. It is now the declared model-structure band below.

## Two standing rulings that condition this arc (excerpted into the scope per the commissioning fix)

These live in the ruling channel and were invisible to the check-outranks-order clearance, which is why they are excerpted here: an agent that hears only through PRs gets its conditioning rulings through PRs.

FIRST, THE MODEL-STRUCTURE BAND (from the gate-G self-audit). Alpha-viscous similarity is the IMPLEMENTED instance; MHD wind-driven transport is the live RIVAL, with a different `Sigma(r, t)` shape and a different Mdot decline. The arc ships with that dispute band DECLARED, the same treatment as the type II divide, so the implemented decline is named as one instance of a contested family rather than the settled law. THE VOCABULARY TRAP, flagged because "wind" now does two jobs in one arc: PHOTOEVAPORATIVE winds DISPERSE the disk (the dispersal race), and MHD winds are the rival TRANSPORT mechanism in the model-structure band. Different physics, and a doc that conflates them misleads every future reader; the two are kept lexically distinct here and must stay so.

SECOND, DISPERSAL IS ENVIRONMENT-CONDITIONED (the DiskGas edge ruling). The race as designed carries INTERNAL photoevaporation only. EXTERNAL irradiation from the birth environment, the layer-4 axis the 26Al draw already conditions on, truncates disks in dense clusters (Orion's proplyds the convicting population). Interim treatment per the sixth rule (admit the alien, do not author around the gap): a TERMS-DROPPED line declares the external term OMITTED with its validity domain (isolated star-forming environments), and the environment hook is NAMED for the follow-on. The prediction is already banked: dense-cluster draws get shorter gas eras and fewer giants, and this arc is where that cashes.

## The validation set, corrected by owner directive: the formation RATE is out, a derived root is in

The earlier framing treated the 0.19 and 0.01 as two rate chords the hindcast would validate against. The owner directive pulls the FORMATION rate from the validation set before it referees anything, and the reason goes deeper than the product-degeneracy blindness noted earlier: the 0.19 was NEVER A RATE, it is a PARTITION SHARE. Its own basis fixes only the PRODUCT of accretion rate, dust column, and opacity (the 1400 K landmark, with the owner re-partitioning), so a hindcast on `Mdot(t_formation) = 0.19` can pass on a compensating dust error and fail on a correct `Mdot_0`. A landmark satisfiable by a wrong answer and violated by a right one is not a referee, so it comes out.

ITS REPLACEMENT IS THE CONSTRAINT IT ALWAYS PHYSICALLY WAS. The basis line "the epoch the 1 AU midplane sits at that front" is an implicit equation, `T_mid(1 AU, t) = T_condensation`, and `t_formation` is its ROOT, solved by root-finding on `Mdot(t)` through the disk's own thermal structure, using the very condensation-temperature module that consumed the 0.19. Zero new values. And it can now convict where it could not before: the degeneracy existed because three product factors were free, but the engine has since DERIVED two of them (the dust column through the composition wire, the opacity through the opacity machinery), so the temperature condition has sharpened from an under-determined product into a genuine referee of `Mdot`. The derived-root referee is a follow-on build; the hindcast meanwhile takes only genuine rate measurements.

THE MATURE ENDPOINT resolves externally and upgrades. The 0.01 is an observed class-II value, and observed values carry the age of their sample, so `t_mature` is THE SOURCE SAMPLE'S AGE, a fetchable chord endpoint, not a reserved number. The upgrade taken while in the source: pull the rate-versus-age LOCUS (Hartmann's own data or the Manara-class compilations) with binned points, bands, and sample conditioning, rather than one synthetic point, so the mature landmarks are observational rather than synthetic and a locus can convict a decline SHAPE where a single point cannot.

With `Mdot_0` DRAWN and `t_visc` DERIVED, the closure is still the inverted hindcast that VALIDATES rather than pins (a stronger gate than the joint pin, which would have made the parameters as reserved as the numbers they retire). What changed is that it trades a synthetic rate pair for one derived root plus a real observational locus, so it gets stronger, not weaker.

THE RETIREMENT RIDER stands: the condensation-temperature consumer evaluates `Mdot(t)` at ITS OWN epoch (the derived root), not at `t = 0`.

## Two more standing rulings (excerpted per the commissioning fix)

THE REPLACEMENT-CIRCULARITY RULE (now standing): a mechanism may never calibrate against the value it retires. Replacements validate against the retiree's SOURCES or against INDEPENDENT data, never against the retiree. So the derived `tau_disk` must NOT calibrate against the reserved `disk_gas_lifetime_myr` it replaces. The refused value gets its proper home as an OUTPUT VALIDATOR: the Haisch-Lada and Mamajek disk-fraction-versus-age data is the population-level hindcast for the derived `tau_disk` DISTRIBUTION across the ensemble once the dispersal race lands, never a calibration input, and NEVER A MEDIAN (the range is the statistic-with-hidden-conditioning class this arc has convicted twice already). The `FORMATION_TIME_MYR` prohibition is ratified: different physical event, different clock zero, never reached for.

## The commissioning fix (process, standing)

The check-outranks-order clearance found no superseding ruling because the conditioning rulings (the model-structure band, the environment-conditioned dispersal, the value-line homes) live in the ruling channel, invisible to an agent that hears only through PRs. The fix, now standing: the coordinator excerpts every standing ruling that conditions an out-of-loop arc into that arc's scope doc at commissioning, so the clearance can see what it must check against. This scope doc's rulings sections above are the first application.

## Gate blindness sets (rule 1, standing): every gate states what it cannot see

The blindness-set rule: every gate's doc names its BLINDNESS SET beside its discriminating power, one line, naming the guard that covers each blind spot. A gate that knows what it cannot see is a gate; a gate that does not is a comfort. The physics reason it binds this arc: a conservation gate certifies FLOW BOOKKEEPING and is structurally BLIND to SHARED-SOURCE errors, because an error in the opening appears on both sides of the identity and cancels (misquadrature the profile by 2x and both DiskGas gates pass serenely, opening wrong, closing wrong by the same amount, books balanced, world wrong). Source truth and flow consistency are different claims guarded by different machinery, which is why the opener-binds-to-quadrature and convergence-twin tests are never redundant with the conservation gates.

The DiskGas gates gain this line verbatim, in this arc's FIRST code commit (after #194 lands and this branch rebases onto the ledger): "blind to opening-source errors and to mass-and-L-proportional errors; covered by the opener-binds-to-quadrature test and the convergence twin." The arc's own new gates carry their blindness sets from birth. The HINDCAST gate (that the derived `(Mdot_0, t_visc, gamma)` satisfies the derived formation-root condition and passes the observed mature rate-versus-age locus): discriminating power is that it convicts any decline the derived root or the mature locus rejects; blind to a joint normalization shifting the whole chord (covered by anchoring `Mdot_0` to the class-0/I peak band) and to the family shape (covered by `gamma`'s gate-G provenance); the formation RATE is excluded entirely as a partition share, not a landmark. The TAU_DISK RACE gate (that the derived dispersal time falls in the observed class-II lifetime band): discriminating power is that it convicts a wind rate or viscous time that disperses too fast or too slow to match observation; blind to a compensating pair of errors in the wind rate and the viscous time that cross at the right lifetime, covered by checking each rate against its own observed band rather than their crossing alone.

## The three rules that bind this arc (from the ruling)

Rule 1, the blindness-set rule, is applied above. Rule 2: the same-fact-two-doors flag on the DiskGas ledger was ruled CLEAN (mass and the sqrt(r)-weighted first moment are different moments of one distribution, and the L gate discriminates a wrong-orbit debit the mass gate cannot see), and the suspicion is what produced rule 1; the carried defect class below stands for the live account. Rule 3: rulings cite their evidence channel (brief or commit), and this arc keeps auditing them against the repository, which is how the premise-check finding was grounded in the actual constants rather than taken on report.

## Couplings and the build order

This arc RETIRES the two formation-era accretion-rate constants onto the clock (the premise-check gate), makes the DiskGas ledger's `from_disk_profile` opening a function of epoch rather than a single static snapshot, and hands the #73 giant gate a derived `tau_disk` in place of the reserved gas lifetime it races the Kelvin-Helmholtz time against. It does not touch the assembly projector (still a stability projector, no time integration) and does not make the assembly's causal merge order into a physical time. The clock is the disk's own, distinct from the assembly's ordering.

Slice one: the Mdot(t) clock and the derived `t_visc`, with the hindcast validating against the derived formation-root condition and the fetched mature locus (not the retired formation rate), byte-neutral until a consumer reads the epoch. Slice two: the wind-versus-accretion race deriving `tau_disk`. Slice three: wiring `tau_disk` into the #73 giant gate and the DiskGas opening, the first run-path behaviour change, gated with both pins re-pinned deliberately.

## Carried defect class: same-fact-two-doors (pre-registered for the live account)

The coordinator named a derivation-level defect class to carry into this arc: SAME-FACT-TWO-DOORS, two carriers of one physical fact inside a single formula, the derivation-level sibling of the diamond (two providers of one fact across the codebase). The concrete question was whether the DiskGas ledger, whose mass and angular momentum both derive from the same surface-density profile, carries one fact through two variables such that a conservation check is quietly self-referential.

Checked against the static slice's arithmetic: it is not. Both gates cancel the account's derived opening value (the mass gate is `m_gas0 + embryo_mass` against `(m_gas0 - drain) + planet_mass`, so `m_gas0` drops out; the L gate is identical with `l_gas0`), so a gate's pass does not depend on the account magnitude, and therefore not on the shared profile provenance of the pair. The account's own correctness is checked by separate tests (the provenance test binds the opening to `disk_gas_content`, the convergence test checks the integral), so the shared fact never flows through one combined invariant. The static slice is safe.

Two things this arc must carry, because the live account is where the class goes load-bearing. FIRST, the honest tautology limit: today the drain debits `gas = planet.mass - core` and the same gas sits inside `planet_mass`, so the giant portion of each conservation gate is partly tautological (the debit is defined to match). The gate's non-trivial content is the terrestrial merge conservation and the giant distributivity residual, not a check that the drain books the RIGHT gas. When this arc makes the account live, the envelope gas should be computed from the accretion physics INDEPENDENTLY of `planet.mass - core`, so the conservation gate stops being tautological on the giants. SECOND, the pre-registered breach: the same-fact-two-doors goes live the moment an invariant COMBINES the account's mass and angular momentum directly, the characteristic lever arm `l/m`, or the wind edge's lever-arm-weighted angular momentum per unit mass. Any such invariant must be checked for self-reference before it is trusted, because it reads the one profile fact through both doors.
