# The phonon-battery tightening spec (the four signed-band derive-downs)

The phonon generator (#53) shipped factor-grade, with three pass-1 bands and one pass-2 band declared, locked, and regression-caught. Each miss is SIGNED with a NAMED mechanism, and a mechanism is a recipe: these are optional derive-downs that convert a declared band into a pre-registered prediction the existing battery re-grades. Filed here so none is lost; build when the input grounds. The owner's re-check (2026-07-14) upgraded the readiness: bands 1 and 4 unblock outright with the constructions pinned below, band 3 is one primary-source verification from closure, band 2 forks into a measured rung now and a substrate build later.

## The principle (the line "without curve fitting" draws)

The forbidden move is tuning coefficients against our own battery, which would launder the gate. The legal moves are: exact identities on banked columns, banked mechanisms recombined, universal class constants from independent literature (never our targets), and compute-once amortization. None of those ever saw the battery, so falsifiability survives. Every fix below converts a declared band into a prediction the SAME battery re-grades, so the tightening is itself falsifiable, the difference between sharpening an estimator and flattering it.

## Band 1: the +3% free-stretch bias is the harmonic-versus-fundamental offset (UNBLOCKED)

Badger constants (Herschbach-Laurie) target the harmonic `omega_e`; spectra report the fundamental `nu_01 = omega_e(1 - 2 x_e)`. For a Morse oscillator `x_e = h-bar omega_e / (4 D_e)` is an identity, so the correction is `nu_01 = omega_e - omega_e^2 / (2 D_e)` in wavenumbers.

The gap the first audit named (a per-bond `D_e` column) dissolves on inspection, because the calibration primary already holds `D_e` in disguise. UCRL-9694 Table II supplies measured `omega_e x_e` for the diatomics directly, and the same Morse identity inverts: `D_e = h-bar omega_e / (4 x_e)`, equivalently `D_e[cm^-1] = omega_e^2 / (4 omega_e x_e)`. So the `D_e` column assembles from the vendored primary with zero new fetches for every diatomic in that table. Worked for O-H off the flagged constants: `omega_e ~ 3738 cm^-1`, `omega_e x_e ~ 84.9 cm^-1` give `D_e = 3738^2 / (4 x 84.9) = 41,100 cm^-1 = 5.1 eV`, the right well depth (`D_e` exceeds `D_0`'s 4.4 eV by the zero-point half-quantum, as it must, an internal sanity check that costs nothing).

So the estimator route needs no Harrison tie-in for calibrated pairs; the Harrison-gap-derived `D_e` remains the ALIEN rung only, correctly two-tier. Note the inversion: the identity runs both directions, spectroscopy to `D_e` for the table's rows, `D_e` to anharmonicity for aliens, one relation, two provenances. The same identity emits overtone positions free (the near-IR opacity consumer wants them). Pre-registered post-correction grade: +-1%. Booking: the `D_e`-from-anharmonicity column books as a two-directional source, [M] via UCRL-9694 Table II.

## Band 2: the +18% ice redshift is bond-order conservation wearing Badger's rule (FORKED)

The H...O contact steals bond order from the covalent O-H (Pauling/Brown conservation), lengthening it from 96 to ~100 pm in ice (neutron value, verify). Badger's `(r - b)^-3` then carries the softening: at a mid-table H-row `b ~ 0.60 A`, `((0.96-0.60)/(1.00-0.60))^3 = 0.729` in `k`, `sqrt` = `omega x 0.854`, landing near the observed ice 3226 cm^-1 (illustrative pending the exact `b` from the vendored UCRL-9694). The Badger-Bauer correlation the literature treats as empirical falls out as a theorem.

The blocker verdict is ratified and the schedule splits. The MEASURED route needs no substrate: per-phase neutron geometry [M] (ice O-H ~100 pm) is a per-phase input that serves ice today. The ALIEN route needs the bond-valence machinery (the `R_0` pair table, `b ~ 0.37 A`, the Brown-Altermatt valence-sum), which was specced in the Ewald seam and never built, so it files behind its own small build: a Brown-lineage cited fetch of the `R_0` table, the same artifact class as UCRL-9694, plus the valence-sum solver. Until then the alien H-bonded phase is the residual honestly unfixed by paper methods (the compute-once tier, dimer curves / MB-pol class, is the legal amortization there). Fork: measured-geometry rung now, bond-valence rung filed as a named small build with its fetch.

## Band 3: the -25% covalent stiffening dispatches to Harrison curvature (ONE VERIFICATION FROM CLOSURE)

For low-`Delta-chi` pairs, take `k` as the second derivative of the bond energy. Harrison's form is `E_bond(d) = -2 sqrt(V_2^2 + V_3^2) + V_0(d)`, covalent-plus-polar attraction against overlap repulsion, with `V_2 = eta h-bar^2 / (m d^2)` already banked and `V_3` d-independent to leading order (a term-value difference). The attractive piece is exact and evaluates analytically off banked columns: `d^2/dd^2 [-2 sqrt(V_2^2 + V_3^2)] = -2 (V_2 / sqrt(V_2^2+V_3^2)) (6 V_2 / d^2)` plus the cross-terms weighted by the banked covalency `alpha_c = V_2 / sqrt(V_2^2 + V_3^2)`.

The honesty boundary is the repulsion `V_0`, which is NOT uniform across Harrison's editions: the lattice-vibrations treatment uses a `V_0 ~ 1/d^4` scaling argument, while later chapters refit the exponent. That repulsion curvature is exactly the coefficient not to guess. Ruling: verify the exponent `n` against the vibrations chapter of the primary, and pre-register the PARAMETER-FREE consistency check that decides whether the route is trustworthy at all. At equilibrium `dE/dd = 0` ties the repulsion's curvature to the attraction's slope for any assumed power `n` (a relation of the class `k = E''_att + ((n+1)/d) |E'_att|`), so once `n` is read from the primary the formula carries NO free coefficient, and silicon's measured 9.2 mdyn/A-class stretch becomes the GATE the route must clear, never the fit it was tuned to. It is a DISPATCH rule keyed on the banked `Delta-chi` columns (low `Delta-chi` to Harrison, else Badger), and it fixes the diatomic and the crystal together (the identical misses said it must). The canonical 1980 quartet is banked (`harrison::eta`, `two_center_matrix_element`, `covalent_energy_v2`, `polar_energy_v3`, `bond_covalency`); only the exponent `n` remains to be read. Booking: the route gains its equilibrium-consistency check as its acceptance spec.

## Band 4: the pass-2 length band closes with Schomaker-Stevenson (UNBLOCKED, construction pinned)

`r_AB = r_A + r_B - 9 |Delta-chi_P|` pm, one number for the whole universe (Schomaker-Stevenson 1941, Trouton-class standing, never saw our targets). SiC inline: `Delta-chi_P(Si,C) = 0.65` gives `-5.9 pm`, so `194.9 - 5.9 = 189.0` against the measured 188.9, the entire 6 pm miss to the decimal.

The `Delta-chi_P` is derived, not looked up, and the construction is pinned so wiring is mechanical. Pauling's thermochemical definition is `(chi_A - chi_B)^2 = Delta_AB / eV` where `Delta_AB = D(AB) - (1/2)[D(AA) + D(BB)]` (his 1932 arithmetic-mean form; the geometric-mean variant is the later refinement, flag both, build arithmetic first). The repo's formation-enthalpy rows plus band 1's new `D_e` machinery supply every dissociation energy the differences need; anchor the scale at `chi_H = 2.20` by convention and solve outward. Two pins hold the derivation honest. First, this yields `chi_P` on Pauling's OWN thermochemical scale, so the `9 pm / |Delta-chi|` Schomaker-Stevenson constant applies as published with no cross-scale conversion, which is the entire reason to derive `chi_P` rather than rescale the repo's Mulliken `chi = (IE+EA)/2`: the Allred-Rochow lesson is that the scales measure different physics, so converting between them would smuggle a fit. Second, the derived column earns an acceptance row against the standard Pauling table (O 3.44, Si 1.90, C 2.55) at few-percent grade BEFORE Schomaker-Stevenson consumes it. Post-correction: the length rung's SiC row lands sub-picometre, and through Badger the ~9% frequency softening collapses. Booking: the `chi_P` derive-down pins to the arithmetic-mean construction with the `chi_H = 2.20` anchor and its acceptance row.

## Readiness ranking (revised 2026-07-14)

Band 1 unblocked outright (`D_e` from the vendored UCRL-9694 Table II via the Morse identity, no fetch). Band 4 unblocked with the arithmetic-mean `chi_P` construction above (depends on band 1's `D_e`, so build band 1 first). Band 3 one primary-source verification from closure (read the repulsion exponent `n` from Harrison's lattice-vibrations chapter, then the equilibrium-consistency check runs parameter-free; if `n` cannot be verified to primary, stop at the check spec, do not guess). Band 2 forked: measured-geometry rung now, bond-valence substrate filed as a named small build behind its `R_0` fetch. The identities are sound; this is purely which inputs ground today, and each fix once built is a pre-registered prediction the existing pass-1/pass-2 battery re-grades.
