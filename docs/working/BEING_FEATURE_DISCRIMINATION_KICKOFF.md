# 2b per-feature discrimination: sharp emitter-specific sorting emerges over the magnitude proxy (design-first)

This is the design opener for the arc the gate ruled next after both predator-prey poles (#144 approach, #146 avoid) and the predation-completion pay-off loop (#148) landed. It closes the honest gap those arcs each flagged: the being channel today keys on signal MAGNITUDE alone, which gives a single NET disposition over every emitter a creature perceives, so a lineage cannot both AVOID a predator and APPROACH a prey when both are warm emitters. Sharp, emitter-specific sorting requires the being channel to key on a distinguishing PHYSICAL FEATURE of the other. This arc demonstrates that such a sort EMERGES from selection over an evolvable per-feature discrimination weight, never an authored "predator" or "prey" identity. Design-first, grounded against source, posted for the gate's ruling before any build code. No mechanism decided here.

## What is already built (do not rebuild)

The per-feature discrimination SUBSTRATE is on main, byte-neutral and off by default. The 2b perceivable-feature floor (`PerceivableFeatureRegistry`, `perceivable_feature.rs`, merged #126) discriminates a scalar into a bucket by a per-channel resolution (`bucket`, `from_channels`). The being-FEATURE perceive pass is built (`runner.rs:6216` onward, a separate gated block so the magnitude-only path stays pin-untouched): a mind-less creature reads each perceived emitter's per-channel surface optical feature (`PerceivableFeatureRegistry::read_emitter` = `physiology::surface_optical_axis`, a DIRECT single-axis read off the emitter's OWN body covering material, `cov.mat(axis)`, zero where the covering declares none), bins it by the perceiver's own resolution, and accumulates the toward-emitter direction per (channel, bucket) through `creature_being_feature_directions` (`learn.rs:1038`). The perceiver's OWN heritable freely-signed per-bucket weight (the controller layout's `being_feature_input_base` block) turns a feature-bucket's accumulated direction into approach or flight. The whole pass is gated on `being_features.is_empty()` (empty by default, `set_being_features` arms it), so an unarmed world does nothing and is byte-identical. The magnitude proxy (`creature_being_direction`, the #118 B3 pole) and both selection poles (#144, #146) and the pay-off loop (#148) are proven.

## What remains: prove the two-way sort EMERGES, then arm it

Every piece exists in isolation. What is not demonstrated is that the per-feature discrimination weights are DRIVEN to OPPOSITE poles by selection when two emitter types carry distinguishing surface features and opposite fitness consequences: the feature-bucket of a wounding emitter to the avoid pole, the feature-bucket of a safe or paying emitter to the approach pole, so a sharp emitter-specific sort emerges that the magnitude-only channel (one net disposition over all emitters) cannot express. This is the mirror of the #144 matter proof and the #146 flee proof, now over TWO feature buckets read out as a two-way sort rather than one weight read out as a pole.

The load-bearing design line: nothing about predator or prey identity is authored at any level. The feature a creature keys on is the emitter's own surface optical datum (a covering material axis), bucketed by the creature's own resolution; the per-bucket weights are founder-zero, symmetric zero-mean, and freely-signed; and which bucket becomes avoid and which becomes approach is set by the fitness consequences the two emitter types carry (one wounds, one is safe or pays), never a stamped label. A lineage that sorts the wounding feature-bucket away and the safe or paying one toward out-survives, so the sort is selected, exactly as the single poles were.

## The emergence claim (for the gate's frame-blind), de-narrativized

"A perceiver lineage seeded with founder-zero, symmetric zero-mean, freely-signed per-feature discrimination weights, selected on its own homeostatic survival, will have those weights driven to OPPOSITE poles on two feature buckets when the two emitter types carry distinguishing surface features and opposite fitness consequences (one wounds, one is safe or a pay-off): the bucket of the wounding emitter to the avoid pole, the bucket of the safe or paying emitter to the approach pole, so a sharp emitter-specific sort emerges that the magnitude-only channel (a single net disposition over all emitters) cannot express. In a matched control where the two emitter types are perceptually indistinguishable (their surface feature falls in ONE bucket) the sort collapses to the single net disposition. The feature is the emitter's own surface optical datum, the discrimination is the perceiver's own resolution, the weights are founder-zero and freely-signed; nothing about predator or prey identity is authored."

## Section-11 smoke self-check (fail-closed, single-pass)

The claim was probed for the authored-coupling seam before proposing it, and it held on four attacks. (1) The feature-to-disposition wire reads a RAW surface optical axis (the covering's material datum), bucketed by the perceiver's own resolution, never a valence, a category, or a "predator" fact, so it is not the template case. (2) The two-way sort needs no authored bucket-to-identity map: the per-bucket weights are founder-zero symmetric, and which bucket becomes avoid versus approach is a consequence of which emitter type wounds versus pays, a DATA outcome of their differing coverings, never wired into the perceiver. (3) Admit-the-alien holds: the feature is the emitter's own covering datum (zero if absent), so an alien body with a different covering buckets differently by the same read, and the perceiver keys on its own resolution. (4) The control is clean: it makes the two emitter types share one feature bucket, so the ONLY difference from the treatment is perceptual distinguishability, isolating that the SORT (not the presence of two emitters) is what the per-feature channel adds. The smoke did not break the claim, so it is a candidate to build, pending the section-10 frame-blind panel before any build code.

## Scoped slices (design-first, for the gate's ruling)

1. Confirm at source that the substrate composes for a two-way sort: the being-feature perceive pass reads each emitter's covering optical feature, buckets it, and fills the per-(channel, bucket) direction block, and the perceiver's per-bucket weight steers on it. Confirm two emitter types with different coverings fall in different buckets as DATA. Wire only what is missing to run the arena (if anything), test-only, the four pins held.
2. The two-way-sort EMERGENCE proof: a deterministic in-run selection arena (the mirror of #144, #146, #148), a perceiver seeded founder-zero symmetric on the per-bucket discrimination weights, TWO feature-distinguished emitter types (a wounding predator in feature-bucket A, a safe or paying prey in feature-bucket B), selection driving the two weights to OPPOSITE poles (avoid A, approach B), a matched held control where the two types share ONE feature bucket (the sort collapses to the magnitude net disposition), deterministic replay. Scoped to what the substrate delivers.
3. Flag ARMING the being-feature channel in a run scenario (the re-pin) as the follow-on, so sharp emitter-specific sorting goes live in the shipped world; flag the corpse-intake sibling (#148, single-eater, awaiting the contested-corpse contention resolver) and the down-live-prey-and-eat loop (awaiting the creature-strike capability) as the siblings this sort completes.

## Hard constraints (carried from #144, #146, #148, and the owner's rulings)

Nothing about predator or prey identity is authored: the per-feature weights are founder-zero symmetric and freely-signed, their signs from cross-generational selection (Principle 8, Principle 9). The feature is the emitter's own surface optical datum, the discrimination the perceiver's own resolution, never a kind read. Admit-the-alien: an emitter with a different or absent covering buckets by the same read, and a lineage that does not sort is a valid selected outcome. The being-feature pass stays a gated opt-in (byte-neutral off), the four canonical pins held for any build slice that does not arm a shipped scenario, and arming is the stated re-pin flagged for a follow-on.

## For the gate

This is the design opener for the 2b per-feature discrimination sort, the sharp emitter-specific sorting the magnitude proxy cannot give and that every flagged predator-prey sibling needs. The substrate is built and off by default; the gap is the two-way-sort emergence proof under selection, control-isolated, with arming flagged. On your ruling I run the section-10 frame-blind panel on the emergence claim (emergence-critical), ground each slice at source, build byte-neutral where it can be (the four pins held), single-pass section-9 self-audit per slice under the cost directive, and post with the pins. #148 is ready for your merge; opening this keeps the bridge.

## Build log (slices 1 and 2 built; the gate waived the section-10 panel as the Nth application of a proven pattern)

Slice 1, the source grounding (the substrate composes for a two-way sort). Confirmed at source that two emitter types with different coverings fall in different buckets as DATA: `read_emitter` reads `opt.emissivity.band_1` off the emitter's covering (`surface_optical_axis`, `cov.mat(axis)`), and at the arena's 20-bucket, 0.05-step resolution a bare-hide covering (kind 0, band_1 0.96) discriminates into bucket 19 and a chitin covering (kind 4, band_1 0.88) into bucket 17. `creature_being_feature_directions` (`learn.rs:1038`) fills a per-(channel, bucket) toward-vector, clamped to the unit disc, and the perceiver's per-bucket MOVE-heading weight (`being_feature_input_base` + 2*bucket) steers on it. Nothing was missing to run the two-way sort; the change is test-only, so the four pins cannot move.

Slice 2, the two-way-sort EMERGENCE proof: `per_feature_sort_emerges_only_when_the_emitters_are_distinguishable` (runner.rs, test-only), the mirror of #144/#146/#148 over two feature buckets. A perceiver keys on each perceived emitter's own covering feature; sixteen founders are seeded symmetric zero-mean across the four sign combinations of the prey-bucket and predator-bucket MOVE-heading weights. Two emitter types carry distinguishing coverings and opposite fitness: a WOUNDING predator (chitin, bucket 17) that culls a co-located perceiver through the one INTEGRITY cull, placed east; a PREY emitter (bare hide, bucket 19) whose cell carries food, placed west, so heading toward it feeds the perceiver and keeps it reproduction-eligible. `distinguishable` is the matched control: when false the predator ALSO wears bare hide, so both emitter types fall in ONE bucket (19) and the sort has no feature to key on. RESULT: under distinguishable emitters the prey bucket is driven to the APPROACH pole (+0.35) and the predator bucket to the AVOID pole (-0.17), OPPOSITE poles off their symmetric zero-mean seed; the sort (prey_b - pred_b) is +0.52. In the matched control the predator's bucket (17) is never populated and holds at its zero seed (-0.00) while the shared bucket carries a muddled net (+0.16), so the sort collapses to +0.16. The predator bucket goes to avoid ONLY when the predator is perceptually distinct: that gap is the sort the per-feature channel adds over the magnitude proxy. Deterministic replay. The four canonical pins hold bit-exact (default `4bbf6b59`, full `1db633b3`, discovery `c9d5cc17`, viability `ad69f2bf`); full sim lib suite green; `cargo clippy -p civsim-sim --all-targets` clean. Single-pass section-9 self-audit clean (the control isolates distinguishability as the sole difference; the per-bucket weights are founder-zero symmetric freely-signed and the approach-versus-avoid assignment is a fitness consequence not an authored tag; the feature is the emitter's own covering datum, so the control is the alien case; no species or kind read; determinism holds).

Honest limits (on record, the two-bucket sibling of #146's honest flee-pole limits). The magnitudes are MODEST: the approach and avoid pulls act on the SAME MOVE heading (the prey lies one way, the predator the other), so the two per-bucket weights are coupled rather than independently selected, and the stationary point-predator only culls the fraction of the population that heads onto its exact cell, exactly the coupling and point-hazard limits #146 recorded for the single flee pole. What is proven is the SIGN sort (opposite poles) and its dependence on distinguishability, not a saturated magnitude; a stronger, decoupled sort is a refinement (independent-axis geometry or a mobile hazard), flagged. The per-channel bucket RESOLUTION is the arena's parameter here; per the gate's standing rule it is a reserved value that the arming follow-on must DERIVE from the perceiver's own sensory acuity, not ship as a fixed 1/20. Slice 3 (arming the being-feature channel in a shipped scenario, the re-pin) stays a flag, carrying that derive-the-resolution hunt.

Build notes (for a similar arena). A culled creature is RETIRED from the walkers vector the same tick, so a live "count the dead" reads zero even when the predator is killing; instrument true deaths through the monotonic offspring counter or the population trajectory instead. The food distance trades off the two pressures: food adjacent to the founders makes the approach pole strong but starves the avoid pole (everyone reaches food, none wander onto the predator), while distant food surfaces the avoid pole at the cost of a weaker approach; the reported run balances them. A bred offspring gets its reserve capacity from its body ORGANS (`Homeostasis::new`), so the perceiver body carries a fat-body organ (kind 0) for the adaptive lineage to compound, the #148 lesson.
