# Copyright 2026 Nathan M. Fraske
# Licensed under the Apache License, Version 2.0; see LICENSE.
#
# CODEOWNERS: the required reviewer for the Stone 0 provenance gate and its integrity anchor. The
# consult (design doc Part D) established that an agent with write access has a cheaper move than
# laundering the password: neuter the gate itself (an early return, a dropped build-dependency, a gutted
# scan, a widened tombstone exclusion). No local mechanism closes that; the anchor is the owner's own
# eyes on any diff that touches the gate, its scripts, its CI wiring, its tombstone list, or its design.
# For this file to bind, apply branch protection on the default branch requiring CODEOWNERS review (see
# docs/working/Q1_STONE0_PROVENANCE_GATE_DESIGN.md, "Owner ritual and setup").

# The gate crate: the library, the binary, and its tests.
/crates/stone0/**            @nathanfraske

# The gate's shell scripts (the pre-push hook and the CI tombstone scan).
/scripts/stone0*             @nathanfraske

# The CI workflow (the Stone 0 job and every other gate lives here).
/.github/workflows/ci.yml    @nathanfraske

# This ownership file itself.
/.github/CODEOWNERS          @nathanfraske

# The tombstone list (a widened or gutted list would silently disarm the retroactive catch).
/calibration/stone0_tombstones.txt   @nathanfraske

# The design of record.
/docs/working/Q1_STONE0_PROVENANCE_GATE_DESIGN.md   @nathanfraske
