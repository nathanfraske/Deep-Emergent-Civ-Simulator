# Threat-and-flee emergence: making the predator-avoidance pole emerge (design-first)

This is the design opener for the follow-on arc the gate ruled after the creature-selection-loop arc (#144). That arc completed the creature reproduction-and-behaviour-selection loop and proved the APPROACH pole of the predator-prey reaction: a founder-zero resource-density weight is driven to the adaptive pole under a real fitness gradient, control-isolated and deterministic (the matter-channel scavenge-and-forage proof). This arc builds the other pole, the FLEE: a founder-zero being-signal weight carried AWAY from a harmful emitter under selection. Design-first, grounded against source, posted for the gate's ruling before any code. No mechanism here.

## Correction (2026-07-11): the "emission seam" this opener was first built on was WRONG

The #144 threat probe reported that the authored predator is causally inert because `spawn_predator` never seeds its `body_temp`. That finding was FALSE, caught on re-grounding at source before any code. `spawn_predator` (runner.rs:7297-7300) DOES seed the predator's `body_temp` to its comfort setpoint (the #120 section-9 fix, "so it EMITS a being-signal like any warm walker"), so the predator emits. The #144 threat-arena bit-identical result was a TEST OMISSION: that arena never called `set_physiology`, and the creature being-perceive block is gated on `emb.physiology` being present (runner.rs:6134-6138), so the whole perceive block was skipped and the prey perceived nothing (not the predator, not each other), moving by explore only. Verified the positive claim with a throwaway probe (perceive path armed, physiology present): a prey ends at x=6 with a warm predator three cells away versus x=5 without, moved toward the emitting predator by its being-signal weight. So the predator emits, a being with perception armed perceives it, and the flee experiment is buildable on the EXISTING substrate. There is no #120 emission seam to fix. This is a Prime-Directive-1 miss on the reporter's part (a conclusion reported without proving it against source), owned and corrected here.

## What really remains for the flee pole

With perception working on the existing substrate, the flee pole is the mirror of the matter-channel approach proof (#144), buildable now:
- The threat-avoidance emergence proof: a symmetric zero-mean seed on the being-signal weight (half toward the emitter, half away), the reproduction beat armed, the authored (emitting) predator as the fitness gradient, physiology armed so perception fires, and a matched held control (predator present versus absent). A lineage carried away from the predator survives the strike-to-death loop and breeds, so the mean weight is driven to the avoidance pole; the control leaves it at its zero seed. Scoped to the MAGNITUDE proxy (a being avoids the stronger or nearer emitter, identity-blind), since with only the B3 magnitude channel a being sorts emitters by signal strength, not by predator-versus-prey identity.

## The narrower derive-versus-author question (the real residual of constraint 1)

The predator's `body_temp` is seeded to `thermal.initial_temp` (its comfort setpoint), so the predator is warm because its setpoint is warm. The honest question the gate raised STANDS in a narrower form: is a being's thermal SETPOINT a physics consequence of its own physiology, or an authored band value? This is a broader thermal-substrate question (it applies to every being's setpoint, not the predator specifically), not a predator emission seam. This arc grounds whether the setpoint derives or is authored and surfaces any value reserved-with-basis, but the flee EMERGENCE proof does not depend on resolving it (the predator emits either way).

## Seam 2: land the 2b per-feature discrimination on the being channel

Even once the predator emits, the magnitude-only being channel (the B3 percept armed in #144) has a prey perceiving EVERY emitter by signal magnitude, including other prey. So a prey can sort emitters only by "stronger versus weaker", the magnitude proxy, never "predator versus prey". The clean flee pole ("avoid the predator specifically") needs the 2b per-feature discrimination (amendment 3's flagged follow-on): the being-signal weight keys on a distinguishing physical FEATURE of the emitter (an optical or thermal feature the predator carries and prey do not), so selection can separate the harmful emitter from a harmless neighbour. The 2b percept-feature floor is built (#126); this arc arms it on the being channel for the threat case and lets the discriminating weight emerge under selection.

## The emergence proof (on the harness #144 built)

The flee emergence proof reuses the selection arena the matter-channel proof established, on the EXISTING substrate (no emission fix needed): a symmetric zero-mean seed on the being-signal weight (half toward the emitter, half away), the reproduction beat armed, physiology armed so the perceive path fires, and the authored (emitting) predator as the fitness gradient. Under the gradient (predator present), a lineage whose weight carries it AWAY from the predator survives the strike-to-death loop and breeds, so the population-mean weight is driven to the avoidance pole; in the matched HELD CONTROL (predator absent) the same regime leaves the weight at its zero seed. The contrast is the proof that selection moved it, deterministic and replayed bit-for-bit, the same rigor the approach-pole proof met. Scoped to the MAGNITUDE proxy (a being avoids the stronger or nearer emitter, identity-blind), the clean "avoid the predator specifically" the 2b follow-on below. Nothing about approach-versus-flee is authored, the sign emerges from selection, and a lineage that evolves neither response is a valid outcome.

## Scoped slices (design-first, for the gate's ruling)

1. The flee emergence proof on the #144 harness (buildable now on the existing substrate): the symmetric-seed selection arena with the emitting predator as the gradient (physiology armed so perception fires) and a matched held control (predator present versus absent), deterministic. Scoped to the magnitude proxy.
2. Ground the predator-setpoint derive-versus-author question (the narrower residual of the gate's constraint 1): whether a being's thermal setpoint is a physics consequence of its own physiology or an authored band value; surface any value reserved-with-basis. The flee proof does not depend on resolving it (the predator emits either way).
3. The 2b per-feature discrimination on the being channel for the threat case (the cleaner follow-on): the being-signal weight keys on a distinguishing physical feature of the emitter, so "avoid the predator specifically" separates from the magnitude proxy under selection.

## Hard constraints (carried from #144 and the owner's rulings)

Nothing about approach-versus-flee is authored: the sign and strength of the being-signal weight emerge from cross-generational selection (Principle 8, Principle 9). The 2b discrimination keys on a physical FEATURE that correlates with harm, never a "predator" label or kind (the identity-blind line). Admit-the-alien: a non-fleeing lineage is a valid selected outcome; the emission and the discrimination key on the being's own data. Active HUNT of live prey stays scoped to the magnitude proxy or the per-feature discrimination this arc lands, never a two-way sort the channel cannot deliver.

## Build log

**Flee emergence proof BUILT (re-scope (a), gate-approved), deterministic, control-isolated.** The gate approved building the flee proof on the existing substrate, magnitude proxy, held control, physiology armed. Built `flee_pole_emerges_only_under_the_predator` (runner.rs), the mirror of the matter-channel arena on the being-signal channel: twelve founder creatures co-located, seeded with a SYMMETRIC zero-mean being-signal attraction weight (half +8 toward the perceived emitter, half -8 away), the reproduction beat armed (eligibility reserve zero so a living co-located pair breeds despite the metabolic drain), the being-directed percept armed WITH `set_physiology` (the fix for the #144 omission: the perceive block is gated on physiology at runner.rs:6134-6138), and the authored emitting predator (`spawn_predator`, `body_temp` seeded to its setpoint) co-located with the cluster. Energy and temperature never floor, so the ONLY cause of death is the predator's whole-body wound through the one INTEGRITY cull: the predator, not starvation, is the isolated fitness variable. A single-pass section-11 self-check ran first (per the gate): the avoid sign is symmetric-seeded and selection-driven, the being keys on signal MAGNITUDE (a physical quantity, never a "predator" label), and the one derive-vs-author residual (the predator's setpoint) is the flagged thermal-substrate follow-on that does not enter the flee mechanism.

**Result:** under the predator the population-mean attraction weight is driven NEGATIVE, to the avoidance pole (-0.615, stable across generations): beings whose weight carried them away from the strong-emitting hazard survived the strike-to-death loop and bred, those carried toward it were wounded. In the matched HELD CONTROL (no predator) the mean stays at its zero seed (-0.002). The contrast (-0.615 versus -0.002) is the proof that SELECTION moved the weight, not the regime; the seed is symmetric so nothing authors the sign, and the experiment replays bit-for-bit (Principle 3, asserted). Full sim lib suite green, fmt and clippy (`--all-targets`) clean.

**Single-pass section-9 self-audit:** confirmation-bias clean (empirical and deterministic, the control matched, perception verified); derive-vs-author clean (the seed is symmetric zero-mean; the arena parameters are a stated experimental regime; the predator emits via its own `body_temp`, the setpoint's derive-vs-author flagged and outside the flee mechanism); alien-feasibility clean (magnitude keying, generic reserves, the predator emits from its own body); Terran-bias clean; steering/Principles clean (the avoid sign emerges from selection, the control isolates it, no "predator" label read, deterministic replay).

**Honest scope carried:** the shift is MODEST (-0.615, versus the matter pole's +8) because the predator is one warm emitter among the prey cluster and a stationary point-hazard, so few approachers are wounded and the magnitude-only channel sorts by strength across all emitters. A clean, strong "avoid the predator specifically" awaits the 2b per-feature discrimination (the flagged follow-on) and a sustained or mobile hazard. This is the honest magnitude-proxy demonstration the gate scoped, not an engineered number. The predator-setpoint derive-versus-author is the flagged broader thermal-substrate follow-on (it connects to the owner's attunement-emerges-via-morphology resolution and the thermal-half-band); the flee proof rides the existing setpoint regardless.

## For the gate

This is the CORRECTED design opener for the threat-and-flee follow-on. The "#120 emission seam" the #144 probe reported was false (owned above): the predator emits (`spawn_predator` seeds `body_temp`), and a being with physiology armed perceives it (verified). So the flee emergence proof is buildable on the existing substrate, scoped to the magnitude proxy, with the setpoint derive-versus-author as a narrower thermal-substrate question and the 2b discrimination the cleaner follow-on. On your re-scope ruling I build byte-neutral where it can be, run the single-pass section-9 self-audit per slice under the cost directive, and post with the pins.
