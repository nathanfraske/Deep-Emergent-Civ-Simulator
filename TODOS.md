# Canonical planetary queue

This is the live queue injected by SessionStart. It contains only work on the
abiotic planet and stellar-system runpath. The preserved civilization,
biology, dawn, compose, scenario, and calibration queue is
`parked/TODOS_LEGACY.md`; it is historical and cannot supply canonical
inputs, evidence, or readiness.

Canonical value policy: the sealed absolute physics floor is the only source
of physical causal values. Exact SI numbers are immutable representation
coordinates, not caller inputs or physical degrees of freedom. The four tiers and seven provenance marks are accounting
only. Derive first. Every irreducible non-derived floor leaf, at any tier,
needs the complete derivation-exhaustion, Buckingham-Pi, Gap Law with typed
Chaos Protocol, and Residual Law receipt before admission. Otherwise the stage refuses. Written state and
contingency are generated only after both an admitted physical measure and an
approved realization-coordinate law exist.

## Canonical abiotic queue

- **P-STELLAR-BIRTH-MEASURE.** IN PROGRESS: the executable two-leaf closure evaluator, exact serialized frontier, typed multi-regime Chaos Protocol, hardened replayable floor, partial exact dimensional census, modular value-free structure and stellar-state schemas, private exact species-state reducer, authority-bound species derivation analysis, bounded structural species packet pair, and conditional physical species proof-graph pair are implemented at `docs/working/STELLAR_BIRTH_MEASURE_CONTRACT.md`, `docs/working/STELLAR_STATE_CLASSIFICATION_CONTRACT.md`, and `docs/working/PR215_LIVE_SOURCE_AUDIT.md`. The live analysis consumes an independently checked projection of all three sealed physical coordinates plus the pure-mass `m_e` expression. Those four `[D]` artifacts remain membership-neutral, bind the root success receipt v5 or refusal receipt v5 plus exact packet and post-projection canary evidence, and are visible through the refusal transcript and read-only viewer. Both root implementations canonicalize source arrival order, bind each identity to its leaf receipt, independently reconstruct the current SI dimension mapping and pure-mass expectation, independently reconnect the final projection to the current sealed packet, and require any production refusal to exact-match a fresh sealed-decision replay. The variable-cardinality dimension basis and data-defined descriptor, relation, constraint, and derivation graph remain open to unfamiliar lawful axes and mechanisms. Production supplies no species-forming descriptor or law, so it returns `no_admitted_species_derivation_rules` with zero members, no coverage, and no authority effect. Conditioned support, coupled gas and dust thermal balance, equation of state, collapse measure, material mass and angular-momentum histories, multicenter circularization law, multiplicity interactions, spectral closure, tensor needs, dynamical regimes, and persistent disk state remain open. Named stellar classes and unfamiliar or thaumic labels supply no physical law or dispatch authority. Acceptance: both Stage 1 leaves close only from repository-owned typed proof artifacts with support, normalization, conditioning, uncertainty, Buckingham-Pi, dimensional closure, dependency admission, validity-domain and global-conservation proofs, Gap Law including Chaos Protocol, Residual Law, unique residual-slot receipts, absolute-floor and schema binding, semantic-checker version, dependency digest, open stellar-state coverage, and joint-measure compatibility, and without a caller, fixed hidden, operating-system, viewer, presentation identity, ordinal, or ordering seed.
- **P-STELLAR-SPECIES-DERIVATION-ROOTS.** PHYSICAL-VOCABULARY SUB-RUNG AND FIRST NON-SPECIES PREMISE LANDED; SPECIES-FORMING PREMISE NEXT: two bounded classifiers independently classify the four sealed-floor roots as zero descriptor roles, four relation targets, and zero constraint laws. Their exact v1 receipt is bound into physical-registry v4, producer v6, watchdog v5, species analysis v4, its v5 watchdog, the canonical refusal bitstream, typed planet views, and the observer-only viewer. The current-input partition is complete, but global physical-vocabulary coverage and membership authority remain false. The frontier records each root's exact identity, tier, provenance tag, and admission route, including unfamiliar irreducible routes, rather than imposing one aggregate Universal `[D]` label. It accepts `no_admitted_species_derivation_rules` and future `physical_vocabulary_coverage_incomplete` as distinct scientific refusals while rejecting integrity failures. A bounded six-lens audit rejected a candidate that selected any sole positive dimensionless scalar and then minted an authored unbroken-gauge graph as Universal `[D]`. The live `eps_0` execution relation proves the generic claim-scoped derived route without claiming a field, excitation, or member. The next rung must bind a species-forming semantic role and applicable physical premises, carry a proof-bearing exact-masslessness object when relevant, and keep producer and watchdog semantic evidence independent. Derive each from the absolute floor when possible. An irreducible survivor must first exhaust derivation, Buckingham Pi, Gap Law including Chaos Protocol, and Residual Law, then occupy one unique owner-reviewed residual slot with its own claim-scoped independent capability pair. Evidence custody, a cited table, `m_e`, a familiar species or stellar class, a caller catalog, and current-input vocabulary coverage cannot admit membership. Registry closure, conditioned support, exact mean particle mass, coupled thermal balance, collapse, and the planetary chain remain blocked until the species-forming dependency graph and separate global coverage authority close.
- **P-PHYSICAL-LAW-PREMISE-AUTHORITY.** FIRST SCIENTIFIC MINT LANDED; SPECIES-FORMING GRAPH BLOCKED: independent sorted-vector and map-reconstruction checkers derive the sealed `eps_0 = e^2 / (2 * alpha * h * c)` execution coordinate from floor ancestry, independently derive its claim-local scale under the Q32.32 significand floor and signed-i128 bound, and bind both implementation identities, both results, two complete 12-case mutation transcripts, and an explicit agreement decision into a v2 pair receipt. The private `VerifiedDerivedContent` capability passes exact claim-role-content selection and remains scoped to this relation with `species_membership_authority=false`, `global_physical_premise_coverage=false`, and `authority_effect=none`. It does not activate the blocked global SI scale policy or admit an electromagnetic field, operator, sector, excitation, support value, or species. Exact zero still requires matching subject, excluded term, symmetry, applicability, and exclusion evidence. Unrelated unfamiliar candidates remain extension-monotone. Acceptance: repeat the claim-scoped route for one species-forming field, operator, state, sector, validity, constraint, excitation, or exact-zero premise; every irreducible survivor must complete derivation exhaustion, Buckingham Pi, Gap Law including Chaos Protocol, Residual Law, one unique owner-reviewed residual slot, and its own independent authority pair before the physical registry may admit a member.
- **P-PERIODICITY-EMERGENCE.** CHAIN MAPPED; FIRST DERIVED PREMISE, LAW SELECTION, AND EXACT THRESHOLD DIAGNOSTICS LANDED; BLOCKED ON SPECIES-FORMING LAW AUTHORITY: `docs/PERIODICITY_EMERGENCE_PIPELINE.md` separates universal law authority, primitive excitations, composite and nuclear bound states, element identity, atomic spectra, periodicity projection, contingent isotope abundances, local atomic weights, chemistry, and material consumers. The live `eps_0` relation proves one narrow claim-scoped derived mint but does not admit an electromagnetic sector or excitation. The selector rejects shape and cardinality fallback, and the threshold pair classifies exact solver-produced level bands while preserving overlap and open channels. The unfamiliar-material acceptance canary requires a lawfully derived many-body phase and material response without a terrestrial identity or desired-property feedback. The terrestrial table remains active-candidate validation only. Acceptance: admitted species-forming law premises and primitive registry; solver, convergence, validity, conservation, complete-channel, decay, and uncertainty receipts; live independently checked bound-state authority; derived core and isotope membership; certified atomic spectra and shell projection; nucleosynthesis-derived local abundance support; no familiar exception list, caller table, or viewer-selected chemistry.
- **P-CAUSAL-IDENTITY-INDEPENDENCE.** BLOCKED BEFORE THE FIRST REALIZATION: `ContingencyDrawId` and `RealizationId` currently inherit a transcript event ordinal. Acceptance: domain-separated identity from the admitted measure receipt, floor or universe binding, exact sampled coordinate, and canonical causal parents; transcript position remains presentation metadata and inserting an audit-only record cannot change causal identity.
- **P-VARIABLE-ROLE-BOUND-FLOOR.** BLOCKED FOR COSMOLOGICAL AND ALIEN EXTENSION: preflight, typed magnitudes, transcript projection, and species analysis require the exact familiar `alpha`, `G`, and `m_e` symbol set and cardinality. Acceptance: variable-membership sealed floor projection keyed by admitted semantic role, dimension, content identity, and receipt, with symbols confined to display and each analysis consuming the passed binding rather than resealing a familiar catalog.
- **P-PHYSICAL-DIMENSION-AXIS-AUTHORITY.** BLOCKED BEFORE UNFAMILIAR-AXIS ACTIVATION: the physical-registry codec is variable-cardinality and its tests accept lawful unfamiliar axes, but production currently supplies only the seven domain-separated SI identities. Before any new axis can enter an admitted artifact, implement a repository-owned semantic basis registry plus an independent admission pair that binds identity, meaning, algebra, and compatibility. Acceptance: an unknown, renamed, duplicated, zero, or semantically substituted axis refuses under both paths without closing the basis at seven.
- **P-DYNAMIC-SQRT-KERNEL-AUTHORITY.** BLOCKED OUTSIDE THE CERTIFIED CONSTANT FOLD: the complete plasma constant square root now has an independent whole-expression certificate and preserves Q32.32 bits. The later state-dependent carrier-density `Fixed::sqrt` remains under the blocked deterministic-kernel claim. Acceptance: independently certify its complete domain, terminal rounding, overflow and refusal behavior, and CPU or GPU parity before causal use.
- **P-BOUNDED-DECIMAL-ADMISSION.** BLOCKED OUTSIDE CERTIFIED CANONICAL INGRESS: the certified floor path already limits source bytes, decimal digits, and net powers before large-integer construction, but the common raw decimal parser remains public and other text or TOML loaders can reach it without a shared resource contract. Define one derive-first admission boundary with explicit byte, digit, exponent, allocation, and refusal limits before `BigUint` construction, then migrate each raw loader without changing admitted canonical bytes. A caller-authored decimal, cited table, or loader-specific default cannot enter the main runpath through this work.
- **P-STAGE1-ADAPTERS.** BLOCKED ON P-STELLAR-BIRTH-MEASURE: the typed measure seam is explicit and fail-closed; no raw mechanism is reachable yet. After both measure obligations close, adapt retained cloud-core thermal, collapse-interval, centrifugal-radius, disk, and embryo-system mechanisms through SI-native typed receipts without exposing old `Fixed` bundles. Acceptance: every absent measure, residual, or out-of-domain branch returns a named refusal; no familiar identity or viewer path selects a result.
- **P-GPU-BIT-PARITY.** BLOCKED ON TYPED STAGE ADAPTERS: preserve variable-scale floor values until an explicit checked projection into Q32.32, then prove each canonical GPU kernel bit-identical to the CPU `Fixed` oracle on every enabled target backend. Acceptance: no production-causal `f32` or `f64`, overflow and refusal semantics match exactly, device tests do not silently count a skipped backend as parity evidence, and GPU scheduling cannot change event or reduction order.
- **P-STAR-DISK-SYSTEM.** Wire Stage 1 from the admitted birth measure through persistent elemental inventory, pre-main-sequence state, collapse, disk structure, branch dispatch, and embryo system. Acceptance: no solar anchor, familiar composition, or fixed body count selects the result; current receipt: named Stage 1 refusal.
- **P-ASSEMBLY-COMPOSITION.** Wire Stage 2 while preserving body identity, complete elemental and phase inventories, mass, and angular momentum through merges and giant dispatch. Acceptance: replay reconstructs every body and no composition is rebuilt from a planet-class label.
- **P-ORBITAL-SECULAR-MOONS.** Wire the former `R-CELESTIAL-SECULAR` work as Stage 3 over the full body set. Acceptance: secular evolution, relativity, moon survival, and tidal state advance every applicable body rather than a selected Earth analogue.
- **P-YOUNG-THERMAL-MATERIALS.** Wire Stage 4 from the formation state into material, thermal, differentiation, crust, and mantle state. Acceptance: property ladders report overlaps and refusals; missing fusion volume is absence rather than zero; no local-Milky-Way or terrestrial composition default enters.
- **P-GEODYNAMICS-DEEPTIME.** Wire Stage 5 per body, including stellar aging, interiors, crust transactions, impacts, volcanism, atmosphere sources, and surface recycling. Acceptance: every body retains state and lineage, all transfers conserve, and no viewer call advances time.
- **P-INTEGRATION-TERMINATION.** Derive physical integration error control and termination from the system. Acceptance: no authored tick, age, frame cadence, target epoch, or hindcast endpoint controls physics; a missing derivation refuses.
- **P-LOAD-FLEXURE.** Complete Stage 6 with distributed two-dimensional loads, typed load geometry, finite-disc treatment, and the `D -> 0` limit. Acceptance: one-dimensional diagnostics cannot stand in for the surface field; conservation and moment residuals are explicit.
- **P-ATMOSPHERE-HYDROLOGY-RECYCLING.** Extract and wire abiotic atmosphere, hydrology, volatile partitioning, weathering, erosion, and recycling without importing organism-facing state from parked sim. Acceptance: reservoirs and fluxes conserve per body and admit non-Earth chemistry.
- **P-SNAPSHOT-TRANSPORT.** IN PROGRESS: the planet owns a sealed borrowed observation token for either an immutable completed snapshot or the exact refusal receipt, and the viewer now projects that token into read-only transcript, stage, refusal, and analysis scenes. Define the completed snapshot schema and save transport without exposing `PlanetRunOutcome` or a causal runner edge. The current seven stages are one Solar-system route, not universal completion authority: activation also requires a sealed route identity, generic obligation applicability, typed not-applicable evidence, and alternate-origin substitutions. Acceptance: the viewer can inspect a refusal or render and sample presentation time, but cannot mutate state, advance physical time, promote a refusal, supply a model value, or treat an inapplicable familiar stage as a universal requirement.
- **P-VISUALIZATION-REPRESENTATION.** DIMENSIONAL SCALE VIEW LANDED, ARTIFACT IDENTITY NEXT: the [canonical derivation and observer-only viewer pipeline](docs/DERIVATION_AND_VIEWER_PIPELINE.md) records the ordered design. A dedicated dimensional scene now exposes phenomenon-local exact input exponents, support identities, missing dependencies, and dropped mechanisms, including the unclosed `G^-1 * sound_speed^3` collapse scale. The viewer exposes exact transcript order, the declared floor-event envelope, exact noncausal SI representation integers, stage status, refusal leaves, obligations, non-admitting census and species-analysis summaries, and the complete physical-root agreement frontier. The frontier includes schema and checker identities, result and resource digests, canary binding, counts, membership-neutral scope, exact refusal, and open obligations. It is borrowed from the planet-owned analysis and cannot alter a receipt or causal outcome. Floor quantity, value bits, tier, and provenance stay explicitly opaque where the permitted viewer boundary has no typed value-record view. Next define domain-separated causal and projection artifact keys, immutable parent-bound materialization tickets, and a manifest hierarchy from system to bodies and fields before adding a content-addressed store. Physical partitioning and resolution must derive from convergence, residual, conservation, and validity proofs. Camera-driven LOD, floating-point conversion, color, meshes, and cache policy begin only after the observation boundary and cannot return to physical bytes. Acceptance: query, camera, style, taxonomy, hardware budget, cache state, and search budget cannot select a realization, coordinate, causal resolution, refusal, or artifact content; unfamiliar lawful carriers remain inspectable without a familiar class.
- **P-DIRECT-EVIDENCE.** IN PROGRESS: one public typed runpath test now proves the exact three `[M]` floor records and custody, derive-first exhaustion, Gap, Chaos, and Residual evidence; the `[D]` `eps_0` ancestry hop; the six-event and seven-stage refusal chain; and absence of realization, snapshot, `[W]`, and `[X]`. Still add property, deep-time, moment-equivalence, and flexure goldens with conservation residuals and perturbation checks. Acceptance: every canonical subsystem is judgeable without the viewer or a parked fixture.
- **P-GRAB-BAGS.** NO-MOVE MAP LANDED: [the canonical file-to-invariant decomposition map](docs/working/CANONICAL_FILE_INVARIANT_MAP.md) records facade, shared-source, path-bound gate, evidence, and split-order constraints. After the remaining direct goldens exist, split canonical files by invariant starting with moment equivalence, geodynamics, deep time, and astro last; keep formula repair in separate reviewed slices. Acceptance: API and gate coverage remain equivalent, exact receipt and replay bytes remain stable, and no move broadens authority.
- **P-ABIOTIC-EVIDENCE.** Work `docs/working/ABIOTIC_EVIDENCE_DEBT.md` candidate by candidate: optics and redox, condensation and crust, melt thermodynamics, transfer, continuous geodynamics, and BHAC15. Acceptance: each candidate derives, completes a machine-resolvable admission receipt in one unique residual slot, or remains a named refusal.
- **P-SOURCE-DEBT.** Clear uncovered collections, stale waivers, custody transitions, duplicate checksums, and independent validations under `VENDORING_CHECKLIST.md`. Acceptance: held evidence closes custody only; every value still derives or passes floor admission.

## Completed structural slices in this worktree

- **P-INDEPENDENT-AUTHORITY-WATCHDOG.** DONE IN WORKTREE: a closed 18-row authority inventory records nine active independently paired claims, eight blocked claims, and one non-authoritative diagnostic. Schema-first and profile-first implementations pin each complete semantic profile and closure; Stone 0 separately pins the bootstrap gate fields and directly executes the inventory, build-wiring, fixed-math, source-evidence, and external-claim gates. The root pair independently extracts and projects the sealed floor into membership-neutral physical-coordinate roots under success receipt v5 or refusal receipt v5. The ninth active pair binds the three measured declarations to exact rows in the pinned NIST CODATA 2018 source through independent fixed-column and record-scan implementations; its offline v2 transcripts bind exact concrete mutations and stable refusal codes, while the explicit online audit separately proves live/archive byte equality. It proves custody and transcription agreement, not scientific truth. Each root verifier re-extracts its sealed packet and recomputes its local resource and canary attestations before accepting the final private manifest. The fixed-math v3 receipt parses roles only from eligible production syntax, exact-matches every selected occurrence by path, line, value, and multiplicity, rejects Rust configuration variability outside root test modules, normalizes every admitted integer and ASCII-whitespace form, and binds the three non-Q32 GPU exemptions to complete roles in `fixed_ln`, `fixed_sin`, and `fixed_cos`. Thirty-six fixed-math canaries and the root projector's separate mutation suite cover their enrolled claims. Native Windows and Linux receipt bytes agree. This proves structural agreement over named bytes, not scientific truth or completeness outside the enrolled owner boundary.
- **P-CERTIFIED-FORMULA-PROJECTION.** DONE IN WORKTREE AS NARROW ARITHMETIC AUTHORITY: independent exact interval parsers certify one resource-bounded formula and selected integer, while independently bound opaque factors prevent repeated invariant transcendental work in sweeps. Recursive producer v5 and shunting-yard watchdog v6 independently bind exponentiation more tightly than unary signs and require one unsigned integer literal as every exponent operand. Grouped negative bases, explicit grouping, signed multiplicative operands, and chained signs are direct success canaries; signed, grouped, decimal, and chained exponent operands are direct refusal canaries. A distinct v2 producer and watchdog also certify the terminal floor cell of one strictly positive complete square-root expression without an intermediate fixed-point radicand. Two separately authored nonrecursive production suites run before issuance and each issued certificate binds both cached canary attestations by schema, suite identity, case count, and full transcript digest. Raw coordinates still carry no physical ancestry and are forbidden in canonical planet source. The SI execution table, representation-scale policy, dynamic square-root kernels, wide-integer arithmetic, and whole-domain CPU/GPU kernels remain blocked claims.
- **P-EXTERNAL-ADVERSE-CLAIM-GOVERNANCE.** DONE IN WORKTREE AS FAIL-CLOSED RELEASE GOVERNANCE: exact text and destination, five independent evidence lineages beyond the subject, a private dossier digest, an unrevoked scoped human signature, and producer/watchdog agreement are required before any adverse publication or author contact. No release row exists, so no outbound adverse action is authorized.

- **P-VERIFICATION-OPERATIONS.** DONE IN WORKTREE AS NONPHYSICAL TOOLING: eligible gates with a closed repository read-set produce bounded receipts tied to exact inputs, commands, platform, semantic environment, and toolchain; custody, external-tool, and Stone 0 boundaries remain uncached. A portable bounded process lock prevents concurrent runners from stampeding the same gate; a content-hash follower rechecks and consumes the leader's receipt, while uncached gates serialize and rerun. Independent gates run in bounded parallel inside fixed authority phases, and a non-certifying fast loop preserves the full PR receipt. One `civsim-stone0-build` anchor runs Stone 0 per Cargo graph, both canonical consumers compile against its success marker, and an independent producer/watchdog pair checks that wiring. Direct WSL and Windows-bridge Cargo routes share native-ext4 per-clone targets under one global marked-target budget, with exact marker checks, symlink refusal, shared live-build and worktree-lock protection, tomb recovery, unthrottled post-Cargo enforcement, bounded maintenance logs, and online trim. This changes verification scheduling and disposable build storage only; it supplies no physical value, proof, admission, closure, or readiness.
- **P-PARKED-COMPAT-PARITY.** DONE IN WORKTREE AS NONCANONICAL TOOLING: the parked simulation privately includes the retained floor bridge required by its shared-source compatibility modules, derives the molar gas constant through the exact SI representation view, and threads the sealed execution capability into the legacy viewer's polarizability diagnostic. The complete parked test command passes. This does not admit a parked package, value, viewer, biology mechanism, or civilization mechanism into the canonical runpath and supplies no physical-readiness evidence.
- **P-FLOOR-NORMALIZATION.** DONE IN WORKTREE: the physical floor contains only the three measured invariant coordinates `alpha`, `G`, and `m_e`. Exact SI definitions are noncausal representation metadata with independent fingerprints and no provenance mark. `eps_0` derives from the three physical coordinates and exact SI definitions; `sigma`, `R`, and the atomic-volume conversion derive from exact SI definitions alone. Their reference decimals cannot supply runtime values.
- **P-FLOOR-REPLAY.** DONE IN WORKTREE: declaration tables cannot authorize themselves. Physical-floor authority v5 binds independent floor-catalog admission v3, derive-first receipt pins, the Pi watchdog, exact fixed-math evidence, and CODATA row-evidence pair v2. Admission v3 reconstructs six domain-separated route receipts per leaf for derivation exhaustion, Buckingham Pi, Gap Law, Chaos Protocol, Residual Law, and the residual slot, then requires an explicit owner-admission record over that exact tuple; changed prose, identity, or owner receipt is a typed refusal. The CODATA pair executes 27 mutation transcripts whose exact changed inputs and detector-specific refusal codes are receipt-bound. Catalog arrival order is canonicalized by identity and does not become physical authority. Every measured floor event carries source, uncertainty, projection, and complete exhaustion receipts. `TranscriptSchema::V9` remains preserved and distinct while current transcripts select `TranscriptSchema::V10`; the V10 stream separates the representation receipt from the three-entry physical floor, emits derived `eps_0` with exact ancestry and complete Chaos Protocol evidence, and run receipt v12 remains fail-closed with the exact Stage 1 leaf frontier, census v4, and species derivation analysis v3 including the root success receipt v5 or refusal receipt v5. A failed representation projection emits a value-free V1 unavailable status and preserves every prior structural refusal.
- **P-STELLAR-DIMENSIONAL-CENSUS.** DONE IN WORKTREE AS PARTIAL V4: checked exact rational RREF computes rank, pivots, free columns, target projections, nonzero dimensional support, and primitive integer null-space bases over the seven SI dimensions. The public dense analyzer refuses with `BasisCellCapacityExceeded` before its worst-case augmented basis exceeds 1,048,576 cells; this is a nonphysical execution bound, and a larger lawful census requires a partitioned or sparse authority path. A modular v2 structure adds value-free component and species registry contracts, six shared index domains, eleven carrier schemas, material and time histories, and the open stellar-state contract. Its rule identities require convergence-derived resolution, named capacity refusal, serialization-only ordinals, and permutation-equivariant component semantics from future implementations; the current schema does not perform those operations. The Stage 1 joint-measure refusal bitstreams 31 value-free variables, seven local phenomenon matrices, target-reachability attempts, missing dependencies, dropped mechanisms, and seven declared coverage gaps. Immutable typed planet views and the modular viewer expose the same details without granting construction authority, including the exact `G^-1 * sound_speed^3` collapse scale and its open similarity-eigenstructure and boundary-measure seams. It has `closure_effect=none` and `coverage_claim=false`; the coordinate-law leaf carries no census and both production proof capabilities remain absent.
- **P-STELLAR-STATE-CLASSIFICATION.** DONE IN WORKTREE AS VALUE-FREE V1 CONTRACTS: five focused modules define complete stellar-state history, open typed coordinates, pre-seal interaction-sector admission, derive-first physical predicates and unique conservative mechanism composition, and a total read-only presentation projection. Named star classes and hypothetical sectors are generality tests only and do not appear as causal schema members. A fully admitted unfamiliar sector uses the identity-blind path; an absent or label-only sector refuses. The new joint-measure obligation and coverage gap keep every realized behavior open.
- **P-STELLAR-SPECIES-REDUCER.** DONE IN WORKTREE AS A SEALED BYTE-NEUTRAL KERNEL: one private module salvages exact complete-support verification and the number-fraction-weighted mean particle mass without a Solar table, named species branch, molecular switch, implicit normalization, or float. It is unreachable in production, its resolver returns `None`, and its authority tokens do not yet bind physical evidence. Acceptance for this slice is structural correctness and non-reachability, not species-registry or Stage 1 closure.
- **P-STELLAR-SPECIES-AUTHORITY-ANALYSIS.** DONE IN WORKTREE AS NON-ADMITTING V3 WITH WATCHDOG V4: the sealed analysis binds the audited floor digest, exact `m_e` compatibility anchor, structure and open registry schema identities, reducer law, checker version, and only the first executable live physical-registry refusal. It invokes the paired floor-coordinate projector, verifies root success receipt v5 or refusal receipt v5, and serializes three scalar coordinates, one membership-neutral mass projection, zero members, the exact refusal, and its six live obligations. `frontier_completeness_claim=false`; authored downstream guesses do not masquerade as an exhaustive frontier. It emits no support, mass result, value, residual slot, membership authority, or closure.
- **P-STELLAR-SPECIES-PACKET.** DONE IN WORKTREE AS A BOUNDED STRUCTURAL PAIR: private producer and watchdog algorithms independently validate canonical content-derived descriptors, complete positive or proved-exact-zero dispositions, exact unit normalization, ancestry and replay bindings, and finite member, rational, intermediate, operation, and byte domains. Synthetic unfamiliar and massless fixtures exercise agreement and mutation refusals. The pair shares only types, exact integer arithmetic, and a low-level SHA-256 byte primitive. It creates no member, physical proof, authority token, value, ledger event, residual slot, transcript field, or production resolver path, so `planet.species-state-support` remains blocked.
- **P-STELLAR-SPECIES-MASS-STATE-AUTHORITY.** DONE IN WORKTREE AS A CONDITIONAL NON-ADMITTING V3 PAIR WITH LIVE FLOOR ROOTS: a bottom-up producer and top-down watchdog independently validate content-bound admitted artifact graphs, exact rational expressions, a bounded variable-cardinality dimension basis, generic physical descriptors and role-to-target relations, constraint laws, data-defined species derivations, exact positive rest mass or proved masslessness, complete registry closure, and bounded canonical bytes. Producer v5 bounds left-to-right powers and performs linear indegree closure; watchdog v4 retains right-to-left rational powers, DFS, reverse tracing, and a forward worklist. Separate resource-contract v2 encoders bind local caps and semantic-work v1, including exact rational, power, and `2V + 2E + D` closure costs. Direct in-bound, cap-boundary, permutation, and allocation canaries agree. A separate independent pair projects the sealed floor into three scalar roots and one membership-neutral pure-mass root without admitting a species. Before consumption, both final verifiers independently re-extract and reproject the sealed floor, reconnect local resource and canary attestations, and refuse omission, duplication, membership-scope escalation, canonical-byte mutation, and admission-receipt mutation. Production artifacts also require a private `VerifiedAdmissionCapability` bound to exact admission and the root pair receipt, so invented receipt fields cannot authorize later Derived or Irreducible artifacts. Synthetic unfamiliar, extra-axis, and massless graphs exercise the same generic physical-registry path through test-only exact capabilities. Production provides no species-forming descriptor or law, so the live repository returns exactly `no_admitted_species_derivation_rules` with zero members; agreement has `authority_effect=none`, cannot reach conditioned support or the reducer, and leaves `planet.species-state-support` blocked.
- **P-OBSERVER-ARTIFACT.** DONE IN WORKTREE AS A READ-ONLY SEAM: `PlanetRunOutcome` is a public query wrapper over private state, and `PlanetObservation` projects only a snapshot with its embedded completion receipt or a refusal receipt. The viewer receives that private-field token, never the causal outcome or runner. Complete-source boundary scans reject public outcome variants, fields, unit forms, and constructors as well as observation construction, mutation, ownership laundering, aliases, grouped imports, and multiline bypasses while preserving typed refusal inspection.
- **P-FLOOR-CAPABILITY.** DONE IN WORKTREE: the ten-identity SI representation view is noncausal and the fourteen-identity execution view requires the independently verified physical-floor capability plus its pinned v5 authority digest. A read-only identity-keyed floor view exposes every typed irreducible-route receipt, its owner-admission receipt, and its independent-watchdog receipt without permitting caller construction. Both execution and representation views have private values and read-only lookup. Physical folds across active physics and materials require the execution capability, while representation-only conversions require only the representation view.
- **P-PLANET-SUBSTRATE-PACKAGE.** DONE IN WORKTREE: fourteen retained planet and stellar modules live in the private `civsim-planet-substrate` package with 374 passing tests. `civsim-planet` still depends at runtime only on ledger and units.
- **P-GATE-AUTHORITY.** DONE IN WORKTREE: `scripts/gates.toml` declares 30 gates across seven tiers. CI, Stone 0, Stop, Just, Make, PowerShell, local CI, and tracked pre-push routes consume the same runner. Input bytes, modes, inventories, runner, and manifest are hashed; race, link, path, process, timeout, and UTF-8 canaries fail closed. Per-gate cross-process locks prevent duplicate build graphs from stampeding one leaf, and the shared Stone 0 build anchor runs one repository-wide scan per canonical Cargo graph. Stone 0 blocks every unrecognized authority failure, requires exact leaf and runner protocol markers before a policy finding can be override-eligible, treats every existing unmarked leaf failure as operational and non-overridable, and prevents a caller-selected secrets path from authenticating that caller's override. Its supported Linux and WSL route uses a root-controlled isolated Python interpreter, a launching-toolchain Rust path, scrubbed import and loader environment, one wrapper-completion receipt, and replay-shim sentinel canaries; native Windows fails closed and uses WSL. The pre-push credential history scan streams patterns through standard input so Git for Windows and Linux inspect the same commits without writing or exposing the pattern bytes.
- **P-WORLD-GPU-SPLIT.** DONE IN WORKTREE: retained physical spatial operators are separate from old worldgen, biome, scenario, and GPU presentation generation. Geology, solve, runout, impact, topology, generic relief, and render-neutral fields remain active; `WorldStructure`, authored biomes, seed-driven maps, camera and quadtree presentation, GPU worldgen, and globe presentation are parked.

## Parked queue pointer

The previous 145 KB live backlog is preserved byte-for-byte at
`parked/TODOS_LEGACY.md`. Do not copy its calibration values, scenarios,
biology mechanisms, or civilization targets into this queue.
