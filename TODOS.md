# Canonical planetary queue

This is the live queue injected by SessionStart. It contains only work on the
abiotic planet and stellar-system runpath. The preserved civilization,
biology, dawn, compose, scenario, and calibration queue is
`parked/TODOS_LEGACY.md`; it is historical and cannot supply canonical
inputs, evidence, or readiness.

Canonical value policy: the sealed absolute physics floor is the only source
of physical causal values. Exact SI numbers are immutable representation
coordinates, not caller inputs or physical degrees of freedom. The four tiers and seven provenance marks are accounting
only. Derive first. Every irreducible non-derived floor leaf, at any tier,
needs the complete derivation-exhaustion, Buckingham-Pi, Gap Law with typed
Chaos Protocol, and Residual Law receipt before admission. Otherwise the stage refuses. Written state and
contingency are generated only after both an admitted physical measure and an
approved realization-coordinate law exist.

## Canonical abiotic queue

- **P-STELLAR-BIRTH-MEASURE.** IN PROGRESS: the executable two-leaf closure evaluator, exact serialized frontier, typed multi-regime Chaos Protocol, hardened replayable floor, partial exact dimensional census, modular value-free structure and stellar-state schemas, private exact species-state reducer, authority-bound species derivation analysis, and bounded structural species packet pair are implemented at `docs/working/STELLAR_BIRTH_MEASURE_CONTRACT.md`, `docs/working/STELLAR_STATE_CLASSIFICATION_CONTRACT.md`, and `docs/working/PR215_LIVE_SOURCE_AUDIT.md`. The census binds 31 typed coordinates, seven phenomenon-local matrices, component and species registry contracts, six shared index domains, eleven carrier schemas, and open coordinate, variable dimension-basis, interaction-sector, physical-regime, and presentation registries to the sealed floor authority without adding values or closing a proof. The reducer checks exact weighted reduction over verified positive support, but its proof and authority tokens have no physical binding or production constructors and its resolver returns `None`. The analysis independently records zero realized members, zero verified support, no value, no residual slot, and open registry, state, sector, support, ancestry, resource, and projection proofs beneath the joint leaf. The packet pair checks bounded canonical descriptors and explicit positive or proved-zero support structurally, but cannot prove any member or physical ancestry. Physical registry membership, repository-owned mass, dimension, state, sector, validity, and dependency proof DAGs, registry closure, conditioned joint-measure authority, reference handling, convergence control, topology validation, and production projection remain open. Named stellar classes and unfamiliar or thaumic labels supply no physical law or dispatch authority. Next derive the authority-bound species mass and state registry, then coupled gas and dust thermal balance, equation of state, collapse measure, material mass and angular-momentum histories, multicenter circularization law, multiplicity interactions, spectral closure, tensor needs, dynamical regimes, and persistent disk state. Afterward derive or fully admit the opaque correlation-preserving joint physical measure and independently close the realization-coordinate law. Acceptance: both leaves close only from repository-owned typed proof artifacts with support, normalization, conditioning, uncertainty, Buckingham-Pi, dimensional closure, dependency admission, validity-domain and global-conservation proofs, Gap Law including Chaos Protocol, Residual Law, unique residual-slot receipts, absolute-floor and schema binding, semantic-checker version, dependency digest, open stellar-state coverage, and joint-measure compatibility, and without a caller, fixed hidden, operating-system, viewer, presentation identity, ordinal, or ordering seed; the coordinate law additionally proves totality and measure-consistent push-forward over joint support, otherwise the exact per-obligation frontier remains visible under the root refusal.
- **P-STELLAR-SPECIES-MASS-STATE-AUTHORITY.** NEXT PHYSICAL RUNG: replace the structural packet's opaque ancestry digests with repository-owned typed proof DAGs for physical content, exact rest mass and dimensions, state coordinates, active sectors, validity regimes, dependencies, and complete reachable-member closure. Independent producer and watchdog algorithms must attempt direct floor-property attribution, elementary-excitation derivation, and composite closure, then return `no_admitted_species_derivation_roots` with no coverage or authority effect on the present floor. `m_e` alone is not species-membership authority, an empty registry is not a complete empty universe, and a cited or familiar table is not an input route. Keep conditioned support, the reducer, Stage 1 proofs, transcript, and `planet.species-state-support` blocked. Acceptance: name-only `m_e`, cited masses, missing closure members, ancestry swaps, cycles, unproved massless states, schema or floor drift, and resource excess refuse; fully admitted unfamiliar and lawful massless synthetic proof graphs agree without minting production authority.
- **P-STAGE1-ADAPTERS.** BLOCKED ON P-STELLAR-BIRTH-MEASURE: the typed measure seam is explicit and fail-closed; no raw mechanism is reachable yet. After both measure obligations close, adapt retained cloud-core thermal, collapse-interval, centrifugal-radius, disk, and embryo-system mechanisms through SI-native typed receipts without exposing old `Fixed` bundles. Acceptance: every absent measure, residual, or out-of-domain branch returns a named refusal; no familiar identity or viewer path selects a result.
- **P-GPU-BIT-PARITY.** BLOCKED ON TYPED STAGE ADAPTERS: preserve variable-scale floor values until an explicit checked projection into Q32.32, then prove each canonical GPU kernel bit-identical to the CPU `Fixed` oracle on every enabled target backend. Acceptance: no production-causal `f32` or `f64`, overflow and refusal semantics match exactly, device tests do not silently count a skipped backend as parity evidence, and GPU scheduling cannot change event or reduction order.
- **P-STAR-DISK-SYSTEM.** Wire Stage 1 from the admitted birth measure through persistent elemental inventory, pre-main-sequence state, collapse, disk structure, branch dispatch, and embryo system. Acceptance: no solar anchor, familiar composition, or fixed body count selects the result; current receipt: named Stage 1 refusal.
- **P-ASSEMBLY-COMPOSITION.** Wire Stage 2 while preserving body identity, complete elemental and phase inventories, mass, and angular momentum through merges and giant dispatch. Acceptance: replay reconstructs every body and no composition is rebuilt from a planet-class label.
- **P-ORBITAL-SECULAR-MOONS.** Wire the former `R-CELESTIAL-SECULAR` work as Stage 3 over the full body set. Acceptance: secular evolution, relativity, moon survival, and tidal state advance every applicable body rather than a selected Earth analogue.
- **P-YOUNG-THERMAL-MATERIALS.** Wire Stage 4 from the formation state into material, thermal, differentiation, crust, and mantle state. Acceptance: property ladders report overlaps and refusals; missing fusion volume is absence rather than zero; no local-Milky-Way or terrestrial composition default enters.
- **P-GEODYNAMICS-DEEPTIME.** Wire Stage 5 per body, including stellar aging, interiors, crust transactions, impacts, volcanism, atmosphere sources, and surface recycling. Acceptance: every body retains state and lineage, all transfers conserve, and no viewer call advances time.
- **P-INTEGRATION-TERMINATION.** Derive physical integration error control and termination from the system. Acceptance: no authored tick, age, frame cadence, target epoch, or hindcast endpoint controls physics; a missing derivation refuses.
- **P-LOAD-FLEXURE.** Complete Stage 6 with distributed two-dimensional loads, typed load geometry, finite-disc treatment, and the `D -> 0` limit. Acceptance: one-dimensional diagnostics cannot stand in for the surface field; conservation and moment residuals are explicit.
- **P-ATMOSPHERE-HYDROLOGY-RECYCLING.** Extract and wire abiotic atmosphere, hydrology, volatile partitioning, weathering, erosion, and recycling without importing organism-facing state from parked sim. Acceptance: reservoirs and fluxes conserve per body and admit non-Earth chemistry.
- **P-SNAPSHOT-TRANSPORT.** IN PROGRESS: the planet owns a sealed borrowed observation token for either an immutable completed snapshot or the exact refusal receipt, and the viewer now projects that token into read-only transcript, stage, refusal, and analysis scenes. Define the completed snapshot schema and save transport without exposing `PlanetRunOutcome` or a causal runner edge. Acceptance: the viewer can inspect a refusal or render and sample presentation time, but cannot mutate state, advance physical time, promote a refusal, or supply a model value.
- **P-VISUALIZATION-REPRESENTATION.** FIRST SCENE SLICE LANDED, ARTIFACT IDENTITY NEXT: the [canonical derivation and observer-only viewer pipeline](docs/DERIVATION_AND_VIEWER_PIPELINE.md) records the ordered design. The viewer exposes exact transcript order, the declared floor-event envelope, exact noncausal SI representation integers, stage status, refusal leaves, obligations, and non-admitting census and species-analysis summaries. Floor quantity, value bits, tier, and provenance stay explicitly opaque because the permitted viewer boundary has no typed value-record view; do not infer them from event names or duplicate the ledger taxonomy. If needed, add the narrowest planet-owned sealed read-only value view. Then define domain-separated causal and projection artifact keys, immutable parent-bound materialization tickets, and a manifest hierarchy from system to bodies and fields before adding a content-addressed store. Physical partitioning and resolution must derive from convergence, residual, conservation, and validity proofs. Camera-driven LOD, floating-point conversion, color, meshes, and cache policy begin only after the observation boundary and cannot return to physical bytes. Acceptance: query, camera, style, taxonomy, hardware budget, cache state, and search budget cannot select a realization, coordinate, causal resolution, refusal, or artifact content; unfamiliar lawful carriers remain inspectable without a familiar class.
- **P-DIRECT-EVIDENCE.** IN PROGRESS: one public typed runpath test now proves the exact three `[M]` floor records and custody, derive-first exhaustion, Gap, Chaos, and Residual evidence; the `[D]` `eps_0` ancestry hop; the six-event and seven-stage refusal chain; and absence of realization, snapshot, `[W]`, and `[X]`. Still add property, deep-time, moment-equivalence, and flexure goldens with conservation residuals and perturbation checks. Acceptance: every canonical subsystem is judgeable without the viewer or a parked fixture.
- **P-GRAB-BAGS.** NO-MOVE MAP LANDED: [the canonical file-to-invariant decomposition map](docs/working/CANONICAL_FILE_INVARIANT_MAP.md) records facade, shared-source, path-bound gate, evidence, and split-order constraints. After the remaining direct goldens exist, split canonical files by invariant starting with moment equivalence, geodynamics, deep time, and astro last; keep formula repair in separate reviewed slices. Acceptance: API and gate coverage remain equivalent, exact receipt and replay bytes remain stable, and no move broadens authority.
- **P-ABIOTIC-EVIDENCE.** Work `docs/working/ABIOTIC_EVIDENCE_DEBT.md` candidate by candidate: optics and redox, condensation and crust, melt thermodynamics, transfer, continuous geodynamics, and BHAC15. Acceptance: each candidate derives, completes a machine-resolvable admission receipt in one unique residual slot, or remains a named refusal.
- **P-SOURCE-DEBT.** Clear uncovered collections, stale waivers, custody transitions, duplicate checksums, and independent validations under `VENDORING_CHECKLIST.md`. Acceptance: held evidence closes custody only; every value still derives or passes floor admission.

## Completed structural slices in this worktree

- **P-INDEPENDENT-AUTHORITY-WATCHDOG.** DONE IN WORKTREE: a closed 16-row authority inventory records seven active independently paired claims, eight blocked claims, and one non-authoritative diagnostic. Schema-first and profile-first implementations pin each complete semantic profile and closure; Stone 0 separately pins the bootstrap gate fields and directly executes the inventory, build-wiring, fixed-math, and external-claim gates. The fixed-math v3 receipt parses roles only from eligible production syntax, exact-matches every selected occurrence by path, line, value, and multiplicity, rejects Rust configuration variability outside root test modules, normalizes every admitted integer and ASCII-whitespace form, and binds the three non-Q32 GPU exemptions to complete roles in `fixed_ln`, `fixed_sin`, and `fixed_cos`. Thirty-six canaries cover the reproduced lexical, role-substitution, source-eligibility, exemption-ownership, and line-ending cases. Its two implementations, the Rust floor consumer, and the inventory v3 pair map CRLF checkouts to canonical Git LF text and reject bare carriage returns. Native Windows and Linux receipt bytes agree. This proves structural agreement over named bytes, not scientific truth or completeness outside the enrolled owner boundary.
- **P-CERTIFIED-FORMULA-PROJECTION.** DONE IN WORKTREE AS NARROW ARITHMETIC AUTHORITY: independent exact interval parsers certify one resource-bounded formula and selected integer, while independently bound opaque factors prevent repeated invariant transcendental work in sweeps. Raw coordinates still carry no physical ancestry and are forbidden in canonical planet source. The SI execution table, representation-scale policy, wide-integer arithmetic, and whole-domain CPU/GPU kernels remain blocked claims.
- **P-EXTERNAL-ADVERSE-CLAIM-GOVERNANCE.** DONE IN WORKTREE AS FAIL-CLOSED RELEASE GOVERNANCE: exact text and destination, five independent evidence lineages beyond the subject, a private dossier digest, an unrevoked scoped human signature, and producer/watchdog agreement are required before any adverse publication or author contact. No release row exists, so no outbound adverse action is authorized.

- **P-VERIFICATION-OPERATIONS.** DONE IN WORKTREE AS NONPHYSICAL TOOLING: eligible gates with a closed repository read-set produce bounded receipts tied to exact inputs, commands, platform, semantic environment, and toolchain; custody, external-tool, and Stone 0 boundaries remain uncached. A portable bounded process lock prevents concurrent runners from stampeding the same gate; a content-hash follower rechecks and consumes the leader's receipt, while uncached gates serialize and rerun. Independent gates run in bounded parallel inside fixed authority phases, and a non-certifying fast loop preserves the full PR receipt. One `civsim-stone0-build` anchor runs Stone 0 per Cargo graph, both canonical consumers compile against its success marker, and an independent producer/watchdog pair checks that wiring. Direct WSL and Windows-bridge Cargo routes share native-ext4 per-clone targets under one global marked-target budget, with exact marker checks, symlink refusal, shared live-build and worktree-lock protection, tomb recovery, unthrottled post-Cargo enforcement, bounded maintenance logs, and online trim. This changes verification scheduling and disposable build storage only; it supplies no physical value, proof, admission, closure, or readiness.
- **P-PARKED-COMPAT-PARITY.** DONE IN WORKTREE AS NONCANONICAL TOOLING: the parked simulation privately includes the retained floor bridge required by its shared-source compatibility modules, derives the molar gas constant through the exact SI representation view, and threads the sealed execution capability into the legacy viewer's polarizability diagnostic. The complete parked test command passes. This does not admit a parked package, value, viewer, biology mechanism, or civilization mechanism into the canonical runpath and supplies no physical-readiness evidence.
- **P-FLOOR-NORMALIZATION.** DONE IN WORKTREE: the physical floor contains only the three measured invariant coordinates `alpha`, `G`, and `m_e`. Exact SI definitions are noncausal representation metadata with independent fingerprints and no provenance mark. `eps_0` derives from the three physical coordinates and exact SI definitions; `sigma`, `R`, and the atomic-volume conversion derive from exact SI definitions alone. Their reference decimals cannot supply runtime values.
- **P-FLOOR-REPLAY.** DONE IN WORKTREE: declaration tables cannot authorize themselves; independent fingerprints seal all physical roots, representation definitions, execution relations, and typed Chaos Protocol receipt branches. Every measured floor event carries source, uncertainty, projection, and complete exhaustion receipts. One sealed v1 authority digest binds those registries and pins. Transcript schema major 9 separates the representation receipt from the three-entry physical floor, emits derived `eps_0` with exact ancestry and complete Chaos Protocol evidence, and run receipt v11 remains fail-closed with the exact Stage 1 leaf frontier, its non-admitting census v4, and its non-admitting species derivation analysis v1.
- **P-STELLAR-DIMENSIONAL-CENSUS.** DONE IN WORKTREE AS PARTIAL V4: checked exact rational RREF computes rank, pivots, free columns, target projections, nonzero dimensional support, and primitive integer null-space bases over the seven SI dimensions. A modular v2 structure adds value-free component and species registry contracts, six shared index domains, eleven carrier schemas, material and time histories, and the open stellar-state contract. Its rule identities require convergence-derived resolution, named capacity refusal, serialization-only ordinals, and permutation-equivariant component semantics from future implementations; the current schema does not perform those operations. The Stage 1 joint-measure refusal bitstreams 31 value-free variables, seven local phenomenon matrices, target-reachability attempts, missing dependencies, dropped mechanisms, and seven declared coverage gaps. Immutable typed views expose the same details without granting construction authority. It has `closure_effect=none` and `coverage_claim=false`; the coordinate-law leaf carries no census and both production proof capabilities remain absent.
- **P-STELLAR-STATE-CLASSIFICATION.** DONE IN WORKTREE AS VALUE-FREE V1 CONTRACTS: five focused modules define complete stellar-state history, open typed coordinates, pre-seal interaction-sector admission, derive-first physical predicates and unique conservative mechanism composition, and a total read-only presentation projection. Named star classes and hypothetical sectors are generality tests only and do not appear as causal schema members. A fully admitted unfamiliar sector uses the identity-blind path; an absent or label-only sector refuses. The new joint-measure obligation and coverage gap keep every realized behavior open.
- **P-STELLAR-SPECIES-REDUCER.** DONE IN WORKTREE AS A SEALED BYTE-NEUTRAL KERNEL: one private module salvages exact complete-support verification and the number-fraction-weighted mean particle mass without a Solar table, named species branch, molecular switch, implicit normalization, or float. It is unreachable in production, its resolver returns `None`, and its authority tokens do not yet bind physical evidence. Acceptance for this slice is structural correctness and non-reachability, not species-registry or Stage 1 closure.
- **P-STELLAR-SPECIES-AUTHORITY-ANALYSIS.** DONE IN WORKTREE AS NON-ADMITTING V1: a sealed analysis authority binds the audited floor digest, exact `m_e` coordinate, structure and open registry schema identities, reducer law, checker version, and a deterministic open-proof frontier. It emits no member, support, mass result, value, residual slot, or closure. Its explicit conditioned-support semantics and exact-resource gaps block activation of the dormant verifier.
- **P-STELLAR-SPECIES-PACKET.** DONE IN WORKTREE AS A BOUNDED STRUCTURAL PAIR: private producer and watchdog algorithms independently validate canonical content-derived descriptors, complete positive or proved-exact-zero dispositions, exact unit normalization, ancestry and replay bindings, and finite member, rational, intermediate, operation, and byte domains. Synthetic unfamiliar and massless fixtures exercise agreement and mutation refusals. The pair shares only types, exact integer arithmetic, and a low-level SHA-256 byte primitive. It creates no member, physical proof, authority token, value, ledger event, residual slot, transcript field, or production resolver path, so `planet.species-state-support` remains blocked.
- **P-OBSERVER-ARTIFACT.** DONE IN WORKTREE AS A READ-ONLY SEAM: `PlanetRunOutcome` is a public query wrapper over private state, and `PlanetObservation` projects only a snapshot with its embedded completion receipt or a refusal receipt. The viewer receives that private-field token, never the causal outcome or runner. Complete-source boundary scans reject public outcome variants, fields, unit forms, and constructors as well as observation construction, mutation, ownership laundering, aliases, grouped imports, and multiline bypasses while preserving typed refusal inspection.
- **P-FLOOR-CAPABILITY.** DONE IN WORKTREE: the ten-identity SI representation view is noncausal and the fourteen-identity execution view requires the independently verified physical-floor capability plus its pinned v1 authority digest. Both views have private values and read-only lookup. Physical folds across active physics and materials require the execution capability, while representation-only conversions require only the representation view.
- **P-PLANET-SUBSTRATE-PACKAGE.** DONE IN WORKTREE: fourteen retained planet and stellar modules live in the private `civsim-planet-substrate` package with 372 passing tests. `civsim-planet` still depends at runtime only on ledger and units.
- **P-GATE-AUTHORITY.** DONE IN WORKTREE: `scripts/gates.toml` declares 29 gates across seven tiers. CI, Stone 0, Stop, Just, Make, PowerShell, local CI, and tracked pre-push routes consume the same runner. Input bytes, modes, inventories, runner, and manifest are hashed; race, link, path, process, timeout, and UTF-8 canaries fail closed. Per-gate cross-process locks prevent duplicate build graphs from stampeding one leaf, and the shared Stone 0 build anchor runs one repository-wide scan per canonical Cargo graph. Stone 0 blocks every unrecognized authority failure, requires exact leaf and runner protocol markers before a policy finding can be override-eligible, treats every existing unmarked leaf failure as operational and non-overridable, and prevents a caller-selected secrets path from authenticating that caller's override. The pre-push credential history scan streams patterns through standard input so Git for Windows and Linux inspect the same commits without writing or exposing the pattern bytes.
- **P-WORLD-GPU-SPLIT.** DONE IN WORKTREE: retained physical spatial operators are separate from old worldgen, biome, scenario, and GPU presentation generation. Geology, solve, runout, impact, topology, generic relief, and render-neutral fields remain active; `WorldStructure`, authored biomes, seed-driven maps, camera and quadtree presentation, GPU worldgen, and globe presentation are parked.

## Parked queue pointer

The previous 145 KB live backlog is preserved byte-for-byte at
`parked/TODOS_LEGACY.md`. Do not copy its calibration values, scenarios,
biology mechanisms, or civilization targets into this queue.
