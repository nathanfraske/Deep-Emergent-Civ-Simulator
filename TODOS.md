# TODOS.md: Live Backlog Mirror

The working view of the research backlog (audit Section 3) and the reserved-values queue. Updated every session: resolved items move to the resolved list, new flags are added, order is adjusted. Each open item is one bullet, identifier first, then the one-line question, then readiness and couplings, so the backlog tool reads it deterministically. The owner sets the order; the readiness tags are guidance.

Counts: 28 resolved, 36 open (R-DIM-HOMO flagged 2026-07-01 as the dimensional-homogeneity load check for laws (Part 58), surfaced from the hardcoded anti-steering guards now built in `crates/physics/tests/steering.rs` (substance content-address label-blindness, no-float-in-canon, provenance-required, symmetric-kernel no-hidden-bias); the R-GPU-CANON-PIN proposal is produced (`docs/working/GPU_CANON_PIN_PROPOSAL.md`, thirteen red findings folded) and awaiting sign-off; R-AGENT-EXEC, the event-driven agent execution model, flagged 2026-07-01 at design.md Part 57 with a scoping pass in `docs/event_driven_execution_research.md`, audit Section 2 and the Section 3 engine-fidelity subsection, the breadth-axis companion to R-TEMPORAL-LOD sharing one event-queue substrate; R-TEMPORAL-LOD, the adaptive per-region temporal clock, flagged 2026-07-01 at design.md Part 32 with a scoping pass in `docs/temporal_lod_research.md`, audit Section 2 and the Section 3 engine-fidelity subsection; the wave-3 electricity-and-magnetism physics floor R-PHYS-W3 consolidated as resolved and built 2026-07-01 on the owner's sign-off, a fourth partial advance of R-DEEPTECH-PHYSICS, at design.md Part 41, Part 55 (the fifth base dimension, electric current), and record 62.22, audit block 1x, built in crates/physics with a four-floor load proof; the wave-2 fluid/chemistry/optics physics floor R-PHYS-W2 consolidated as resolved 2026-07-01, a third partial advance of R-DEEPTECH-PHYSICS, at design.md Part 41 and record 62.21, audit block 1w, the kernel build following; the body arc R-BUILD-PHYS, R-WOUND, and R-FLUID resolved together 2026-07-01, the per-part body promoted from the anatomy vector with wounds measured over the R-PHYS-MECH floor and fluids as data-defined substances, at design.md Part 35, record 62.20, audit block 1v, built in crates/sim/src/body.rs, pending owner morning review; R-BEHAVIOR-EVOLVE flagged then resolved 2026-07-01, behaviour evolved from physics and selection across four built-and-proven stages, at design.md Part 8.4, record 62.19, audit block 1u; R-PHYS-BIO and R-PHYS-MECH resolved by the physics-substrate fan-out's waves 0 and 1, with R-SOURCE-VECTOR and R-BIO-REGISTRY added as wave-0 carried seams; wave 1's named reach ceilings are wave-2 and thermal-deepening scope under the open R-DEEPTECH-PHYSICS, not new counted items; R-BIOSPHERE resolved as the distinct reader of the biology floor, a former candidate exploration so the open count is unchanged). Source of truth for the full reasoning is `docs/design.md` and `docs/audit.md`.

## Wave 3 (electricity and magnetism) resolved and built (R-PHYS-W3, signed off 2026-07-01)

SIGNED OFF and consolidated as resolved and built (design.md Part 41 wave-3 subsection and blockquote, Part 55 fifth base, record 62.22, Part 63 group, audit block 1x, counts 27 to 28; crates/physics/data/em_floor.toml, 15 kernels in laws.rs, the fifth-base Dimension change, the volt promotion, 19 tests, four-floor load proof, workspace green). The owner accepted the recommendations (S-ELEC-DIM the fifth base = SI ampere; potential and emf kept role-distinct; the volt promotion; reserved values surfaced-with-basis; the named deferrals). The reserved-values batch below is the owner's to set via the calibration manifest. The vehicle `docs/working/ELECTROMAGNETISM_SUBSTRATE_PROPOSAL.md` (electrostatics and current: 9 axes, 7 laws; magnetism and induction: 4 axes, 5 laws; ten confirmed red findings folded). E and M is a discoverable deepening tier under R-DEEPTECH-PHYSICS (three instrument-and-theory rungs: electrostatics, the pile and Ohm, then Oersted and Faraday), not a Tier-0 floor. It stays a proposal (counts unchanged, 27 resolved / 33 open) until sign-off, then consolidates into Part 41/55/58 and builds on the wave-2 recipe. The owner's decisions gating the build:

- **S-ELEC-DIM (the load-bearing gate): a fifth base dimension.** Electricity is the first quantity dimensionally independent of length, mass, time, and temperature, so it forces a fifth `i8` exponent into the locked `Dimension` monomial (Part 55). The Gaussian half-integer alternative is unrepresentable in `i8`; the recommendation is the SI ampere (electric current) as the fifth base, matching every datasheet, backward-compatible (every earlier axis unchanged at current exponent 0), neutrality-by-construction preserved over five bases. This touches the locked representation, so it is the owner's Principle-9 call. Couples to Part 55 and R-UNITS-PIN.
- **S-ELEC-EMF (optional collapse): `elec.potential` and `elec.emf` share the volt** and are kept role-distinct on the wave-2 `fluid.driving_pressure` precedent; the owner may collapse them to one axis.
- **The volt promotion of `chem.standard_potential`** (dimension-and-scale only, no numeric re-fit, range stays [-5, 5] V), unifying the wave-2 electrochemistry proxy with `elec.potential`/`elec.emf`.
- **The reserved-values batch** (axis ranges and per-quantity scales for charge, current, potential, resistance, resistivity, capacitance, field, flux density, flux, permeability, moment, inductance; the caps V/I/R/B/PHI_MAX; K_COULOMB on a reserved x1e9 scale; MU_0; the coil turn-count and turn-density construction data; the tick duration DT the induction finite-difference reads), each surfaced with its basis.
- **Deferred (named, not faked):** full Maxwell wave propagation, AC/frequency-domain and the tuned LC resonant circuit (the radio gateway), the sin(theta) dipole-torque angular factor, and the Nernst and Arrhenius/Tafel transcendentals, all to R-GPU-CANON-PIN or wave 4.



## Wiring decisions (owner, 2026-07-01), for when the living-creature stack is integrated into a running World

The owner's calls on what it takes to wire the biosphere, dawn, evolved controller, homeostasis, locomotion, and body arc into one running World, made when the arc was reviewed:

- **Behaviour tier: defer 25.10, seed controllers at the dawn.** Animals do not evolve behaviour during the pre-dawn radiation at the pool tier; controllers seed at the dawn and evolve at the individual/sampled-episode tier (what is built). The quantitative breeding-value tier (25.10) stays deferred, so the pure-frequency deep-time controller evolution is not built now.
- **Life cadence: keep 1 in-world year** until we profile 1 in-world month; revisit and tune the reserved life-process values then.
- **Promotion / level-of-detail (recommendation, to confirm at build time): promote within the focus/observed region**, the Part 11/54 significance thresholds riding as reserved values.
- **Entity storage (hecs-vs-grow): stay StableId-keyed for the first integrated slice**, decide at scale when the full tick is profiled.
- **The 20 reserved `body.*`/`behavior.*`/`physiology.*` values** (plus the R-UNITS-PIN scales) are the calibration batch for a fail-loud Calibrated production world; a dev-fixture demo runs without them.

The build plan toward the first end-to-end emergent slice (generated map, seeded bands, the naming game over the primes, emergent dialogue) is in `ROADMAP.md`. Its milestones:

- **M0: lock the determinism keying contract.** R-RNG-COORD resolved (the canonical `DrawKey` schema: a uniform coordinate with the tick always present, a registered phase set, and a per-phase slot so two draw sites do not collide on counter zero, implemented in `crates/core` and adopted at every draw site). Still to pin: R-REDUCE-ORDER for the gossip-conflict apply and the weighted pick (the serial tick already applies in a pinned order, so it is not blocking the slice).
- **M1: a generated visual map (CPU, headless). Done.** Topology module, a data-driven terrain and biome substrate, fixed-point worldgen, the chunk grid and quadtree, and a headless glyph frame, all landed in `crates/world`. The zoomable-map spatial skeleton (phases 1 to 3 of the 13-phase living-map program) completes it: `topology` widened to i32 axes (Part 56, large-scale worlds; the cell key folds by FNV-1a, neighbours compute in i64, squared distance in i128-saturate, so an edge cell cannot overflow); `lod` (the chunk grid `ChunkCoord`/`CHUNK` and the one canonical level-of-detail quadtree over the tile map per Part 6.2, each node summarised by its dominant biome with a lowest-id tiebreak, the whole tree folded by `StateHasher` so a seed yields a bit-identical tree); `view` (the `Camera` and the multi-zoom glyph reads `whole_map_frame`/`Camera::frame`, pure reads that never write canon, Principle 10). An end-to-end integration test (`crates/world/tests/zoom.rs`) and a runnable example (`cargo run -p civsim-world --example zoom_map`) walk the zoom ladder from a single whole-world glyph to per-tile detail. 34 world tests pass, clippy and rustdoc `-D warnings` clean, the full workspace green. No research item (worldgen is unbuilt, not unresolved). Defer R-GPU-CANON-PIN, R-WEATHER, R-CATASTROPHE, R-SAVE-SCHEMA, R-UNITS-PIN. Next on the living-map track: phase 4, the located-identity sim-to-world join (kept StableId-keyed so the hecs-versus-grow entity-world fork stays open to phase 8); then the living-content track gated on R-BIOSPHERE (species over the biology floor) and R-VIEW-ELAB (the superfine, non-authoritative view of individuals).
- **M2: seed bands at the dawn of sentience. Begun.** Being registries and a `Race` definition, the genome and genotype-to-phenotype map, the allele-frequency pool with Hardy-Weinberg promotion, intrinsic-belief seeding, the axiom and value update kernel, and `seed_dawn_populations` onto the map. Defer R-BUILD-PHYS; register the genome and axiom projections ahead of R-PROJ-REGISTER. Reserved axiom, genome, and value-metric calibrations are surfaced, set as dev fixtures, fail-loud under Calibrated. Landed so far: the genome representation and genotype-to-phenotype expression (`crates/sim/src/genome.rs`, R-GENOME 25.1/25.3/25.4); the per-race `GeneticScheme` and individual inheritance (segregation, recombination, discrete-state mutation; 25.2/25.4/25.5); the aggregate-tier `GenePool` (Wright-Fisher drift, directional selection, frequency-distance, declared speciation, Hardy-Weinberg promotion and demotion; 25.7/25.8); and the Dobzhansky-Muller incompatibility layer (`IncompatibilityTable`, `reproductively_isolated`, `HybridOutcome`; 25.7). The four method-fork decisions are confirmed and built: exact Wright-Fisher drift (defer the large-Ne Gaussian approximation), the interim frequency-distance measure, speciation by distance OR incompatibility count, and a pure-aggregate pre-dawn tier. Their reserved values are surfaced with bases in `calibration/reserved.toml`. The genome-evolution engine is the keystone the pre-dawn biosphere epoch (R-BIOSPHERE) runs on. The genome-to-`Mind` bridge is landed: `Mind::from_genome` expresses the three cognition channels (reasoning acuity, memory capacity, belief plasticity) from a being's genes through `GeneSet::express`, so a mind's sharpness is its inheritance; acuity is wired live into inference and perception, memory and plasticity are carried as the phenotype with their consumers (belief deterioration, update-rate modulation) the named follow-on. The value-distance metric (R-VALUE-METRIC, Part 21) core is landed in `crates/sim/src/value.rs`: per-race `ValueStructure` (independent, weighted graph, relationship matrix), the offline `GroundMetric` compiler (exact-integer all-pairs shortest paths, Floyd-Warshall, proven against hand-computed rings and paths), the pinned independent-axes Euclidean distance (over `Fixed::sqrt`, new in core), the cross-race etic projection plumbing (`EticSubstrate`, `EmicProjection`, `project_to_etic`, `cross_race_distance` with the reserved incommensurability floor, untranslatable axes as theory-of-mind blind spots), and `conflict_pressure`. The structured-distance formula is a flagged seam (see below). The axiom kernel (R-AXIOM, Part 28) brick 1 is landed in `crates/sim/src/axiom.rs`: the representation (the multi-field `Axiom` separating position from hold, `EpistemicStance`, `EvidenceTag`/`EvidenceRing`, `AxiomAxisDef`/registry, `IntrinsicBeliefs`) and the single-mind appraisal/update kernel (evidential pressure = salience x source_weight x (1 - dogmatism); the AGM entrenchment gate: assimilate below, accommodate above; the revelation jump emergent from one formula). Four select-confirm decisions taken (non-final): AxiomDomain as a data registry seeded with the five standard domains (verified against the design before committing); the stubbornness anchor a per-axiom base modulated by the epistemic stance (`effective_stubbornness`); the evidence ring FIFO-by-recency; the revelation jump emergent. Build directives applied: source modes are a data registry with no hardcoded mode ids in the kernel; source weights normalized to unit-sum at construction (no hot-path division); the emergent revelation jump dissolves the separate revelation-jump-threshold reserved value. Deferred to later bricks (dependencies unbuilt): enculturation (Friedkin-Johnsen anchored average over a group), bounded-confidence schism (prestige arm blocked until a status system exists), inheritance (heritable+encultured seed blend on a new keyed `Phase`, adds the mutation-spread reserved value), calcification of unchallenged axioms, and the two LOD group regimes. Steering boundary recorded: the kernel is clean (an innate disposition is an authored input, like gene effect sizes), but the Steering Audit must bite at axiom *consumers* (Parts 8/19/36) so no system maps a stance to a named institution or religion. The dawn convergence is landed: `World::seed_dawn_populations` (`crates/sim/src/world.rs`) reads a `Race` record (`crates/sim/src/race.rs`: gene set, allele-frequency pool, ploidy, `IntrinsicBeliefs` seed, environment baseline) and a list of `BandSpec`s, and for each band member mints an id, promotes a genome from the pool (Hardy-Weinberg, keyed by the being id, 25.8), expresses its mind via `from_genome` (25.6), seeds its innate disposition (Part 28), and places it; the World now carries per-being genome and intrinsic-belief maps. Genesis-time deterministic; the genome, value, and axiom work first run together here. Enculturation, bounded-confidence schism, and inheritance are landed (`crates/sim/src/axiom.rs`, `world.rs`). Enculturation: `confidence_weighted_mean` (the canonical 128-bit group aggregate) and `enculturate` (the Friedkin-Johnsen anchored average); `World::enculturate_band` + `set_intrinsic`. A band with distinct seeds reaches a persistent spread, proven over twenty rounds. Schism: `bounded_confidence_mean` and `confidence_weighted_variance`; `World::enculturate_band_bounded` (synchronous Hegselmann-Krause round), `is_fissioning` (variance vs a reserved threshold), `stance_clusters` (the sects, 1-D epsilon-gap clusters); two far clusters never merge, proven. Inheritance: `inherit_seed` (h*parent + (1-h)*local_mean + bounded counter-RNG mutation under the new `Phase::AXIOM_INHERIT`); `World::inherit_child`; a child resembles parent and culture and varies. Calcification: `Axiom::calcify`/`weaken` and `World::calcify_band` (an unchallenged axiom hardens toward a reserved cap; near-miss challenges make it brittle). Full birth: `World::birth` bears a child of two parents with a genome recombined under the race's `GeneticScheme` (`reproduce`), a mind from `from_genome`, and inherited intrinsic beliefs, the point where the genome and axiom kernels meet (`Race` now carries its scheme and derives ploidy). Scenario loader: `Scenario` parses `scenarios/*.toml` into a typed profile (postures + per-dial `Direction` tokens); the four canonical files load. A capstone lifecycle integration test (`tests/dawn_lifecycle.rs`) runs dawn, schism, calcification, and births as one loop and proves it replays bit for bit with its emergent signatures. Remaining deferred axiom bricks: the conformist and prestige transmission biases (prestige needs a status system; the conformist continuous form is a stress-test-batch fork, below), the integer-Gaussian mutation shape (25.10, a reserved method), and the two LOD group regimes (need the pool tier extended with axiom profiles). Also next: the pre-dawn biosphere epoch on the pool tier (R-BIOSPHERE), layering a scenario over the manifest into a runnable `World` (gated on the reserved magnitudes), the full-birth integration into a tick loop, and the stress-test-batch formulas.
- **M3: the naming game over the primes.** Load the roughly sixty-five NSM primes as data, a thin concept-and-lemma representation, and the bounded once-per-culture dawn dynamic with the reserved `lang.dawn_round_cap`. Defer R-LANG-TYPOLOGY.
- **M4: emergent dialogue (implement R-CONVERSE) and the loop. Implemented (serial tick).** In `crates/sim/src/dialogue.rs`: the move and force registries, the content gate, move-as-event with a versioned big-endian payload, and the conversation query (a connected component over participants, topic, in-reply-to, and a time window). In `crates/sim/src/world.rs`: the `converse()` step on the serial deterministic tick (opt-in via `set_dialogue` and `promote`, a no-op otherwise), speak-as-intent for promoted co-located pairs, the response loop, grounding as said-evidence (no common-ground prior), the felicity misfire, move selection by realised affordance (content-blind), and gossip skipping promoted speakers so the two fidelities never double-count; new `Phase::CONVERSE`. The two Steering Audit invariants are enforced as `tests/dialogue_steering.rs` (content-blindness of force, channel swap at equal capacity), and `examples/conversation.rs` is the narrated scene (canonical gist plus non-canon flavor) showing co-location, promotion, felicity, the content gate, and grounding, replaying bit for bit. Still on the serial path: the four parallel-form determinism pins ride the open scheduler cluster (R-CMD-ORDER, R-REDUCE-ORDER); repair, the per-tick budget, the promotion-threshold and demotion hysteresis, the speaking-versus-acting arbitration, and the deliberation/persuasion/negotiation couplings remain deferred with their reserved values surfaced.

---

## Time, tick rate, and playback (landed 2026-07-01)

The observer's playback machinery is built and determinism-safe (`docs/time_and_playback.md`). `crates/sim/src/clock.rs` (a `Steppable` trait, a `SimClock` for the reserved base-tick duration, and a pure view-side `PlaybackDriver` with pause, single-step, speed control, and a `lod_debt` catch-up seam); `crates/sim/src/epoch.rs` (a `Radiation` one-generation stepper, proven bit-identical to the batch); `crates/sim/src/genesis.rs` (a `WorldGenesis` staged driver, proven equal to `genesis`); and the viewer now animates the pre-dawn radiation live at an adjustable speed (plus a headless `--radiate` trace). The coarse deep-time end of the vision is watchable today. Two determinism provisos stand for any future live loop: advance only by whole ticks (skipping active ticks is the open Part 32 problem), and never drive a birth or promotion from a view-time path (allocation-order id keying, `id.rs`).

- **Fine end (one in-world second per real second, people walking around).** Not started. An emergent-locomotion build: beings with map coordinates that change per tick, moved by drives resolving against beliefs and pathfinding (physics authored in via anatomy, terrain, energetics; where and why emergent, Principle 9), plus the dawn-`World`-to-map bridge. Couples to Parts 8, 9, 13, 20, 25, 35, and the located join.
- **Coarse end (years per second) at general fidelity.** The pre-dawn radiation view covers it for deep time; the general case is temporal level of detail (Part 32) plus the non-authoritative view elaboration R-VIEW-ELAB, both open research. The `PlaybackDriver`'s `lod_debt` is the seam a temporal-LOD absorber would consume.

Reserved-values queue (surfaced 2026-07-01, for the owner to set and confirm via a test; recommendations and bases in `docs/time_and_playback.md`): the base-tick duration (world-seconds per tick, recommend 1.0, already reserved in Part 54); the life-cadence period (base ticks per aging and mortality beat, derive from Part 20 and the base tick); and the generation-to-years mapping (in-world years per pre-dawn radiation generation, tie to the Part 25 organism generation time). The owner set these on 2026-07-01: base tick 1 in-world second, life cadence 1 in-world year (with a note to revisit 1 month if the compute budget allows), generation 10000 years; recorded in `calibration/reserved.toml` and confirmed by a test. The view-layer playback knobs (default speed, step factor, rate bounds, per-frame cap) are free-to-tune development defaults, not reserved calibration values, since they never enter canon.

## Emergent locomotion (landed 2026-07-01) and the behaviour seam

The movement substrate is built (`crates/sim/src/locomotion.rs`, `examples/walkers.rs`): positioned beings walk on the map at a physics-bounded speed derived from body morphology and terrain, navigate by perceived-and-remembered knowledge, and explore to discover what they do not yet know, all deterministic and keyed on the seed, the being, and the tick (never the camera). Two Principle-9 catches from the owner were fixed in the build: mobility is gated on the body's locomotion organ, not the kingdom, so a mobile autotroph (a walking tree) can emerge (the `sessile = layer == 0` rule in `sample_body_plan` is replaced by an emergent `rooted_prior` draw, a strong-but-not-absolute role bias, with `producer_rooted_prior`/`consumer_rooted_prior` reserved on `GeneratorParams`); and a being knows only what it has perceived (a small true sensory range) or remembered, so it cannot head for water it has never seen, it must explore to find it (the omniscient nearest-resource query is gone).

- **R-BEHAVIOR-EVOLVE: resolved 2026-07-01** (design.md Part 8.4, record 62.19, Part 63 bibliography, audit block 1u and the Section 3 resolved bullet; resolved count 23, open 36, on the owner's sign-off of the design pass and the completed staged build). Behaviour is evolved from physics and selection rather than authored: a data-defined homeostatic physiology of draining reserves (`crates/sim/src/homeostasis.rs`), the morphological affordance primitives a body permits, the non-omniscient percept, a heritable controller expressed from a new genome channel (`Channel::Controller`; `crates/sim/src/controller.rs`, a fixed-point clamped-linear reaction norm graduating to a small fixed-point recurrent network by the reserved hidden width), and selection by homeostatic survival wired into the aggregate-tier `GenePool::select` (`crates/sim/src/evolve.rs`). Built and proven across four stages, each determinism-audited and tested: Stage 1 (physiology and affordances), Stage 2 (the controller channel and its reaction-norm expression, retiring the authored `Behaviour` from the locomotion path while it stays the deliberative tier's interface; the `walkers` example shows foragers survive and a blank-controller being dies), Stage 3 (the homeostatic-survival scorer and an individual-based evolutionary loop that produces water-seeking behaviour from random controllers, plus the pool-recurrence wiring), Stage 4 (the recurrent-network graduation and the full search-based dawn episode). The evolved controller underlies the sentient layers (Parts 21, 28, 37). Reserved calibrations surfaced in `calibration/reserved.toml` (`physiology.*`, `behavior.*`). Honest limits (also in record 62.19): the pure-frequency deep-time controller expression couples to the deferred quantitative tier (25.10) and open temporal LOD (Part 32), so the built proof is the individual and sampled-episode tier; the proxy's honesty is the reserved thing to validate at scale; the intake yield stands in for the R-PHYS-BIO edibility measure until wired to the located matter.

---

## Open, ready to take (substrate or pattern already resolved)

- **R-INFRA.** The etic-and-emic question applied to the buildings institutions inhabit (infrastructure). Ready: reuses the etic-and-emic substrate pattern resolved for institutions (R-INST). Couples to Parts 36, 46.
- **R-LANG-TYPOLOGY.** Hardening the grammar typological parameters (word order, head direction, morphological type, alignment) from a closed enum to a data-defined substrate. Ready: reuses the data-substrate pattern; surfaced by the R-LANG-DET generalization audit. Couples to Parts 33, 25. Substrate built prototype-in-isolation 2026-07-02 in `crates/sim/src/typology.rs` (owner directed "implement after a quick sanity check"; the scoped proposal is `docs/working/LANG_TYPOLOGY_SCOPED_PROPOSAL.md`): the open `TypologyRegistry` (parameters, values, the data-defined anchor-first `sample_priority`), the per-parameter `TypologyPrior` (integer WALS counts as data, per-race overridable), the `HarmonyModel` (Greenberg/Dryer correlations as directional bias rows, a non-correlating pair carrying no row), the seeded `sample_profile` (keyed `DrawKey(culture, tick, Phase::LANG_TYPOLOGY, slot)`, tilt gated by a reserved disharmony probability), the `TypologyProfile` carried by `Language` and inherited through `fork`, and `typology_distance` for the 33.5 grammatical component; `wals_seed` ships thirteen WALS parameters, every count verified against wals.info on 2026-07-02. Steering holds proven by test (gloss-blind sampling, the typology-permutation invariant, harmony-as-tendency, a forward-conditioning bias a load error, the tier weights and disharmony fail-loud from the manifest). Four reserved manifest entries surfaced with basis (`lang.typology_harmony_strong`/`_weak`, `lang.typology_disharmony`, `lang.typology_distance_weights`). The drift operator is deferred behind the static generator per the proposal's sequencing option. Stays OPEN (like R-CMD-ORDER: mechanism built, item open until consolidated): the design-document consolidation (Part 33.4 mechanism plus a Decided-and-reserved blockquote, a Part 62 record, a Part 63 bibliography group, the audit Section 1/2/3 updates and the counts) awaits the owner's sign-off on the five open decisions the proposal lists non-final (parameter-set breadth, per-race prior default, harmony granularity, drift-in-scope, genome coupling), implemented here as the proposal's recommended defaults.
- **R-WOUND: resolved 2026-07-01** (body arc). A wound is measured, not stored: an insult over a data-defined damage mode meets a part's tissue and the resolved R-PHYS-MECH floor laws compute the damage; the mode taxonomy is a data registry (cut/pierce/blunt/burn grounded, corrosion/freeze/curse as exotic data). Built in crates/sim/src/body.rs. Part 35, record 62.20, audit 1v.
- **R-FLUID: resolved 2026-07-01** (body arc). A fluid is a data-defined kind with a composition over the biology floor and a conserved volume stock; loss bleeds from a breached part, death past the critical fraction. Built in crates/sim/src/body.rs. Part 35, record 62.20, audit 1v. Corrosion and full fluid chemistry wait on wave-2.

## Open, physics-and-materials substrate fan-out (waves 0 and 1 resolved)

The reach-bounding authored layer (Principle 9), taken in waves per `docs/working/PHYSICS_SUBSTRATE_FANOUT_PLAN.md`. Wave 0 (the biology-and-composition floor, R-PHYS-BIO) and wave 1 (the mechanical-and-materials floor, R-PHYS-MECH) are resolved and consolidated (Parts 41, 58, records 62.16 and 62.17, audit blocks 1r and 1s). Wave 1's vehicle is `docs/working/MECHANICAL_MATERIALS_SUBSTRATE_PROPOSAL.md`. Wave 2 (fluids, chemistry, optics) is the next fan-out, and it absorbs wave 1's named reach ceilings (buoyancy and hydrostatics, radiant and convective heat, heat-to-work). Update (2026-07-01): the wave-2 fan-out has been RUN and produced a hardened proposal, `docs/working/FLUIDS_CHEMISTRY_OPTICS_SUBSTRATE_PROPOSAL.md`, awaiting owner sign-off before consolidation (design docs and counts unchanged, the wave-1 pattern). Three green domains (fluids 9 axes / 9 laws, chemistry 8 / 5, optics 10 / 7), each red-teamed (19 confirmed steering/determinism/reach/fabrication findings folded), six consumer contracts checked. It absorbs the named ceilings (hydrostatics, buoyancy, convective and radiant heat, the Carnot heat-to-work limit) and adds the dynamic-pressure force family drag and aerodynamic lift (the owner-named lift, flooring flight and realistic ballistics as reduced-order lumped-coefficient laws). Five unification decisions taken (one canonical density axis with its range widened down to the lightest gas, folding a confirmed red finding; a canonical `therm.temperature` state axis owned by the energy sub-domain and read cross-domain, grounding the wave-1 conduction and combustion inputs; one energy-per-mass scale across reaction, fuel, and phase energy; one activation-barrier concept; the pinned megapascal). Honest ceilings deferred (transcendentals to R-GPU-CANON-PIN: Clausius-Clapeyron, Arrhenius and Tafel, Snell and Beer-Lambert; the real engine cycle; turbulence and the drag polar to wave 3). On the owner's sign-off (2026-07-01, the two unification decisions plus 'consolidate and implement, wire, and audit'), CONSOLIDATED as resolved (R-PHYS-W2, Part 41 wave-2 subsection and blockquote, record 62.21, Part 63 group, audit block 1w, resolved 26 to 27). Two consumer-lens completeness passes hardened it first (twenty Tier-0 folds: the weather water cycle and acoustics; radiative equilibrium, the colour stack, and dissolution; wave 1 needed no addition), banked in the proposal addenda (Sections 11 and 12). The kernel build into crates/physics (axes as floor data, the closed-form kernels in laws.rs with the determinism harness and load proof, on the wave-1 recipe) and the immediate-consumer wiring follow; then wave 3 (electricity and magnetism). The open umbrella for the deepening tiers and the other domains is R-DEEPTECH-PHYSICS (far-horizon, below); the Part 55 dimensionless Ratio class and derived-monomial registrations the floors need couple to R-UNITS-PIN. Owner direction (post-consolidation, select-confirm): the canonical pressure scale is pinned to the megapascal (the first reserved value set in the fan-out); the next effort is to build and prove the two floors (the QuantityAxis/InteractionLaw/Substance registry that loads them, the determinism and a Stage-14-style convergence proof, then toward R-BIOSPHERE and the living slice) rather than widen, so wave 2 (fluids leading) waits until the representation is validated; the reserved values stay in the records and seed calibration/reserved.toml when the engine consumes them. Update: the substrate engine is built (`crates/physics`, phases 1 to 3: the QuantityAxis/InteractionLaw/Substance representation and registry, the 24 closed-form fixed-point law kernels with the determinism harness, and the two floors as data with the load proof; 41 tests green, the full workspace green). Update 2 (2026-06-30): the owner set the floor axis ranges (select-confirm over seven grounded decisions: cited datasheet maxima, theory-fixed intervals, gravity 9.80665 with a per-world override, energy_density stored gross, the tolerance-zero direction, the law-cap policy), set in the floor data files; the law caps and engine-derived constants are recorded in audit Section 6b and laws.rs; two axes (the geometry second moment of area, the per-class reference tolerance) stay reserved on their per-quantity scale (R-UNITS-PIN). Next steps now buildable: a physics caps manifest the kernels read when wired, a first consumer reading the floor (world materials, the sim body or biosphere), and R-BIOSPHERE generating species over the biology floor.

- **R-SOURCE-VECTOR.** Lift the biology floor's eater half from a fresh-tissue composition vector to a draw over a source vector, so field-and-gradient feeders are first-class and the Part 16 `GrowthInput`, Part 17 `FoodSource`, and Part 19 `MaterialProps` closed structures are subsumed by the physics-substrate axis registry. The F5 seam of R-PHYS-BIO; deferred behind the matter-eater completeness scope. Couples to Parts 16, 17, 19, 41, 58.
- **R-BIO-REGISTRY.** Populate and Steering-Audit the biology floor's nutrient and toxin class registries so the genesis seeding does not privilege terran biochemistry, breaking the circular axis-set-versus-need channel. The F4 seam of R-PHYS-BIO. Couples to Parts 16, 17, 20, 25, 35, 41.

## Open, coupled or partly reachable

- **R-COMMS.** Long-range communication and information flow. Partly reachable: the mail half is a composition of resolved institutions (R-INST) and open infrastructure (R-INFRA); the signal half waits on deep technology. Couples to Parts 41, 36, 46, 9.
- **R-RELATION.** How membership, role, lineage, and social edges are typed without a back-door taxonomy. Waits on the unified graph substrate; most-wired, so taken late. Couples to Part 10 and the graph substrate.
- **R-EVENT.** The event schema without an authored event enum. Most-wired item; taken last with R-RELATION. Couples to Part 7 and nearly everything that writes history.

## Open, far-horizon (held by standing instruction)

- **R-DEEPTECH-SCIENCE.** The emergence of abstract, explanatory, mathematical knowledge as a compounding enabler. Waits on the technology layer built and proven at small scale. Couples to Parts 41, 28, 9.
- **R-DEEPTECH-PHYSICS.** The tiered, deepening, discoverable physics substrate and its completeness and audit. The reach of technology equals the reach of this substrate. Couples to Part 41.
- **R-DEEPTECH-DEPTH.** Deep but emergent prerequisite structure from physical and logical necessity, as a standalone proof, not an authored tech tree. Largely satisfied structurally by the resolved composition mechanism; remains as its own proof. Couples to Part 41.
- **R-DEEPTECH-SCALE.** Running the whole compounding process over deep time at the aggregate tier, cheaply and deterministically. Couples to Parts 41, 54.
- **R-TEMPORAL-LOD.** The adaptive per-region simulation clock: quiet regions advance in cheap coarse statistical steps, active or observed ones run fine per tick, and regions at different local clocks reconcile deterministically on interaction. Scoped against primary literature in `docs/temporal_lod_research.md` (binomial tau-leaping for the conserving coarse population advance, conservative parallel discrete-event simulation for cross-region reconciliation, Part 54's significance-and-seed schedule extended to the time axis). The build is the per-subsystem coarse-step registry and the conservative cross-region synchronization core, each prototyped in isolation. Flagged at Part 32; couples to R-TIER-CONSIST (the conservation contract), R-VIEW-ELAB and R-LIVELINESS (view-side siblings), the Part 57 scheduler cluster (the total orders), and R-AGENT-EXEC (the shared event-queue substrate).
- **R-AGENT-EXEC.** The event-driven agent execution model: an idle agent sleeps until a relevant event wakes it rather than being polled every tick, with a bit-reproducible wake order at world scale. The breadth-axis companion to R-TEMPORAL-LOD (deep time cheap on the time axis; a dense present cheap on the breadth axis). Scoped in `docs/event_driven_execution_research.md`: the wake order is R-CMD-ORDER's total key on the time axis (the discrete-event-simulation field-standard, with Parallel DEVS's confluent function the alternative), the wake triggers are a self-scheduled homeostatic-reserve crossing and the co-location subscription, and the event queue is a solved O(1) structure (calendar/ladder queue). Honest limit: it removes only the idle-agent tax, so a dense crowd falls back to a flat sweep above a reserved crossover. Flagged at Part 57 (the agent execution model, that part's second foundation); shares one event-queue substrate with R-TEMPORAL-LOD's cross-region core, so the two build to one design. Prerequisites R-CMD-ORDER, the deterministic scheduler (Part 57's first foundation), and R-HARNESS-COVER; couples to R-TIER-CONSIST and R-BEHAVIOR-EVOLVE.
- **R-VIEW-ELAB.** The non-authoritative view elaboration that must stay consistent with the canonical tiers without writing canon. Waits on the level-of-detail model being built. Couples to Part 54. Scoped alongside R-TEMPORAL-LOD in `docs/temporal_lod_research.md`.

## Open, world-content enum-opening (deferred until their session)

- **R-BUILD-PHYS: resolved 2026-07-01** (body arc). The raw morphology is the primitive; the per-part body is promoted from the anatomy vector and the mechanical stats (reach, strength) are derived from it through the resolved floor laws. Built in crates/sim/src/body.rs. Part 20, Part 35, record 62.20, audit 1v.
- **R-CATASTROPHE.** Emergent catastrophe representation without an authored catastrophe enum. Couples to Part 26.
- **R-CONTACT.** Emergent first-contact and contact-origin representation without an authored enum. Couples to Part 21.
- **R-DOMAIN.** The sacred-domain content that feeds the belief axis, without an authored domain enum. Couples to Part 38.
- **R-WEATHER.** Emergent weather representation without an authored weather enum. Couples to Part 18.

## Open, engine-foundation hardening (surfaced by the determinism red/green audit)

The design-level seams the red and green audit found, where a determinism, conservation, referential-integrity, or observer-independence invariant is asserted but not yet structurally guaranteed. The implemented-bedrock defects the same audit found are already fixed in code. These sit with the engine, scheduling, and level-of-detail foundations.

- **R-CANON-WALK.** Enforce StableId-sorted iteration over every canonical container so no hash or walk is built over hash-map order. Site Part 3.5; couples to Parts 2, 7, 9, 19, 55.
- **R-CMD-ORDER.** A total command-application order independent of the thread count. Site Part 4.3; couples to Parts 2, 7. Mechanism built prototype-in-isolation in `crates/core/src/command.rs` (2026-07-01): `CommandKey` the total order (tick, primary, kind, ordinal), `content_id` for content-addressed ids, `EventQueue<P>` the deterministic future-event list both engine levers drain, and `CommandBuffer<C>` the `ActionApply` barrier (design Part 4.1) that drains buffered commands in `CommandKey` order and mints spawn ids at the barrier, proven thread-count-independent by construction. The mechanism is complete; the item stays open until adopted in the live tick (the `ActionStage`/`ActionApply`/`Events` phase split, whose one live structural-change site is the dialogue write pass at world.rs:~1709, with a positional `in_reply_to` linkage to re-plumb) with the R-HARNESS-COVER thread-sweep bit-identity proof. Live adoption can change canonical dialogue ordering, so it wants an explicit go before the refactor.
- **R-GPU-CANON-PIN.** Pinned fixed-point multiply, divide, and transcendentals, and the full backend set, for cross-vendor bit-identity. Backend confirmed CubeCL (design Part 5; Rust #[cube] kernels, multi-target). Stood up and tested on the owner's RTX 5090 (via a cupy/NVRTC spike as the quickest on-device check; crates.io is reachable, so the production path is CubeCL): the CPU transcendental oracle is built (crates/core), the Stage 0 mul/div gate is bit-identical on-device over 2M+ cases each, and a canonical fixed-point diffusion field runs bit-identical CPU-vs-GPU at ~78x (crates/core/examples + docs/working/gpu_*.py). Stream A next: port the physics-law field kernels to CubeCL, and the noise/eikonal kernels, gated by the Stage 0 pattern. PROPOSAL PRODUCED and awaiting sign-off (`docs/working/GPU_CANON_PIN_PROPOSAL.md`, thirteen red findings folded): the CPU oracle verified exact, the GPU limb-multiply and restoring-divide pinned, exp/ln/sin/cos/atan/asin/pow specced buildable in crates/core (each gated on one reserved precision target), the noise/flow-field canonicity ruled, the backend set and Stage 0 gate defined. On sign-off: consolidate (Part 3.4/5.4, record 62.23, counts 28->29 resolved, 34->33 open) and build the transcendentals as the oracle, then audit. Site Parts 3.4, 5.4; couples to Parts 12, 13, 60.
- **R-DIM-HOMO.** A load-time check that a law's declared output `Dimension` matches the dimensional combination its kernel forms from its inputs, so a mistyped or hallucinated law with a wrong output dimension is a load error rather than a review miss (the axis-monomial discipline extended to laws): give each `InteractionLaw` a symbolic product-and-quotient formula the loader reduces by the existing `Dimension` multiply/divide. Surfaced 2026-07-01 from the hardcoded-steering-guards pass (the built content-address, no-float, provenance, and symmetric-kernel guards are in `crates/physics/tests/steering.rs`). Site Part 58; couples to Parts 41, 55.
- **R-PROJ-REGISTER.** Each two-tier subsystem registers an exact conserved projection rather than a lossy mean. Site Part 58; couples to Parts 9, 11, 25, 33, 36, 54.
- **R-REDUCE-ORDER.** A total order for every non-associative canonical combine. Site Part 57; couples to Parts 9, 15, 17, 25, 41, 58.
- **R-SAVE-SCHEMA.** Versioned saves, restored id high-water marks, and a pinned on-disk layout. Site Part 7.3; couples to Parts 2, 40, 58.
- **R-HARNESS-COVER.** Extend the determinism harness over phases, command buffers, gossip, mutation, a thread sweep, and save and replay. Site Part 3.5; couples to Parts 4, 7, 9, 11, 54. Harness built (2026-07-01): the core harness (`crates/core/tests/determinism.rs`) gained a command-application worker sweep (workers produce commands concurrently round-robin, the barrier drains in `CommandKey` order and mints spawn ids, asserted bit-identical across 1/3/4/machine-width workers, the order-sensitive counterpart to the existing accumulation/associative-reduction/near-overflow cases); the sim harness (`crates/sim/tests/determinism_harness.rs`) added the real dawn-tick replay (state hash and event-log hash bit-identical each tick over the gossip partition, dialogue write pass, naming game, and structural mutation), a seed-sensitivity check, and a two-tier save/load/continued-replay cycle. Exercises every path the item names except the tick-level thread sweep, which awaits the parallel tick (Rayon held off per the scheduler pass); stays open, built with that sight, so when Rayon lands the same replay harness runs the tick at a sweep of worker counts and closes this and R-CMD-ORDER's adoption proof.
- **R-UNITS-PIN.** Pin the Part 55 unit system: scales, canonical direction, overflow policy, and exact round trips. Site Part 55; couples to Parts 3, 9, 40.

## Gaps and holes cluster (traced from the gaps audit, in the open count)

Nine gaps from the owner's `docs/working/GAPS_AND_HOLES.md`, traced against the actual parts by a three-scout fan-out and a verification pass, registered as counted backlog items in audit Section 3. Each is a research-and-audit brief: work its audit questions before its research, and it may be overturned on evidence. Buildable-now means a first brick could be built today against existing code; blocked names the missing substrate. The three buildable substrate halves are now built as engine bricks (R-EMOTION, R-AGING, and R-SENSORIUM, engine status per item); they stay open research items because their deeper questions and reserved calibrations remain. The clear root blocker for the lifespan layer was that the engine had no clock-driven life-process loop (`World::tick` advanced cognition only); R-AGING's keystone per-entity loop (`age_step`, `apply_mortality`, `remove_being`) now exists, with the pool-tier half still to come.

- **R-EMOTION.** Transient event-driven affect, decay and trauma, modulating decision, as a data-driven axis set. Confirmed gap, the most load-bearing. Engine status: built (`crates/sim/src/affect.rs`, the affect-axis registry plus per-being `AffectState` with decay and trauma hardening, and a drive-derived `AppraisalBinding`, wired through `World::appraise`/`decay_affect`/`harden_affect`); the owner's two non-final choices (separate data-driven layer, drive-derived appraisal) fixed this build. Still open: wiring affect into the Part 8 decision considerations, and the reserved gains/decay/trauma calibrations. Couples to Parts 8, 20, 24.
- **R-AGING.** Individual aging, senescence, mortality, and demography for sentients. Confirmed gap, the lifespan keystone. Engine status: the per-entity life-process loop is built (`World::age_step`, `World::apply_mortality` against an age-hazard curve keyed on the being and its age under `Phase::MORTALITY`, `World::remove_being` pruning every per-being map; age seeded at the dawn and at birth; a generational-turnover capstone test). Still open: the pool-tier age distribution and symmetric pool-tier mortality the code `Pool` cannot yet carry, the reserved cadence period and hazard shape, and wiring the cadence into `World::tick`. Couples to Parts 20, 17, 25, 53, 54.
- **R-REST.** Rest and sleep discharging the fatigue drive, and the circadian coupling. Underspecified. The rest-discharge brick is buildable now; the circadian half is blocked on the day-night system (R-CELESTIAL candidate). Couples to Part 20.
- **R-SENSORIUM.** Spatial propagation of a stimulus so perception is set by physical reach, a sense a data-defined channel reader. Underspecified (the channel substrate of 33.3 is built; spatial propagation is the gap). Engine status: the channel-gate brick is built (`crates/sim/src/sensorium.rs`, `SenseChannelId` plus per-being `Sensorium`, wired into `World::perceive`: a trace carries a channel and a being perceives it only on a channel it reads, with no sensorium meaning read-all for back-compat). Still open: the spatial reach-and-attenuation half, blocked on the physics-substrate medium/field set. Couples to Parts 9, 6, 33, 35; R-WEATHER, R-COMMS.
- **R-REPRO.** Mate choice, sexual selection, courtship, gestation, rearing. Underspecified. Mate-choice scoring is buildable now over value distance and axiom stances; gestation is blocked on the life-process loop (R-AGING) and the selection feedback is design-open. Couples to Parts 25, 53, 21, 28.
- **R-CHILDHOOD.** An integrated, age-gated developmental schedule and the cross-generation carry/drift/loss of culture. Underspecified integration gap (Part 10 is the principal home; critical periods already in age-keyed plasticity; `enculturate`/`inherit_seed`/`birth` are built). Depends on R-AGING for the maturity clock, then mostly integration. Couples to Parts 10, 20, 28, 33, 23.
- **R-JUSTICE.** The act-to-detection-to-judgment-to-sanction-to-deterrence loop. Underspecified (Part 36 norms carry sanction and enforcement; detection has a home in witnessing). Detection buildable now; judgment-to-sanction-to-deterrence blocked on the Part 36 institution code (unbuilt) and the decision layer. Couples to Part 36, Part 9, Parts 8, 37, 28.
- **R-LIVELINESS.** Believable non-work daily life at the focus scale. Confirmed gap, cross-cutting. Individual non-work actions buildable now; the believable whole is blocked transitively on R-EMOTION, R-REST, and R-SENSORIUM. Couples to Parts 8, 42.
- **R-TECTONICS.** A deterministic plate model causing mountains, ore, volcanoes, and earthquakes from boundary state. Confirmed gap, but demand-closure finds it an enrichment rather than load-bearing (assigned geology suffices for what is specified). A plate-field worldgen stage is buildable now; catastrophe coupling is blocked on R-CATASTROPHE. Couples to Parts 12, 26, 18, 25.

---

## Recorded candidate explorations (not yet formally flagged, not in the open count)

Four exploration documents the owner asked to record now and queue for later. They are stored verbatim under `docs/working/`, are vehicles for future research items rather than counted backlog entries, and do not change the 25-open total until they are formally flagged through the workflow. R-EMBODIMENT and R-CONFIG sit far out, sequenced after the simulation core and the epistemics are built and proven; R-BIOSPHERE and R-CELESTIAL both read through the physics-and-materials substrate and are sequenced alongside it, with R-BIOSPHERE also connecting to the near-term living-world track (Track 1).

- **R-BIOSPHERE: resolved 2026-06-30.** (Was a candidate exploration; resolved and consolidated into design.md Part 25.11 to 25.13, record 62.18, Part 63 bibliography, audit block 1t and the Section 3 fan-out subsection, resolved count 22, on owner sign-off. The mechanism vehicle is `docs/working/BIOSPHERE_MECHANISM.md`; the stock-and-flow substrate and species-lineage container are built in `crates/sim`. The history below is retained for reference.) How a generated world is populated with many plant and animal varieties, and how edibility, nutrition, toxicity, and medicinal value are determined, without hand-authoring each species. The on-ideology direction: a species is a generated point over the trait axes the flora and fauna structures already declare (sampled through seed-keyed counter-RNG, validated against a food-web-closure and biome-fit constraint, then radiated into ecotypes and daughter species by the genome machinery over deep time), and edibility is a measured relational consequence of an organism's composition vector and a consumer's physiology (nutrient needs, toxin tolerances, digestive capability), never a stored flag, so the same berry is food to one race, poison to another, and medicine to a third. Composition axes are heritable, so defensive toxins and the domestication syndrome fall out of selection rather than being scripted, and dietary knowledge is emergent and fallible through the evidence system. Reserved and not guessed: the trait-axis catalogue, the nutrient, toxin, and digestibility composition axes, the niche and closure validation criteria, the biome-fit and edibility and harm law forms, and the seeding parameters (founder counts, diversity targets, resample bounds). Vehicle: `docs/working/BIOSPHERE_GENERATION_GUIDE.md`. Couples to the stocks-and-flows, flora, fauna, materials, drives, disease-and-poison, technique, genome, anatomy, and evidence systems, and to the physics-and-materials substrate it reads its composition axes from; sequenced after that substrate and the genome are built. Directly informs Track 1: the edibility-as-relation reframe is the faithful way to build "what is edible" in the living-world needs loop. Owner direction (2026-06-30): run the biosphere's deep-time evolution as a distinct pre-dawn genesis epoch at the aggregate (allele-frequency pool) tier, so plants and animals seed, radiate into ecotypes and daughter species, co-evolve (defensive toxins, domestication-ready stock), and go extinct over deep time before the dawn, and the people arrive into a mature, self-made ecology with established niches, edibility, and toxicity to discover. This is the natural endpoint of the Part 25 deep-time evolution plus this candidate; it sequences the genome inheritance and the two-tier allele-frequency pools (the evolution engine) and the composition/edibility substrate ahead of the dawn, the whole pre-dawn epoch deterministic and seed-keyed so it is part of the world's reproducible identity, with individual organisms promoted from the pools on demand only when the people interact with them. The world-genesis sequence becomes: worldgen, then the pre-dawn biosphere epoch, then the dawn, then play. Update (2026-06-30): the physics-and-materials substrate's biology-and-composition floor that R-BIOSPHERE reads its composition, edibility, and toxicity axes from is now resolved (R-PHYS-BIO, Parts 41 and 58, record 62.16), so the axes a generated species is a point over and the laws that measure edibility as a relation now exist; R-BIOSPHERE remains the distinct reader (species generation, niche and closure validation, and radiation over the floor), with its founder counts, diversity targets, and resample bounds still reserved. Update 2 (2026-06-30): R-BIOSPHERE is now scoped (the owner chose to scope it before diving; vehicle for the scope is `docs/working/BIOSPHERE_DIVE_SCOPE.md`, produced by a nine-scout grounding fan-out, synthesized, and red-teamed against the code). The scope is bounded to two gaps (generate-and-validate species seeding; wiring organisms and consumers to the resolved floor plus the pre-dawn epoch driver), since the edibility/nutrition/harm laws, the genome engine, and the map are built. Four high-leverage forks confirmed by the owner: food-web closure is the hybrid (topological at seed time, then stock dynamics cull and log); no mandatory Tier-0 toxin floor (defensive chemistry emerges from selection, no authored plant-defense ecology); the intelligence-dial cut-points are per-world calibration data; sentient races seed fresh at the dawn for now (the pre-dawn epoch is flora and fauna only). The other thirteen forks ride on the doc's recommendations (two with provisional ideology-aligned defaults, F4 DECOMPOSER demand-closure-driven and F13 AGG-CONSUMER-PROMOTION a reduced-summary-expands-on-promotion). Next dive step (sequence step 4): the trait-axis catalogue research fan-out (ecology, nutrition, toxicology) producing every axis reserved-with-basis, then hardening the closed lists (GrowthInput, FoodSource, Succession, FireResponse, EngineerEffect, TrophicLevel, domesticable, ClimateEnvelope, the temperament palette) as the structs are first written, then the pre-dawn epoch driver. Two coherence risks bound the dive (the interim biallelic distance metric the speciation keys off, and the unbuilt cheap large-Ne path bounding the pre-dawn generation count). A new candidate exploration was recorded: full sentient pre-dawn co-evolution (heritable axiom seeds radiating per lineage through the reserved `AXIOM_INHERIT` phase), deferred per the owner. Update 3 (2026-06-30): the dive's trait-axis catalogue step (sequence step 4) is done, vehicle `docs/working/BIOSPHERE_TRAIT_AXES.md`, produced by an eight-scout ecology/nutrition/toxicology fan-out, synthesized into one extensible registry, and red-teamed against the code and the floor. Eighty-six candidate axes across five groups (plant, animal, shared, composition-beyond-floor, fantasy-profile-gated), every range reserved-with-basis (no fabrication), no stored verdict (successional role, fire response, trophic level, biome membership, domesticability all derived at read time), no floor duplication. The red team caught and the doc folded: the mis-folded biological disease-immunity channel (added `animal.disease_resistance`), missing animal-tissue material and natural-weaponry/armor axes and a delivered-venom channel and an aquatic niche dimension (added), three consumer-less axes dropped to `TraitDef.correlations`, `tolerance_of_sentients` re-keyed off the per-relationship referent (not the hardcoded "sentient" notion), and the terran-chemistry caveat on the soil-chemistry axes. Two pinning couplings surfaced: `animal.body_mass`'s seventeen-order range needs a per-quantity scale (R-UNITS-PIN) and the niche-response kernel form is recommended piecewise-linear (closed-form, no exp) with any exp kernel deferred to R-GPU-CANON-PIN. Pathogen-side axes (virulence, transmissibility, host breadth) scoped as a sibling catalogue with the Part 22 / R-WOUND affliction work. Next dive steps: harden the closed lists as the flora/fauna structs are first written (Succession, FireResponse, GrowthInput/FoodSource under R-SOURCE-VECTOR, ClimateEnvelope, EngineerEffect, domesticable, TrophicLevel), then the pre-dawn epoch driver.
- **R-EMBODIMENT (candidate).** How a player could control a character or lead a band while holding determinism, observer-independence, the per-individual epistemics, and emergence. The on-ideology direction keeps the learned axes as the lens and the limit: the player supplies will and intent, the existing systems resolve perception, capability, and social outcome, player input enters the event log as exogenous events for replay, and the decision source becomes a per-agent data property (Principle 11). The hard build is the epistemic-rendering interface, showing a belief store rather than canon, including absence and falsehood. Vehicle: `docs/working/PLAYER_EMBODIMENT_EXPLORATION.md`. Couples to Parts 7, 8, 9, 33, 36, 37.
- **R-CONFIG (candidate).** How an observer configures a world, and where the line falls between tunable and restricted. The control surface is the profile, not the engine: selection levers are free, value overrides are free within their declared ranges, and generative levers run the steering and computability gates. Canon-affecting levers are seed-time and reproducible; observer-side controls are runtime and never enter canon. Open within it: the exact runtime-mutable set, the validation of override ranges against the substrate, and the configuration interface. Vehicle: `docs/working/WORLD_CONFIGURATION_AND_LEVERS.md`. Couples to Principles 3, 8, 9, 10, 11 and the world-profile and demand-closure model.
- **R-CELESTIAL (candidate).** How a world's sky is modelled (suns, moons, stars) and how day and night, seasons, tides, lunar phases, and eclipses arise, without authoring an Earth-like default. The on-ideology direction: a celestial body is a generated point over property axes (luminosity, spectrum, reflectance, mass, orbital elements, apparent size, plus exotic axes such as mana emission on the same extensible floor as the physics axes), the sky a generated profile-level configuration (rotation period, axial tilt, a set of emitters and reflectors, an optional starfield) that is part of the world's deterministic identity, and day and night, seasons, tides, phases, and eclipses are measured fixed-point consequences of the bodies' positions and properties rather than stored states, the same way chopping is measured from physics. The sky is upstream of fields that exist: it sources the light field (Part 5) with a diurnal cycle, gives climate (Part 18) a physically caused seasonal and diurnal swing, gates perception (R-EVIDENCE, Part 9) by light level, and adds a day-active or night-active trait axis to the being model (Part 20). Use is emergent and reach-bounded: celestial navigation as a technique (Part 23) where a perceptible regular pattern and a real need exist, calendars on the emic measurement hook (Part 55, a time unit from a celestial cycle), cosmology and myth and religion through belief (R-EVIDENCE, R-AXIOM, Part 28), and magic as an authored celestial axis under Principle 9 (Part 34). Reserved and not guessed (the doc's Section 8 research questions): the orbital-mechanics law form (which reduced model gives deterministic fixed-point positions cheaply at world scale, and how its error is bounded), the illumination law and day-night threshold, the climate coupling forms, the tide law and its couplings, the phase and eclipse geometry, the perception-gating law, the starfield model and precession, the full body-property axis catalogue, every parameter (real-with-source or fantasy-reserved-with-basis), and the owner decision on whether to re-ground seasons on axial tilt and orbit. Vehicle: `docs/working/CELESTIAL_AND_DAY_NIGHT_GUIDE.md` (its part references were verified on intake: all sound except topology, which is Part 56 and Part 40 rather than Part 12, and the emic measurement hook, precisely Part 55, both recorded in an editorial note appended to the vehicle). Couples to the light field (Part 5), climate (Part 18), worldgen and topology (Parts 12, 56, 40), perception and belief (Parts 9, 28), technique (Part 23), measurement (Part 55), magic (Part 34), and the being model and food web (Parts 20, 17); reads its exotic axes and provenance discipline from the physics-and-materials substrate, so it is sequenced alongside that substrate (with R-BIOSPHERE), since the illumination and exotic axes read through it.

---

## Audit follow-ups (deferred, from the engine faithfulness/steering audit)

The four-reviewer audit of the implemented engine found it faithful and clean (no fabricated values, the content gate has real teeth, both Part 41 invariants hold). These are the deferred, latent, or policy-bound follow-ups it surfaced, none a live defect.

- **First-order facet channel tag.** First-order `EvidenceRef` does not record the access channel, so a said-derived facet is indistinguishable from a co-witnessed one in the first-order store (design 9.5 names this tagging). Latent: the load-bearing grounding guarantee holds via the channel-aware nested frame, and no common-ground store exists. Deferred because threading the channel through `consider`/`add_evidence` touches the perception, gossip, and dialogue paths broadly; take it when a consumer needs first-order provenance.
- **Drift multi-rule ordering.** `drift_languages` applies form-change rules in vector order; faithful while `innovate` emits one rule per generation (the current cap). When multi-rule generations are enabled, sort the rules by the canonical content key (dim, from, to) before applying, per design 33.4.
- **Dialogue reserved-value manifest seeding.** The dialogue reserved values (felicity bands, uptake step, inquiry salience, contact touch, contiguity window, repair distance, budget, promotion thresholds, demotion hysteresis, speaking-versus-acting arbitration) are surfaced in design record 62.14 but not yet in `reserved.toml`. Consistent with the manifest policy of adding entries as the sub-features are built; seed each when its mechanism lands.
- **Calibrated-gate coverage.** `World::from_manifest`'s `Calibrated` gate lists only the nine `evidence.*`/`tom.*`/`gossip.*` keys; language, drift, and dialogue are gated by being inert until installed and fail loud per-subsystem at install. Extend the top-level `required` list when those subsystems become part of the production wiring.

## Reserved questions for the stress-test batch (non-final, select-confirm at stress-test time)

Design forks deferred whole to stress-testing, to be decided in a batch under the provisional principle (nothing final; a choice rides the fidelity-versus-compute scale and is retuned at scale). New fully-deferred forks surfaced during a build are appended here rather than guessed.

- **Structured value-distance formula (R-VALUE-METRIC build).** The value-metric core is built (`crates/sim/src/value.rs`), but the exact weighted distance for a structured (graph or relationship) value space is not pinned: record 62.3 fixes the ground metric and the independent=Euclidean reduction, reserves the structured weighted form as a design choice, and lists "the weighted form and the transport form agree where both apply" as still to be proven. `value_distance` returns `None` for a structured metric rather than running on a fabricated formula; the `GroundMetric` it would consume is built and proven. The candidate forms are a tree or graph Wasserstein (Le et al., NeurIPS 2019, exact closed form over the ground metric) and a Mahalanobis quadratic over a positive-semidefinite relationship matrix (the family that reduces to Euclidean for the identity). Decide the form, then implement and test that the weighted and transport forms agree where both apply. Surfaced 2026-06-30; owner deferred the formula to this batch.
- **Axiom evidence-ring eviction: recency-weighted-pressure hybrid (R-AXIOM build).** Brick 1 evicts the bounded evidence ring FIFO-by-recency, the design's specified form (`crates/sim/src/axiom.rs`, `EvidenceRing`). The owner chose FIFO for now and asked to re-explore a recency-weighted-pressure hybrid (keep the strongest evidence as well as the most recent) if compute allows at stress-test time. The hybrid adds a reserved weighting and a deterministic tiebreak.
- **Conformist transmission-bias form for continuous opinions (R-AXIOM schism).** The schism brick built bounded confidence (which alone fractures a band into sects), deferring the conformist and prestige transmission biases that sharpen it. The prestige arm is blocked until a status system exists. The conformist arm has no blocking dependency, but its form is underspecified: the literature's cubic frequency-dependent map (Henrich and Boyd) is for discrete or categorical traits, and the faithful continuous-opinion analogue (overweighting the denser region of the neighbourhood, or pulling the mean toward the mode) is a modelling choice, not a fact to copy. Decide the continuous conformist form (and confirm its determinism), then add it to `enculturate_band_bounded`.

---

## Canonical test worlds (scenario set)

The owner's four canonical starting scenarios, the worlds the engine is built and tuned against and the stress tests determinism and the Steering Audit must hold in. Defined in `docs/working/TEST_WORLDS.md`, scaffolded as data in `scenarios/` (one TOML per world plus a README). Mirror: grounded baseline, no magic, the control. Tempest: grounded, no magic, every change-and-extremes dial cranked high. Arcanum: high-magic stress test, minimally grounded. Confluence: grounded reality with abundant magic, the headline target. Each is a profile over real dials with directions named and magnitudes reserved (no fabrication); the magic and some change dials in Arcanum and Confluence are named by mechanism until Part 34 and the change systems land. Next build: the scenario loader that reads a `scenarios/*.toml` into a `World` as a named override set over the manifest; until it exists the examples hand-wire the same inputs.

## Working principle: nothing chosen is final (owner standing instruction, 2026-06-30)

Every reserved value and every method fork surfaced to the owner is provisional, not a commitment. The assumption is that a setting will not survive scale unchanged: where a system eats the compute budget, tune its detail down (fewer loci, a coarser sampler, a shorter pre-dawn, an approximation for an exact sum); where there is headroom, add detail back. The dials ride a sliding scale between fidelity and cost, and a value set today is a hypothesis awaiting a playtest. Recorded in the `calibration/reserved.toml` header and the test-worlds doc.

## Reserved-values queue

The numbers the resolved mechanisms need are surfaced, not invented, and are the owner's to set. The queue is the set of `status = "reserved"` entries in `calibration/reserved.toml` (see the runbook). Each carries its basis and a pointer to its mechanism. Work them through the reserved-values panel; set on the stated basis, validate against the stated target, then graduate the entry to `set` and annotate the matching reserved list in `docs/design.md`. No reserved value is decided by the agent.

## Inconsistencies

- **Inconsistency 5.** Part 23's authored technique web versus Part 41's emergent design space and content gate. Owner decision, reserved. The composition promotion gate (R-DEEPTECH-COMPOSE) now leans on this, since it promotes a stabilised technique into the join space as it promotes an artifact. Flagged in the Part 41 blockquote, record 62.10, and audit Section 4.
- **Inconsistency 6.** EventId as a dense storage index (Part 7.2) versus the truncation of demoted-entity events (Part 7.3). Surfaced by the determinism audit. Owner decision, reserved. A note is at the Part 7.3 site and the entry is audit Section 4, item 6.

---

## Resolved (28)

R-BEING-REP, R-VALUE-METRIC, R-AXIOM, R-GENOME, the six language-and-meaning items (R-LANG-CONCEPT, R-LANG-DISTANCE, R-LANG-LEARN, R-LANG-DISTORT, R-LANG-WRITING, R-LANG-GEN), R-EVIDENCE, R-INST, R-TIER-CONSIST, R-DEEPTECH-COMPOSE, R-TOM-UPDATE, R-LANG-DET, R-LANG-MODALITY, R-CONVERSE, R-RNG-COORD, R-PHYS-BIO, R-PHYS-MECH, R-BIOSPHERE, R-BEHAVIOR-EVOLVE, R-BUILD-PHYS, R-WOUND, R-FLUID, R-PHYS-W2, R-PHYS-W3. Each has a mechanism in the design document, a record in Part 62, a bibliography group in Part 63, and a consolidation block in audit Section 1, with its calibrations reserved. R-BEHAVIOR-EVOLVE resolved 2026-07-01: behaviour is evolved from physics and selection rather than authored, in five layers (a data-defined homeostatic physiology of draining reserves, the morphological affordance primitives a body permits, the non-omniscient percept, a heritable controller expressed from a new genome channel as a fixed-point reaction norm graduating to a small recurrent network, and selection by homeostatic survival wired into the aggregate-tier `GenePool::select`); built and proven across four determinism-audited stages (`crates/sim/src/homeostasis.rs`, `controller.rs`, `evolve.rs`), the individual-based loop producing water-seeking behaviour from random controllers without it being authored, the authored `Behaviour` retired from the locomotion path while it stays the sentient deliberative tier's interface, the evolved controller underlying the sentient layers (Parts 21, 28, 37); the pure-frequency deep-time tier couples to the deferred quantitative tier (25.10) and open temporal LOD (Part 32) as the honest limit (Part 8.4, record 62.19, audit block 1u). R-BUILD-PHYS, R-WOUND, and R-FLUID resolved together 2026-07-01 as the body arc: a per-part body promoted from the Part 25.14 anatomy vector, a wound measured from the R-PHYS-MECH floor (never a stored kind, the damage modes a data registry), and fluids as data-defined substances with conserved volume and bleeding, closing the R-BEHAVIOR-EVOLVE loop (integrity the derived reserve the controller reads, a strike affording a measured wound); built in `crates/sim/src/body.rs`, honest limits (temperature exchange, corrosion, digestion) reserved (Part 35, record 62.20, audit block 1v), pending owner morning review. R-TOM-UPDATE resolved 2026-06-29: the recursive theory-of-mind update is the evidence engine run recursively on whether a target believes a thing, fed only by access evidence (a data registry, not a closed enum) so it diverges from projection (Part 37, record 62.11). R-LANG-DET resolved 2026-06-30 as a deep focused session with two bounded adversarial rounds: the order-sensitive language procedures are pinned (minted concept ids, innovation-index form-change ordering, a widened leaky salience accumulator, a symmetric matching-free distance over the shared semantic substrate, a deterministic dawn trigger), worded over the generic form substrate so R-LANG-MODALITY adds only data; it surfaced the new sibling item R-LANG-TYPOLOGY (Part 33, record 62.12). R-LANG-MODALITY resolved 2026-06-30 by a five-facet fan-out and a three-skeptic verification pass: language is generalized to a data-defined modality substrate (production modalities, reception senses, feature dimensions, media; a channel a derived pairing; a form primitive a canonical simultaneous-feature bundle; per-being produce/perceive channels from the genome), adding only data so no R-LANG-DET pin reopens, with acquired injury-loss reserved to the open R-WOUND; the work retired R-LANG-DET's optional cross-modality ceiling and added a modality-swap invariant to the Steering Audit (Part 33, record 62.13). R-CONVERSE resolved 2026-06-30 by a five-facet fan-out and a steering-and-determinism verify pass: modelled dialogue is the promoted-tier refinement of the gossip loop, communicative acts first-class canonical events over a data-defined dialogue-move registry whose force composes from resolved primitives, a conversation an emergent query, speaking a utility-AI intent, the listener-side update the Part 37 single-utterance procedure with grounding reduced to said-evidence (no new common-ground prior); the verify caught two steering leaks (the register knob removed from canon, the conversational common-ground channel) and a determinism dependency on the open cluster, and added two Steering Audit invariants (Part 9.5, record 62.14). Its deeper couplings (deliberation, persuasion, negotiation) are scoped and reserved. R-RNG-COORD resolved 2026-06-30 as M0 of the build roadmap: the canonical `DrawKey` schema (a uniform coordinate of region, primary locus, secondary locus, tick, phase, and slot, with the tick always present, a registered phase set, a per-phase slot so two draw sites do not collide on counter zero, and an absent-coordinate degrade sentinel) implemented in `crates/core` and adopted at all six simulation draw sites, removing the hand-assigned magic numbers; no calibration reserved, the phase and slot numbers being engine mechanics (Part 3.2, record 62.15). R-PHYS-BIO resolved 2026-06-30 as wave 0 of the physics-and-materials substrate fan-out, the biology-and-composition floor: tiered per-tissue composition axes and a terran-chemistry toxin registry as registry points over the Part 58 substrate (each partial lump carrying a residual child for exact additivity), a per-race heritable extensible registry of consumer relation kinds, and three closed-form fixed-point laws (Liebig net nutrition, per-(toxin-class, consumer) integer-Hill harm with the saturation guard ordered before the power and the dose ratio formed by checked division, and edibility as a measured tuple with the gain-versus-danger valuation moved to the agent layer); produced by a four-facet fan-out and twice red-team audited, the second pass recomputing the Q32.32 arithmetic and catching the incomplete first fix and the steering migration one level down. A partial advance of R-DEEPTECH-PHYSICS, surfacing the new open items R-SOURCE-VECTOR and R-BIO-REGISTRY (Part 41, Part 58, record 62.16, audit block 1r). R-PHYS-MECH resolved 2026-06-30 as wave 1, the mechanical-and-materials floor: one unified registry (about thirty-three axes at one canonical scale per quantity, subsuming the Part 19 MaterialProps struct, with a shared gravitational axis) and about twenty-two closed-form fixed-point laws (contact, cutting, stress, fracture, buckling, shear, Hertzian contact, wear, impact, lever, friction, reach, weight, mechanical power, and the conduction, sensible-heat, phase-change, combustion, and thermal-stress set); crush versus pierce a contact-geometry difference so a mace and a morningstar are one design; produced by a green and red fan-out, twice red-team audited with the second pass recomputing the Q32.32 arithmetic and the synthesis-write then verified, the parallel-integration scale clashes unified and three product-before-divide overflows reordered. A second partial advance of R-DEEPTECH-PHYSICS; reach ceilings (buoyancy, radiant and convective heat, heat-to-work) carried as wave-2 scope (Part 41, Part 58, record 62.17, audit block 1s). R-BIOSPHERE resolved 2026-06-30 as the distinct reader of the biology floor (a former candidate exploration, so the open count is unchanged): a seed-keyed generate-and-validate species sampler over an extensible eighty-six-axis trait registry (validated by representability, biome-fit, least-fixed-point food-web closure the hybrid, and niche openness, with a bounded resample and a closure-preserving fallback), a bounded pre-dawn radiation epoch (drift, an environment-to-coefficient selection kernel, founder-fork, declared speciation into a parent-pointer lineage tree, and deterministic extinction), and edibility as the floor's measured tuple with a relational medicinal credit, dose-and-preparation dependence, and heritable stick-breaking composition; produced by a trait-axis catalogue fan-out and a four-cluster mechanism fan-out, each red-teamed (no fabrication, the steering relocations hold, every law a bounded closed-form integer kernel, the additivity and closure provably conservative), the owner's four forks and the recommended forks adopted, the stock-and-flow substrate and species-lineage container built in `crates/sim` (Part 25.11 to 25.13, record 62.18, audit block 1t).
